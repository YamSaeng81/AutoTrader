# CryptoAutoTrader — PROGRESS.md

> **▶ 지금 뭘 해야 하는지 모르겠다면 → [`docs/NEXT.md`](NEXT.md) 를 먼저 볼 것.**
> 2026-09-14 기준: 첫 주 알파 **−0.571%p**(시장에 짐). 실전은 조건 미충족. 다음 확인 2026-09-21(월) `bash scripts/status.sh`

> **목적**: `/clear` 후 새 세션에서 이 파일을 먼저 읽어 현재 상태를 파악한다.
> **갱신 규칙**: 이 파일은 **최신 작업 이력(최근 세션 몇 개) + 보류/결정 대기 항목 + 프로젝트 참조 정보**만 담는다. 오래된 상세 이력은 [`docs/old_progress.md`](old_progress.md)(2026-08-06 이전 전체 백업)와 [`docs/CHANGELOG.md`](CHANGELOG.md)를 참조.
> **2026-08-06 / 2026-08-19**: 파일이 비대해질 때마다 날짜별 상세 이력을 `old_progress.md` 로 이관하고 이 파일에는 요약만 남긴다. 상세 근거·재현 과정이 필요하면 `old_progress.md` 에서 날짜로 검색할 것.
> **마지막 갱신**: 2026-09-18 (4차) — 🔴🟢 **진입 수수료 차감 + 백테스트 규칙 버전 v3 — 재검증 기준선 재설정**. ⚠️ **재배포 + 운영 조합 Walk-Forward 전량 재실행이 필요하다. 그 전까지 게이트는 모든 조합을 차단한다(의도된 동작).** (1) **`executeTrade` 의 SELL 순손익이 진입 수수료를 빼지 않았다** — 3차에서 발견해 테스트로 박아만 뒀던 것을 해소. 범위는 처음 적은 것보다 좁다: 주 SELL 경로 3곳은 이미 `entryFee` 를 빼고 있었고, 누락은 `executeTrade` 를 쓰는 **Partial Fill 이월과 기간 종료 강제청산 두 경로**뿐이었다. 다만 강제청산은 3차에 신설한 경로라 **미청산 포지션이 남는 모든 백테스트**에 걸렸다. 운영(DYNAMIC/PAPER)의 `realizedPnl` 은 진입·청산 수수료를 모두 뺀 순손익이므로 이건 회계 오차가 아니라 **엔진 패리티 위반**이었다 — 백테스트만 유리하게 계산했고 **회전율이 높은 전략일수록 편향이 커진다**. 부분 청산은 **청산 비율만큼만** 진입 수수료를 인식한다(전액을 빼면 잔여 포지션 청산 때 같은 수수료를 두 번 뺀다). 실측 −0.20% → **−0.24% 로 자산 기준과 일치**, `UnrealizedPnlInMetricsTest` 의 단정을 `isGreaterThan` → `isCloseTo(offset 0.01)` 로 조였다. (2) **`EXIT_RULES_VERSION` → `BACKTEST_RULESET_VERSION` 개명 + v2→v3**. 🔴 **오늘 배포된 코드는 버전을 올리지 않아, 게이트가 09-08~09-17 의 v2 결과를 여전히 유효한 근거로 인정하고 있었다** — 게이트는 조합별 최신 실행 하나만 보므로 재실행되지 않은 조합은 수정 전 판정으로 실자본을 계속 승인한다. 이름이 "청산 규칙"이라 **청산 규칙을 안 바꿨으니 올릴 필요 없다고 넘어갈 뻔했다** — 실제 기준은 그보다 넓다: **같은 입력에 다른 결과를 내게 만드는 변경이면 전부** 올린다(전략 구성·평가 입력·손익 회계·look-ahead 수정 포함). 상수 javadoc 에 판단 기준과 v3 변경 내역 6건을 명시하고, 게이트 거부 사유 문구도 v1 전용 표현("SL 5% 고정·TP 10%")에서 버전 중립 표현으로 교체했다. DB 컬럼명 `exit_rules_version` 은 유지(마이그레이션 없음 = 배포 위험 0), 엔티티 javadoc 에 이름이 흔적임을 명시. 검증: **840건 전부 통과**(strategy-lib 86 · core-engine 249 · web-api 505). 수수료 차감을 뮤테이션으로 개별 검증(`.subtract(entryFee)` 제거 → 해당 테스트 실패 확인). 🟢 **3단계 완료 — 기준선 재실행까지 끝났다**(`scripts/rebaseline_wf_0918.sh`). 운영 중인 5조합 × 대표 5코인 = 25조합을 재실행해 **전부 `exit_rules_version=3` 으로 저장 확인**(배포본이 신 코드임을 실증). 🔴 **결과: 통과 0건** — OVERFITTING 24 · INSUFFICIENT_DATA 1 · ACCEPTABLE/CAUTION **0**. OOS 기대값이 음수인 조합이 18/25. 가장 나쁜 칸이 하필 **유일한 실자본 고정 세션의 조합**(MEANREV_BB/KRW-XRP/M15)인데, 표본 7건으로 하한을 넘겼는데도 INSUFFICIENT_DATA 다 — 사유가 표본 부족이 아니라 **IS 수익률이 양수인 윈도우가 하나도 없어** 하락률을 잴 대상이 없었다는 뜻이다. **학습 구간에서조차 벌지 못했다.** 게이트가 OFF 라 지금 거래가 막히지는 않지만, 뒤집어 말하면 **운영 함대 전체가 WF 불합격 조합으로 돌고 있다**. 상세·미결 결정 3건은 아래 09-18 절 참조. 📌 **캔들 확인 결과 M15 도 부실하지 않았다** — 5코인 전부 16만 건씩 2021-12-31 부터 있다(09-08 의 "M15 캔들 0건/5개월 낡음"은 다른 코인 얘기였거나 그 사이 수집됐다). 캔들 끝날짜가 코인마다 달라(ETH·XRP 09-17 / BTC·DOGE·SOL 09-07) **공통 구간인 09-07 로 끊었다** — 안 맞추면 두 코인만 10일치를 더 보고 비교가 어긋난다. 📌 문서 정리: PROGRESS.md **2,582 → 1,336줄**, 09-04 이전 상세와 종료된 동결기간 일정을 `old_progress.md` 로 이관(이관분에 "v1/v2 산출물이라 인용 금지" 경고 부착). 🔴 **이어서 감시범위 19코인으로 확장 측정**(WF 57 + 백테스트 57 = 114조합). 판정만 보면 ACCEPTABLE 4 · CAUTION 3 이 나왔지만 **전부 덫이다** — 6건은 OOS 표본이 5~14건(하한 5)이고, 표본이 쓸 만한 유일한 건(SUI n=31)은 **절대 수익률 −22.1%** 다. 거래 기록으로 실측한 **시장 노출률 중앙값이 3.0%** 라, 단순보유 대비 알파가 코인 방향과 기계적으로 일치한다(폭락 코인 +78~+92%p / 급등 코인 −291~−1060%p) — **실력이 아니라 노출 감소다.** 착수 전 세운 판독 기준('통과가 나오면 배치 문제')을 **정정한다**. 🔴 **WLD·ONDO·MIRA·WLFI 로 재배치 금지.** 🟢 세션 200(REAL) 정지 — 어느 근거에도 걸리지 않았다. 🟡 세션 92(REAL) 판단 대기. 📌 조사 대상 1건: MOMENTUM_V2/DOGE 는 **오른 코인에서** 보유 +32.5% 대비 +109.2%(145거래·PF 1.44·노출 8.4%)인데 WF 는 OVERFITTING 이다. 🔴 **부수 발견 — 캔들 신선도 스케줄러가 조용히 손을 뗐다**: 대상이 8종 고정이라 동적 워치리스트는 자동 갱신 대상이 아니고(NEAR·KAITO·SHIB 등은 캔들 0건), `MAX_GAP_DAYS=7` 을 넘으면 영구히 포기하며(8종 중 6종이 이미 그 상태), 알림 없이 `log.warn` 만 남는다. **30종이 08-30 에 멈춰 있다.** **Wave 3 선행 조건이다** — 캔들이 밀려 있으면 L 수정 후 재측정 자체가 불가능하다. **다음: ① 캔들 백필 + 스케줄러 구멍 수정 ② 프런트엔드 흰 화면 수정 배포 ③ 그 뒤에 Wave 3 (L→I→H→K).**
> 이전 갱신: 2026-09-18 (3차) — 🔴🟢 **전략 검토 Wave 2 — 백테스트 신뢰성 3건**. ⚠️ **저장된 모든 백테스트 성과의 의미가 바뀐다. 배포 전 검토 필요.** (C) **Hold-out 이 게이트가 아니라 표시용 창이었다** — `runWithHoldOut()` 은 홀드아웃 백테스트를 돌려 마지막 윈도우로 붙이기만 했고, 반환값의 `verdict`·`overfittingScore`·`aggregatedOutSampleMetrics` 는 전부 튜닝 구간 값이었다. 홀드아웃 거래를 모으는 `allOosTrades` 는 **만들어만 놓고 쓰이지 않았다**. 즉 **홀드아웃이 큰 손실이어도 최종 판정이 안 바뀌었다**. `holdOutMetrics`·`holdOutPassed` 필드 신설 + `VERDICT_HOLD_OUT_FAILED` 신설 + `WalkForwardValidationGate` 차단 연결. 통과 기준은 튜닝 OOS 와 동일하게 맞췄다(표본 ≥5 AND 기대값 양수) — 기준이 갈라지면 "홀드아웃은 통과인데 게이트는 차단"이 생긴다. 튜닝 OOS 지표에 홀드아웃 거래를 **섞지 않는다**(독립 표본 하나가 다수에 묻힌다). 📌 운영 영향 0 — `holdOutCutoff` 를 넘기는 운영 경로가 현재 없다(테스트 전용). 연결될 때 제 역할을 하도록 고친 것. (D) **BTC Guard 가 미래를 참조했다** — BTC 포인터를 `btcCandle.time <= nextCandle.time` 까지 전진시켰는데 `Candle.time` 은 **시작** 시각이라, 체결 순간에 시작하는 BTC 봉의 **종가·고가**로 판정했다. 그 값은 체결 시점에 미확정이다 — 봉 후반의 BTC 급락을 미리 알고 피한 셈. 캔들 간격을 대상 코인 캔들에서 직접 재어 **종료된 봉만** 참조하도록 수정(결측봉이면 간격이 커져 더 보수적으로 동작). 🔴 **BTC 캔들 주입을 5개 경로 전부로 통일** — 단일 백테스트만 주입하고 **Walk-Forward·전략비교·벌크·배치 4개 경로는 주입이 없었다**. 같은 전략·기간이라도 어느 API 로 부르냐에 따라 Guard 적용 여부가 달랐다. (E) **미청산 손익이 성과 지표에서 빠져 있었다** — `MetricsCalculator` 는 청산된 SELL 만 보고, 열린 포지션은 `finalEquity` 에만 담겼다. **화면의 finalEquity 와 순위·WF 판정에 쓰는 totalReturn 이 서로 다른 손익 범위를 재고 있었다.** 기간 종료 시 마지막 종가로 강제청산하되 **수수료·슬리피지를 실제 청산과 동일 적용**(비용 없이 탈출하면 또 다른 편향). 🔴 **새로 발견 — `executeTrade` 의 SELL pnl 이 진입 수수료를 빼지 않는다.** metrics 가 자산 기준보다 진입 수수료만큼 높게 나온다(실측 −0.20% vs −0.24%). E 와 별개 결함이라 이번엔 고치지 않고 `UnrealizedPnlInMetricsTest` 에 단정으로 박아 드러냈다 — 수치가 바뀌면 깨져서 재검토를 강제한다. 검증: **840건 전부 통과**(strategy-lib 86 · core-engine 249 · web-api 505). 신규 회귀 9건. **각 항목을 뮤테이션으로 개별 검증**(홀드아웃 게이트 제거 / BTC 포인터 원복 / 강제청산 제거 → 전부 해당 테스트 실패). ⚠️ D 의 look-ahead 테스트는 **급락 봉을 매수 체결 시각(i=115)에 정확히 맞춰야** 판별된다 — 처음엔 i=200 에 두어 뮤테이션이 통과했다. **다음: Wave 3(ADX Wilder 평활 · GRID 레벨 해제 · MTF 시각 정렬 · RSI/delta 경계).**
> 이전 갱신: 2026-09-18 (2차) — 🔴🟢 **전략 검토 Wave 1 — 전략 생성 단일화 + 실행 단위 상태 격리**. ⚠️ **매매 거동 변경 있음. 배포 전 검토 필요.** (A) **COMPOSITE_BREAKOUT 정의가 3개였다** — 운영(ATR0.5+VD0.3+MACD0.2+**RsiVeto**) / `BacktestService.compositeBreakoutBt()`(ATR0.4+VD0.3+RSI0.2+EMA0.1) / **`StrategySelector` static 폴백**(구버전, Spring 없는 환경용). 세 번째는 검토 문서도 못 잡은 것으로, **core-engine 단위 테스트 전체가 운영이 실행하지 않는 전략을 검증해 왔다**. 프리셋 14종 전부를 `core-engine/selector/CompositePresets` 단일 진실 원천으로 모으고 세 호출처를 위임으로 축소(`CompositePresetRegistrar` 248줄→26줄). 🔴 **COMPOSITE_ETH 가중치를 운영 구성(ATR0.5/OB0.3/EMA0.2)으로 통일** — BT 전용 ATR0.7/OB0.1/EMA0.2 는 "호가 캔들 근사 보정"이 의도였으나 결과적으로 운영과 다른 전략의 성과를 COMPOSITE_ETH 검증 결과로 읽게 했다. 근사 불확실성은 가중치를 몰래 바꾸는 대신 드러내는 방향으로 다룬다. 🔴 **COMPOSITE 백테스트가 적응형으로 바뀐다** — 기존엔 전체 캔들로 레짐을 한 번 감지해 고정 조합으로 굳혔다(= 전체 기간 정보를 초기 선택에 쓰는 **미래 참조**). 이제 `RegimeAdaptiveStrategy` 가 시점별로 갱신한다. (B) **실행·세션마다 전체 전략 트리 신규 생성** — `BacktestEngine` 이름 기반 `run()` 이 공유 인스턴스를 가져오고 초기화도 없었다. `StrategySelector.ws()` 도 하위 전략을 공유 레지스트리에서 꺼내 **바깥 래퍼만 새로 만들어도 내부 GRID·레짐 감지기가 공유**됐다. 둘 다 팩토리 경로로 교체. Walk-Forward 는 모든 호출이 이름 기반이라 **IS→OOS 상태 누출(§3.5)도 함께 해소**. 📌 `StrategyRegistry.isStateful` → **`hasFactory` 로 개명** — 팩토리 등록 여부와 "상태를 가지는가"는 다른 질문이고, 성분 하나 갈아끼우면 조용히 바뀐다. 복합 프리셋은 상태 유무와 무관하게 전부 팩토리 등록. 📌 프리셋 등록이 `StrategySelector` 클래스 로딩에 의존해 **그 클래스를 안 건드리는 경로에선 프리셋이 아예 없었다**(Spring 밖에서 "알 수 없는 전략"). `ensureRegistered()` + static 블록으로 로딩 순서 의존 제거. 검증: **830건 전부 통과**(strategy-lib 86 · core-engine 240 · web-api 504). 신규 회귀 7건. ⚠️ **뮤테이션으로 판별력을 실증했다** — 첫 작성본은 공유 인스턴스로 되돌려도 **전부 통과**했다(COMPOSITE_BREAKOUT 은 필터가 강해 합성 데이터 체결 0건 → 빈 목록끼리 비교, GRID 오염은 가중 0.2로 희석). 체결이 아니라 **신호 트레이스**(reason 에 성분 목록이 들어가 HOLD 끼리도 구별 — 200회 중 114회 상이)로 비교하고, 격리는 상태가 실제로 새는 **GRID**(공유 시 2건→1건)로 바꿔 두 뮤테이션 모두 잡히는 것을 확인했다. **다음: Wave 2(Hold-out 게이트화 · BTC Guard 시간정렬 · 전체자산 기준 지표).**
> 이전 갱신: 2026-09-18 — 🟢 **전략 검토 Wave 0 적용**(`STRATEGY_LIBRARY_REVIEW.md` 후속, 저위험 3건). (1) **ATR=0 에서 예외로 죽던 경로 차단** — OHLC 가 전부 같은 무변동 구간이면 매수·매도 기준선이 같아져 비돌파 HOLD 분기의 `(현재가−매도기준)/(매수기준−매도기준)` 이 `ArithmeticException: / by zero`. 잘못된 파라미터 없이 **기본값에서** 발생했다. 기준선 계산 직후 한 번에 막아 같은 원인의 강도 계산 나눗셈 2곳도 함께 해소. (2) **MACD_STOCH_BB 쿨다운이 백테스트에서 영구 차단이었다** — 경과 봉 수를 `candles.size() − lastBuyCandleCount` 로 셌는데 BacktestEngine 창은 최대 500 **고정**이라 매 평가마다 size 가 같아 경과가 항상 0. 최초 BUY 이후 모든 BUY 가 막혔다. 봉 **시각** 기준으로 교체(결측봉은 시간 구간이 아니라 관측 봉 수로 센다). (3) **백테스트 평가 params 에 `timeframe` 주입** — LIVE/DYNAMIC/PAPER 는 넣는데 BacktestEngine 만 `coinPair` 만 넣어, COMPOSITE 계열이 WeightOverrideStore 를 coin·regime 수준으로만 조회했다. H1/M15 별로 저장한 가중치 override 가 **백테스트에서만** 무시돼 운영과 다른 가중치로 평가됐다. ⚠️ **(3)은 거동 변경이다** — 주입 시점부터 백테스트가 시간봉별 override 를 집으므로 **기존 저장 성과와 직접 비교 불가**. 검증: strategy-lib 82→86 · core-engine 231→233, 실패 0. 신규 회귀 6건(ATR 무변동 HOLD 1 · 쿨다운 3 · 평가 컨텍스트 2). 쿨다운 테스트는 **창 길이를 89 로 고정한 채** 슬라이딩해 구 구현이면 반드시 실패하도록 짰다. 📌 검토 문서가 놓친 발견: **COMPOSITE_BREAKOUT 정의가 3개다** — 운영(ATR0.5+VD0.3+MACD0.2+RsiVeto) / BacktestService(ATR0.4+VD0.3+RSI0.2+EMA0.1) 에 더해 `StrategySelector` static 폴백이 **구버전 구성을 등록**한다(Spring 없는 환경용). 즉 **core-engine 단위 테스트는 운영 전략을 검증한 적이 없다**. `CompositePresetRegistrar` 클래스 javadoc 도 구버전 구성 그대로. Wave 1(전략 생성 단일 팩토리 통합)에서 함께 정리할 것.
> 이전 갱신: 2026-09-15 — 🟢 **구조 점검 후속 조치 P2~P9 적용**(매매 거동 변화 없음). Grafana 대시보드 프로비저닝 신설(볼륨 유실 시 복구 불가였음) · CI 에 Playwright E2E job 추가(`docker` job 이 게이트로 받음) · `.env.example` ↔ prod compose 환경변수 불일치 해소(`STRATEGY_SIGNAL_EXIT_ENABLED` 는 prod compose 에 아예 없어 운영에서 켤 방법이 없었다) · `graphify-out/` 3,117 파일(22MB) 추적 해제 · README 재작성 · `scripts/README.md` 색인(상시용 3 / 1회성 28). 🟢 **CI 가 6개월간 red 였던 원인 3건 전부 해소**. `e2e` job 추가로 CI 를 처음 돌려 보고 드러났다. (1) `gradlew` 가 100644(실행 불가) — Windows `core.filemode=false` 탓, Linux 러너에서 `./gradlew` 즉사. (2) CI 가 테스트에 PostgreSQL 접속정보를 주입 — 테스트는 H2 인메모리라 `Driver org.h2.Driver claims to not accept jdbcUrl` 로 컨텍스트 전멸(2026-03-23부터). (3) 프런트엔드 lint error 88건. **(1)이 (2)를 가리고 있었다** — `./gradlew` 가 실행 안 돼 테스트 단계에 도달한 적이 없었다. 서비스 컨테이너 제거로 실패가 41초→5초로 앞당겨지며 특정됐다. (3)은 규칙을 끄지 않고 전부 타입으로 해소(`no-explicit-any` 86 + `set-state-in-effect` 2). ⚠️ **`any` 가 실제 결함을 가리고 있었다** — `PaperOrder` 필드 4개 누락, null 값을 객체 인덱스로 사용, LLM 집계행/목록행 키 이름 불일치가 타입 부착 후 컴파일러에 잡혔다. 검증: 백엔드 817건 · lint 0 errors · next build · E2E 44건 전부 통과. 재발 방지 주석을 ci.yml 두 자리에 남겼다. 🟢 **P1(스케줄러 풀) 실측 결과 조치 불필요**. 운영 DB 조회: RUNNING 세션 동적 7(전부 PAPER, 워치리스트 70)+고정 PAPER 40, **실거래 포지션은 2026-08-16 1건이 마지막이라 5초 reconcile 경로가 비어 있다**. 캔들 닫힘→평가 지연 p95 = PAPER M15 57초 / DYN_PAPER M15 71초로 60초 tick 주기 안. 판별 논리 — DYN_PAPER 의 M15 와 H1 은 같은 tick·같은 스레드인데 **M15 만 건강**했으므로 풀 경합이 아니다. 무거운 계산은 닫힌 캔들 갱신 때만 돌아(`lastEvaluatedClosedCandle` 스킵) 현재 부하엔 8칸으로 충분. **재검토 조건: 실거래 재개 또는 동적 세션·워치리스트 증설 시** — Grafana **스레드 포화** 패널(`executor_active_threads{name="taskScheduler"}` 가 풀 크기 8 에 붙어 있는지)로 확인. ⚠️ **`executor_queued_tasks` 는 적체가 아니다** — 스케줄러 큐에는 실행 시각 전 예약 작업이 전부 들어앉는다(운영 실측 33 = @Scheduled 34개 중 1개 실행 + 33개 대기, 정상). 2026-09-15 배포 검증 중 오독했다가 정정. ⚪ "H1 캔들 지연" 가설은 **기각** — 측정 한계였다(포지션 보유 중 POSITION_MONITORING 이라 워치리스트 미평가, SCANNING 복귀 시점 일괄 평가가 캔들 경계와 무관하게 섞임). 📌 부수 확인된 설계 특성: **포지션 보유 중에는 워치리스트 나머지 코인의 진입 신호를 관측하지 않는다**(세션당 1포지션). 보유가 긴 H1 세션일수록 신호 표본이 M15 보다 구조적으로 적게 쌓이므로 전략 비교 시 고려할 것. 상세: [`PROJECT_STRUCTURE_ANALYSIS.md`](../PROJECT_STRUCTURE_ANALYSIS.md) 13절.
> 이전 갱신: 2026-09-15 (문서 정리) — 🟢 **노후 문서 정리**. 낡은 문서가 현행처럼 읽혀 잘못된 근거가 인용되는 문제를 정리했다. `docs/README.md` 3단 분류 신설 · 과거 분석 9건 `docs/archive/` 이동 · **낡은 문서 15건에 경고 배너** · 상대 링크 406개 검사해 27개 복구 · `GRAPH_REPORT.md` cp949→UTF-8. `20260415_analy.md` 는 소스 20개 파일이 `Tier N §M` 로 인용하므로 옮기지 않고 "지적 18건 전부 해소됨" 배너만 달았다. 🟢 **전략 가이드 보강 완료**: `SINGLE_STRATEGIES_GUIDE` 11종→**14종 전부**(FVG·HeikinAshiStoch·MacdStochBb 추가) · `COMPOSITE_STRATEGIES_GUIDE` 6종→**등록 프리셋 14종 전부**(MTF 계열·REGIME_ROUTER·PULLBACK_MTF·MEANREV_BB 등 8절). ⚠️ 중간 집계 오류 정정 — "복합전략 19종"은 틀렸다. 정규식이 `_V2` 를 `_V` 로 자르고 `_BASE`·`_CB` 같은 **래퍼 내부 조립 이름**을 등록 전략으로 센 결과였다. 실제 등록 프리셋은 14종. 이 함정을 가이드 13절에 문서화했다. `COMPOSITE_BREAKOUT_VD` 는 문서에만 있고 코드엔 없어 경고 배너 추가.
> 이전 갱신: 2026-09-08 (6차) — 🟢 **정합성 감사 1~4단계 완료**. 닫힌 캔들 이름 통일 · naive UTC 17컬럼 → TIMESTAMPTZ(V77) · 규약을 테스트로 이전. **가드를 뮤테이션으로 전수 검증 — 7건 중 2건이 "통과하지만 안 잡는" 상태였다**(기존 PaperLiveAlignmentTest 포함). 가드 76건 체계 완성. 남은 미해소: BACKTEST 청산 규칙 · weight_optimizer 타임프레임.
> 이전 갱신: 2026-09-08 (5차) — 🔴 **신호 분석이 H1/M15 를 통째로 섞고 있었다**. `strategy_log` 에 timeframe 이 없어 집계가 구조적으로 (전략,코인) 으로만 묶였다 — MTF_BTC 사후 4h 가 H1 −1.428 / M15 +0.046 로 **방향이 반대인데 합산 −0.319 하나로 판정**돼 왔다. 09-04·09-07 전략 순위 분석은 코인·시각만 통제했지 타임프레임은 통제한 적이 없다. 🟢 V76 컬럼 추가 + 소비자 2곳 수정 + 테스트 6건. 🔴 `weight_optimizer_snapshot` 은 미해소.
> 이전 갱신: 2026-09-08 (4차) — 🔴 **백테스트가 실전과 다른 청산 규칙으로 돌고 있다**. BACKTEST SL 항상 5% 고정(ATR 무시)·TP 10% vs 실전 SL 5~8%·TP ≤8%, **time stop 없음**(운영 청산의 47%가 TIME_STOP). 09-08 손절 수정 후에도 WF 결과는 실전 거동을 반영하지 못한다. 🟢 `ENGINE_PARITY.md` 4엔진 매트릭스 확장 + `EngineParityTest` BACKTEST 축 4건으로 고정. 해소는 WF 재검증 완료 후 별건.
> 이전 갱신: 2026-09-08 (3차) — 🟢 **정합성 감사 1단계**. 결함들이 무작위가 아니라 하나의 패턴이었다 — **부분 수정 + 그 수정과 같은 범위의 가드**. `EngineConstantParityTest` 신설(리플렉션으로 중복 상수를 스스로 찾는다) · 공유 상수 9개 중 8개를 `TradingConstants`/`ExitRuleConfig` 로 위임해 **드리프트를 불가능하게** 함. 뮤테이션으로 기존 가드의 구멍 실증(FEE_RATE 드리프트를 기존 테스트는 놓쳤다). 거동 변화 없음.
> 이전 갱신: 2026-09-08 (2차) — 🟢 **3엔진 TP 트레일링 통합** (LIVE 급등게이트 / PAPER 매틱 / DYNAMIC 없음 → 공용 함수). 🔴 **BacktestEngine 도 같은 함수라 WF 350건 전부 0.3% 손절 기준 — 무효**. 🔴 WF 가 M15 를 검증한 적 없는데 운영 함대 40/52 세션이 M15 · M15 캔들은 4코인 0건+4코인 5개월 낡음. 🟢 flaky 테스트 제거. ⚠️ 배포 필요(매매 거동 변경).
> 이전 갱신: 2026-09-08 — 🔴 **고정코인 PAPER 손절가 버그** (설정 5% 손절이 실제 −0.43% 로 걸려 청산 534건 중 85.4% 가 휩쏘 손절, 누적 −1,270만원). LIVE 가 08-06 에 고친 것과 동일한 오류가 PAPER 전용 경로에 남아 있었다. 🟢 수정 배포 확인(position 3577 이 −5.000%) · PAPER 세션 96→40 정리 · 동적 83·91 정지. ⚠️ auto-stop 은 새 표본 n≥20 까지 계속 OFF.
> 이전 갱신: 2026-09-07 10:40 — 🟢 **통제 4종 전량 적용** (공용 유니버스 고정 · 전략 SELL 청산 OFF · HOLD 기준선 백필 · executed_funds 명시). 테스트 465건 통과. 운영 DB 보정은 스크립트로 분리(`fix_data_integrity_0907.sh`, DRY_RUN 기본).
> 이전 갱신: 2026-09-07 09:50 — 🔴 **전략 순위가 통제하면 무너진다** (사후수익 백필이 BUY/SELL 에만 돌아 **대조군이 없었다** — HOLD 74,462건 전량 미평가) · 코인·시각 통제 시 MTF_CONFIRMED −1.25%→−0.16%, MTF_BTC −0.48%→**+1.21%** · 🟢 HOLD 기준선 백필 신설 · 세션 49 총자산 손상(+212,043) 발견.
> 이전 갱신: 2026-09-04 09:50 — 🔴 **전략 검토: 문제는 전략이 아니라 배치다** (MTF 계열이 코드에 "부적합"으로 적힌 소형 알트만 사고 있다 — 함대 손실 전부) · 🔴 게이트 판단이 하루 만에 뒤집혔다(표본 30→51건) · 서킷 2건 발동(79·80) · 📦 PROGRESS.md 2,655→1,347줄 정리.
> 이전 갱신: 2026-09-03 09:13 — 🟢 **증설 효과 확인** (청산 3→17건/일, 5전략 전부 n≥20 도달 가능) · WF 통과 3→9조합 · 🔴 **게이트 ON 시뮬레이션: 수익의 91% 사라진다**.
> 이전 갱신: 2026-09-02 09:00 — 🔴 **09-14 판정이 불가능한 경로였다** (5전략 중 1개만 n≥20 도달 · 함대가 산 12조합 중 10개가 WF 미검증). 캔들·WF·세션 3종 보정 스크립트 준비. ⚠️ 초기 진단에서 candle_data 대신 market_data_cache 를 본 오류 정정 포함.
> 이전 갱신: 2026-09-02 08:15 — 🟢 **일일 점검 이상 없음**. 게이트 OFF 후 거래 회복 확정(WF 차단 0건) · WS 실시간 감시 지속 · 지문 2종은 H1/M15 구성 차이로 정상.
> 이전 갱신: 2026-09-01 저녁 — 🟢 **WS 실시간 감시 페이퍼 적용 확정** (첫 `실시간 익절(WS)` 청산, 편차 0.04%) · 🟡 텔레그램 서식 거부 시 평문 폴백 추가(“유실”은 오진 — 정정 포함).
> 오후 갱신: 2026-09-01 — 🟢 **함대 점검 이상 없음** · 🔴 **WS 실시간 감시가 오늘에서야 실제 반영** · 🔴 **LLM 응답 본문 유실 버그 수정**(SIGNAL_ANALYSIS 27건 중 24건이 비어 있었다).
> 오전 갱신: 2026-09-01 — 🔴 **PAPER 세션이 화면·안전망에서 통째로 빠져 있었다**(session_kind 하드코딩 4곳) · SL 미점검 알림 오탐 수정 · 세션 LLM 분석 신규.
> 이전 갱신: 2026-08-31 — 🔴 **COMPOSITE_PULLBACK_MTF 정지 결정** — 실거래(143거래 −3,135원)와 Walk Forward(928거래 7코인 전부 음수)가 독립적으로 같은 결론. WF 42조합 중 6개만 게이트 통과.

---

## 🔴 2026-09-18 v3 기준선 — 운영 5조합이 **전부 불합격**이다

규칙 v3 로 재실행한 25조합(운영 중인 5조합 × 대표 5코인)의 결과다.
`scripts/rebaseline_wf_0918.sh` · 전부 `exit_rules_version = 3` 으로 저장 확인.

### 결과 — 통과 0건

| 판정 | H1 | M15 | 계 |
|---|---|---|---|
| OVERFITTING | 15 | 9 | **24** |
| INSUFFICIENT_DATA | 0 | 1 | **1** |
| ACCEPTABLE / CAUTION | 0 | 0 | **0** |

`OVERFITTING` 은 **IS→OOS 성과 하락률 평균이 50% 를 넘었다**는 뜻이다(학습 구간에서 벌던 것이
검증 구간에서 절반 넘게 사라진다). 25조합 중 **18개는 OOS 기대값 자체가 음수**다.

| TF | 전략 | 코인 | 판정 | OOS 기대값 | n | 하락률 |
|---|---|---|---|---|---|---|
| H1 | MEANREV_BB | BTC / DOGE / ETH / SOL | OVERFITTING | −0.566 / −0.652 / −0.204 / −1.358 | 5 / 5 / 10 / 10 | 1.93 / 0.83 / 1.73 / 1.17 |
| H1 | MEANREV_BB | **XRP** | OVERFITTING | **+1.919** | 9 | 1.26 |
| H1 | MOMENTUM_ICHIMOKU_V2 | BTC / DOGE / ETH | OVERFITTING | −0.341 / −0.535 / −0.687 | 26 / 25 / 27 | **10.08** / 1.35 / 4.57 |
| H1 | MOMENTUM_ICHIMOKU_V2 | SOL / XRP | OVERFITTING | +0.059 / +0.135 | 28 / 15 | 2.20 / 0.99 |
| H1 | MTF_CONFIRMED | BTC / DOGE / ETH / XRP | OVERFITTING | −0.552 / −1.407 / −1.146 / −0.743 | 12 / 16 / 16 / 10 | 1.31 / 0.91 / 4.51 / 1.26 |
| H1 | MTF_CONFIRMED | SOL | OVERFITTING | +0.336 | 16 | 0.83 |
| M15 | MEANREV_BB | BTC / DOGE / ETH | OVERFITTING | −0.780 / −0.149 / −0.473 | 10 / 12 / 8 | 1.70 / 1.00 / **8.82** |
| M15 | MEANREV_BB | SOL | OVERFITTING | +0.576 | 19 | 1.31 |
| M15 | MEANREV_BB | **XRP** | **INSUFFICIENT_DATA** | −1.766 | 7 | — |
| M15 | MTF_CONFIRMED | BTC / ETH / SOL / XRP | OVERFITTING | −0.157 / −0.580 / −0.244 / −0.142 | 27 / 43 / 37 / 31 | 0.62 / 7.33 / 1.01 / 0.83 |
| M15 | MTF_CONFIRMED | DOGE | OVERFITTING | +0.193 | 49 | 0.86 |

### 🔴 가장 나쁜 칸 — 유일한 **실자본 고정 세션**의 조합이다

`MEANREV_BB / KRW-XRP / M15` 는 지금 돌고 있는 고정 세션의 정확한 조합인데,
표본이 7건(하한 5 이상)인데도 `INSUFFICIENT_DATA` 가 나왔다. 사유가 표본 부족이 아니다 —
**IS 수익률이 양수인 윈도우가 하나도 없어서** 하락률을 잴 대상 자체가 없었다는 뜻이다
(`dropRateSamples == 0`). 즉 **학습 구간에서조차 벌지 못했다.** OOS 기대값도 −1.766 이다.

### 이 숫자를 어떻게 읽을 것인가

v2 시절 09-09 재검증은 80조합 중 10개 통과(12.5%)였다. 지금은 25조합 중 **0개**다.
**직접 비교는 할 수 없다** — 조합 구성도 다르고(그때는 전 전략, 지금은 운영 중인 것만),
무엇보다 v3 는 다른 전략을(구성 통일) 다른 손익 정의로(진입 수수료 차감) 미래를 보지 않고
(look-ahead 제거) 잰 값이다. **v3 가 더 정직한 숫자이고, 그 정직한 숫자가 전부 불합격이다.**

### 그래서 지금 당장 무슨 일이 일어나는가 — 아무 일도 일어나지 않는다

`REQUIRE_WALK_FORWARD_GATE` 가 **false** 라 이 판정은 거래를 막지 않는다.
바꿔 말하면 **지금 운영 함대는 전부 "WF 불합격" 조합으로 돌고 있다.**
게이트를 켜면 5조합 전부 정지한다.

### 결정이 필요한 것 (미결)

1. **게이트를 켤 것인가** — 켜면 함대가 멈춘다. 08-31 에 한 번 같은 일을 겪고 껐다(신호 18건 중 17건 차단).
2. **전략을 바꿀 것인가** — 통과하는 조합을 먼저 찾아야 한다. 운영 5조합 밖을 탐색해야 한다는 뜻이다.
3. **기준이 과한가를 되물을 것인가** — 하락률 50% 는 임의 임계값이다. 다만 **OOS 기대값 음수 18/25** 는
   임계값과 무관한 사실이라, 기준을 완화해도 대부분은 살아나지 않는다.

⚠️ **이 기준선은 메이저 5종만 덮는다.** 동적 세션은 실제로 ONDO(20)·TRUMP(19)·ENA/XLM/LA(각 14)
같은 소형 알트도 산다(최근 6개월 포지션 기준). 09-04 분석의 "MTF 계열이 부적합으로 적힌 소형
알트만 사고 있다" 가 유효하다면 **실매매 대상은 여기서 측정되지 않았다.** 알트는 캔들 이력이 짧아
3년 WF 가 성립하지 않을 수 있어 별도 판단이 필요하다.

---

## 🔴 2026-09-18 감시범위 확장 측정 — "통과 7건"은 덫이다

`wf_watchlist_0918.sh` 로 19코인 × 3전략을 WF 57 + 백테스트 57 = **114조합** 돌렸다.
(이날 누적 WF 82건, 전부 `exit_rules_version=3`.)

### 판정만 보면 통과가 나왔다

| 판정 | 건수 |
|---|---|
| OVERFITTING | 64 |
| INSUFFICIENT_DATA | 11 |
| ACCEPTABLE | **4** |
| CAUTION | **3** |

### 그런데 7건을 뜯어보면 전부 같은 이유다

| 조합 | 판정 | OOS n | 백테스트 절대수익 |
|---|---|---|---|
| MTF_CONFIRMED / WLD | ACCEPTABLE | **5** | +7.8% (10거래) |
| MTF_CONFIRMED / ONDO | CAUTION | **5** | +11.6% |
| MOMENTUM_V2 / MIRA | ACCEPTABLE | **9** | **−4.8%** |
| MOMENTUM_V2 / WLD | CAUTION | **7** | +21.6% (17거래) |
| MEANREV_BB / WLFI | ACCEPTABLE | **7** | +0.8% (6거래) |
| **MOMENTUM_V2 / SUI** | ACCEPTABLE | **31** | **−22.1%** |
| MEANREV_BB / ONDO | CAUTION | **14** | +9.8% |

6건은 OOS 표본이 5~14건이다 — 판정 하한이 5이니 **하한에 걸친 잡음**이다.
표본이 쓸 만한 건 SUI 하나(n=31)인데 **절대 수익률이 −22.1%** 다. SUI 가 −47.3% 떨어져서
"통과"한 것이지 번 것이 아니다. 그리고 7건이 **전부 폭락한 알트**에 몰려 있다.

### 🔴 근본 원인 — 시장 노출률 중앙값 **3.0%**

거래 기록으로 실제 보유 시간을 재 보니, 이 전략들은 **전체 기간의 3% 만 시장에 들어가 있다**
(최대 MOMENTUM_V2/ETH 10.2% · 최소 MEANREV_BB/LA 0.1%).

그래서 단순보유(100% 노출) 대비 알파가 **코인 방향과 기계적으로 일치**한다:

```
폭락 코인  ARB −93% · ONDO −73% · MIRA −93% · WLD −70%   →  알파 +78 ~ +92%p
급등 코인  SOL +1045% · BTC +418% · XRP +343%            →  알파 −291 ~ −1060%p
```

**이건 실력이 아니라 노출 감소다.** 현금을 97% 들고 있으면 자동으로 나오는 모양이다.

### 그래서 "통과가 나오면 배치 문제"라는 판독 기준은 틀렸다

착수 전에 세운 기준("통과가 하나라도 나오면 배치 문제")을 **정정한다.** 통과는 나왔지만
통과한 이유가 "안 샀기 때문"이라 그 기준이 성립하지 않는다.

🔴 **WLD·ONDO·MIRA·WLFI 로 세션을 재배치하지 말 것.** 이 데이터가 놓은 덫이다 —
"이 코인에서 전략이 먹힌다"가 아니라 "이 코인이 죽는 동안 안 샀다"이다.
옮기면 **하락장에 과적합**하는 것이다.

### 설명이 필요한 예외 하나 — MOMENTUM_ICHIMOKU_V2 / KRW-DOGE

유일하게 **오른 코인에서 실제 표본으로 이겼다.**

```
백테스트 +109.2%  vs  단순보유 +32.5%   알파 +76.7%p
145거래 · 승률 44.1% · PF 1.44 · MDD −21.6% · 노출 8.4%
```

노출 8.4% 로 보유의 3배를 냈다. 다만 **WF 는 OVERFITTING**(OOS n=25~26)이다.
배포 후보가 아니라 **조사 대상**이다 — 왜 DOGE 에서만 이런지가 실마리일 수 있다.

### 유의 — 코인 단위 백테스트는 함대의 동작 모델이 아니다

노출 3% 는 **코인 하나** 기준이다. 동적 세션은 워치리스트 ~20종을 돌며 먼저 뜨는 것을 잡으므로
함대의 실제 노출은 훨씬 높다. 함대를 재는 단위는 함대 자신이고, 그건 08-31 규칙 동결로
이미 돌고 있다(09-14 측정 알파 −0.571%p, n=23).

### 조치

- 🟢 **세션 200 (REAL · MEANREV_BB/KRW-XRP/M15) 정지** — WF 는 학습 구간에서조차 수익 윈도우 0개,
  백테스트는 −13.5%/11거래/노출 0.7%(같은 기간 보유 +343%). 어느 쪽에도 근거가 없었다.
- 🟡 **세션 92 (REAL · MOMENTUM_ICHIMOKU_V2/H1) 는 유지 중** — 판단 대기.

---

## 🔴 2026-09-18 캔들 신선도 스케줄러가 조용히 손을 뗐다

위 측정을 준비하다 발견. `candle_data` H1 최신일 분포(오늘 09-18):

```
2026-09-17   2종   ← ETH·XRP (고정 격자)
2026-09-07   8종
2026-09-01   6종
2026-08-31   9종
2026-08-30  30종   ← 19일 밀림
```

원인은 `CandleDataFreshnessScheduler` 의 두 가지 설계다.

**① 대상이 8종 고정이다.** `candle-freshness.coins` 기본값이
BTC·ETH·XRP·SOL·ADA·DOGE·LINK·AVAX 뿐이다. **동적 워치리스트 코인은 자동 갱신 대상이 아니다** —
ONDO·TRUMP·ENA·MIRA·WLFI 가 밀려 있고, NEAR(60일 3,885회 평가)·KAITO·SHIB·O·ONG·ZAMA·
SOPH·TREE·CHIP·CAP 은 캔들이 **0건**이다. **감시는 하면서 검증할 수단이 없다.**

**② 갭이 7일을 넘으면 영구히 포기한다.** `MAX_GAP_DAYS = 7` 을 넘으면 건너뛴다. 한 번 밀리면
갭은 매일 커지기만 하므로 **되돌아오지 못한다.** 실제로 설정된 8종 중 6종이 이미 그 상태다
(09-07 = 11일 갭). 신선한 건 ETH·XRP 둘뿐이다.

**③ 알림이 없다.** 건너뛴 조합은 `log.warn` 만 남는다. 아무도 보지 않는다.
09-14 에도 "candle_data 가 일주일 멈춰 있었다"를 기록했는데 **같은 일이 반복됐다** —
그때는 증상을 고쳤고 이 구조는 그대로였다.

### 🔴 그리고 범위를 제대로 재 보니 훨씬 컸다

최근 30일 100회 이상 평가된 코인(= 함대의 실제 감시 범위)이 **104종**인데:

```
캔들 0건   56종   ← NEAR·SHIB·BCH·INJ·FIL·SAND·JUP·CRO ... 절반이 넘는다
밀린 것    48종   ← 그중 30종이 08-30 정지(19일)
최신        2종   ← ETH·XRP (고정 격자)
```

**함대가 감시하는 코인의 절반 이상을 백테스트·WF 가 검증할 수 없다.**
09-18 의 "통과 0건" 기준선은 나머지 46%(48/104) 안에서, 그마저 밀린 데이터로 잰 것이다.

### 조치

- 🟢 **스케줄러 3구멍 수정** — 대상을 `strategy_log` 최근 감시 기록에서 산출(설정값은 하한으로만
  유지) · 긴 갭은 건너뛰는 대신 **7일 조각으로 잘라 요청**(한 실행 40요청 상한) ·
  캔들 이력이 없는 조합은 **텔레그램 알림**. 요청 간격을 설정값으로 빼 테스트 가능하게 했다.
  회귀 5건 신설, **뮤테이션 3건 전부 확인**(갭 건너뛰기 복원 / 감시기록 무시 / 알림 제거 → 각각 실패).
- 🟢 **`scripts/backfill_candles_0918.sh` 신설** — 이미 벌어진 갭은 스케줄러가 스스로 못 메운다.
  STEP=1 밀린 48종 / STEP=2 캔들 0건 56종(2024-01-01~). all-or-nothing 수집이 긴 사슬에서
  통째로 실패하므로 단계를 나눴다. `--status` 로 커버리지 확인.

🔴 **Wave 3 의 선행 조건이다.** L(MTF 시각 경계)을 고치면 규칙 버전을 올리고 재측정해야 하는데,
캔들이 밀려 있으면 **재측정 자체가 불가능**하다.

---

## 🟢 2026-09-20 캔들 백필 결과 — 그리고 09-18 수정이 만든 새 결함

### 백필 결과

```
감시 104종 커버리지      09-18        09-20
  최신(2일 이내)           2종   →     85종
  캔들 0건                56종   →     18종
  밀림                    48종   →      0종
```

`candle_data` H1 이 95종 140만 건이 됐고 09-19 까지 들어와 있다. 밀린 48종은 전부 따라잡았다.

남은 18종(ONG·TREE·SOPH·BLAST·HOME·ZKP·BCH·SHIB·FIL·SAND·SC·JUP·WAXP·PUNDIX·PROS·CVC·
STORJ·INJ)은 **목록에서 빠진 것이 아니라 STEP 2 에 넣었는데 한 건도 안 들어온 것**이다.
BCH·SHIB·FIL·SAND·JUP·INJ 는 상장 이력이 충분한 코인이므로 데이터가 없어서가 아니라,
`UpbitCandleCollector` 의 all-or-nothing 수집이 **긴 사슬 중 한 번 실패로 통째로 버린** 것이다
(09-08 에 M15 822회 요청이 6/6 실패한 것과 같은 모양). 29종·27종 덩어리도 아직 컸다 —
재시도 시 더 잘게 쪼갤 것.

🟡 **M15 커버리지는 22종뿐이다.** 백필 스크립트를 H1 만 돌게 짰는데, 운영에 M15 세션이 있다
(동적 86 · MTF_CONFIRMED/M15). "알트는 M15 캔들이 없으니 H1 만" 이라고 정했던 것이 원인과
결과를 뒤집은 판단이었다 — 없어서 안 쓴 게 아니라 **안 채워서 없는** 것이다.

### 🔴 09-18 수정이 새 기아(starvation) 결함을 만들었다

09-20 새벽 스케줄러 첫 실행에서 **"요청 상한 도달" 알림만 오고 "수집 불가" 알림은 오지 않았다.**
그 조합이 결함을 가리켰다.

상한에 걸렸을 때 `break outer` 로 **루프 전체를 끊었는데**, 순회가 고정 순서
(`for tf { for coin }`)였다. 그래서:

- H1 처리 도중 상한에 걸리면 **M15 는 평가조차 되지 않는다** → 이력 없는 조합이
  `needsBackfill` 에 담기지 않아 **알림이 아예 안 온다**. 실측에서 정확히 그랬다.
- 목록 뒤쪽 코인은 **매 실행마다** 도달하지 못해 영원히 굶는다.

**고치기 전 결함(갭 7일 초과 시 영구 포기)과 같은 형태다** — 조용히, 일부만, 영원히 빠진다.

🟢 수정: ① 분류(맵 조회뿐이라 비용 없음)는 상한과 무관하게 **끝까지** 훑는다
② 실제 요청은 **가장 많이 밀린 것부터** 처리한다(`gapDays` 내림차순). 밀린 순으로 돌면
한 번 채워진 조합은 다음 실행에서 뒤로 가므로 **스스로 균형이 맞는다.**
회귀 2건 추가(상한 도달 시에도 이력없음 전수 보고 / 목록 뒤쪽 최다지연 조합 우선),
**뮤테이션 2건 확인**(정렬 제거 / 분류 중단 → 각각 실패).

---

## 📋 Wave 3 · Wave 4 — 남은 전략 검토 항목 (`STRATEGY_LIBRARY_REVIEW.md` 후속)

Wave 0~2 는 완료(09-18). 아래는 **미착수**다. 착수 순서는 기준선 확보 뒤로 미뤘다 —
ADX 를 먼저 건드리면 v3 기준선과 섞여 무엇이 무엇을 바꿨는지 분리할 수 없다.

### Wave 3 — 지표 계산 정확도 (구현 6일 / 재검증 10일)

| 항목 | 내용 | 구현 | 재검증 | 판단 |
|---|---|---|---|---|
| **K. ADX Wilder 평활** | ADX 가 단순평균으로 계산되고 `adxList` 의 창 길이·개수도 정의와 다르다 | 1.5일 | **5일+** | ⚠️ **가성비 최악.** 12개 파일이 ADX 에 의존하고, 임계값 20/25/35 가 전부 **현재(잘못된) 분포에 튜닝**돼 있다. 고치면 국면 분류가 통째로 바뀌어 국면별 성과표까지 재계산해야 한다 |
| **I. GRID 레벨 해제** | 매수 레벨과 해제 레벨이 구조적으로 겹치지 않아 한 번 잡은 레벨이 사실상 안 풀린다 | 1.5일 | 2일 | 격자 전략 거동에 직결 |
| **L. MTF 시각 경계 집계** | 상위 타임프레임 봉을 조회 시작점 기준으로 묶어, **언제 조회했느냐에 따라 상위봉 구성이 달라진다** | 2일 | 2일 | MTF 계열이 운영 3조합 중 2개를 차지 → 우선순위 높음 |
| **H. RSI/VolumeDelta 무변동 경계** | 가격·거래량이 전혀 안 움직일 때의 경계 처리 | 1일 | 1일 | 저위험. Wave 0 의 ATR=0 과 같은 부류 |

**권장 순서: L → I → H → K.** L 은 운영 비중이 가장 크고, K 는 비용이 가장 큰데 편익이
"정확해진다" 에 그친다(성과가 좋아진다는 근거는 없다). K 는 임계값 재튜닝을 **함께** 계획하지
않으면 착수 자체가 손해다.

### Wave 4 — 계약·구조 (미산정)

| 항목 | 내용 | 판단 |
|---|---|---|
| **M. 파라미터 스키마 단일화** | 같은 이름의 파라미터가 곳에 따라 단위가 다르다 — `stopLossPct` 가 **1.5(%) vs 0.02(비율)** 로 공존한다 | 조용한 오작동을 만드는 부류. Wave 3 보다 위험도가 높을 수 있다 |
| **N. 워밍업/최소 캔들 계약** | 전략마다 요구 캔들 수 정의가 제각각이고 강제되지 않는다 | 중 |
| **O. 신호 intent 분리** | BUY/SELL/HOLD 에 "진입/청산/관망"이 뒤섞여 있다 | 🔴 **지금 하지 말 것.** 전 전략·전 엔진을 건드린다. 기준선이 안정되기 전엔 착수 불가 |

### Wave 2 의 남은 꼬리 (미해소)

- **Hold-out 게이트가 운영에서 발동할 수 없다.** `VERDICT_HOLD_OUT_FAILED` 와 차단 연결은
  만들었지만, `WalkForwardBatchRequest` 에 `holdOutCutoff` 필드가 없어 **운영 경로가 홀드아웃을
  넘기지 않는다.** 현재 순수 테스트 전용이다. 연결은 별건이며, 연결하면 지금보다 판정이 더 엄해진다.

---

### 🟡 2026-09-14 첫 주 성적 — 손절 수정은 성공, 그런데 시장에는 지고 있다

손절 수정 배포 후 첫 점검(청산 n=23, 09-08~09-12).

### 손절 수정은 확실히 작동한다

| 지표 | 수정 전 | 지금 |
|---|---|---|
| 손절 폭 실측 | −0.43% | **−5.000%** (n=27) |
| 평균 보유시간 | 0.9시간 | **17.2시간** |
| STOP_LOSS 비중 | 85.4% | **13.0%** |
| TIME_STOP | 경로 없음 | 47.8% |

TIME_STOP 47.8% 는 백테스트에 time stop 을 넣은 근거였던 운영 실측치(47%)와 일치한다.

### ⚠️ 벤치마크를 잘못 잡아 한 번 오판했다 — 기록해 둔다

처음에 *"함대 −1.21% vs 시장 −4.26%, +3%p 우위"* 로 읽었다. **틀렸다.**
−4.26% 는 5일 단순보유 수익률인데 실제 포지션 평균 보유는 **17시간**이다.
**보유 구간이 다른 것을 비교했다.**

포지션마다 **그 포지션의 보유 구간 동안** 그 코인이 얼마나 움직였는지로 다시 재니:

```
함대 −1.238%   시장 −0.667%   알파 −0.571%p     ← 시장에 졌다
```

교훈이 명확하다 — **벤치마크는 기간·대상이 정확히 겹쳐야 한다.** 안 그러면 부호까지 뒤집힌다.
`scripts/status.sh` 를 알파 기준으로 고쳐 같은 실수가 반복되지 않게 했다.

### 질문② 중간 경과 — 방향이 좋지 않다

| 그룹 | n | 시장 이긴 비율 | 평균 알파 | 알파 σ |
|---|---|---|---|---|
| WF PASS | 10 | **0.0%** | **−1.095%p** | 0.875 |
| WF FAIL | 13 | 46.2% | −0.168%p | 0.831 |

**WF 통과 조합이 10전 10패.** 표준오차(0.875/√10 ≈ 0.277) 대비 알파가 4σ 아래라
우연으로 보기 어렵다.

**다만 확정은 아니다**: PASS 청산 10건 중 **7건이 DOGE** 라 "PASS" 와 "DOGE" 가 완전히
겹친다. 지금 숫자는 *"DOGE 매매가 DOGE 보유보다 나빴다"* 이상을 말하지 못한다.
DOGE 아닌 PASS 조합의 표본이 쌓여야 갈린다.

### 코인별 알파

| 코인 | n | 함대 | 시장 | 알파 |
|---|---|---|---|---|
| AVAX | 4 | −0.14 | −0.63 | **+0.49** |
| SOL | 2 | +0.37 | +0.60 | −0.23 |
| BTC | 2 | −0.03 | +0.24 | −0.27 |
| PROM | 4 | −3.01 | −2.61 | −0.40 |
| ADA | 2 | +1.51 | +1.99 | −0.48 |
| EUL | 1 | +0.86 | +1.90 | −1.04 |
| LINK | 1 | −3.66 | −2.61 | −1.06 |
| DOGE | 7 | −2.40 | −1.05 | **−1.35** |

8코인 중 알파가 양수인 것은 **AVAX 하나**다.

### `candle_data` 가 일주일 멈춰 있었다

| 테이블 | 최종 | 용도 |
|---|---|---|
| `market_data_cache` | 2026-09-13 | 매매 (매 틱 갱신) |
| **`candle_data`** | **2026-09-07** | **백테스트·WF** |

`candle_data` 는 배치 수집 API 가 호출될 때만 채워지고 **스케줄러가 없었다.**
WF 가 최근 구간을 통째로 못 본 채 — 에러도 경고도 없이 — 판정을 내던 상태다.
2026-08-24 에 48조합 중 25개가 데이터 부족으로 조용히 누락된 것과 같은 뿌리다.

→ `CandleDataFreshnessScheduler` 신설. 매일 04:10 KST, **갭만**, 7일 초과 갭은 건너뛰고
경고(긴 구간은 연 단위로 쪼개는 백필 스크립트 몫 — 09-08 에 822회 사슬이 통째로 실패한 전례).

### 조치

| | 내용 | 상태 |
|---|---|---|
| 1 | `status.sh` 알파 기준 전환 | 완료 |
| 2 | `scripts/replace_eul_prom_0914.sh` — EUL·PROM → ETH·XRP | 스크립트 준비, 운영 실행 필요 |
| 3 | `CandleDataFreshnessScheduler` | 코드 완료, 배포 필요 |

EUL·PROM 은 M15 캔들이 4,138 / 2,540 행뿐이다(정상 코인 163,880). 격자의 25% 가
**판정 불가** 상태로 놀고 있었고, PROM 은 시장이 오른 구간에서 손실을 낸 유일한 코인이다.
ETH·XRP 는 다른 6코인과 같은 2021-12-31 부터 전 이력이 있다.

### 실전 전환 조건 (날짜가 아니라 조건)

1. WF PASS 그룹 n ≥ 30 · 2. PASS 평균 알파 > 0 · 3. PASS 알파 > FAIL 알파
4. DOGE 비중 < 50% · 5. 상승·하락 국면이 모두 표본에 있을 것

빨라야 3~4주. 5번 때문에 더 걸릴 수 있다.

**예외** — 체결 오차 측정용 소액 LIVE 1세션(10만원)은 위 조건과 무관하게 진행 가능하다.
페이퍼로는 체결 지연·부분 체결·실제 슬리피지를 절대 알 수 없고, `ExecutionDriftTracker`
(`execution_drift_log`)가 이미 LIVE 경로에 붙어 있다.

---

## 🟢 2026-09-09 (2차) WF 판정 표본 하한 — "잴 것이 없다"를 "양호"로 읽던 문제

09-09 재검증 결과를 읽다 발견한 결함 2건을 고쳤다. 둘 다 **판정을 관대한 쪽으로 틀리게**
만드는 종류다.

### ① `verdict` 가 표본 크기를 보지 않았다

과적합 점수(`avgDropRate`)는 **인샘플 수익률이 양수인 윈도우**에서만 계산된다. 거래가 거의
없으면 그런 윈도우가 하나도 없고, 그러면 `dropRateSamples == 0` 이라 점수가 0 으로 남아
**가장 좋은 판정인 `ACCEPTABLE`** 이 나왔다 — "하락이 없다" 가 아니라 "잴 것이 없다" 인데도.

운영 실측 (09-09 재검증 80조합):

```
MTF_BTC   / KRW-EUL  / M15  →  ACCEPTABLE,  기대값 −4.160%,  n=2
ICHIMOKU  / KRW-PROM / H1   →  ACCEPTABLE,  기대값  0.000%,  n=0
```

`WalkForwardTestRunner.empty()` 도 같은 모양이었다 — **캔들이 부족해 윈도우를 하나도 만들지
못한 경우에도 `ACCEPTABLE`** 을 돌려줬다.

게이트는 자체 하한(`MIN_TRADES=5`)으로 걸러 실피해는 없었다. 하지만 **화면·텔레그램·리포트는
verdict 만 본다** — 거기서는 "양호"로 읽혔다. 판정을 내는 쪽에서 막는 것이 옳다.

**조치**: `INSUFFICIENT_DATA` 판정 신설(`MIN_OOS_TRADES_FOR_VERDICT = 5`).
"과적합이 아니다"와 "아무 말도 할 수 없다"를 값으로 구분한다. 게이트는 검증 이력이 없는 것과
동일하게 차단하고, 텔레그램은 `➖` 로 별도 집계한다.

### ② 배치 요약이 존재하지 않는 판정값을 세고 있었다

`BacktestJobService.buildWalkForwardSummary` 가 `"ROBUST"` 를 세는데, **어떤 코드도 그 값을
만들지 않는다**(러너가 내는 값은 `ACCEPTABLE`/`CAUTION`/`OVERFITTING`). `robustCount` 는 항상 0
이었고, `overfittingCount` 를 뺄셈으로 구하는 탓에 **양호 판정이 전부 과적합으로 집계**됐다.

**조치**: `ACCEPTABLE` 을 세도록 수정하고 `insufficientCount` 를 추가. 기존 화면 호환을 위해
`robustCount` 키에도 같은 값을 싣는다.

### 검증

전체 스위트 통과. 신규 `WalkForwardVerdictSampleFloorTest`(4) + 게이트 테스트 1건 추가.
뮤테이션 3/3 잡음 — 표본 하한 제거 · `empty()` 를 ACCEPTABLE 로 되돌림 · 게이트가
`INSUFFICIENT_DATA` 를 통과시킴.

### 재실행이 필요한가

**아니다.** 이 수정은 판정 문자열만 바꾸며 백테스트 계산 자체는 건드리지 않으므로
`EXIT_RULES_VERSION` 은 2 그대로다. 다만 09-09 재검증 결과의 `verdict` 컬럼에는 옛 값이
남아 있으므로, 화면에서 EUL/PROM 조합이 여전히 `ACCEPTABLE` 로 보인다 — 다음 WF 실행부터
`INSUFFICIENT_DATA` 로 바뀐다. 게이트 판정은 이미 표본 하한으로 막고 있어 영향 없다.

---

## 🟢 2026-09-09 WF 전면 재검증 결과 — 80조합 중 10개만 살아남았다

청산 규칙을 네 경로에 통일한 뒤(V78, `exit_rules_version=2`) 5전략 × 8코인 × {H1, M15}
= 80조합을 다시 돌렸다. **누락 없이 80건 전부 완료**(H1 40 / M15 40).

### 결과 — OVERFITTING 61/80 (76%)

| | 조합 | OVERFITTING | 게이트 통과 |
|---|---|---|---|
| **H1** | 40 | **34 (85%)** | **2** |
| **M15** | 40 | 27 (68%) | 8 |

`OVERFITTING` 은 "기대값이 음수"가 아니라 **인샘플 성적이 아웃오브샘플에서 유지되지 않는다**는
뜻이다. H1 은 사실상 전멸했고, 남은 2개 중 하나(`MEANREV_BB/AVAX` **+0.023%**, n=11)는
왕복 수수료에도 못 미치는 노이즈다. H1 에서 실질적 근거를 가진 것은
`MTF_BTC/KRW-SOL`(+0.319%, n=45) 하나뿐이다.

### 통과한 10조합

| TF | 전략 | 코인 | verdict | exp | n |
|---|---|---|---|---|---|
| M15 | `MEANREV_BB` | SOL | ACCEPTABLE | +1.991% | 16 |
| M15 | `MEANREV_BB` | LINK | ACCEPTABLE | +1.216% | 20 |
| M15 | `MTF_BTC` | AVAX | ACCEPTABLE | +0.756% | 77 |
| M15 | `MTF_CONFIRMED` | LINK | CAUTION | +0.256% | 53 |
| M15 | `MTF_CONFIRMED` | DOGE | ACCEPTABLE | +0.204% | 75 |
| M15 | `MTF_BTC` | DOGE | CAUTION | +0.199% | 110 |
| M15 | `ICHIMOKU` | DOGE | ACCEPTABLE | +0.144% | 107 |
| M15 | `ICHIMOKU_V2` | DOGE | ACCEPTABLE | +0.114% | 149 |
| H1 | `MTF_BTC` | SOL | CAUTION | +0.319% | 45 |
| H1 | `MEANREV_BB` | AVAX | ACCEPTABLE | +0.023% | 11 |

**DOGE 편중** — 10개 중 4개가 DOGE 다. BTC · ADA · EUL · PROM 은 어떤 전략으로도 통과하지
못했다. 전략의 우위인지 코인 특성인지 아직 갈리지 않았다는 뜻이며, 특히 M15 4전략 중
3개가 **오직 DOGE 로만** 통과한다.

표본이 큰 쪽(n≥75)은 기대값이 전부 +0.1~0.2%대로 얇고, 두꺼운 쪽(+1.2~2.0%)은 n=16~20 으로
표본이 작다. **둘 다 아직 확정적이지 않다.**

### 함대 대조 — 페이퍼 40세션이 WF 격자와 1:1

```
                                 ADA  AVAX   BTC  DOGE   EUL  LINK  PROM   SOL
COMPOSITE_MEANREV_BB               O     E     E     O     O     P     N     P
COMPOSITE_MOMENTUM_ICHIMOKU        O     O     O     P     N     O     N     O
COMPOSITE_MOMENTUM_ICHIMOKU_V2     E     O     O     P     N     O     N     O
COMPOSITE_MTF_BTC                  O     P     E     P     N     O     N     O
COMPOSITE_MTF_CONFIRMED            O     O     O     P     N     P     N     O
```
`P` 근거 있음 8 · `O` OVERFITTING 19 · `N` 표본부족 9 · `E` 기대값≤0 4

**32세션에 근거가 없는 것은 결함이 아니다.** WF 게이트를 PAPER 에 걸지 않는 것은 08-06 의
의도된 설계다 — *"페이퍼는 그 자격을 얻기 전에 검증하는 도구"*. 이 표의 값어치는 차단 목록이
아니라 **승격 후보가 8개로 좁혀졌다**는 것이다.

### 실자본 노출 0 — 조치할 것 없음

| 구분 | 세션 | 모드 |
|---|---|---|
| 고정코인 | 40 | PAPER (전부 M15) |
| 동적 | 12 | **전부 PAPER** (H1 6 / M15 6) |
| LIVE | 0 | — |

`gateEnabled=false` 이고, `DynamicTradingService.createSession` 의 WF 게이트 호출은
`if (!isPaper)` 안에 있어 페이퍼 동적 세션은 애초에 게이트를 타지 않는다(설계대로).
따라서 이번 판정으로 막히거나 위험해진 것은 없다.

다만 **게이트를 켜게 되면** 동적 H1 6세션 중 4개(`ICHIMOKU`×2 · `ICHIMOKU_V2` · `MTF_CONFIRMED`)는
통과 조합이 하나도 없어 실자본 전환이 불가능하다는 점은 기록해 둔다.

### V78 가드가 운영에서 실제로 동작했다

게이트 판정에서 구버전 근거를 가진 전략들이 정확히 거부됐다:

```
COMPOSITE_PULLBACK_MTF   25개 조합 전부 FAIL — 실행 규칙 v미상 < 현재 v2
COMPOSITE_BREAKOUT        5개 조합 전부 FAIL
COMPOSITE_MOMENTUM        5개 조합 전부 FAIL
HEIKIN_ASHI_STOCH         1개 조합 FAIL
```

이 36조합은 어제까지 "PASS" 근거로 남아 있었다. 재실행되지 않았으니 V78 이 없었다면
**SL 5% 고정 · TP 10% · time stop 없음으로 나온 성적으로 계속 통과**했을 것이다.

### 데이터에서 드러난 결함 2건 (신규, 미해소)

**① `verdict` 가 표본 크기를 보지 않는다.**

```
MTF_BTC / KRW-EUL / M15   → ACCEPTABLE, exp −4.160%, n=2
ICHIMOKU / KRW-PROM / H1  → ACCEPTABLE, exp  0.000%, n=0
```

거래가 0~2건인데 ACCEPTABLE 이 나온다. 인샘플·아웃샘플이 둘 다 비면 "성과 하락 없음"으로
계산되기 때문이다. 게이트는 `MIN_TRADES=5` 로 막지만 **verdict 자체는 표본이 작을 때
무의미**하다 — 화면·리포트에서 verdict 만 보면 오판한다. 판정에 표본 하한을 넣어야 한다.

**② EUL · PROM 은 검증이 구조적으로 불가능한데 워치리스트에 있다.**

상장이 2026-07-26 / 08-12 라 19조합이 n≤4 다. 고정 격자에서 10세션(5전략 × 2코인)을
쓰는데 어느 것도 판정을 낼 수 없다. 08-31 에 기록한 *"지표를 못 채우는 코인과 돈을 잃는
코인이 같은 집합"* 과 같은 대상이다.

### 이번 작업이 처음으로 가능하게 만든 것

09-08 이전에는 백테스트와 페이퍼가 **다른 청산 규칙**으로 돌아서 둘을 비교할 수 없었다.
이제 같은 규칙이므로, 위 8개 `P` 조합의 **실제 페이퍼 성적 vs WF 기대값**을 직접 대조할 수 있다.
두 값이 어긋나면 백테스트가 여전히 현실을 모사하지 못한다는 뜻이고, 맞으면 그때 승격을
논할 근거가 생긴다. **`kill-criteria.auto-stop` 은 그 대조 전까지 계속 OFF.**

---

## 🟢 2026-09-09 미해소 결함 1~5 전량 해소

09-08 감사가 남긴 미해소 5건을 순차로 처리했다. **전부 "축이 하나 빠져 있다" 또는
"규칙이 한 경로에만 적용됐다" 한 가지 모양**이다.

| # | 결함 | 조치 | 마이그레이션 |
|---|---|---|---|
| 1 | BACKTEST 청산 규칙이 실전과 다름 | 공식을 `core-engine`(`ExitRuleFormula`)로 올려 4경로 공유 + 규칙 버전 게이트 | V78 |
| 2 | `weight_optimizer_snapshot` 타임프레임 없음 | 2층 누적 + 4단 폴백, 세 엔진이 `timeframe` 주입 | V79 |
| 3 | 전략 SELL 게이트 네 벌 중복 | `SignalExitGate` 단일 판정 | — |
| 4 | `tickCandleCache` PAPER 전용 | `TickCandleCache`(ThreadLocal 스코프) 세 엔진 공용 | — |
| 5 | 폐기 판정이 재생성을 못 막음 | `strategy_timeframe_enabled` 신설 + 게이트 연결 | V80 |

### 1번 — 원인은 "잊었다"가 아니라 패키지 배치였다

`BacktestEngine`(core-engine)이 실전 공식(`ExitRuleCalculator`, web-api)을 **모듈 의존
방향상 호출할 수 없었다.** 그래서 백테스트만 SL 5% 고정 · TP 10% · time stop 없음으로 돌았고,
운영 청산의 **47%(83건 중 39건)를 차지하는 TIME_STOP 경로가 아예 없어 거래 모집단이 달랐다.**

검사를 추가해도 못 고치는 종류다 — 배치를 바꿔야 사라진다. 공식을 `ExitRuleFormula`로 올리고
`ExitRuleCalculator`는 세션 오버라이드만 해석하는 위임층으로 남겼다.
**실전 세 엔진의 계산 결과는 바뀌지 않는다**(상수·로직 그대로 이동). 백테스트만 달라진다.

**부수 발견 — 게이트에 영구 구멍이 있었다.** WF 게이트는 조합별 *최신* 실행 하나만 보므로,
재실행되지 않은 조합은 수정 전 판정을 **영원히** 유지한다. `EXIT_RULES_VERSION`(현재 2)을
`backtest_run`에 기록하고 그보다 낮은 실행을 근거로 인정하지 않도록 했다. 09-08 이전 실행은
전부 NULL이라 자동으로 걸러진다.

### 2번 — 미룬 사유가 사실과 달랐다

*"`position`에 timeframe이 없어 session_id 조인이 필요하다"*로 미뤄 왔는데,
**가중치 쿼리 두 개가 이미 `live_trading_session`을 조인하고 있었다.** 컬럼 하나만 더 고르면
되는 일이었다. 추정 비용이 실제보다 크게 잡혀 있었던 셈이다.

같은 거래를 `regime`과 `regime@tf` 양쪽에 누적하고
`regime:coin@tf → regime:coin → regime@tf → regime → 기본값` 순으로 폴백한다.
표본이 부족한 동안에는 종전 동작이 유지되므로, 이 변경이 가중치를 **개선하는 대신 지워 버리는**
일이 없다.

### 5번 — "의도된 한계"는 맞았지만 그 결과가 구멍이었다

`strategy_type_enabled`가 전략명만 키로 쓰는 것, 그래서 "모든 변형이 죽었을 때만" 끄는 것은
**옳은 판단이다**(멀쩡한 `@H1`을 `@M15` 때문에 막을 수 없다). 그런데 그 우회의 결과로:

```
MEANREV_BB@M15 KILL  →  세션 정지                          O
                     →  MEANREV_BB@M15 새 세션 생성 차단?   X
```

`KILL_CRITERIA.md` §5가 전략 비활성화를 두는 이유가 정확히 *"세션만 정지하면 같은 전략으로
새 세션을 만들어 재개할 수 있다"*인데, 타임프레임 단위 폐기에서는 그 목적이 달성되지 않았다.
`kill-criteria.auto-stop`이 OFF라 아직 실제 동작은 아니었다 — **켜는 순간 구멍이 된다.**
켜기 전에 `strategy_timeframe_enabled`(V80)로 막았다.

### 뮤테이션 검증 — 8/8

새 가드가 같은 병(= 수정의 사각을 물려받음)에 걸리지 않았는지 일부러 깨뜨려 확인했다.

| 뮤테이션 | 결과 |
|---|---|
| WF 게이트가 구버전 실행을 통과시킴 | ✅ 잡음 |
| 틱 캐시 스코프를 닫지 않음 | ✅ 잡음 |
| SELL 게이트 손실 탈출 조건 뒤집기 | ✅ 잡음 |
| 백테스트 time stop 비활성화 | ✅ 잡음 |
| 백테스트 SL을 고정 5%로 되돌림 | ✅ 잡음 |
| 가중치 키에서 타임프레임 제거 | ✅ 잡음 |
| 타임프레임 차단 무시 | ✅ 잡음 |
| DYNAMIC에서 공용 SELL 게이트 제거 | ✅ 잡음 |

**작업 중 실제로 걸린 오탐 1건**: `EngineParityTest`가 `contains("calculateStopLevels")`로
검사했는데, 호출을 없앤 뒤에도 **그 사실을 적은 주석** 때문에 통과했다. 08-06
`getTrailingSlMargin` 오탐과 같은 유형이라 `stripComments()`를 도입해 주석을 제외했다.

**빈 루프가 될 뻔한 가드 1건**: `RulesetFingerprintTest`가 `ExitRuleCalculator`의 필드를
리플렉션으로 훑는데, 상수를 `ExitRuleFormula`로 옮기면 **0회 돌면서 통과**한다.
검사 대상을 옮기고 `checked == exposed.size()` 단언을 추가했다.

### 테스트

**503개 전량 통과.** 신규: `BacktestExitRuleParityTest`(7) ·
`WeightOverrideTimeframeTest`(5) · `TickCandleCacheTest`(6) ·
`KillCriteriaTimeframeDisableTest`(6). 갱신: `EngineParityTest`(알려진 결함 3건 → 해소 가드로
전환) · `WalkForwardGateTimeframeTest`(+3) · `RulesetFingerprintTest` ·
`StrategyWeightOptimizerTest`(컬럼 순서).

> `RateLimiterEmergencyStopTest`가 전체 실행에서 간헐 실패한다(동시성 타이밍). 단독 3회 연속
> 통과했고 이번 변경과 무관한 파일이다 — 별건으로 볼 것.

### 배포 시 주의

- **Flyway 4건이 함께 돈다**: V76·V77(09-08 미배포) + V78·V79·V80.
  V77은 `ALTER COLUMN TYPE`이라 테이블 락이 걸리지만 대상이 전부 설정·로그·뉴스 캐시다.
- **백테스트 결과가 달라진다.** 배포 후 WF를 다시 돌려야 게이트가 통과시킨다
  (구버전 실행은 전부 거부된다). STEP 2/3를 새 빌드로 재실행할 것.
- `kill-criteria.auto-stop`은 **계속 OFF**. 새 지문 표본이 n≥20에 이를 때까지 유지한다.

---

## 🟢 2026-09-08 (6차) 감사 4단계 + 1~4 종합 점검

#### 4단계 — 나머지 미해소 결함 3종

**결함 #3 닫힌 캔들 게이트 이름 불일치 → 해소.** DYNAMIC 만 `lastEvaluatedCandle` 이던 것을
LIVE·PAPER 와 같은 `lastEvaluatedClosedCandle` 로 통일(6개 지점).

문서가 *"순수 리네이밍이라 우선순위 낮음"* 으로 미뤄 온 항목인데, **우선순위 판단이 틀렸다.**
이 저장소의 반복 결함은 "규칙이 한 엔진에만 적용됐는지" 를 사람이 확인하다 놓치는 것이고,
`ENGINE_PARITY.md` 는 그 확인을 grep 으로 한다 — 이름이 갈리면 **감사 도구가 오탐을 내
감사 자체가 헛돈다**(그 문서를 쓰는 중 실제로 두 번 틀렸다고 기록돼 있다).
`EngineParityTest.closedCandleGateNamingIsConsistent` 로 고정.

**naive UTC 10테이블 → 해소 (V77).** 9개 테이블 17개 컬럼을 `TIMESTAMPTZ` 로 변환.

미뤄 온 사유가 *"운영 조회 코드를 동시에 고쳐야 해서"* 였는데 확인해 보니 그렇지 않았다 —
해당 엔티티가 **전부 `Instant`** 를 쓴다(`LocalDateTime` 이었다면 값이 이동했겠지만
`Instant` 는 절대시각이라 `timestamptz` 가 오히려 자연스러운 매핑). 저장값이 UTC 임도
운영 DB 로 확인했다: `now()` 06:13:37+00 vs `news_item_cache.fetched_at` 최댓값 06:08:41
— 5분 전, UTC 로 일치. 따라서 `AT TIME ZONE 'UTC'` 변환이 무손실이다.
덤으로 `hibernate.jdbc.time_zone` 미설정 탓에 naive 컬럼 해석이 **JVM 타임존에 의존**하던 것도 사라진다.

규칙 자체를 문장에서 **테스트로 옮겼다** — `TimestampConventionTest` 가 V78 이후
마이그레이션에 naive `TIMESTAMP` 가 들어오면 깨진다.

**결함 #2 `tickCandleCache` → 보류 유지 (근거 갱신).** 착수 조건이 "세션을 늘릴 때" 인데
함대가 오히려 줄었다(PAPER 96→40, DYNAMIC 14→12, LIVE 0). 다만 09-08 M15 백필이
레이트리밋으로 6/8 실패한 것은 **같은 API 예산을 다투는 문제**다 — 함대를 다시 늘리기 전에
이 항목을 먼저 볼 것.

#### 1~4 종합 — 가드가 실제로 잡는지 뮤테이션으로 전수 확인

이번 감사의 전제가 *"가드가 수정 범위를 물려받아 맹점이 생긴다"* 였으므로,
**새로 만든 가드가 그 병에 걸리지 않았는지**를 깨뜨려 보는 방식으로 확인했다.
실제로 두 번 구멍이 나왔고 둘 다 이 방식으로만 발견됐다:

| 뮤테이션 | 결과 |
|---|---|
| PAPER `FEE_RATE` 0.0005 → 0.0004 | 새 `EngineConstantParityTest` ✓ 잡음 / 기존 `PaperLiveAlignmentTest` ✗ **놓침**(감시 목록에 없던 상수) |
| `ExitRuleChecker` 손실구간 SL 조임 복원 | ✓ 잡음. 단 `slNeverTightensOnLoss` 는 SL 값이 느슨하지 않아 **통과해 버렸다** → 테스트 데이터 수정 |
| WF 게이트 키를 코인만으로 되돌림 | ✓ 잡음 |
| `BacktestEngine` 경로에 프로브 삽입 | ✓ 파일을 실제로 읽고 있음 확인 |
| V77 에서 `regime_change_log` 변환 제거 | **✗ 놓침** → 원인: 주석까지 매칭. `stripComments` 적용 후 ✓ |
| `LogController` 그룹키에서 타임프레임 제거 | ✓ 잡음 |
| DYNAMIC 이름을 `lastEvaluatedCandle` 로 되돌림 | ✓ 잡음 |

**교훈: 테스트를 추가하는 것과 그 테스트가 잡는지 확인하는 것은 다른 일이다.**
7건 중 2건이 "통과하지만 아무것도 안 잡는" 상태였다.

#### 최종 가드 현황

| 테스트 | 건수 | 축 |
|---|---|---|
| `ExitRuleCheckerTest` | 37 | 청산 규칙 거동(값 검증) |
| `PaperLiveAlignmentTest` | 12 | PAPER↔LIVE 상수·배선 (기존) |
| `EngineParityTest` | 11 | 4엔진 규칙 적용 여부 + BACKTEST 축 |
| `StrategyLogTimeframeTest` | 6 | 신호 집계 타임프레임 |
| `WalkForwardGateTimeframeTest` | 5 | WF 게이트 타임프레임 |
| `EngineConstantParityTest` | 3 | **상수 중복 자동 발견** |
| `TimestampConventionTest` | 2 | 타임스탬프 규약 |

#### 남은 미해소 (우선순위 순)

1. 🔴 **BACKTEST 청산 규칙이 실전과 다르다** — SL 5% 고정(ATR 무시)·TP 10%·time stop 없음.
   WF 재검증 완료 후 착수(도는 중에 바꾸면 그 결과도 버려야 함)
2. 🔴 **`weight_optimizer_snapshot` 타임프레임 없음** — `regime:coin` 키. 마이그레이션 2건 필요
3. 🟡 전략 SELL 게이트 판정 로직이 네 벌(상수는 통일, 로직은 복제)
4. 🟡 `tickCandleCache` PAPER 전용 — 함대 확장 시 착수
5. 🟡 `strategy_type_enabled` 전략명만 키 — 의도된 한계(사유 코드에 있음)

### 🔴 2026-09-08 (5차) 감사 3단계 — **신호 분석이 H1/M15 를 통째로 섞고 있었다**

타임프레임 축을 전수 점검했다(전략 컬럼이 있는 테이블 11개 → `timeframe` 없는 것 → 그 집계 코드).

#### 근본 원인 — `strategy_log` 에 `timeframe` 컬럼이 없었다

컬럼이 없으니 이 테이블을 집계하는 경로가 **구조적으로** (전략, 코인) 으로만 묶인다.
그런데 운영은 두 타임프레임을 동시에 돌리고 M15 가 3~5배 많다 — 사실상 M15 통계에
H1 이 잡음으로 섞이는 구조였다(08-01 이후, 세션 조인으로 갈라 본 값):

| 전략 | H1 | M15 |
|---|---|---|
| COMPOSITE_MTF_CONFIRMED | 7,833 | 41,826 |
| COMPOSITE_MTF_BTC | 10,259 | 39,849 |
| COMPOSITE_MOMENTUM_ICHIMOKU | 7,182 | 39,114 |

#### 섞으면 결론이 뒤집힌다 (BUY 신호 사후 4h 수익률)

| 전략 | H1 | M15 | 합산(수정 전) |
|---|---|---|---|
| **COMPOSITE_MTF_BTC** | **−1.428** | **+0.046** | −0.319 |
| COMPOSITE_MTF_BTC_STRICT | −0.509 | +0.518 | +0.266 |
| COMPOSITE_MOMENTUM_ICHIMOKU_V2 | −0.367 | +0.142 | +0.052 |

MTF_BTC 는 H1 과 M15 의 **방향이 반대**인데 합산 −0.319% 하나로 판정돼 왔다.
세 전략 모두 합산은 양수/중립인데 H1 만 보면 음수다.

**⚠️ 09-04 "전략 검토" · 09-07 "전략 순위가 통제하면 무너진다" 분석은 코인·시각은 통제했지만
타임프레임은 통제한 적이 없다.** 두 분석의 전략 순위는 이 축에서 다시 봐야 한다.
(09-07 에 "코인·시각을 통제하니 MTF_BTC 가 −0.478 → +1.210 으로 부호 반전" 이라 적은 것도
타임프레임이 빠진 상태의 수치다.)

#### 조치

- **V76 마이그레이션** — `strategy_log.timeframe` 컬럼 + `(전략, 코인, TF, 시각)` 인덱스.
  소급 백필은 하지 않는다(세션 3종을 `session_type` 따라 갈라 조인해야 하고 삭제 세션은 복원 불가).
  NULL = V76 이전 행이며 집계 시 `"?"` 로 **별도 그룹**이 된다 — 아무 그룹에나 합쳐지지 않는다.
- 세 매매 엔진 로그 기록부에 `.timeframe(session.getTimeframe())` 추가
- 소비자 2곳 그룹 키에 타임프레임 추가 — `LogController.buildByStrategy`(신호 품질 화면),
  `StrategyDegradationWatchdog.groupByKey`(성능 저하 경보). 경보 `Alert` 에도 타임프레임을 실어
  운영자가 H1/M15 를 구분할 수 있게 했다
- `StrategyLogTimeframeTest` 6건 신설 — 그룹 분리·과거행 격리·세 엔진 기록·마이그레이션 존재
- H2 테스트 스키마에도 컬럼 추가(마이그레이션과 별도 관리라 함께 고쳐야 통합 테스트가 산다)

#### 점검 결과 요약

| 테이블 | 상태 |
|---|---|
| `backtest_run` (WF 게이트) | ✅ 09-08 수정 |
| `kill_criteria_judgment` | ✅ 원래 `groupByStrategyTimeframe` 로 올발랐음 |
| **`strategy_log`** | ✅ **이번 수정 (V76)** |
| `strategy_type_enabled` | ⚠️ 의도된 한계 — 전략명만 키. 그래서 "모든 변형이 폐기일 때만" 비활성화 |
| **`weight_optimizer_snapshot`** | 🔴 **미해소** — `regime:coin` 키에 TF 없음 |

`StrategyWeightOptimizer` 는 청산 포지션을 `regime:coin` 으로 묶어 전략 가중치를 낸다.
두 타임프레임의 부호가 반대인 경우가 있으므로 **가중치가 상쇄될 수 있다.**
집계 원천 `position` 에도 `timeframe` 이 없어(=`session_id` 조인 필요) 마이그레이션 2건 +
소비자 변경이 필요하다 — `strategy_log` 수정 효과를 관측한 뒤 착수.

### 🔴 2026-09-08 (4차) 감사 2단계 — **백테스트가 실전과 다른 청산 규칙으로 돌고 있다**

`ENGINE_PARITY.md` 를 4엔진 매트릭스로 확장하면서 `BacktestEngine` 을 규칙별로 대조한 결과.

문서는 08-19 작성 이래 *"매매 엔진이 셋"* 으로 시작했다. 그런데 `BacktestEngine` 도 같은
`ExitRuleChecker` 를 쓰고 **그 결과가 WF 게이트를 통해 실자본 배정을 결정한다.**
매매를 실행하지 않는다고 축에서 빼면, **그 엔진이 만든 근거로 매매를 허가하는 경로**가
감시 밖에 남는다 — 09-08 에 WF 350건이 통째로 무효가 된 원인이 정확히 이 누락이다.

#### 대조 결과

| 규칙 | LIVE | DYN | PAPER | BACKTEST |
|---|---|---|---|---|
| `ExitRuleCalculator` (SL/TP) | O | O | O | **✗ 다른 공식** |
| `shouldTimeStop` | O | O | O | **✗ 없음** |
| `BtcMarketGuard` · `MarketRegimeDetector` · TP 래칫 | O | O | O | O |
| 전략 SELL 게이트 | 인라인 복제 | 인라인 복제 | 인라인 복제 | `allowsSignalExit` |

#### 핵심 — SL/TP 공식이 다르다

`BacktestEngine` 은 `ExitRuleChecker.calculateStopLevels`, 세 매매 엔진은
`ExitRuleCalculator.resolveStopLossPct` 를 쓴다. **이름이 비슷한 다른 함수다.**

| | BACKTEST | LIVE · DYNAMIC · PAPER |
|---|---|---|
| SL 폭 | **항상 5.0% 고정** | `clamp(ATR(14)/가격 × 1.5, 5%, 8%)` → 5~8% |
| ATR 반영 | **안 함** | 함 |
| TP 폭 | **SL × 2 = 항상 10%** | `min(SL × 2, 8%)` → ≤8% |
| 전략 제안 SL | 그대로 채택 | `.min()` — 더 넓은 쪽 |
| 시간 초과 청산 | **없음** | `maxHoldHours` (운영 24h) |

원인: `ExitRuleConfig.atrStopLossEnabled` 기본값이 `false` 인데
`RiskManagementService.toExitRuleConfig()` 가 이 필드를 **설정하지 않는다** — DB 로 켤 수단도 없다.
그래서 백테스트는 ATR 을 인자로 넘겨받고도 쓰지 않고 고정 5% 로 떨어진다.

#### 영향 — 판정이 양방향으로 왜곡된다

- 워치리스트는 ATR 하한을 통과한 **고변동 알트**다. 실전 SL 은 8% 까지 넓어지는데 백테스트는
  5% 고정 → **백테스트가 실전보다 훨씬 자주 손절된다.**
- 백테스트 TP 10% 는 실전이 결코 설정하지 않는 값이다. `ExitRuleCalculator.TP_PCT_MAX` javadoc 이
  적어 둔 그대로 — *"넓은 SL 은 반드시 맞고 넓은 TP 는 사실상 안 맞는다"*(07-31 개편 후 5일간
  익절 0건/손절 3건). 실전이 8% 로 자른 근거가 백테스트엔 없다.
- 운영 동적 세션 청산 83건 중 **TIME_STOP 39건(47%)** 인데 백테스트엔 그 경로가 아예 없다 —
  거래 모집단 자체가 다르다.

**⚠️ 즉 09-08 손절 버그를 고친 뒤에도 WF 결과는 실전 거동을 반영하지 못한다.**
지금 도는 재검증 결과도 이 한계 안에서 읽어야 한다.

#### 조치 — 지금은 고정, 해소는 별건

- `ENGINE_PARITY.md` 4엔진 매트릭스로 확장 + 격차를 미해소 결함으로 명시
- `EngineParityTest` 에 BACKTEST 축 4건 추가 — 해소되면 테스트가 깨지도록 고정.
  경로가 실제로 파일을 읽는지 **프로브를 심어 확인**했다(조용히 통과하는 가드를 만들지 않기 위해)

**지금 고치지 않는 이유**: 백테스트 청산 규칙을 바꾸면 과거 모든 백테스트와 비교 불가가 되고,
`BacktestConfig` 에 `maxHoldHours` 필드가 없어 신규 필드·마이그레이션이 필요하다.
**WF 재검증이 도는 중에 바꾸면 그 결과도 버려야 한다** — 재검증 완료 후 착수할 것.

#### 부수 발견 — 전략 SELL 게이트가 네 벌

`BACKTEST` 만 `ExitRuleChecker.allowsSignalExit` 을 호출하고, 세 매매 엔진은 같은 판정을
자체 상수로 **인라인 구현**한다. 상수는 1단계에서 `ExitRuleConfig` 위임으로 통일했지만
**판정 로직 자체는 여전히 네 벌**이다. 조건식 한쪽만 바뀌면 아무도 모른다.

### 🟢 2026-09-08 (3차) 정합성 감사 1단계 — 상수 드리프트 가드

### 왜 감사를 시작했나

09-08 하루에 나온 결함이 전부 같은 모양이었다 — **부분 수정 + 그 수정과 같은 범위의 가드**.
가드가 수정의 맹점을 물려받으니 남은 축은 다음 사고까지 살아남는다.

| 시점 | 고친 것 | 놓친 것 | 발견 |
|---|---|---|---|
| 08-06 | LIVE 급락 SL 조임 제거 | PAPER 경로(`ExitRuleChecker`) | 09-08, −1,270만원 |
| 08-06 | `PaperLiveAlignmentTest` 신설 | DYNAMIC 축 (문서가 이미 인정) | 미해결 |
| 08-19 | `EngineParityTest` 신설 | 문자열 검사라 다른 구현을 같다고 봄 | 09-08 (TP 3원 분기) |
| 08-24 | WF 게이트 전략→전략×코인 | 타임프레임 축 | 09-08 |

전면 코드 리뷰(3서비스만 6,779줄)가 아니라 **축 기반 감사**로 간다. 결함은 여러 축에
걸쳐 성립해야 하는 규칙에만 산다.

### 1단계 결과 — `EngineConstantParityTest` 신설

기존 두 가드의 구멍:
- `PaperLiveAlignmentTest` — **이름을 손으로 나열**한다. 나열 안 한 상수는 갈려도 모른다.
- `EngineParityTest` — **소스 문자열 검사**라 "호출된다"까지만 본다.

새 테스트는 세 엔진의 `static final` 필드를 **리플렉션으로 열거해 이름이 겹치는 것을 스스로 찾고**
값이 같은지 본다. 새 상수를 두 엔진에 복제하는 순간 아무도 이 파일을 건드리지 않아도 감시 대상이
된다 — 가드가 수정 범위를 물려받는 고리를 끊는 지점이다. 의도된 차이는 사유와 함께 면제 등록하고,
**면제 목록이 낡는 것도 검사**한다.

**뮤테이션 테스트로 구멍을 실증했다**: PAPER 의 `FEE_RATE` 를 0.0005 → 0.0004 로 바꾸니
새 테스트는 잡았고 **기존 `PaperLiveAlignmentTest` 는 통과했다**(감시 목록에 없던 상수).

### 검사보다 구조 — 중복 8/9 제거

값이 같은지 **검사**하는 것보다 한 곳에만 둬서 드리프트가 **불가능**하게 하는 쪽이 낫다.
발견된 공유 상수 9개 중 8개를 위임으로 바꿨다(`CANDLE_LOOKBACK` 이 이미 쓰던 패턴).

| 상수 | 있던 곳 | 이동 |
|---|---|---|
| `FEE_RATE` | LIVE·DYNAMIC·PAPER + `DynamicSessionController` (4곳) | `TradingConstants` |
| `ACTIVE_ORDER_STATES` | LIVE·DYNAMIC | `TradingConstants` |
| `CLOSING_TIMEOUT_MINUTES` | LIVE·DYNAMIC | `TradingConstants` |
| `SL_STALE_WARN_MINUTES` | LIVE·DYNAMIC | `TradingConstants` |
| `MIN_HOLD_MINUTES_FOR_SIGNAL_EXIT` | LIVE·PAPER | `ExitRuleConfig.defaults()` |
| `MIN_PNL_PCT_FOR_SIGNAL_EXIT` | LIVE·PAPER | `ExitRuleConfig.defaults()` |

`FEE_RATE` 가 특히 위험했다 — 모든 실현손익에 곱해지므로 한 곳만 갈리면 그 엔진 성적 전체가
조용히 어긋난다. 값은 전부 일치한 상태였으므로 **거동 변화 없음**(배포해도 매매가 달라지지 않는다).

남은 1개 `MAX_CONCURRENT_SESSIONS`(LIVE 10 / PAPER 120)는 의도된 차이라 사유와 함께 면제 등록.

### 다음 단계

2. `ENGINE_PARITY.md` 매트릭스에 **BacktestEngine 축 추가** — 문서는 "매매 엔진이 셋"으로
   시작하는데, 실제로는 넷이다. `BacktestEngine` 이 `ExitRuleChecker` 를 공유하고 그 결과가
   WF 게이트를 통해 실자본 배정을 결정한다. 09-08 에 WF 350건이 무효가 된 원인이 이 누락이다.
3. 타임프레임 축 전수 점검 (WF 외에 또 있는지)
4. 나머지 미해소 결함 (#2 캐시, #3 이름 불일치, naive UTC 10테이블)

### 🟢 2026-09-08 후속 — 3엔진 트레일링 통합 · WF 전면 재검증 필요 판명

#### 세 엔진이 TP 트레일링을 세 가지로 하고 있었다

원칙: **세 엔진은 체결 오차 외 모든 로직이 같아야 한다.** 그 기준으로 다시 보니
`ENGINE_PARITY.md` 가 `LIVE O / PAPER O / DYNAMIC ✗` 로 적어 둔 칸이 실은 3원 분기였다.

| 엔진 | 통합 전 발동 조건 | 기준가 |
|---|---|---|
| LIVE | `spikeUp && pnl>0` — **30초 +2.0% 급등한 틱에서만** | 현재가 × (1−0.5%) |
| PAPER | `candleHigh > entryPrice` — 수익이면 매 틱 | 고가 × (1−0.5%) |
| DYNAMIC | **없음** — 진입 시 TP 한 번 정하고 끝 | — |

완만히 오르는 포지션은 LIVE 에서만 TP 가 고정됐고 DYNAMIC 은 아예 안 움직였다.
결함 #1 을 문서대로 "PAPER 것을 DYNAMIC 에 이식"으로 처리했다면 **09-08 에 제거한
손실구간 SL 조임을 동적 엔진에 새로 심을 뻔했다.**

**조치** — 셋 다 `ExitRuleChecker.updateTrailingStops` 호출로 통합:
- `LiveTradingService` — `spikeUp` 게이트 제거, 공용 함수로 교체 (`spikeUp` 은 throttle 판정에만 잔존)
- `DynamicTradingService` — TP 트레일링 신설 + `exitChecker()` 헬퍼
- `PaperTradingService` — 호출부 정리
- `ExitRuleChecker` — `candleLow` 파라미터 **제거**. 남겨 두면 세 호출부가 의미 없는 인자를
  넘기게 되고 그 자리가 조임 로직이 되살아나는 자리가 된다
- `ENGINE_PARITY.md` 결함 #1 해소 표기 + 매트릭스에 `updateTrailingStops` 행 추가
- `EngineParityTest.trailingUsesSharedCalculatorInAllEngines` 신설
  (기존 `knownGap_dynamicHasNoTrailing` 을 뒤집음)

#### 🔴 BacktestEngine 도 같은 함수를 쓰고 있었다 — WF 350건 전부 무효

컴파일 에러로 **네 번째 호출자**가 드러났다: `BacktestEngine:125`.
백테스트가 운영과 같은 청산 규칙을 쓰는 것은 설계 의도지만, 그래서
**2026-09-08 이전 백테스트·Walk Forward 결과는 전부 0.3% 손절 기준**이다.
`WalkForwardValidationGate` 가 실자본 배정을 이 판정으로 막고 있으므로 방치하면
잘못된 근거로 전략을 계속 차단/허용하게 된다.

#### 🔴 WF 가 M15 를 한 번도 검증한 적이 없다

`backtest_run` 350건이 **전부 H1** 인데 운영 함대는:

| | H1 | M15 |
|---|---|---|
| 고정코인 PAPER 40세션 | 0 | **40** |
| 동적 12세션 | 6 | 6 |

운영 대부분이 검증된 적 없는 타임프레임으로 돌고 있다.

#### 🔴 M15 캔들이 없거나 5개월 낡았다 — 백필이 선행돼야 한다

운영 8코인 `candle_data` 실측: **M15 가 AVAX·EUL·LINK·PROM 은 0건**, ADA·BTC·DOGE·SOL 은
2026-03-30 이 마지막(5개월 전). H1 은 전 코인 08-30~09-01 까지 있다.
이대로 WF 를 돌리면 절반이 조용히 누락된다 — 08-24 에 실제로 겪은 사고(48조합 중 25개 누락).

→ `scripts/revalidate_walk_forward_0908.sh` (STEP=1 백필 → 2 재검증 → 3 판정).
   Step 2 는 5전략 × 8코인 × {H1, M15} = **80조합**. 누락 점검 쿼리를 스크립트에 포함.

#### 🟢 flaky 테스트 제거 — 운에 기대 통과하고 있었다

`VolumeDeltaStrategyTest §포화점_낮을수록_강도_높음_재스케일_검증` 이 3회 중 1회 실패.
`TestDataHelper` 가 `Math.random()` 으로 거래량을 만드는데, `VolumeDeltaStrategy` 는
`buyRatio` 가 캔들 모양으로 고정(상승 0.75)이라 `delta ∝ volume` 이 되고 BUY 조건
"후반부 평균 Delta > 전반부"가 **후반부 거래량이 더 큰가**로 환원된다 — 평평한 난수에서 동전 던지기.

시드만 고정하니 **일관되게 실패**했다(통과가 시드 뽑기였다는 뜻). 근본은 데이터가 추세를
나타내지 않는 것이므로, 추세 캔들의 거래량이 **캔들당 +3 씩 증가**하도록 고쳤다 —
상승 추세는 매수 압력이 강화되는 국면이고 그게 이 전략이 잡으려는 신호다. 하락 추세도
같은 램프로 `deltaWeakening` 이 성립한다. 횡보는 지터만 유지. 5회 반복 실행 결정성 확인.

#### 🔴 M15 백필 1차 실패 (6/8) — 원인은 타임프레임이 아니라 요청 사슬 길이

STEP=1 첫 실행에서 M15 8코인 중 6개가 "캔들 데이터 수집 실패", **H1 은 8/8 성공**.
경계가 정확히 코인당 요청 수에 있었다:

| 요청 | 코인당 요청 수 | 결과 |
|---|---|---|
| H1 2022~2026 | 206회 | 8/8 성공 |
| M15 KRW-PROM (07-26 상장) | 13회 | 성공 |
| M15 KRW-EUL (08-12 상장) | 21회 | 성공 |
| M15 2022~2026 | **822회** | **6/6 실패** |

`UpbitCandleCollector.fetchCandles` 의 수집 루프는 **all-or-nothing** 이다 — 822회 중
1회만 실패해도 `RuntimeException` 이 터지며 그때까지 모은 캔들을 전부 버린다
(`UpbitCandleCollector:81`). 게다가 운영 함대 52세션이 같은 Upbit 초당 10회 예산을
나눠 쓰고 있어(동적 8세션만으로 264 req/분 ≈ 4.4 req/s) 90초짜리 연속 호출은 버티지 못한다.
206회(≈23초)는 통과하고 822회(≈90초)는 못 버틴 것이다.

M15 가 원리적으로 안 되는 것은 아니다 — ADA·BTC·DOGE·SOL 에 이미 M15 가 148,000행씩
들어 있다(과거에 742회 요청이 성공한 적 있다는 뜻).

**조치**: `revalidate_walk_forward_0908.sh` STEP=1 을 **갭 인식 + 연 단위 분할**로 재작성.
`GET /data/summary` 로 코인·타임프레임별 보유 구간을 먼저 읽어 **없는 구간만** 요청한다.
전 구간을 다시 요청하면 822회짜리 호출이 되살아나 또 실패하기 때문이다.

산출 로직을 운영 DB 스냅샷으로 드라이런한 결과 **14개 호출 / 호출당 최대 176회**
(성공이 확인된 H1 206회 미만). 남은 실제 작업량:

| 대상 | 남은 구간 | 호출 |
|---|---|---|
| ADA·BTC·DOGE·SOL | 2026-03-30 ~ 오늘 | 각 1회 (78~79회 요청) |
| KRW-LINK | 2022 ~ 오늘 (M15 전무) | 연 단위 5회 |
| KRW-AVAX | 2022-02-08 ~ 2025-12-31 | 연 단위 4회 |
| EUL·PROM | — | **완결** (상장일부터 09-07 까지 보유) |

초안에서 걸러낸 낭비 2종:
- **상장 전 구간 요청** — EUL(2026-07-26 상장)·PROM(2026-08-12)에 2022 구간을 요청하면
  빈 응답이라 영영 안 채워지고 매 실행마다 반복된다. 어떤 타임프레임이든 가장 이른 캔들을
  그 코인의 하한으로 삼아 제거(20개 호출 절감).
- **1일짜리 꼬리 호출** — `max(time)` 이 항상 오늘보다 조금 뒤처져 매번 1회 호출이 생긴다.
  최근 2일 이내면 최신으로 간주(12개 호출 절감).

둘을 고쳐 45개 → **14개 호출**이 됐고, 수집 성공 후 재실행하면 계획이 0개가 되는
**멱등성**도 시뮬레이션으로 확인했다. 실패한 구간이 있으면 그대로 재실행하면 된다.

**남은 구조적 결함**(별도 판단): `fetchCandles` 가 부분 저장·재시도 없이 전부 버리는 것.
긴 백필은 원리적으로 실패하게 돼 있다. 청크 단위 저장 + 요청 단위 재시도가 근본 해법이며,
`backtest_job` 에 이미 `total_chunks`/`completed_chunks` 가 있으니 같은 방식을 쓸 수 있다.
#### 🔴 WF 게이트가 타임프레임을 전혀 보지 않았다

STEP=2 전에 "옛 WF 350건을 지워야 하나" 를 확인하다 발견. 결론은 **지울 필요 없음** —
`WalkForwardValidationGate` 는 이미 조합별 **최신 1건**만 보므로 새 실행이 들어오면
옛 것은 자동으로 판정에서 빠진다(`latestPerCoin` + createdAt desc + `putIfAbsent`).
오히려 옛 판정은 "이 버그가 전략 판단을 얼마나 왜곡했나" 를 재는 **유일한 대조군**이라 남긴다.

그런데 그 코드를 읽다 진짜 결함이 나왔다 — **게이트에 `timeframe` 참조가 0건이었다.**
08-24 에 전략 단위 → 전략×코인 단위로 좁히면서 같은 이유가 적용되는 타임프레임 축을 놓쳤다.
`latestPerCoin` 키가 코인뿐이라 같은 (전략, 코인) 의 H1 과 M15 가 한 자리를 다투고,
**나중에 실행된 쪽이 다른 쪽 판정을 덮어쓴다** — `evaluate(String)` javadoc 이
"실행 순서에 따라 결과가 좌우되는 우연" 이라고 적어 둔 것과 정확히 같은 결함이다.

운영 실태가 이 구분을 요구한다: 고정코인 PAPER 40세션 전부 M15, 동적 12세션 H1 6 / M15 6 인데
WF 350건은 전부 H1 이었다 — **M15 세션이 H1 근거로 통과하고 있었다.**
STEP=2 는 H1 → M15 순으로 던지므로, 고치지 않았다면 M15 가 H1 판정을 전부 덮었을 것이다
(지금 운영이 M15 위주라 우연히 덜 틀릴 뿐, 근거가 우연인 것은 그대로다).

**조치**:
- `BacktestRunRepository` — `...AndTimeframeAnd...` 조회 추가
- `WalkForwardValidationGate` — `evaluate(전략, 코인, TF)` / `evaluateStrategy(전략, TF)` 로 확장,
  `latestPerCombo` 키를 `coinPair + "@" + timeframe` 으로 변경. `timeframe=null` 이면 종전 동작(하위 호환)
- 호출부 3곳에 타임프레임 전달 — LIVE 생성(코인·TF 앎), DYNAMIC 생성(`throwIfBlockedByTimeframe`,
  코인은 스캔 후지만 TF 는 생성 시점 확정), DYNAMIC 매수 직전(둘 다 앎)
- `WalkForwardGateTimeframeTest` 신설(5건). **뮤테이션 테스트로 검증** — 키를 예전(코인만)으로
  되돌리니 "같은 코인의 H1/M15 가 서로를 덮지 않는다" 가 정확히 실패했다.

#### 배포 필요

이번 변경은 **실제 매매 거동을 바꾼다**(LIVE TP 갱신 빈도 증가, DYNAMIC 트레일링 신설).
실거래 세션이 0건(`live_trading_session` RUNNING 0, 마지막 LIVE 포지션 08-07)이라
페이퍼·동적에서 먼저 관찰할 수 있는 시점이다.

## 🔴 2026-09-08 고정코인 PAPER 손절가 버그 — **5% 손절이 0.43% 로 걸리고 있었다**

운영 DB(`dynamic_session` 14세션 + `paper_trading.virtual_balance` 96세션)를 전수 조회했다.

### 총평

| 구분 | RUNNING | 자본 | 평균 수익률 | 누적 손익 | 승률 |
|---|---|---|---|---|---|
| 동적(코인 자동탐색) | 14 | 세션당 1만원 | −0.27% | −384원 | 44.6% (83거래) |
| **고정코인 PAPER** | 96 | 세션당 1,000만원 | **−1.32%** | **−12,700,966원** | **13%** (534거래) |

동적 세션은 전부 `trading_mode='PAPER'` — REAL 49건은 08-06 이전 DELETED 라 **실거래 중인 동적 세션은 없다.**

### 🔴 근본 원인 — `updateTrailingStops` 의 손실 구간 SL 조임

`virtual_balance.stop_loss_pct = 5.00` 이고 진입 시점 ATR 기반 SL 계산도 정상인데,
`position.stop_loss_price` 는 진입가 대비 **평균 −0.43%** 로 찍혀 있었다.

```
position 3574  MTF_BTC/KRW-DOGE  cfg_sl=5.00
  entry 124.124 → stop 123.628 (−0.400%)
  진입 23:15:28 → 손절 23:22:32 (7분)
```

`ExitRuleChecker.updateTrailingStops()` 가 저가 < 진입가이면 SL 을 `저점 × (1 − trailingSlMargin)`
으로 상향한다. 호출부(`PaperTradingService:733`)가 **현재가를 candleHigh/candleLow 양쪽에** 넣으므로,
가격이 진입가를 **1틱**만 밑돌아도 SL 이 즉시 `현재가 × 0.997` 로 끌어올려진다 — 5% 손절이 0.3% 손절이 된다.

| 청산사유 | 건수 | 비중 | 평균 보유 |
|---|---|---|---|
| **STOP_LOSS** | **456** | **85.4%** | **0.9h** |
| TAKE_PROFIT | 67 | 12.5% | 2.4h |
| TIME_STOP / 기타 | 11 | 2.1% | — |

수수료 4,235,455원(손실의 33%)도 이 과다 회전이 만든 것이다 — 수수료 전 기준 −8,706,989원.

**이 버그는 LIVE 에서 2026-08-06 에 이미 같은 진단으로 제거됐다**(`LiveTradingService:2645`
"급락 처리 — 2026-08-06 제거. SL을 좁히지 않는다"). 그때 LIVE·DYNAMIC 만 고치고
`ExitRuleChecker` 는 PAPER 전용 경로라 누락돼 한 달 넘게 살아 있었다. 동적 세션의 SL 폭은
−5.10%/TP +7.89% 로 설정대로 정상이다 — 같은 증상이 없는 이유가 이것이다.

### 🟢 조치

- `ExitRuleChecker.updateTrailingStops` — 손실 구간 SL 조임 **제거**. TP 래칫(이익 잠금)은 유지.
- `RulesetFingerprint` — `exit.slTightenOnLoss=false` 키 추가. 설정값이 그대로라 이 키가 없으면
  **수정 전후 거래가 같은 지문으로 한 표본에 섞인다**.
- `ExitRuleCheckerTest` — 기존 "손실 구간 SL 조임" 테스트를 회귀 방지 2건으로 교체
  (진입가 1틱 아래에서도 5% SL 유지 검증). 전체 테스트 통과.

### 🔴 구배치 40세션이 유령으로 남아 이중 체결 중

RUNNING 96건인데 `(strategy, coin, timeframe)` 고유 조합은 56개 — **40조합이 2건씩 동시 가동**이다.

| 배치 | 세션 | id | 설정 | 평균 | 누적손익 |
|---|---|---|---|---|---|
| 구배치 | 56 | 118~248 | `stop_loss_pct`·`strategy_params` 전부 NULL | −0.95% | −5,308,246 |
| 신배치 | 40 | 330~369 | 정상 | −1.85% | −7,392,720 |

같은 초·같은 진입가로 두 건이 체결된다(position 3573/3574). 08-24 신배치 기동 시 구배치 미정리.
구배치는 `stop_loss_pct` 가 NULL 이라 `resolveStopLossPct` 의 floorPct 가 0 — **ATR 하한이 아예 없다.**

→ `scripts/stop_duplicate_paper_sessions.sh` (조합별 최신 id 만 유지, 40건 정지).
   단독 잔존 16건(id 118·121·141·144·148·151·159·162·166·169·173·176·180·183·187·190, 전부 H1)은
   대응 조합이 없어 유지하지만 **`stop_loss_pct` NULL 문제는 그대로 남는다 — 별도 판단 필요.**

### 🔴 폐기 판정이 집행되지 않고 쌓이고 있다

`StrategyKillCriteriaService` 는 정상 작동해 최근 10일 **KILL 664건**을 냈지만
`kill-criteria.auto-stop` 이 false(기본값, 어느 yml 에도 미설정)라 **auto_stop_applied 가 전부 false** 다.

- 동적 83(MTF_BTC M15, 9거래 −15.98%) · 91(MEANREV_BB M15, 7거래 −15.34%) → 오늘 KILL/CAPITAL_LOSS.
  MDD 여유가 각각 3.54%p·4.18%p 뿐이라 손절 한 번이면 서킷브레이커(20%)로 어차피 정지된다.
  → `scripts/stop_killed_dynamic_sessions_0908.sh` 로 판정을 수동 집행.
  같은 전략의 다른 변형은 멀쩡하므로(MTF_BTC@M15 세션 90 +1.03%, MEANREV_BB@H1 세션 76 +14.29%)
  **세션 정지까지만 하고 `strategy_type_enabled` 는 건드리지 않는다.**

- ⚠️ **auto-stop 을 지금 켜면 안 된다.** 같은 날 PAPER 80건에 찍힌 NEGATIVE_EV KILL 은
  위 SL 버그가 만든 음의 기대값이다. 버그가 만든 통계로 전략을 폐기하게 된다.
  수정 배포 + 새 지문으로 표본이 쌓인 뒤 재판정할 것.

### 동적 세션 성적 (참고 — 표본이 얇다, 전략당 4~15거래)

| 전략 | TF | 세션 | 평균 | 거래 | 승률 |
|---|---|---|---|---|---|
| MEANREV_BB | H1 | 1 | **+14.47%** | 4 | 75.0% |
| MOMENTUM_ICHIMOKU | H1 | 2 | +6.25% | 10 | 60.0% |
| MOMENTUM_ICHIMOKU | M15 | 2 | +3.05% | 11 | 45.5% |
| MOMENTUM_ICHIMOKU_V2 | M15 | 1 | +3.01% | 6 | 50.0% |
| MTF_BTC | H1 | 1 | +0.28% | 7 | 28.6% |
| MTF_CONFIRMED | M15 | 2 | −1.77% | 12 | 33.3% |
| MOMENTUM_ICHIMOKU_V2 | H1 | 1 | −2.67% | 4 | 50.0% |
| MTF_CONFIRMED | H1 | 1 | −6.04% | 7 | 42.9% |
| MTF_BTC | M15 | 2 | −9.18% | 15 | 33.3% |
| MEANREV_BB | M15 | 1 | −15.34% | 7 | 14.3% |

H1 이 M15 보다 일관되게 낫다. 다만 09-07 결론(코인·시각 통제 전에는 전략 순위를 믿지 않는다)이
그대로 적용되는 표본 크기다 — **이 표로 전략을 쳐내지 않는다.**

### ✅ 집행 결과 (2026-09-08 09:26 KST 배포 후)

**수정 배포 확인** — 배포 전후 경계가 포지션 단위로 뚜렷하다.

| position | 세션 | 코인 | sl_gap | 진입(KST) |
|---|---|---|---|---|
| 3565·3568·3572 | 335 | LINK | −0.400 / −0.343 / −0.342% | 00:30 / 01:00 / 05:15 |
| 3574·3576 | 357 | DOGE | −0.400% | 08:15 / 09:15 |
| **3577** | **357** | **DOGE** | **−5.000%** | **09:26** |

09:15~09:26 사이에 백엔드가 새 이미지로 올라왔고, 이후 첫 진입 3577 이 설정값 그대로
−5.000% 로 찍혔다. 이전 5건은 전부 −0.4% 근처에서 손절됐다.
(중간 점검 시 나온 평균 −1.15%/n=6 은 배포 전후가 섞인 값이었다. `'2026-09-08'` 이
클라이언트 세션 타임존(KST)으로 해석돼 UTC 09-07 15:00 부터 잡힌 것도 겹쳤다.)

**세션 정리** — 고정코인 PAPER 96 → 56 → **40**

| 그룹 | 조치 | 건수 |
|---|---|---|
| 구배치 중복 (194~248) | STOPPED — `stop_duplicate_paper_sessions.sh` | 40 |
| 단독 잔존, 설정 NULL (118·121·141·144·148·151·159·162·166·169·173·176·180·183·187·190) | STOPPED — `stop_null_sl_paper_sessions_0908.sh` | 16 |
| 신배치 (330~369) | 유지 | 40 |

단독 16건을 남기지 않은 이유: `stop_loss_pct`·`strategy_params` 가 둘 다 NULL 이라
floorPct=0 — **ATR 하한 없이** 도는 유일한 설정 상태였고, 32일간 청산이 16세션 합쳐
44건(세션 141 은 0건)이라 표본을 거의 못 만들었다. 성적이 −3.53%~+3.50% 로 평평한 것도
"좋아서"가 아니라 "거래를 안 해서"다. 오늘 판정도 NEGATIVE_ALPHA KILL 8건 + NO_SIGNAL WARN 3건.
floorPct=0 arm 을 대조군으로 남기는 대신, 손절폭 A/B 는 `ExitRuleOverrides`(strategy_params)
라는 제대로 된 경로를 쓴다 — 설정 누락을 실험군처럼 쓰지 않는다.
정지 시점에 16건 모두 보유 포지션 0건이라 강제청산은 없었다.

**동적 세션** — 83·91 STOPPED (`stop_killed_dynamic_sessions_0908.sh`).
미청산 포지션(XLM·ORCA) 강제청산으로 −15.98%→−16.24%, −15.34%→−15.92% 로 확정.
RUNNING 동적 세션 14 → 12.

세 스크립트 모두 ID 하드코딩이 아니라 API 응답에서 대상을 다시 뽑으므로 **멱등**하다
(재실행 시 "대상 없음"으로 종료). 판별 로직은 각각 운영 DB 스냅샷으로 드라이런해
의도한 집합과 정확히 일치함을 확인한 뒤 실행했다.

### 다음 판정

새 지문(`exit.slTightenOnLoss=false`)으로 n≥20 이 쌓인 뒤 승률·기대값을 이전 표본과
갈라서 비교한다. `ruleset_hash` 가 갈리므로 섞이지 않는다.

```sql
SELECT p.ruleset_hash, count(*) AS n,
       round(100.0*count(*) FILTER (WHERE p.realized_pnl>0)/count(*),1) AS winrate,
       round(sum(p.realized_pnl),0) AS pnl
FROM paper_trading.position p
JOIN paper_trading.virtual_balance v ON v.id = p.session_id
WHERE p.status='CLOSED' AND p.closed_at >= '2026-09-08'
GROUP BY 1 ORDER BY 2 DESC;
```

⚠️ `kill-criteria.auto-stop` 은 그때까지 계속 false 로 둔다 — 켜면 SL 버그가 만든
PAPER NEGATIVE_EV 80건까지 한꺼번에 집행된다.

---


## 🔴 2026-09-07 페이퍼 세션 전수 분석 — **전략 순위를 믿을 수 없었다**

운영 DB의 `DYN_PAPER` 전량(2026-08-07~09-07, 42세션·청산 335건·보유 13건)을 직접 조회했다.

### 총평

| 지표 | 값 |
|---|---|
| 승률 | 35.5% (119/335) |
| 순손익 | −8,633원 |
| Profit Factor | 0.85 |
| 거래당 기대값 | −0.347% |

손익비 1.54면 손익분기 승률이 39.4%인데 35.5%라 4%p 모자란다.

### 🔴 대조군이 없었다 — 지금까지의 전략 평가는 전부 근거가 약하다

`SignalQualityService` 의 사후수익 백필이 `signal IN ('BUY','SELL')` 로 걸려 있어
**HOLD 74,462건(DYN_PAPER)이 전량 미평가**였다. 비교 기준이 없으니 "BUY 신호 사후 −1.25%" 가
신호가 나쁜 건지 그 시기 그 코인이 빠진 건지 구분할 방법이 없었다.

**같은 코인·±1시간에 둘 이상의 전략이 BUY를 낸 건만 골라 코인·타이밍을 통제하니 순위가 무너진다:**

| 전략 | 통제 전 24h | **통제 후 24h** | 변화 |
|---|---|---|---|
| MTF_CONFIRMED | −1.250 | **−0.160** | +1.09 |
| **MTF_BTC** | −0.478 | **+1.210** | **+1.69 (부호 반전)** |
| MOMENTUM_ICHIMOKU | +0.390 | +0.099 | −0.29 |
| MOMENTUM_ICHIMOKU_V2 | −1.518 | −1.327 | +0.19 |
| PULLBACK_MTF | +1.263 | +2.406 | +1.14 |
| MTF_BTC_STRICT | +2.240 | +2.224 | ≈0 |

→ **전략별 성적 차이(−3,579~+1,015), H1 vs M15 갈림, 승률 순위는 전부 "그 세션이 무슨 코인을
언제 잡았나"와 뒤엉켜 있다.** 08-31 PULLBACK_MTF 정지와 09-04 MEANREV_BB 호평이 이미 표본
확대로 뒤집힌 것도 같은 원인이다(MEANREV_BB: n=5 승률 60% → n=32 승률 28%).
**표본 수집 방식을 고치기 전에는 전략을 쳐내지 않는다.**

### 통제와 무관하게 남는 것 3가지

1. **전략 SELL 청산이 손실 원천** — 청산사유별로 보면 TP(+39,924)와 SL(−38,973)은 거의 상쇄되고,
   **적자 전액이 STRATEGY_SIGNAL 청산 110건(−10,298, 평균 −1.15%)** 에서 나온다. 같은 거래
   모집단 안에서 청산 방식만 다른 비교라 코인 편차가 양쪽에 똑같이 걸린다 — 살아남는 비교다.
   그냥 24시간 기다린 TIME_STOP 66건은 **+0.28%**. 전략 SELL을 끄면 나머지 합이 **+2,609원**.
2. **손절 감시 지연** — SL 체결가가 SL 가격보다 평균 H1 −0.72%p / M15 −0.97%p 아래.
   꼬리 8건이 1%p 초과, 최악 −19.66%p(FOLD −23.8% 확정). `processMonitoringTick` 이 SL 판정을
   캔들 종가로 한다. FOLD·GRVT는 31h·49h 보유 후 **세션 EMERGENCY_STOP 시각에 동시 청산** —
   SL도 TIME_STOP(24h)도 못 잡았다. 24.5h 초과 보유 11건.
3. **SL/TP가 사실상 고정** — 청산 348건 전부 TP +7.89%, SL 296/348이 −5.09%. ATR 적응이 없다.

### 데이터 정합성

- 41/42 세션은 `total_asset_krw = initial + Σrealized_pnl` 이 **오차 0**. RUNNING 세션의 ±100원은 평가손익(정상).
- 🔴 **세션 49만 손상**: `total_asset_krw=221,876` (실제 9,832, **오차 +212,043**, 화면상 +2,118%).
  DELETED 상태라 매매엔 무영향이지만 누적 통계에 들어가면 전체를 오염시킨다. **미조치 — 운영 DB 쓰기라 승인 대기.**
- ⚠️ 정정: 초기 진단에서 "매수 수수료 미계상 → 손익 0.05% 낙관"이라 했으나 **오진**이었다.
  매수 수수료는 `avgPrice` 에 녹아 있어(`avgPrice = investedKrw / quantity`) **실현손익은 정확하다.**
  `position_fee` **컬럼만** 청산 시 매도분으로 덮어써지고 있었다(아래 수정).
- `order.quantity` 가 BUY 행에서는 KRW 금액, SELL 행에서는 코인 수량 — 같은 컬럼 두 의미. **미조치.**
- 페이퍼 슬리피지는 고정 ±0.1% 합성값. 손실 상위가 전부 저유동 소형 알트(ANKR −3,321, STX −2,179,
  NCT −2,118, RE −1,399 = −9,017, 전체 적자 초과)라 **실거래는 이 수치보다 나쁠 것**이다.
- 세션 간 상관이 사실상 1 — 현재 14세션 중 13세션 동시 보유, ETH×2·SOL×2·ONDO×2 중복.
  전 기간으로는 ETH 한 종목을 17개 세션이 샀다. 코인·일자 조합의 **38.9%가 다중 세션 중복**.
  독립 표본 14개가 아니라 사실상 1~2개다.

### 조치 완료

- `V75__hold_baseline_index.sql` — HOLD 백필용 부분 인덱스.
- `StrategyLogRepository.findPendingHoldFor{4h,24h}Eval` — **(코인, 정시) 당 1건만** 평가하는
  DISTINCT ON + NOT EXISTS 쿼리. NOT EXISTS 가 핵심 — 없으면 대표 1건이 평가되는 순간 같은
  시간대의 다음 행이 새 대표가 되어 74,462건을 전부 부른다(distinct coin-hour 는 7,658건).
- `SignalQualityService.evaluateHoldBaselineLoop` — 예산 200건/실행으로 BUY/SELL 평가와 분리
  (기준선이 신호 평가를 굶기면 안 된다). 전량 실패 시 조기 종료로 무한 루프 방지.
- `calcReturn` — HOLD 를 **BUY 와 같은 롱 방향**으로 계산. SELL 방향이면 부호가 뒤집혀 대조군 구실을 못 한다.
- `DynamicTradingService.finalizeDynamicSell` — `setPositionFee(fee)` → `add(fee)` (컬럼을 왕복 총액으로).
- `DynamicSessionController.exitFeeOf` — 컬럼 폴백을 왕복 총액 기준으로 보정.
- 테스트 `SignalQualityHoldBaselineTest` 4건 신설. 전체 460건 통과.

### 추가 조치 (2026-09-07 오후, 승인 후 전량 적용)

**1. 공용 종목 유니버스 고정** — `SharedUniverseService` 신설.
   - 세션별 갱신 시계 대신 **벽시계 버킷**(`epochSecond / (refreshMin*60)`)으로 끊는다.
     세션들이 서로 다른 시각에 tick 해도 같은 버킷이면 같은 스냅샷을 받는다.
   - 캐시 키에 필터 파라미터를 전부 넣는다 — 설정이 다른 세션은 비교 대상이 아니므로 공유하지 않는다.
   - `trading.shared-universe.force-timeframe: H1` 기본값 — 이걸 비우면 H1/M15 유니버스가
     따로 생겨 **타임프레임 간** 비교가 여전히 교란된다. 값을 줘서 함대 전체를 단일 유니버스로 묶었다.
   - 빈 결과는 캐시하지 않는다(Upbit 일시 장애 시 버킷 내내 함대 전체가 스캔 정지하는 것 방지).
   - 되돌리기: `trading.shared-universe.enabled: false` 한 줄.

**2. 전략 SELL 청산 기본 OFF** — `trading.strategy-signal-exit.enabled: false`.
   - 신호 자체는 계속 기록하고 `blocked_reason` 만 붙인다 → **반사실 측정 유지**.
     차단하지 않았다면 어땠을지를 사후수익으로 계속 재야 이 결정을 뒤집을 근거가 생긴다.
   - 되돌리기: `enabled: true` 한 줄.

**3. `executed_funds` 명시** — 4번 항목("order.quantity 컬럼 분리")의 재설계.
   - ⚠️ 정정: 컬럼을 쪼개는 건 **틀린 처방**이었다. `quantity` 의 이중 의미는 Upbit price 타입
     주문 규격이 강제하는 것이고, 이미 `OrderAmounts` 유틸이 흡수하고 있으며 `filled_quantity` 는
     전 구간 정확하다. 컬럼을 건드리면 실주문 제출 경로가 깨진다.
   - 진짜 빈틈은 **페이퍼 주문의 `executed_funds` 가 전 행 NULL** 이었던 것. 이제 매수/매도 모두
     체결 KRW 를 명시적으로 남긴다. 과거 행은 스크립트로 백필.

**4. 운영 DB 보정 스크립트** — `scripts/fix_data_integrity_0907.sh`
   - 세션 49 총자산 보정(221,876 → 9,832) + `executed_funds` 백필.
   - `DRY_RUN=1` 기본(미리보기·자동 롤백), 트랜잭션 1개, ID 하드코딩 없이 조건으로 탐지.
   - 운영 DB 읽기 검증 완료: 탐지 조건이 **세션 49만** 잡고(정상 41세션 미포함),
     백필 공식은 BUY **353/353 오차 0**, SELL **341/341** 실현손익과 정합(최대 0.0094).
   - **아직 실행 안 함** — 운영 서버에서 `DRY_RUN=0` 으로 돌릴 것.

테스트 465건 통과(신규 9건: `SignalQualityHoldBaselineTest` 4, `SharedUniverseServiceTest` 5).

### 다음

1. 배포 후 `fix_data_integrity_0907.sh` 를 `DRY_RUN=0` 으로 실행.
2. HOLD 기준선 백로그 소진 — 35,691 coin-hour × 2, 30분 주기 200건이면 약 4일.
3. 현 14세션 재시작(공용 유니버스·전략 SELL OFF 적용된 새 표본으로).
4. 2주 재수집 → **그때 전략 순위 판정.** 그 전에는 쳐내지 않는다.

---


---

## 📦 이관된 상세 이력 → [`docs/old_progress.md`](old_progress.md)

> 2026-09-04 정리. 아래 사건들은 결론만 남기고 상세 과정을 `old_progress.md` 로 옷겼다.
> 근거·재현 과정이 필요하면 그 파일에서 **날짜로 검색**할 것.

> **2026-09-18 추가 이관** — 결론이 이미 코드·설정에 반영됐거나 기간이 지나 행동을 바꾸지
> 않는 구간을 옮겼다(PROGRESS.md 2,582 → 1,219줄).
> ⚠️ 이관 구간의 백테스트·WF 숫자는 전부 **규칙 v1/v2** 산출물이라 무효다 — 인용 금지.

- 🔴 2026-09-04 전략 검토 — 문제는 전략이 아니라 전략을 잘못 배치한 것이었다
- 🔴 2026-09-04 게이트 판단이 하루 만에 뒤집혔다 (표본 30 → 51건)
- 🔴 2026-09-04 서킷 브레이커 2건 발동 + ANKR 반복 손절 / 🟢 그 외 세션 상태
- 🟢 2026-09-03 3종 보정 결과 · 🔴 게이트 ON 시뮬레이션(수익 91% 소멸) · 🟡 노출 상한 병목
- 🔴 2026-09-02 09-14 판정이 불가능한 궤도였다 — 캔들·WF·세션 3종 보정
- 🟢 2026-09-02 일일 점검 — 게이트 OFF 후 거래 회복 확정
- 🟢 2026-09-01 WS 실시간 감시 페이퍼 적용 · 텔레그램 폴백 · 함대 재기동 점검
- 🔴 2026-09-01 LLM 응답 본문 유실 · PAPER 세션 `session_kind` 하드코딩 4곳 · SL 알림 무한반복
- 🟢 2026-09-01 동적 세션 상세 화면 보강 · 세션 LLM 분석 신설
- 🔴 2026-08-31 PULLBACK_MTF 정지 · WF 커버리지 확장 · PAPER↔LIVE 규칙 일치 · 함대 재기동+규칙 동결
- 🔴 2026-08-31 23:15 WF 게이트 OFF — 켜니 함대가 멈췄다
- 🔴 2026-08-30 노출 상한 1 → 2, PAPER 면제 철회
- 📅 점검 일정 — 규칙 동결 기간(2026-08-31 ~ 09-14). **기간 종료**
- ✅ 최근 작업 이력 요약 (2026-08-06 및 이전)

- 🟢 2026-08-28 정기 점검 — 전 항목 정상, 방어 장치 3종 실전 검증
- 🔴 2026-08-28 WF 커버리지 확장 — 착수했더니 **데이터가 없었다**
- 🟡 2026-08-31 워치리스트 최소 이력 조건 — 하드코딩 15를 전략 요구량으로 교체
- 🔴 2026-08-27 P0 — 세션 하나가 21시간 조용히 죽어 있었다 (중복 포지션 + 상태 불일치)
- 🔴 2026-08-26 운영 전면 점검 — 진입 게이트가 역선택을 하고 있었다
- 🟢 2026-08-26 DYNAMIC 스캔 루프 캔들 조회를 캐시 경유로 전환 — 진짜 스로틀 병목 발견
- 🟢 2026-08-25 DYNAMIC 운영 가능성 점검 — P0 데드락 발견·수정 + WF 게이트 코인 단위 재설계
- 🟠 2026-08-24 운영DB 로그 분석 — DYN_PAPER(동적 페이퍼) / PAPER(고정코인 페이퍼)
- 🟢 2026-08-19 지문 구멍 마감 (2) — `SignalQualityDampenGate` (`dampen.*`)
- 🟢 2026-08-20 프런트엔드 정비 — 타입 가드 복구 · 죽은 코드 제거 · 테마 페인트
- 🟢 2026-08-20 e2e 스위트 가동 (`@playwright/test`) — 보류 항목 해소
- ✅ 2026-08-20 오전 점검 결과 (09:05 KST 실측)
- 🗓️ 2026-08-18 ~ 08-19 작업 요약


---

## 🔴 보류·결정 대기 항목

> **최우선 (2026-08-25 — DYNAMIC을 메인으로 쓰기로 방향 잡음, 아래 섹션 참조)**
>
> 1. **DYNAMIC을 언제 "메인"으로 실제 전환할지 결정 필요.** 08-25 세션에서 DYNAMIC 운영 가능성을 점검하다가 스캔 루프가 코인별 WF 검증을 전혀 안 보고 있던 것(전략은 검증됐어도 실제로 사는 코인은 검증 안 됐을 수 있음)과, 29시간 동안 전체 동적 세션을 멈춰 세운 자기 자신과의 DB 데드락을 발견·수정했다(아래 08-25 섹션). 지금은:
>    - WF 게이트를 스캔 루프에 연결 완료(PAPER는 탐색용으로 제외, LIVE만 강제)
>    - 실제 감시목록 18코인 중 **7~8개에서만** 검증된 (전략,코인) 조합이 나옴 — 나머지는 아직 미검증
>    - Upbit REST 공유 스로틀 경합으로 사이클이 가끔 수 분씩 밀림(부분 완화함, 08-25 섹션 참조)
>    전환 전에 최소한 검증 커버리지를 더 넓히고(WF를 더 많은 후보 코인에 정기적으로 돌리는 체계 필요), 스로틀 경합이 실제로 사이클 지연을 얼마나 줄였는지 하루 정도 관찰한 뒤 판단할 것.

> **차순위 (2026-08-06 벤치마크 측정 결과 — 아래 별도 섹션 참조)**
>
> 1. **실자본 매매 중단 → 페이퍼 전환 검토** — 시스템이 시장 대비 알파 음수(동적 −3.30% vs 알트 −1.63%), 누적 실현손실 −21,854원, 승률 8.9~11.1%. 지금 속도(6일 8거래)로는 통계적 유의성에 도달하기 전에 계속 잃는다. 페이퍼로 전환하면 **잃지 않으면서 같은 데이터를 얻는다**. ✅ **2026-08-06 페이퍼를 LIVE와 동일 로직으로 정렬 완료**(위 별도 섹션) — 이제 페이퍼 결과가 실전 예측에 유효하다. `PaperTradingService`는 07-01 이후 미가동 상태이므로 재가동만 하면 된다.
> 2. **자본 증액 금지** — 기대값이 음수인 상태에서 금액을 늘리면 손실 속도만 빨라진다. 증액 조건은 "벤치마크 대비 알파가 양수임이 통계적으로 증명될 때" 하나뿐.
> 3. ~~**벤치마크 비교를 시스템에 내장**~~ ✅ **2026-08-07 완료** — `BenchmarkAlphaService` + 대시보드 상단 고정 노출(위 별도 섹션).
> 4. ~~**신호 반전 가설 검증**~~ ✅ **2026-08-07 기각** — 반전이 오히려 더 나쁘다(평균 −4.85% → −6.01%). 신호는 거꾸로인 게 아니라 **방향성이 없다**. 손실의 정체는 무작위 진입 + 마찰비용(위 별도 섹션).
> 5. ~~**중단 기준(kill criteria)을 미리 문서화**~~ ✅ **2026-08-18 완료** — [`docs/KILL_CRITERIA.md`](KILL_CRITERIA.md) 제정 + `StrategyKillCriteriaService`로 집행(매일 09:00 KST). 자본 보호 3종은 표본 무관 즉시 발동, 엣지 2종은 n≥20. 아래 08-18 섹션 참조. ⚠️ **자동 정지는 아직 꺼져 있다**(`kill-criteria.auto-stop=false`) — 판정이 며칠간 옳게 나오는지 관찰 후 켤 것.
> 6. **LIVE 세션을 H1로 재생성 필요** — 실전(M5)과 페이퍼(H1)의 타임프레임이 달라 A/B 비교가 성립하지 않는다. 세션 생성 후 타임프레임 변경 API가 없으므로 **정지 후 H1로 재생성**해야 한다(현재 포지션 0건이라 안전). 동적 멀티코인은 REAL/PAPER 쌍이 이미 H1로 맞춰져 있어 문제없다.

> 아래는 코드가 준비돼 있고 실행 여부·시점만 운영 판단이 필요한 것들.

- **워치리스트 최소 이력 조건** (2026-08-26 발견) — 상장 직후라 캔들이 201개 미만인 코인이
  워치리스트에 들어온다(KRW-LIT M15 184개, 08-25 06:50 최초 등장). 각 전략이 자체 최소 캔들
  가드를 갖고 있어 **잘못된 신호가 나오진 않지만**, EMA200 등 장기 지표가 조용히 비활성화된
  채 **축소된 지표 집합으로 도는 것을 아무도 모른다**. 세션 61이 실제로 LIT 포지션을 보유했다.
  `WatchlistFilterService` 에 최소 캔들 수 조건을 넣을지, 아니면 신호 로그에 "축소 지표" 플래그를
  남겨 사후 분리 가능하게 할지 판단 필요. 후자가 표본을 버리지 않아 더 낫다는 의견.
- **Walk Forward 게이트 활성화** (`REQUIRE_WALK_FORWARD_GATE`) — `WalkForwardValidationGate` 구현 완료, 기본값 비활성. 대부분 전략이 Walk Forward 이력이 없어 켜는 즉시 신규 세션 생성이 전면 중단된다. 켜기 전에 `GET /api/v1/strategies/walk-forward-gate-status`로 어떤 전략이 막히는지 먼저 확인하고, 운영 중인 전략들(COMPOSITE_MOMENTUM_ICHIMOKU_V2 등)에 Walk Forward부터 돌려 통과시킬지 판단할 것.
- ~~**LIVE time stop 활성화** (`maxHoldHours`)~~ ✅ **2026-08-18 완료** — 기본값 0 → 24 (V68, LIVE·DYNAMIC·PAPER 3종 동시). 운영 RUNNING 10세션에도 소급 적용. 아래 08-18 섹션 참조. ⚠️ 켜자마자 LIVE 경로에서 중복 SELL 1건이 재현됐다(같은 섹션 "새로 드러난 것" 참조) — 미해결.
- **신호 기대값 자체가 음수인 문제** — 최근 7일 동적 세션 BUY 신호 사후수익률 4h −2.17%/24h −4.47%(n=50). Walk Forward 게이트는 "검증 안 된 전략을 막는" 것이지 "전략 자체를 고치는" 게 아니라서, 게이트를 켜도 이 문제는 해결되지 않는다. 전략/신호 모델 자체를 봐야 하는 별도 과제.
- ~~**시간 초과 청산(time stop) 텔레그램 알림 부재**~~ ✅ **2026-08-18 완료** — `notifyTimeStop` 신설, LIVE·DYNAMIC 배선. 08-18 오전 XRP 4건 청산에 알림이 안 간 것이 실측 사례였다.
- **LIVE 유령 포지션 자동 정산** — 헬스체크(`OperationalHealthCheckService`)는 감지·알림까지만 하고 자동 정산은 안 한다. DYNAMIC의 `reconcileDynamicGhostPositions`에 대응하는 LIVE용 자동 정산을 추가할지는 실거래 자금에 직접 손대는 범위라 별도 검토 필요.
- ~~**e2e 스위트(`@playwright/test`) 미설치**~~ ✅ **2026-08-20 완료** — 설치·수정 후 **42통과/0실패/1스킵**. 아래 08-20 섹션 참조. `Header.tsx` 등 죽은 코드는 08-20 정비에서 삭제 — 현재 **44통과/0실패/0스킵**.
- **StrategyDegradationWatchdog을 DYNAMIC까지 확장할지** — 현재 LIVE(`sessionType=REAL`)만 감시. 저하 발견 시 Discord 알림에 그치지 않고 Walk Forward 게이트와 연동해 자동 재차단할지도 별건.
- **Walk Forward 미리보기 API를 `/strategies` 페이지에 노출** — 현재 API만 있고 프런트 표시 없음.

---

---


## 프로젝트 개요

- **서비스**: 업비트 기반 가상화폐 자동매매 시스템
- **운영 환경**: Ubuntu 서버, Docker Compose (`docker-compose.prod.yml`)
- **기술 스택**: Spring Boot 3.2 (Java 17) + Next.js 16.1.6 / React 19.2.3 (TypeScript) + TimescaleDB + Redis

### 모듈 구조

```
crypto-auto-trader/
├── web-api/          # Spring Boot 백엔드 (Gradle 멀티모듈)
│   ├── core-engine/      # 백테스팅 엔진, 리스크, 포트폴리오
│   ├── strategy-lib/     # 전략 22종 (단일 11 + 복합 11)
│   ├── exchange-adapter/ # Upbit REST/WebSocket
│   └── web-api/          # REST API, 스케줄러, 서비스
├── crypto-trader-frontend/  # Next.js 16.1.6 / React 19.2.3 프론트엔드
├── docs/                    # 설계 문서 및 진행 기록
└── docker-compose.prod.yml  # 운영용 (backend + frontend + db + redis + db-backup)
```

### 구현된 전략 22종

**단일 전략 (11종)**: VWAP / EMA Cross / Bollinger Band / Grid / RSI / MACD / Supertrend / ATR Breakout / Orderbook Imbalance / Stochastic RSI / Volume Delta

**복합 전략 (11종)**:

| 전략 | 구성 | 실적합 코인 | 요약 |
|------|------|------------|------|
| COMPOSITE | Regime 자동 선택 | 범용 | — |
| COMPOSITE_MOMENTUM | MACD×0.5 + VWAP×0.3 + Grid×0.2, EMA 필터 | ETH·SOL | ETH +53.6%, SOL +59.8% |
| COMPOSITE_ETH | ATR×0.5 + OB×0.3 + EMA×0.2 | ETH | 구버전 평균 +48.7% (재검증 필요) |
| COMPOSITE_BREAKOUT (CB) | ATR×0.5 + VD×0.3 + MACD×0.2, EMA+ADX+RSI Veto 필터 | **BTC·ADA** | BTC **+106.71%**, ADA **+86.98%** |
| COMPOSITE_MOMENTUM_ICHIMOKU (CMI_V1) | CB_MOMENTUM + Ichimoku 필터 | XRP | XRP +1.04% (유일 양수) |
| COMPOSITE_MOMENTUM_ICHIMOKU_V2 (CMI_V2) | MACD×0.5 + SUPERTREND×0.3 + Grid×0.2 + Ichimoku 필터 | **DOGE** | DOGE **+124.77%** |
| COMPOSITE_BREAKOUT_ICHIMOKU | CB + Ichimoku 필터 | — | ⚠ CB와 동일 (ADX 중복) |
| COMPOSITE_REGIME_ROUTER (CRR) | ADX/ATR 레짐 → CB/V1/V2 자동 위임 | **SOL·ETH** | SOL **+65.38%**, ETH +65.09% |
| COMPOSITE_MTF_BTC | CB(H1) + Supertrend(H4) | **ETH·AAVE·CHZ** | ETH **+127.70%**, AAVE +28.15% |
| COMPOSITE_MTF_MOMENTUM | CMI_V2(H1) + Supertrend(H4) | **BLUR·DOGE** | BLUR **+48.06%**, DOGE +83.40% |
| COMPOSITE_MTF_CONFIRMED | CRR(H1) + Supertrend(H4) | **XRP** 범용 | XRP **+3.37%** (유일 흑자) |
| MACD_STOCH_BB | MACD + StochRSI + 볼린저 6조건 AND | ❌ 비활성화 | BTC -2.32%, 거래 극희소 |

전체 코인별 백테스트 수치(2026-04-30 H1 FULL, 17코인 × 7전략)와 Walk-Forward 결과는 `old_progress.md` 참조.

---

## 🟢 배포 권고 / 🚨 배포 금지 (2026-04-30 MTF 백테스트 기준, 마지막 갱신)

> H1 FULL 2022~2026-04-30 백테스트 결과 기반. 이후 재검증 없음 — 오래된 기준이니 신규 배포 판단 시 최신 Walk Forward로 재확인 권장.

### Tier 1 — 백테스트 검증 통과

| 코인 | 권장 전략 | FULL 수익률 | MDD | Sharpe |
|------|-----------|------------|-----|--------|
| **BTC** | COMPOSITE_BREAKOUT | +106.71% | -8.88% | 1.24 |
| **ETH** | COMPOSITE_MTF_BTC | +127.70% | -7.24% | 1.35 |
| **SOL** | COMPOSITE_REGIME_ROUTER | +65.38% | -14.93% | 0.76 |
| **DOGE** | CMI_V2 | +124.77% | -30.75% | 0.87 |
| **ADA** | COMPOSITE_BREAKOUT | +86.98% | -14.14% | 0.96 |

### Tier 2 — 흑자 전환·신규 발굴, 관찰 후 투입

| 코인 | 권장 전략 | FULL 수익률 | MDD |
|------|-----------|------------|-----|
| **XRP** | COMPOSITE_MTF_CONFIRMED | +3.37% | -15.67% |
| **AAVE** | COMPOSITE_MTF_BTC | +28.15% | -31.01% |
| **BLUR** | COMPOSITE_MTF_MOMENTUM | +48.06% | -11.91% |
| **CHZ** | COMPOSITE_MTF_BTC | +14.09% | -14.88% |

### 🚨 배포 금지

- 전 코인 × M15 타임프레임(오버트레이딩 -99%), 전 코인 × FAIR_VALUE_GAP(구조 결함)
- ETH/XRP/ADA/CHZ/AAVE × 특정 전략 조합 다수 — 상세는 `old_progress.md` 2026-04-30 섹션 참조
- MOVE/SUPER/FLOCK/AXL/BIO/KERNEL — 거래수 부족으로 통계 신뢰성 없음

---

## Dev Workflow Orchestrator

`.claude/` 6개 서브에이전트(SparkAI → PLAN → Design → Do → Check → Report) 파이프라인 — 상세는 `.claude/ORCHESTRATOR.md` 및 프로젝트 루트 `CLAUDE.md` 참조.






