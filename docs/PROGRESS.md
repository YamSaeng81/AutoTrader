# CryptoAutoTrader — PROGRESS.md

> **목적**: `/clear` 후 새 세션에서 이 파일을 먼저 읽어 현재 상태를 파악한다.
> **갱신 규칙**: 이 파일은 **최신 작업 이력(최근 세션 몇 개) + 보류/결정 대기 항목 + 프로젝트 참조 정보**만 담는다. 오래된 상세 이력은 [`docs/old_progress.md`](old_progress.md)(2026-08-06 이전 전체 백업)와 [`docs/CHANGELOG.md`](CHANGELOG.md)를 참조.
> **2026-08-06 / 2026-08-19**: 파일이 비대해질 때마다 날짜별 상세 이력을 `old_progress.md` 로 이관하고 이 파일에는 요약만 남긴다. 상세 근거·재현 과정이 필요하면 `old_progress.md` 에서 날짜로 검색할 것.
> **마지막 갱신**: 2026-09-02 09:00 — 🔴 **09-14 판정이 불가능한 경로였다** (5전략 중 1개만 n≥20 도달 · 함대가 산 12조합 중 10개가 WF 미검증). 캔들·WF·세션 3종 보정 스크립트 준비. ⚠️ 초기 진단에서 candle_data 대신 market_data_cache 를 본 오류 정정 포함.
> 이전 갱신: 2026-09-02 08:15 — 🟢 **일일 점검 이상 없음**. 게이트 OFF 후 거래 회복 확정(WF 차단 0건) · WS 실시간 감시 지속 · 지문 2종은 H1/M15 구성 차이로 정상.
> 이전 갱신: 2026-09-01 저녁 — 🟢 **WS 실시간 감시 페이퍼 적용 확정** (첫 `실시간 익절(WS)` 청산, 편차 0.04%) · 🟡 텔레그램 서식 거부 시 평문 폴백 추가(“유실”은 오진 — 정정 포함).
> 오후 갱신: 2026-09-01 — 🟢 **함대 점검 이상 없음** · 🔴 **WS 실시간 감시가 오늘에서야 실제 반영** · 🔴 **LLM 응답 본문 유실 버그 수정**(SIGNAL_ANALYSIS 27건 중 24건이 비어 있었다).
> 오전 갱신: 2026-09-01 — 🔴 **PAPER 세션이 화면·안전망에서 통째로 빠져 있었다**(session_kind 하드코딩 4곳) · SL 미점검 알림 오탐 수정 · 세션 LLM 분석 신규.
> 이전 갱신: 2026-08-31 — 🔴 **COMPOSITE_PULLBACK_MTF 정지 결정** — 실거래(143거래 −3,135원)와 Walk Forward(928거래 7코인 전부 음수)가 독립적으로 같은 결론. WF 42조합 중 6개만 게이트 통과.

---

## 🔴 2026-09-02 09-14 판정이 불가능한 궤도였다 — 캔들·WF·세션 3종 보정

09-02 점검 후 "표본을 기다리는 것 말곤 할 게 없나"를 확인하다가, **09-14 에 결정 자체를
못 하게 되는 두 가지**를 찾았다.

### 문제 1 — 5전략 중 1개만 n≥20 에 닿는다

재기동 후 2.09일 실측 vs 동결 선언 때 세운 예상표:

| 전략 | 실제/일 | 예상표 | 12일 후 예상 n | n≥20 |
|---|---|---|---|---|
| MTF_BTC | 1.43 | 3.4 | 20.2 | 겨우 |
| MTF_CONFIRMED | 0.96 | 2.0 | 13.5 | ✗ |
| MOMENTUM_ICHIMOKU | 0.48 | 1.8 | 6.8 | ✗ |
| MEANREV_BB | **0.00** | 2.4 | 0 | ✗ |
| MOMENTUM_ICHIMOKU_V2 | **0.00** | 1.8 | 0 | ✗ |

> 예상표가 틀린 이유는 단순하다 — 그 숫자는 **구 함대(16세션, PULLBACK_MTF 5세션 포함)**
> 속도였고 지금은 9세션이다. **분모를 같이 적지 않아서** 두 번(거래량 목표 26~37건/일,
> 표본 속도) 연달아 오독했다. 앞으로 속도·목표치를 적을 때는 반드시 세션 수를 함께 적을 것.

### 문제 2 — WF 실행 이력이 비어 있다 (⚠️ 최초 진단은 틀렸다)

> 🔴 **먼저 정정.** 처음에 "워치리스트 20코인 중 WF 가능한 것은 3개뿐"이라고 적었는데,
> 그건 **`market_data_cache`**(실거래 시세 캐시)를 본 것이었다.
> 백테스트·WF 가 읽는 것은 **`candle_data`** 다.
>
> | 테이블 | H1 규모 | 용도 |
> |---|---|---|
> | `candle_data` | **903,030행 / 52코인** | 백테스트·WF 백필 저장소 |
> | `market_data_cache` | 44,314행 / 65코인 | 실거래 틱용 캐시(최근 몇 주) |
>
> 두 테이블이 비슷한 이름·비슷한 스키마라 그대로 진단을 썼다.
> **쓰는 쪽(수집)과 읽는 쪽(WF)이 같은 테이블인지 먼저 확인했어야 했다.**

`candle_data` 기준 실제 커버리지는 **19코인 중 12개 WF 가능**이다:

| 구분 | 코인 |
|---|---|
| ✅ WF 가능 (12) | BTC 40,803 · ETH 40,802 · SOL 40,801 · XRP 40,800 · ONT 40,747 · ICX 40,687 · ARB 29,937 · ONDO 19,334 · UNI 16,246 · TRUMP 13,491 · AXL 10,647 · ENA 9,948 |
| ❌ 부족 (7) | PROM 276 · CRV 242 · ZKC·DOS·LA·MIRA·0G 각 0행 |

**진짜 공백은 캔들이 아니라 WF 실행 이력이다.** ICX(40,687행)·ARB(29,937)·UNI(16,246)·
AXL(10,647)은 캔들이 충분한데 **WF 를 한 번도 안 돌렸다.** 데이터가 없어서가 아니라
그냥 안 한 것이다. → **수집을 기다릴 필요 없이 60조합(12코인 × 5전략) WF 를 바로 돌릴 수 있다.**

다만 통과 조합이 적다는 사실은 그대로다 — 워치리스트 중 통과가 있는 코인은
**BTC·ENA·ONDO 3개뿐**이고, **함대가 실제로 산 12조합 중 10조합이 WF 이력 0건**이다.
FLOCK +617 · DOS −437 · TREE −436/−450 — 전부 미검증 코인에서 나온 손익이다.

> ⚠️ **곁가지 — M15 는 WF 를 못 돌린다.** `candle_data` 의 M15 는 18코인 / 최신 캔들
> 03-30 에 멈춰 있다. **M15 세션(81~84 등)은 WF 검증 자체가 불가능**하며,
> 이것도 09-14 결정의 입력값이다.

### 병목은 전략마다 다르다 (증설 설계의 근거)

게이트 반영(09-01 09:37) 이후 22시간:

| 전략 | 세션 | BUY | 체결 | '이미 보유' 차단 | 진단 |
|---|---|---|---|---|---|
| MTF_CONFIRMED | 2 | 9 | 3 | **6** | 자리 부족 → 증설 효과 큼 |
| MOMENTUM_ICHIMOKU | 2 | 7 | 2 | **5** | 자리 부족 → 증설 효과 큼 |
| MTF_BTC | 2 | 5 | 3 | 0 | 신호가 적음 |
| MEANREV_BB | 1 | 1 | 1 | 0 | M15 세션이 아예 없음 |
| MOMENTUM_ICHIMOKU_V2 | 2 | **1** | 1 | 0 | **신호 부족 — 증설 무의미** |

### 조치 — 스크립트 3종 (전부 동결 허용 범위)

| # | 스크립트 | 내용 |
|---|---|---|
| 1 | [`collect_watchlist_candles_0902.sh`](../scripts/collect_watchlist_candles_0902.sh) | 부족한 **7코인**만 H1 백필. 나머지 12코인은 이미 충분하다 |
| 2 | [`walk_forward_watchlist_0902.sh`](../scripts/walk_forward_watchlist_0902.sh) | 5전략 × 자격코인 WF. PRECHECK 가 `/data/summary`(=`candle_data`)를 보므로 **수집 없이 지금 돌려도 12코인이 잡힌다** |
| 3 | [`expand_dynamic_fleet_0902.sh`](../scripts/expand_dynamic_fleet_0902.sh) | 세션 9 → 16 (+7) |

**증설 내역** — MTF_CONFIRMED · MOMENTUM_ICHIMOKU · MTF_BTC 각 H1+M15 (+6),
MEANREV_BB M15 신설 (+1). **ICHIMOKU_V2 는 의도적으로 늘리지 않는다.**

> 🔴 ICHIMOKU_V2 를 빼는 것은 게으름이 아니라 판단이다. 22시간에 BUY 1건 / HOLD 1,284건은
> 표본 문제가 아니라 **전략이 신호를 안 내는 것**이다. 09-14 에 n=0 이면 그건
> "판정 불가"가 아니라 **"이 전략은 거래를 하지 않는다"는 것 자체가 판정 근거**다.
> 세션을 늘려 억지로 표본을 만들면 그 사실이 가려진다.

### 동결과의 관계

세션 **개수**는 금지 목록(게이트 임계값·노출 상한·청산 규칙·워치리스트 필터·전략
파라미터·신규 A/B)에 없다. 새 세션은 기존과 파라미터가 완전히 동일하므로
**지문도 같아 표본을 합칠 수 있다.** 규칙은 그대로고 관측 채널만 는다.

현재 지문: H1 = `eb87e77d4b86` · M15 = `95a8d3370709`
→ 새 세션 첫 진입 후 이 값과 같은지 **반드시 확인**한다. 다르면 표본이 갈라진 것이므로
새 세션을 정지해야 한다.

### 남는 위험 — 09-07 에 볼 것

**노출 상한(2)이 새 병목이 될 수 있다.** 09-02 현재 상한 차단은 0건이지만, 세션이 16개가
되면 같은 코인에 3세션 이상 몰리는 일이 늘어난다. 그러면 "누가 먼저 잡느냐"가 표본을
가르고 그건 편향이다. **09-07 점검에서 차단 사유에 노출 상한이 등장하는지 반드시 확인할 것.**

### 신규 상장 코인 문제 (09-14 결정의 입력값)

ZKC·DOS·LA·MIRA·0G 는 `candle_data` 가 0행이라 수집해도 2,000행에 못 미칠 가능성이 높다
(AXL 10,647 · ARB 29,937 은 이미 충분하다 — 첫 진단에서 신규 상장으로 묶은 것은 틀렸다).
거래소에 이력 자체가 없기 때문이다. **그건 실패가 아니라 결론이다** —
"게이트 ON = 신규 상장 코인 영구 배제"라는 교환을 받아들일지가 09-14 결정의 일부가 된다.
얇은 표본으로 WF 를 강행하면 "통과"로 나와 검증됐다고 착각하게 만든다(08-28 교훈).


---

## 🟢 2026-09-02 08:15 일일 점검 — 이상 없음, 09-01 미결 항목 판정 완료

### ① 게이트 OFF 후 거래 회복 — 확정

게이트 반영(09-01 09:37:56) 이후 **WF 차단 0건**. BUY 22건 중 체결 9건이고,
나머지 13건은 전부 정상 사유였다.

| 차단 사유 | 건수 | 성격 |
|---|---|---|
| POSITION_MONITORING — 이미 보유 중 | 11 | 정상 (한 세션 한 포지션) |
| BLACK_SWAN_GUARD (거래량 9.6배 + 1시간 −5.46%) | 2 | 정상 방어 |

> ⚠️ **"하루 26~37건" 목표치는 잘못된 기준이었다.** 그 숫자는 **구 함대(16세션)** 시절
> 속도다. 현재는 9세션이다. 표본 축적 예상표의 전략별 합계가 11.4건/일인데
> 09-01 진입 8건 · 09-02 08시까지 3건이므로 **정상 범위**다.
> 판정 기준을 세울 때 분모를 함께 적지 않으면 다음에 또 오독한다.

### ② WS 실시간 감시 — 계속 작동

| 경로 | 건수 |
|---|---|
| 실시간(WS) | 3 (DOS 손절 · ENA 손절 · FLOCK 익절) |
| 폴링 | 2 (KRW-TREE 2건) |

**KRW-TREE 만 2/2 폴링**이다. WS 와 폴링은 경합 관계(먼저 도달한 쪽이 처리)이므로
WS 가 죽은 게 아니라 **TREE 의 WS 틱이 드물어 60초 폴링이 먼저 닿은 것**이다.
신규 상장·저유동성 코인은 실전에서도 같은 지연을 겪는다 — 페이퍼가 실전을 그대로
반영하고 있다는 뜻이기도 하다. 조치 불필요.

### ③ 지문 2종 — 동결 위반이 아니다

`eb87e77d4b86`(H1 세션 76·77·79·80) / `95a8d3370709`(M15 세션 81~84) 로 갈렸다.
확인해보니 **`watchlist_refresh_min` 이 H1=60 · M15=30 으로 다른 것**이 유일한 차이고,
나머지 인자(손절률·투자비율·최대보유·후보수·ATR·스프레드)는 전부 동일하다.

> 09-01 에는 M15 세션 2건만 진입해서 지문이 하나로 보였다. H1 세션이 거래를 시작하며
> 두 번째가 나타난 것이다. **타임프레임 구성이 둘이니 지문도 둘이 정상**이며,
> 집계할 때 이 둘을 합치지만 않으면 된다.

### ④ 그 외

- 9세션 RUNNING, **서킷 발동 0**, 불변식 3종·② 전부 0
- 노출 상한 — KRW-ARB(77·80), KRW-CRV(81·84) 각 2세션. **상한 2에 정확히 걸려 있고 초과 없음**
- `SL 미점검` 알림 **0건** (배포 전 시간당 20~31건 → 0). 오탐 수정 확정
- 주문 BUY 9 · SELL 6 전부 FILLED. FAILED 4건은 전부 07-31 세션 38 / 08-18 세션 198 잔재로
  **현 함대와 무관**
- 앞선 스냅샷의 `idle in transaction` 1건은 재조회 시 사라짐 — 순간값이었다

### ⑤ 주시할 것

**세션 79 (MTF_BTC H1) MDD 11.25%** — 한도 20%까지 8.75%p 여유. DOS(−437) · ENA(−423)
연속 손절 2건이 원인이다(연속 손실 한도 5). 함대 최고치이므로 09-07 에 다시 볼 것.

| 세션 | dd% | 여유 |
|---|---|---|
| 79 | 11.25 | 8.75%p |
| 84 | 4.78 | 15.22%p |
| 77 | 4.55 | 15.45%p |

**표본 축적** — 09-01 09:37 이후 청산 6건. 09-14 까지 12일 남았고 현 속도(~5건/일)면
총 60건, 전략별 ~12건이다. **5전략 전부 n≥20 도달은 어려울 수 있다.**
09-07 에 속도를 다시 재고, 미달이면 판정 방식을 조정할지 그때 결정한다(동결 중 변경 아님).


---

## 🟢 2026-09-01 WS 실시간 감시 페이퍼 적용 — 실측으로 확정됐다

**세션 84(DYN_PAPER)의 KRW-FLOCK 청산이 결정적 증거다.**

```
position 3245  세션 84  KRW-FLOCK  TAKE_PROFIT  +617원  10:04:01
order    10379 사유: 실시간 익절(WS) — 현재가 61.8 ≥ 61.77600000
```

**DYN_PAPER 이력 96건 중 0건이던 `실시간(WS)` 청산이 처음 나왔다.** 익절가 61.776 에 61.8 체결
— **0.04% 이내**다. 60초 폴링이었다면 이보다 벌어졌을 값이고, 실제로 이전 페이퍼 청산은
전부 `손절 —`(폴링) 문구였다.

부수 확인:
- `[WsSubscription] 구독 갱신: [KRW-FLOCK, KRW-ONDO]` — 둘 다 페이퍼 보유분
- 배포(09:37:56) 이후 `SL 미점검` 알림 **0건**, 로그도 0건
- 게이트 OFF 반영 후 거래 재개 — 세션 83 이 KRW-TREE 신규 진입(10:15)

> 이로써 **페이퍼가 실전의 리허설**이라는 전제가 SL/TP 감시 주기에서도 성립한다.
> 08-31 에 "해소"라고 적었던 것이 실제로는 오늘 반영됐다는 점은 위 항목에 기록했다.

---

## 🟡 2026-09-01 텔레그램 서식 폴백 — 그리고 내가 틀린 진단을 했던 기록

### 먼저, 앞선 진단은 틀렸다

`llm_call_log` id=1069 는 본문을 정상 저장했는데(`resp_len=828`)
`telegram_notification_log` id=92855 가 `success=false` 로 남은 것을 보고
**"별표 때문에 MarkdownV2 파싱이 깨져 메시지가 유실됐다"**고 판단했다. 그래서 LLM 전송
경로를 전부 평문으로 바꿨다.

**틀렸다.** 사용자가 받은 10:13 메시지가 바로 그 건이었다 — **메시지는 정상 도착했고
`**제목**` 서식도 제대로 렌더링됐다.** 10:00 이후 발송 이력은 그 한 건뿐이라 다른 후보도 없다.

> 서식이 깨졌다면 별표가 글자 그대로 보였을 것이다. 화면에는 안 보였다 —
> **파싱은 성공한 것**이고, 나는 그 반대 증거를 보고도 로그의 `success=false` 만 믿었다.
>
> 그대로 배포했다면 잘 나오던 서식을 잃고 `**1. 진단**` 이 글자로 노출됐을 것이다.
> **고장 신호(로그)와 실제 결과(수신 화면)가 어긋날 때는 실제 결과가 우선한다.**

평문 전환은 되돌렸다. LLM 분석은 MarkdownV2 를 그대로 쓴다.

### `success=false` 의 실제 원인

`sendMarkdown` 은 HTTP 200 이 아니면 무조건 `false` 를 돌려줬다 — **"거부당함"과
"응답을 못 받음"을 구별하지 않았다.** 10:12 건은 후자로 보인다(요청은 전달됐고 응답만
못 받은 상태). 그래서 도착한 메시지가 실패로 기록됐다.

### 그럼에도 남는 진짜 위험

`escapeMarkdownV2` 가 `*` `[` `]` `` ` `` 를 이스케이프하지 않는 것은 사실이다. 알림 템플릿이
그 문자로 굵게·코드 서식을 쓰기 때문인데, 템플릿은 짝을 맞춰 쓰니 안전하고 **밖에서 온
텍스트**는 그렇지 않다. 짝이 안 맞는 응답이 오면 400 이 나고 그때는 **정말로 유실된다.**
(최근 7일 `WF_BATCH_COMPLETE` 3건이 3건 모두 실패로 기록된 것도 같은 계열일 수 있다 —
확인은 못 했다.)

### 그래서 넣은 것 — 조건부 폴백

| 상황 | 동작 | 이유 |
|---|---|---|
| HTTP 200 | 그대로 종료 | 서식 유지 |
| **non-200 (거부)** | **평문으로 1회 재전송** | 전달되지 않았음이 확실 — 서식을 버리고 내용을 살린다 |
| 응답 미수신(타임아웃 등) | **재전송하지 않음** | 이미 도착했을 수 있다 — 재전송하면 중복 알림 |

이스케이프 목록을 늘리는 건 답이 아니다. `*` 를 이스케이프하면 **기존 템플릿의 의도된
굵게·코드가 전부 글자 그대로** 나온다 — 고치려다 다른 걸 깨는 셈이다.

부수 변경:
- `post(text, parseMode)` 로 전송을 분리하고 **HTTP 상태코드를 그대로 반환**한다.
  응답 미수신만 `-1` 이다 — 둘을 같은 `false` 로 뭉개면 폴백 여부를 정할 수 없다.
- `SendMessageRequest` 에 `@JsonInclude(NON_NULL)` — `parse_mode` 를 필드째 뺀다.
- `telegramApi` · `requestTimeout` 을 상수에서 필드로. `static final String` 은 컴파일 시
  인라이닝돼 테스트에서 스텁 서버로 돌릴 방법이 없고, 10초를 실제로 기다리는 테스트는
  아무도 안 돌린다.

### 테스트

`TelegramMarkdownFallbackTest` (6건, 스텁 HTTP 서버) — 200 성공 시 재전송 없음 /
400 거부 시 평문 폴백 / 폴백까지 실패하면 실패 보고 / 폴백 본문에 `parse_mode` 없음 /
**응답 미수신 시 폴백 안 함** / 연결 거부.

> ⚠️ 이 테스트를 처음 썼을 때 **뮤테이션이 안 잡혔다.** 스텁 서버의 기본 실행기가 요청을
> 한 줄로 순차 처리해서, 첫 요청이 지연되는 동안 폴백 요청이 큐에 갇혀 "폴백이 안 왔다"로
> 보였기 때문이다. `server.setExecutor(FixedThreadPool)` 로 바꾸고 나서야 잡혔다.
> **테스트가 통과하는 것과 그 테스트가 회귀를 잡는 것은 다른 명제다.**

전체 456건 통과.


---

## 🟢 2026-09-01 DYNAMIC 함대 재기동 다음날 점검 — 이상 없음

세션 76~84, 재기동 2026-08-31 06:25 기준. 운영 DB 읽기 전용 조회.

| 항목 | 결과 |
|---|---|
| 세션 가동 | 9세션 RUNNING, 마지막 로그 2~4분 전 |
| 서킷 브레이커 | **발동 0건**, trip_count 전부 0 |
| MDD 고점 | 81 = 0.33% · 84 = 1.09%, 나머지 NULL |
| 불변식 3종 | 중복 OPEN 0 · SCANNING인데 보유 0 · idle in transaction 0 |
| 불변식 ② | 위반 0 (역방향 고아 잔고도 0) |
| 최소 이력 가드 | 정상 — 요구량 미만 평가 **0건** |
| 노출 상한 2 | 같은 코인 동시 보유 **최대 1세션** |
| 주문 | BUY 12·SELL 20 전부 FILLED, 실패 0 · 미체결 잔류 0 |
| 규칙 지문 | `95a8d3370709` 단일 |

거래는 진입 2건(81 KRW-ONDO 06:45, 84 KRW-FLOCK 08:30), 청산 0건.

### ⚠️ 최소 이력 가드 — 첫 집계는 오독이었다

처음에 KRW-FLOCK·ICX가 "캔들 0개인데 평가됨"으로 나와 가드 실패를 의심했다. 그런데
**H1 캔들만 센 쿼리**였다. 세션 81·84 는 M15 이고 FLOCK 은 M15 캔들 480개, ONDO 는 1,076개다.
세션 타임프레임에 맞춰 다시 보니 요구량 미만으로 평가된 코인은 0건이다
(H1 최소 498개 · M15 최소 386개). **가드는 정상이다.**

> 교훈: 타임프레임별로 저장되는 데이터를 단일 타임프레임으로 집계하면 없는 문제가 보인다.

### WF 게이트 — `.env` 변경이 재빌드에서야 반영됐다

08-31 15시부터 BUY 차단이 시작돼 **09-01 06:45:05까지** 계속됐다. 재기동 이후 누계
**BUY 21건 중 체결 2건(9.5%)**, WF 차단 17건. 7세션이 하루 넘게 한 건도 사지 못했고
그중 4세션(76·77·80·82)이 게이트 때문이었다(78·79·83 은 BUY 신호 자체가 0 — `점수 미달`,
전략 정상 동작).

`.env` 는 `REQUIRE_WALK_FORWARD_GATE=false` 였다. 09-01 09:37:56 재빌드로 새 컨테이너가
그 값을 읽고 떴다. **08-31 23:15 의 변경이 오늘 재빌드에서야 적용된 것**으로 보인다
(`docker compose restart` 는 `.env` 를 다시 읽지 않고 기존 컨테이너 환경을 그대로 쓴다 —
확정은 아니고 가장 그럴듯한 설명).

> ⚠️ **08-31 23:21 보고를 정정한다.** 그때 "WF 차단이 17건 → 0건으로 떨어졌으니 게이트가
> 꺼진 것이 행동으로 확인됐다"고 적었는데, 근거가 약했다. 그 6분간 **BUY 신호 자체가 0건**이라
> 막을 것이 없었을 뿐이다. **차단 0을 게이트 OFF 의 증거로 읽으면 안 된다** — 분모를 봐야 한다.

---

## 🔴 2026-09-01 WS 실시간 감시는 오늘에서야 실제로 반영됐다

08-31 에 "PAPER 에 WS 실시간 SL/TP 감시를 열었다"고 기록했지만, **운영에는 반영되지 않은
상태였다.** 오늘 09:37 배포로 처음 동작했다.

### 증거 — SL 미점검 알림 이력

| 시각 | 알림 |
|---|---|
| 09-01 06시 | 5 |
| 09-01 07시 | 20 |
| 09-01 08시 | 31 |
| 09-01 09시 (09:35:45까지) | 24 |
| **09:37:56 배포 이후** | **0** |

원문을 보면 세션 81(ONDO)·84(FLOCK)이 **정확히 3분마다 번갈아** 경고했다.
`lastSlCheckAt` 은 오직 `doOnRealtimePriceEvent` 에서만 기록되므로, 계속 미점검이었다는 건
**그 핸들러가 페이퍼 포지션에 대해 한 번도 돌지 않았다**는 뜻이다.

전체 이력에서도 같은 그림이다 — `실시간(WS)` 청산이 LIVE 27건 · DYNAMIC(실전) 6건인데
**DYN_PAPER 는 96건 중 0건**이었다.

### 배포 후 확정

```
[WsSubscription] 구독 갱신: 2개 코인 [KRW-FLOCK, KRW-ONDO]
```
둘 다 PAPER 보유분이다. 그리고 배포 후 20분간 `SL 미점검` **로그가 0건** —
새 워치독은 알림을 안 보내도 로그는 항상 남기므로, 로그가 없다는 건 미점검 상태 자체가
발생하지 않았다는 뜻이다. **실시간 핸들러가 페이퍼에서 정상 동작한다.**

> ⚠️ **09-01 오전 진단을 정정한다.** 이 알림 폭주를 "거래가 뜸한 알트코인의 정상적인 틱 공백"
> 으로 진단했는데, **절반만 맞았다.** 알림이 과했던 건 사실이지만 근본 원인은
> **감시가 실제로 비어 있었던 것**이다. 오탐이 아니라 진짜 경고였고, 내가 그걸 소음으로 읽었다.
> 다행히 그 수정에 "구독 산정을 실제 OPEN 포지션 기준으로" 바꾼 것이 함께 있어 둘 다 해소됐다.

---

## 🔴 2026-09-01 P0 — LLM 응답 본문이 몇 주간 조용히 사라지고 있었다

세션 LLM 분석을 실행했더니 텔레그램에 **머리말만 오고 본문이 없었다.** 파보니 오늘 만든
기능의 문제가 아니라 **기존 프로바이더의 파싱 버그**였고, 12시간 리포트에도 계속 영향을
주고 있었다.

### 증상

`llm_call_log` id=1068: `success=true`, `completion_tokens=1474`, **`response_content` 길이 0**.
모델은 1,474 토큰을 실제로 생성했는데 저장된 본문이 없다.

### 원인

`ClaudeProvider` 가 `json.at("/content/0/text")` 로 본문을 읽었다. 그런데 Messages API 의
`content` 는 **블록 배열**이고 첫 블록이 항상 텍스트가 아니다 — 모델이 생각하면 `thinking`
블록이 앞에 붙는다. 그러면 `/content/0/text` 가 없어 `asText("")` 가 **빈 문자열**을 돌려준다.
그게 `success=true` 로 나가니 호출부는 "분석 결과가 빈 것"을 정상으로 받아 **빈 리포트를 발송**했다.

### 피해 규모 (최근 14일)

| 태스크 | 호출 | 빈 응답 | 평균 출력 토큰 |
|---|---|---|---|
| LOG_SUMMARY | 28 | 0 | 315 |
| NEWS_SUMMARY | 36 | 10 | 471 |
| REPORT_NARRATION | 20 | 11 | 724 |
| **SIGNAL_ANALYSIS** | 27 | **24 (89%)** | 977 |

**출력이 길수록 빈 응답이 많다** — 길고 어려운 요청일수록 모델이 생각하기 때문이다.
토큰 비용은 그대로 나가면서 결과만 사라졌다.

### 수정

1. **모든 `type=="text"` 블록을 순서대로 이어 붙인다** — thinking·tool_use 는 건너뛴다.
   텍스트가 여러 블록으로 쪼개져 오는 경우도 함께 처리된다.
2. **토큰을 썼는데 본문이 비면 `success=false`** 로 돌린다. 조용히 빈 리포트가 나가는 것보다
   "실패했다"고 말하는 편이 낫다. 로그에 `stopReason` 과 블록 종류를 남겨 원인을 바로 본다.

### 테스트

- `ClaudeResponseParsingTest` (7건) — thinking 선행 / 다중 텍스트 블록 / tool_use 혼재 /
  잘린 응답 / 깨진 JSON
- `ClaudeProviderHttpTest` (4건) — **스텁 HTTP 서버로 `complete()` 전체 경로**를 탄다

> ⚠️ 처음엔 `extractText` 단위 테스트만 썼는데, `complete()` 안에서 `/content/0/text` 로
> 되돌리는 뮤테이션이 **통과했다** — 단위는 잡아도 배선을 안 잡았기 때문이다.
> HTTP 레벨 테스트를 추가하니 그 뮤테이션이 잡혔다. 파싱 함수를 고쳤다는 것과
> 그 함수를 실제로 쓴다는 것은 다른 명제다.

전체 446건 통과.


---

## 🔴 2026-09-01 PAPER 세션이 화면·안전망에서 통째로 빠져 있었다 — `session_kind` 하드코딩 4곳

프런트엔드 보강 요청("동적 상세보기의 손익 분해·보유 코인 이력이 안 맞는 것 같다")에서
출발했는데, 원인이 화면이 아니라 **백엔드가 페이퍼 데이터를 조회조차 못 하고 있던 것**이었다.

### 무슨 일이 있었나

동적 세션의 포지션은 `session_kind` 로 REAL/PAPER 를 가른다 — REAL=`DYNAMIC`, PAPER=`DYN_PAPER`(V67).
그런데 조회·감시 경로 네 곳이 `"DYNAMIC"` 을 **문자열로 박아** 두고 있었다.

| 위치 | 증상 | 심각도 |
|---|---|---|
| `DynamicSessionController#positions` | 모의 세션 **보유 코인 이력 0건** (실제로는 거래가 있었는데도) | 화면 |
| `DynamicSessionController#buildPnlBreakdown` | 모의 세션 **손익 분해 전부 0** · 승률 `—` | 화면 |
| `RiskManagementService#checkCircuitBreaker(Dynamic…)` | 모의 세션 **연속 손실 차단이 통째로 무력** | 🔴 안전망 |
| `OperationalHealthCheckService` (유령·고착 포지션) | 모의 포지션이 점검 대상에서 제외 | 관측 |

**화면이 스스로 모순됐다.** 현재 포지션 패널만 `currentPositionId` 로 직접 조회해 정상
표시되니, "코인을 들고 있는데 이력은 0건이고 손익은 0원"인 화면이 나왔다. 페이퍼 함대
9세션이 전부 이 상태였다. 사용자가 "정보가 안 맞는 것 같다"고 한 게 정확했다.

### 서킷 브레이커 쪽이 진짜 문제였다

MDD 책은 세션 필드만 보므로 정상 작동했고, **08-29~30 발동 5건이 전부 MDD 였던 것이
이 결함의 증거**다 — 연속 손실 책은 페이퍼에서 한 번도 걸릴 수 없었다. 조회가 항상
비었기 때문이다. 페이퍼는 실전의 리허설이므로 안전망도 같아야 한다
(`DynamicPaperLiveRuleParityTest` 가 지키는 원칙).

> ⚠️ **동결 기간 중 동작 변경**이다. 08-31~09-14 동결은 "규칙 변경 금지 / P0 버그 수정 허용"
> 이고, 이건 의도된 안전망이 꺼져 있던 것을 되살린 것이라 P0 로 분류했다. 다만 앞으로
> 페이퍼 세션이 연속 손실로 정지될 수 있다 — 그게 원래 설계다.

### 고친 방식

문자열을 다시 적지 않는다. `DynamicTradingService.sessionKind(session)` 을 `public` 으로
열어 **단일 출처**로 쓴다. `FEE_RATE` 도 같은 이유로 공개해 컨트롤러가 체결 경로와 같은
값을 참조한다.

- 회귀 테스트: `DynamicSessionPaperVisibilityTest` (8건) — PAPER/REAL 을 **같은 방식으로**
  만들어 양쪽에서 같은 데이터가 보이는지 본다. 한쪽만 보이면 실패한다.
- 뮤테이션 확인: `kindOf(...)` → `"DYNAMIC"` 으로 되돌리면 **5건이 정확히 실패**한다.

---

## 🟢 2026-09-01 동적 세션 상세 화면 보강 — 손익·수수료·사유

`session_kind` 를 고치고 나서, 실제로 볼 수 있게 된 데이터를 화면에 붙였다.

### 수수료를 어떻게 표시할 것인가 (주의)

`realizedPnl` 은 **이미 매수·매도 수수료를 모두 뺀 순손익**이다 — 매수 시 `avgPrice` 를
수수료 포함 취득단가(`investedKrw / quantity`)로 잡기 때문이다. 화면에서 "손익 − 수수료"를
또 빼면 **이중 차감**이 된다. 그래서:

- `entryFee` / `exitFee` / `totalFee` — 참고용으로 각각 환산해 내려보낸다
- `grossPnl` = 순손익 + 수수료 — "수수료가 없었다면" 의 가정값. 마찰비용이 성과를 얼마나
  갉아먹었는지 보는 용도지 실제 손익이 아니다
- 화면에 이 구분을 문장으로 명시했다

`position.position_fee` 컬럼은 청산 시 **매도 수수료로 덮어써진다**(총합이 아니다).
그대로 표시하면 매수 수수료가 사라진 것처럼 보이므로 컨트롤러에서 양쪽을 각각 계산한다.
쓰기 경로는 동결 중이라 건드리지 않았다.

### 추가된 정보

| 영역 | 추가 |
|---|---|
| 손익 분해 | 지불 수수료 · 수수료 전 손익 · 건당 평균 · 평균 보유시간 · 최고/최대손실 종목 · **청산 사유 분포** |
| 보유 코인 이력 | **청산가** · 청산 사유 배지(손절/익절/시간초과…) · 매수·매도 수수료 · 수수료 전 손익 · 손절가·익절가 · 규칙 지문 · 매수/매도 **체결 시각** |

청산 사유는 `ExitReason` enum(V73) 축을 그대로 쓴다 — "손절 대 익절 대 시간초과 비율"이
성과 수치보다 먼저 읽혀야 한다. `FORCED_STOP` 이 섞이면 "전략 성과로 읽지 말라"고 명시한다.

---

## 🟢 2026-09-01 세션 LLM 분석 → 텔레그램 (신규)

전략 로그 화면에서 세션을 하나 고르고 **[LLM 분석]** 을 누르면, 그 세션의 누적 통계를
LLM 에 보내 전략 진단을 받고 결과를 텔레그램으로 받는다.

- 진입점: `/logs` 화면 · `POST /api/v1/logs/llm-analysis?sessionType=&sessionId=&hours=`
- 구현: `SessionLlmAnalysisService` — 기존 `LlmTaskRouter`(`SIGNAL_ANALYSIS` 태스크) 재사용
- 구간 선택: 6시간 / 24시간 / 3일 / 7일 (포지션은 세션 전체 기간)

### 설계 판단

**원문 로그를 통째로 보내지 않는다.** 세션 하나에 로그가 수만 건이라 컨텍스트를 넘기고,
더 중요하게는 **LLM 이 원문에서 다시 집계하면 그 숫자를 믿을 수 없다**. 통계는 코드가
정확히 내고 LLM 에는 **해석만** 맡긴다. 원문은 판단 근거용으로 최근 30건만 표본으로 붙인다.

프롬프트에 못 박은 규칙:
- 청산 20건 미만이면 **"표본 부족"을 먼저 말하고** 승률·수익률을 결론 근거로 쓰지 말 것
- 주어지지 않은 지표(샤프·MDD)를 **추정하지 말 것**
- 듣기 좋은 말 대신 문제를 지목할 것

집계에 규칙 지문(`ruleset_hash`) 분포를 넣어, 여러 규칙이 섞인 표본이면 LLM 이 그걸
먼저 지적하도록 했다.

### 운영상 주의

- **비동기** — LLM 이 수십 초 걸려 HTTP 를 붙잡으면 프록시 타임아웃에 걸린다. 즉시 접수
  응답을 주고 결과는 텔레그램으로 보낸다. `@Lazy self` 로 프록시를 경유시켰다
  (self-invocation 이면 `@Async` 가 무시된다 — `DynamicTradingService.self` 와 같은 이유).
- **2분 쿨다운** — 버튼 연타가 그대로 과금되면 안 된다.
- **로그가 0건이면 호출하지 않는다** — 빈 입력으로 부르면 그럴듯한 소설이 돌아오는데,
  그게 가장 나쁜 결과다.
- 호출 원문·응답은 `llm_call_log` 에 남으므로 텔레그램을 놓쳐도 `/logs/llm` 에서 볼 수 있다.
- 텔레그램 4096자 한도를 넘으면 나눠 보낸다 — 잘려서 결론이 사라지면 안 된다.

> 로그 화면 필터에 **`DYN_PAPER`(동적/모의) 탭이 없었다** — 페이퍼 함대 9세션의 로그를
> 탭으로 걸러볼 방법이 아예 없었다. 같이 추가했다.

---

## 🔴 2026-09-01 "SL 미점검 3분 초과" 알림이 무한 반복되던 문제

사용자 보고: 이 메시지가 텔레그램으로 계속 오고 있다. 원인은 **두 가지가 겹친 것**이었다.

### ① 대상 선정이 LIVE 와 달랐다

LIVE 워치독은 "**OPEN 포지션이 실제로 있는** 세션"만 본다. DYNAMIC 은
`scanState == POSITION_MONITORING` 이라는 **세션 필드**만 봤다.

상태 필드와 실제 포지션이 어긋나면(08-25 세션 60이 그랬다) 실시간 핸들러는 감시할 포지션을
못 찾아 `recordSlCheck` 에 도달하지 못한다 → 워치독은 그걸 미점검으로 읽는다 →
**60초마다 영원히** 경고. 게다가 `forceRefreshPrice` 는 시세 조회에 성공하므로 알림은 매번
**"감시를 재개했습니다"라고 사실이 아닌 말**을 했다.

### ② 자가복구 성공까지 알렸다

이 워치독은 미점검을 발견하면 REST 로 시세를 한 번 끌어와 **스스로 복구한다**. 거래가 뜸한
알트코인은 WS 틱이 3분 넘게 안 오는 게 정상이라, 그때마다 "이상 → 복구" 알림이 반복됐다.
동적 함대는 워치리스트 알트코인을 도는 전략이라 이 조건에 상시 해당한다.

> **상시 울리는 알림은 아무도 안 본다** — 진짜 고장이 왔을 때 구별할 방법이 사라진다.
> 그게 이 알림의 존재 이유였는데도.

### 고친 규칙 — 자가복구가 실패했을 때만 사람을 부른다

| 조건 | 알림 |
|---|---|
| REST 강제 갱신 자체가 실패 (거래소 장애) | 🔔 즉시 |
| 코인이 WS 구독 목록에 없음 (배선 파손) | 🔔 구독 재산정 후에도 없으면 |
| 강제 갱신을 **5분 연속** 했는데도 점검이 안 잡힘 | 🔔 이벤트 경로 사망 신호 |
| 그 외 (복구가 먹히는 중) | 로그만 |

알림 문구도 원인별로 나누고, "60초 폴링은 계속 돌고 있으므로 손절 자체가 멈춘 것은
아니다"를 명시해 과잉 반응을 막았다.

### 곁가지 — WS 구독 산정 기준도 같이 맞췄다

`refreshWsSubscription` 이 구독 코인을 `scanState` 기준으로 뽑고 있었다. 워치독은 이제
OPEN 포지션 기준이므로 **둘이 달랐다** — "구독도 안 됐는데 워치독은 감시 중이라고 믿는"
사각지대가 생긴다. 구독도 실제 OPEN 포지션 기준으로 바꿨다. 부수 효과로, 상태가 어긋난
세션의 포지션이 실시간 감시에서 조용히 빠지던 구멍도 닫혔다.

### 테스트

- `DynamicSlWatchdogNoiseTest` (6건, 신규) — 오탐 침묵 / 자가복구 중 침묵 / 진짜 고장 시 알림
- `DynamicTradingSlWatchdogRecoveryTest` — 08-06 작성분 중 2건이 **바로 이번에 고친 동작**을
  고정하고 있어 갱신했다(성공 시 알림 → 성공 시 침묵). 왜 바뀌었는지 테스트에 기록.
- 뮤테이션 확인: 알림 억제 조건 제거 → 1건 실패 / OPEN 포지션 게이트 제거 → 3건 실패

전체 435건 통과.


---

## 🔴 2026-08-31 COMPOSITE_PULLBACK_MTF 정지 — 두 방법이 독립적으로 같은 답을 냈다

### 판단 근거

| 방법 | 표본 | 결과 |
|---|---|---|
| 실거래(페이퍼) | **143거래** | 승률 30.8%, **−3,135원** |
| Walk Forward | **928거래** | **7코인 전부 out-of-sample 기대값 음수** |

WF 상세 (08-31, H1, 2022-01-01~, 게이트 통과 **0/7**):

| 코인 | 판정 | n | OOS 기대값 |
|---|---|---|---|
| KRW-GAS | OVERFITTING | 172 | −0.162% |
| KRW-LSK | OVERFITTING | 218 | −0.155% |
| KRW-MLK | OVERFITTING | 154 | −0.305% |
| KRW-ONT | OVERFITTING | 196 | −0.103% |
| KRW-ZRO | ACCEPTABLE | 116 | **−0.303%** |
| KRW-BEAM | ACCEPTABLE | 72 | **−0.014%** |
| KRW-SLX | ACCEPTABLE | 0 | 표본 없음 |

**서로 독립적인 두 측정이 일치했다** — 실거래는 최근 2주 실제 체결, WF 는 4년치 out-of-sample.
표본이 있는 6코인 전부 음수다.

### 왜 청산 A/B 를 더 기다리지 않았나

진행 중이던 실험은 "손실 구간에서 전략 SELL 을 막으면 살아나는가"였다. 그런데 WF 는
**진입 신호 자체에 엣지가 없다**고 말한다. 청산을 고치면 음수 기대값에서 마찰비용을 덜
잃는 정도지 양수로 뒤집히지 않는다 — **실험의 전제가 무너졌다.**

### 부수 효과

이 전략이 페이퍼 함대 서킷 브레이커 발동 **5회 중 3회**의 주범이었다(세션 56·71·68,
08-29~30, 전부 MDD 20% 초과). 정지하면 함대 노이즈가 크게 준다.

- 스크립트: [`scripts/stop_pullback_mtf_fleet.sh`](../scripts/stop_pullback_mtf_fleet.sh)
  — RUNNING 5세션(63·69·70·72·73) 정지. 세션 72가 KRW-DKA 보유 중이라 청산된다.
- **정지된 세션은 지우지 않는다** — 청산 이력이 KILL_CRITERIA 집계와 사후 분석에 필요하다.
- **되살리려면 진입 신호를 고친 다른 전략으로 만들어야 한다.** 같은 이름으로 재기동하면
  같은 결과가 나온다 — 이미 928거래로 확인됐다.

---

## 🟢 2026-08-31 WF 커버리지 확장 완료 — 42조합 중 6개 통과

신규 7코인 × 6전략. `MIN_TRADES=5` · OOS 기대값 > 0 · verdict ≠ OVERFITTING 이 통과 조건.

| 코인 | 전략 | n | OOS 기대값 |
|---|---|---|---|
| BEAM | MOMENTUM_ICHIMOKU | 10 | **+2.023%** |
| BEAM | MTF_CONFIRMED | 10 | **+1.665%** |
| GAS | MOMENTUM_ICHIMOKU_V2 | 61 | **+1.164%** |
| BEAM | MOMENTUM_ICHIMOKU_V2 | 9 | +0.913% |
| GAS | MTF_BTC | 31 | +0.650% |
| MLK | MEANREV_BB | 6 | +0.213% |

**LSK · ONT · SLX · ZRO 는 6전략 전부 탈락.** ONT 는 08-30 실거래에서도 −5.74% 였다 —
WF 와 실거래가 또 일치했다.

전략별 통과율: ICHIMOKU_V2 2/7 · 나머지 4전략 1/7 · **PULLBACK_MTF 0/7**.

> 통과율 14%(6/42)는 낮아 보이지만 **이것이 정상이다.** WF 는 "검증 안 된 것을 막는" 장치이지
> 통과시키는 장치가 아니다. 지금까지 DYNAMIC 은 이 검증 없이 전 코인을 매매하고 있었다.

### 전체 커버리지 — 5전략 × 29코인 = 145조합 중 **18개 통과** (12.4%)

기존 22코인 + 신규 7코인 합산. 조합별 최신 WF 실행 기준.

| 전략 | 통과 | 종목 |
|---|---|---|
| MEANREV_BB | 4/29 | EUL, LINK, MLK, SUI |
| MTF_BTC | 4/29 | AVAX, GAS, SUI, WLD |
| MTF_CONFIRMED | 4/29 | ADA, BEAM, BTC, ONDO |
| MOMENTUM_ICHIMOKU | 3/29 | BEAM, BTC, ONDO |
| MOMENTUM_ICHIMOKU_V2 | 3/29 | BEAM, GAS, SUI |

전략별로 3~4개씩 고르게 나왔다 — **특정 전략만 좋은 게 아니라 전 전략이 소수 종목에서만
엣지가 있다.** 통과 종목은 11개(SUI·BEAM 3전략, BTC·ONDO·GAS 2전략, 나머지 1전략).

> 🔴 **현재 감시목록 31종목 중 통과는 5종목뿐**(ADA·BTC·ONDO·SUI·WLD).
> 통과 종목 11개 중 6개(BEAM·GAS·MLK·EUL·LINK·AVAX)는 **감시목록에 아예 없다** —
> 워치리스트 필터(거래대금·ATR·스프레드)가 안 뽑아서, 검증된 종목인데 거래를 못 하고 있다.

---

## 🔴 2026-08-31 PAPER↔LIVE 규칙 일치 — WF 게이트 면제 철회 + 원칙을 테스트로 고정

### 사용자 지적이 설계를 바로잡았다

게이트를 켜려고 보니 스캔 루프가 `!session.isPaper()` 로 걸려 있어 **PAPER 는 전면 면제**였다
(08-25, 내가 넣음). 함대 9세션이 전부 PAPER 라 켜도 아무 일도 안 일어난다.

처음에는 **세션 파라미터로 arm 을 갈라 A/B 하려 했다.** 그런데 지적을 받았다:

> "paper를 운영하는 이유는 live 실전 매매시 어떨지 확인하는 용도로 실제 비용을 발생 안 하고
> 데이터를 쌓아서 그걸 가지고 투자를 하려는 건데, 모의랑 실전 매매랑 모든 게 동일해야 하는 거
> 아냐? 매수매도시 오차범위 안에서 매매가 이뤄질 수 있다는 건 알고 있어. 그거랑은 다르게
> 모든 게 같아야 하지"

**맞는 지적이고, 그러면 면제를 A/B 대상으로 삼을 게 아니라 없애야 한다.** A/B 설계를 폐기했다
(`sessionRequiresWalkForwardGate` 플래그·테스트·스크립트 전부 삭제).

### 08-25 면제 논리가 왜 틀렸나 — 역할 혼동

당시 근거는 "페이퍼는 아직 검증 안 된 코인을 안전하게 테스트해 **새 조합을 찾아내는 탐색
도구**"였다. 두 방향에서 무너진다:

1. **실측** — 미검증 코인은 탐색이 아니라 그냥 손실이었다(08-30 실현손익:
   KRW-RE −5.85% · KRW-ONT −5.74% · KRW-BEAM −5.48%).
2. **역할 혼동** — **탐색은 Walk Forward 가 하는 일이다.** 페이퍼는 실전 리허설이다.
   페이퍼를 돌리는 이유는 "실전에서 어떨지 비용 없이 미리 보는 것"인데, 매매 규칙이 하나라도
   다르면 그 예측력이 사라진다.

이 원칙은 이미 코드베이스에 있었다 — `PaperLiveAlignmentTest` 가 고정코인 페이퍼↔LIVE 일치를
강제하고, 08-06 에 "페이퍼를 LIVE 와 동일 로직으로 정렬 완료 — 이제 페이퍼 결과가 실전 예측에
유효하다"고 기록했다. **08-25 면제가 그 원칙을 깬 것이었다.**

### 전수 조사 — DYNAMIC 의 페이퍼 분기 3종류

| 분류 | 내용 | 판정 |
|---|---|---|
| 체결 시뮬레이션 | `executePaperBuy` / `executePaperSell` | ✅ 페이퍼의 정의 |
| 집계·저장 분리 | `session_kind`, 지문 라벨, 알파 집계 제외 | ✅ 매매 규칙 아님 |
| **매매 규칙 차이** | ① WF 게이트 면제 ② **WS 실시간 SL/TP 감시 제외** ③ SL 워치독 제외 | 🔴 |

①은 이번에 철회했다. ②는 **아직 미해결**이다 — LIVE 는 WS 로 5초마다 SL/TP 를 보는데
PAPER 는 60초 폴링만 한다. 급락장에서 **손절 체결가가 갈린다**(방향은 보수적 — 페이퍼가 더
나쁘게 나온다 — 이지만 여전히 동일한 규칙은 아니다). ③은 ②의 파생으로 알림 오탐 방지용이라
매매 결과에는 영향이 없다.

### 조치 — 원칙 자체를 테스트로 고정

신규 [`DynamicPaperLiveRuleParityTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/DynamicPaperLiveRuleParityTest.java) **4건**:

- WF 게이트에 페이퍼 예외가 없다 (08-25 → 08-31 철회를 잠금)
- 노출 상한에 페이퍼 예외가 없다 (08-25 → 08-30 철회를 잠금)
- **페이퍼 분기가 허용 목록 밖으로 늘어나지 않는다** — 새 분기가 생기면 실패하고, 그때
  "체결/집계인가 매매 규칙인가"를 사람이 판단하게 만든다
- ⚠️ 알려진 미해결 차이(WS 감시)를 문서로 고정 — 해소되면 이 테스트를 지우도록 명시

**뮤테이션 검증 2종**: WF 면제를 되돌리면 2건 실패, 노출 상한 면제를 되돌리면 2건 실패.
`:web-api:test` **71개 클래스 그린**. **미배포**.

> 같은 실수가 **두 번**(WF 게이트·노출 상한) 나왔다. 둘 다 "페이퍼니까 좀 다르게 해도 된다"는
> 판단이었고 둘 다 실측으로 뒤집혔다. 이 테스트는 세 번째를 막는다.

### ✅ ②도 같은 날 해소 — WS 실시간 감시를 페이퍼에 열었다

사용자 판단: **"열어줘. 동일해야 해, 실전과 페이퍼는."**

| | 이전 | 이후 |
|---|---|---|
| SL/TP 판정 주기 | LIVE 5초(WS) / PAPER **60초 폴링** | **양쪽 5초(WS)** |
| 구독 대상 | `!s.isPaper()` 필터 | 필터 제거 |
| 실시간 핸들러 | `if (session.isPaper()) continue;` | 제거 |
| 포지션 조회 | `SESSION_KIND` 하드코딩 | `sessionKind(session)` — 페이퍼 포지션을 실제로 찾는다 |
| SL 미점검 워치독 | 페이퍼 제외 | 포함 |

페이퍼 매도는 `executeSell` → `executePaperSell`(시뮬레이션)로 가므로 **실주문 위험은 없다.**
구독 코인은 `distinct()` 로 합쳐지고 `WsSubscriptionManager` 가 LIVE 와 공유해 부하도 제한적이다.

> 워치독 포함이 특히 중요하다 — 08-24 에 페이퍼를 뺀 이유는 "PAPER 는 `recordSlCheck` 를 아예
> 안 불러서 오탐이 무한 반복된다"였는데, 이제 페이퍼도 WS 감시를 받으니 그 전제가 사라졌다.
> 오히려 **포함해야 한다**: 페이퍼의 SL 감시가 멈추면 그 성과는 실전을 예측하지 못하게 되는데,
> 제외해두면 그 사실조차 알 수 없다.

### 결과 — DYNAMIC 의 페이퍼 분기가 4개만 남았다

```
1407  executePaperBuy    체결 시뮬레이션 (매수)
1594  executePaperSell   체결 시뮬레이션 (매도)
1891  executePaperSell   체결 시뮬레이션 (강제정지)
2656  sessionKind()      position/order 저장 분리
```

**매매 규칙 차이는 0이다.** 남은 것은 전부 "주문을 거래소에 보내는 대신 슬리피지·수수료로
계산한다"와 "데이터를 섞지 않는다"뿐 — 페이퍼의 정의 그 자체다.

### 검증

[`DynamicPaperLiveRuleParityTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/DynamicPaperLiveRuleParityTest.java)
**5건** — WF 게이트 예외 없음 / 노출 상한 예외 없음 / **페이퍼 분기 허용목록**(체결·저장 3종만) /
WS 실시간 감시가 페이퍼를 덮음(구독·핸들러·`sessionKind` 3중 확인) / SL 워치독이 페이퍼를 덮음.

**뮤테이션 검증 5종** — 세 면제(WS 구독·실시간 핸들러·SL 워치독)를 각각 되돌리면 1~2건씩,
WF 게이트·노출 상한 면제를 되돌리면 각 2건씩 실패한다.

`:web-api:test` **71개 클래스 그린**. **미배포**.

> ⚠️ **배포 후 관찰**: 페이퍼의 SL/TP 체결 시점이 빨라져 **성과 수치가 달라진다**. 이전 페이퍼
> 기록과 이후 기록을 그대로 이어 붙이면 안 된다 — 다만 이 변경은 지문에 실리지 않으므로
> (코드 상수가 아니라 감시 경로의 변화) **배포 시각으로 갈라야 한다.**

### 🟡 남은 차이 — `createSession` 의 자본 배정 게이트 2종

`createSession` 은 PAPER 에 두 게이트를 적용하지 않는다(08-06 판단):
`strategyLiveStatusRegistry.isBlocked()` 와 `walkForwardValidationGate.throwIfBlocked()`.

이건 **매매 규칙이 아니라 입장(admission) 규칙**이다 — "이 전략이 돌 자격이 있는가"를 묻지
"무엇을 언제 사고 파는가"를 정하지 않는다. 같은 전략으로 만든 PAPER 세션과 REAL 세션은
**돌기 시작하면 완전히 동일하게 행동**하므로 예측력은 유지된다.

> 08-31 실측: 현재 5전략 모두 `BLOCKED` 가 아니고(ENABLED 3 · EXPERIMENTAL 2) WF 검증 조합도
> 각각 3~4개씩 있어, **REAL 로 만들어도 두 게이트를 통과한다** — 지금은 실질적 차이가 없다.

닫을지는 판단 대상으로 남긴다. 닫으면 "차단된 전략을 페이퍼로 재검증해보는" 경로가 사라진다.

---

## 🔴 2026-08-31 함대 재기동 + **규칙 동결 2주** — 리셋 악순환을 끊는다

### 사용자 지적

> "계속해서 변경되면서 매번 새롭게 데이터를 누적시키는게... 문제야. 매번 처음부터 새로 시작이니."

정확한 지적이다. **문제는 리셋 장치가 아니라 변경 속도다.** 08-19 이후 12일간 DYN_PAPER
지문이 **18개**로 갈렸다. 12일에 규칙을 5번 넘게 바꾸면 어떤 측정 체계도 무너진다.

### 그럼에도 지금은 재기동해야 하는 이유 세 가지

1. **누적 성과가 혼합물이다** — 노출 상한 1→면제→2, WF 게이트 면제→철회, 최소 캔들 가드,
   WS 실시간 감시 개방이 전부 이 함대 위에서 일어났다.
2. **오늘의 WS 변경은 지문에 안 실린다** — 코드 상수가 아니라 감시 경로의 변화라서
   사후 구분이 시각뿐이다.
3. **MDD 고점이 발목을 잡는다** — 서킷은 `mdd_peak_capital` 기준이라 과거 고점이 따라다닌다:

   | 세션 | 자본 | 고점 | MDD | 서킷까지 |
   |---|---|---|---|---|
   | 64 MTF_BTC M15 | 11,910 (**+19%**) | 14,143 | 15.79% | **4.21%p** |
   | 54 MEANREV_BB H1 | 8,599 | 10,001 | 14.01% | **5.99%p** |

   세션 64는 **성과가 좋은데도** 조금만 빠지면 멈춘다.

### 잃는 것은 없다

`position` 행은 세션 상태와 무관하게 남고 KILL_CRITERIA 는 `strategy_type` 으로 집계한다.
재기동해도 판정 표본(MTF_BTC 26 · MEANREV_BB 22 등)은 그대로다. **기존 세션은 정지만 하고
삭제하지 않는다.**

### 🔴 핵심 — 재기동은 동결 선언과 한 세트다

```
2026-08-31 ~ 09-14 (2주) 매매 규칙 변경 금지

금지: 게이트 임계값 · 노출 상한 · 청산 규칙 · 워치리스트 필터 · 전략 파라미터 · 신규 A/B
허용: P0 버그 수정(버그는 규칙이 아니다) · 관측/로깅 추가 · WF 배치 · 캔들 수집
```

**2주인 근거** — 현재 거래 속도로 KILL_CRITERIA 판정선(n≥20)에 닿는 시간:

| 전략 | 속도 | 소요 |
|---|---|---|
| MTF_BTC | 3.4건/일 | 6일 |
| MEANREV_BB | 2.4건/일 | 9일 |
| MTF_CONFIRMED | 2.0건/일 | 10일 |
| MOMENTUM_ICHIMOKU | 1.8건/일 | 12일 |
| MOMENTUM_ICHIMOKU_V2 | 1.8건/일 | 12일 |

09-14 에는 **5전략 전부를 같은 규칙 위에서 판정**할 수 있다 — 지난 12일 동안 한 번도 못 한 일이다.

- 스크립트: [`scripts/restart_dynamic_fleet_clean_baseline.sh`](../scripts/restart_dynamic_fleet_clean_baseline.sh)
  — 9세션 정지 후 동일 설정으로 재생성. 워치리스트 필터를 **명시적으로 넘긴다**(기준선이
  나중에 `risk_config` 변경으로 흔들리지 않도록 — 08-07 에 필터가 조용히 바뀌어 감시 코인이
  62종→10종으로 붕괴한 전례).
- **선행 조건**: 08-31 빌드(PAPER↔LIVE 규칙 일치)가 배포돼 있어야 한다. 배포 전에 만들면
  옛 규칙으로 시작해 재기동 의미가 없다.

> 09-14 전에 규칙을 바꾸고 싶어지면, **그 변경이 2주를 다시 기다릴 만큼 급한지** 먼저 물을 것.

---
## 🔴 2026-08-30 노출 상한 — 08-25 판단을 실현손익으로 뒤집었다

### 무슨 일이 있었나

08-28 → 08-30 이틀 사이 PULLBACK_MTF 가 무너졌다:

| | 08-28 | 08-30 |
|---|---|---|
| 청산 | 95건 | **143건** |
| 승률 | 37.9% | **30.8%** |
| 손익 | **+2,014원** | **−3,135원** |

서킷 브레이커도 3번 더 발동했다 — **전부 PULLBACK_MTF**(세션 56·71·68, MDD 20.03~20.16%).
그중 56은 **청산 A/B 의 H1 대조군**이라 그 축의 비교가 깨졌다.

### 원인 — 내가 08-25 에 넣은 PAPER 면제였다

진입 시각으로 갈라보니 명확했다:

| PULLBACK_MTF | n | 승률 | 건당 |
|---|---|---|---|
| 면제 **전** | 63 | 36.5% | **+0.23%** |
| 면제 **후** | 82 | 25.6% | **−0.70%** |

결정적인 것은 **동시진입 세션 수별 실현손익**(DYN_PAPER 전체, 면제 후):

| 동시 진입 | n | 승률 | 건당 |
|---|---|---|---|
| 단독 | 61 | 37.7% | −0.35% |
| **2세션** | 21 | 47.6% | **+1.75%** |
| **3세션** | 30 | 20.0% | **−1.33%** |
| **4세션** | 16 | 6.3% | **−2.13%** |

### 왜 08-25 에 반대로 판단했나 — 사후수익률과 실현손익의 괴리

08-25 근거는 차단된 BUY 의 **24시간 사후수익률**이었다(+4.51%·승률 69.3%). 그래서
"여러 전략이 동의한 신호일수록 잘 맞는데 한 세션만 먹고 있다"고 읽었다.

**사후수익률은 24시간 뒤 가격일 뿐 그사이 경로를 무시한다.** 여러 세션이 한 코인에 몰리는
시점은 변동성이 큰 구간이라 **24시간 뒤엔 올라 있어도 그 전에 SL 을 맞는다**. 실현손익에는
그 손절이 찍히지만 사후수익률에는 안 찍힌다.

> **교훈**: 신호 품질 지표(사후수익률)로 **진입 규칙**을 정하면 안 된다. 진입 규칙은
> 실현손익으로 검증해야 한다. 08-25 의 "게이트가 역선택을 한다"는 결론은 측정 지표를
> 잘못 고른 데서 나왔다.

### 조치 — 상한 1 → **2**, PAPER 면제 철회, 지문 등재

- 데이터가 가리키는 값은 **2**다. 1은 과하고(2세션 구간이 +1.75%로 가장 좋다), 3 이상은
  명확히 해롭다. PAPER·REAL 공통.
- `crossSessionExposureBlockReason(boolean isPaper, long)` → `(long)` 로 되돌렸다.
- **지문 등재** `scan.maxSessionsPerCoin` — 08-25 면제 당시 이 상수가 지문에 없어 면제 전후
  거래가 같은 해시로 섞였고, 사후 분리를 **진입 시각으로만** 할 수 있었다. "동작을 바꾸는
  코드 상수는 지문에 싣는다"는 규칙(08-19)을 어긴 것이라 이번에 바로잡았다.
  ⚠️ **모든 해시가 다시 갈린다** — 진행 중이던 청산 A/B 표본(실험군 12·대조군 22)은 리셋된다.
  지금 표본은 crowding 노이즈로 오염돼 있어 그대로 모아도 신뢰하기 어렵다는 판단.

**검증**: [`CrossSessionExposureTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/CrossSessionExposureTest.java)
재작성 — 1건 보유 시 **통과**(2세션 허용), 2건 이상 차단, 상한값 2 고정.
[`RulesetRegistryCompositionTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/RulesetRegistryCompositionTest.java)
필수 키에 `scan.maxSessionsPerCoin` 추가. **뮤테이션 검증 2종**: 지문 키를 지우면 1건 실패,
상한을 1로 되돌리면 2건 실패. `:web-api:test` **69개 클래스 그린**. **미배포**.

### 🟡 함께 드러난 것 — 미검증 코인이 실제로 돈을 잃고 있다

면제 후 PULLBACK_MTF 코인별 손실 상위가 전부 **candle_data 도 WF 도 없는 소형 코인**이었다:

| 코인 | n | 건당 |
|---|---|---|
| KRW-RE | 1 | −5.85% |
| KRW-ONT | 2 | −5.74% |
| KRW-BEAM | 1 | −5.48% |
| KRW-CHIP | 7 | −3.46% |
| KRW-PROM | 2 | −3.46% |
| KRW-SLX | 11 | −1.15% |

08-28 에 "백테스트 데이터도 없는 코인을 사고팔고 있다"고 지적한 것이 **실제 손실로 확인**됐다.
캔들 수집 → WF 확장이 성능 문제이기도 하다는 뜻이다.

### 전략 판정 현황 (n≥20 도달)

| 전략 | 청산 | 승률 | 손익 | 판정 |
|---|---|---|---|---|
| PULLBACK_MTF | 143 | 30.8% | **−3,135** | 음수 확정 |
| MTF_BTC | 24 | 54.2% | **+3,059** | 양수 |
| MEANREV_BB | 21 | 23.8% | **−3,399** | 음수 확정 |

PULLBACK_MTF 는 **죽이기 전에 청산 A/B 를 마저 돌려야 한다** — "청산 규칙을 고치면 살아나는가"가
아직 답이 없고, 지금 죽이면 그 답을 영영 못 얻는다. 다만 이번 지문 변경으로 A/B 는 재시작이다.

---

## 🟢 2026-08-28 정기 점검 — 전 항목 정상, 방어 장치 3종 실전 검증

08-27 P0 4연속 배포 다음날. **이상 없음.**

| 점검 | 결과 |
|---|---|
| 불변식 3종(중복 포지션·상태 불일치·좀비) | **0 / 0 / 0** |
| 불변식 ②(포지션 보유 중 원금 안 묶임) | 위반 **없음** |
| 불변식 ①(포지션 없는데 KRW 묶임) | 위반 **없음** |
| 세션 틱 신선도 | 16세션 전부 **6~8분** 이내 |
| `maxHoldHours` 초과 방치 포지션 | **없음** |
| 노출 상한 차단(24h) | **0** — 면제 유지 |
| 주문 실패 | **0** (FILLED 61건) |

### 방어 장치 검증 현황

| 장치 | 상태 | 근거 |
|---|---|---|
| `pickMonitoredPosition`(중복에서 안 죽기) | ✅ **검증** | 08-27 00:58 중복 2건에서 2997 골라 감시 재개 → SL 청산 |
| 서킷 브레이커 데드락 수정 | ✅ **검증** | 08-27 00:59 실제 발동, 좀비 0, 연쇄 정지 없음 |
| 자가복구 가드(SCANNING+포지션) | ⏸ 미발동 | 불일치 0이라 기회 없음 — 정상 |
| `transitionToScanning` 가드 | ⏸ 미발동 | 잔존 사례 0 — 정상 |
| 불변식 ② 알림 | ⏸ 미발동 | 위반 0 — 정상 |

미발동 3종은 **문제가 아니라 좋은 신호**다 — 막으려던 상황 자체가 안 생기고 있다.

### 노출 상한 면제 — 효과 지속, 집중도는 완화

| 날짜 | 진입 |
|---|---|
| 08-21~25(기준선) | 15~21 |
| 08-26 | **37** |
| 08-27 | **26** |

같은 코인 동시 보유는 **KRW-ETH 9세션 → 5세션**으로 완화됐다. 어제 우려한 "함대 전체가 한
종목에 몰림"은 일시적 현상이었던 것으로 보인다 — 계속 관찰.

### 전략별 누적 표본 — PULLBACK_MTF 판정 가능 도달

| 전략 | 청산 | 승률 | 손익 | KILL_CRITERIA |
|---|---|---|---|---|
| **PULLBACK_MTF** | **95** | 37.9% | **+2,014** | ✅ **판정 가능** — 양수라 존속 |
| MOMENTUM_ICHIMOKU_V2 | 15 | 33.3% | −1,577 | 5건 부족 |
| MEANREV_BB | 15 | 26.7% | −2,683 | 5건 부족 |
| MTF_BTC | 14 | 64.3% | +3,540 | 6건 부족 |
| MTF_CONFIRMED | 10 | 50.0% | +314 | 10건 부족 |
| MOMENTUM_ICHIMOKU | 9 | 33.3% | −2,607 | 11건 부족 |
| MTF_BTC_STRICT | 6 | 66.7% | +1,015 | 14건 부족 |

### A/B 진행 — 방향은 가설대로, 아직 판정 불가

| arm | STRATEGY_SIGNAL 청산 | 승률 | 평균 | 합계 |
|---|---|---|---|---|
| **실험군(72·73)** | **2** | **100%** | **+54** | +108 |
| 대조군(56·63) | **8** | 12.5% | −100 | −799 |

세션 73의 손실 구간 SELL 차단 **65건** — 억제가 확실히 걸린다. 다만 실험군 n=2 라
**아무 판정도 하지 말 것**. arm 당 20건까지 3~4주.

### 정지 상태 세션 2개 — 재기동 (스크립트 준비 완료, 미실행)

| 세션 | 전략 | peak → total | MDD | trips |
|---|---|---|---|---|
| 60 | MOMENTUM_ICHIMOKU_V2 H1 | 10,639 → 8,444 | **20.64%** | 1 |
| 62 | MOMENTUM_ICHIMOKU M15 | 10,630 → 8,405 | **20.93%** | 0 |

**⚠️ 그냥 `start` 하면 안 된다** — `startSession()` 은 status 만 되돌릴 뿐
`mdd_peak_capital` 을 손대지 않는다. 두 세션 다 **이미 MDD 한도(20%)를 넘어 있어** 재시작하면
다음 틱에 서킷 브레이커가 즉시 재발동한다(연속손실은 `startedAt` 이후만 집계해 리셋되지만
MDD 는 피크 기준이라 리셋되지 않는다). → **같은 설정으로 신규 세션 생성**이 유일한 방법.

[`scripts/restart_stopped_dynamic_sessions.sh`](../scripts/restart_stopped_dynamic_sessions.sh)
— DB 에서 읽은 실제 설정값을 그대로 복제(지문 동일 → 기존 표본과 이어짐). 정지된 60·62 는
**지우지 않는다**: 청산 이력이 KILL_CRITERIA 집계에 필요하고, 08-27 서킷 브레이커 발동 기록은
데드락 수정의 검증 근거다.

---

## 🔴 2026-08-28 WF 커버리지 확장 — 착수했더니 **데이터가 없었다**

DYNAMIC 메인 전환의 전제조건인 WF 커버리지를 넓히려고 미검증 조합을 뽑았는데,
**미검증 21개 코인 중 18개가 `candle_data` 0행**이었다. WF 는 `candle_data` 를 읽으므로
실행 자체가 불가능하다. **순서가 캔들 수집 → WF 다.**

| 코인 | 3일 평가 | candle_data(H1) |
|---|---|---|
| KRW-SLX | 332 | **0행** |
| KRW-ONT | 314 | **0행** |
| KRW-PROM | 167 | 276행(부족) |
| KRW-META2 | 125 | **0행** |
| KRW-LIT | 108 | **0행** |
| KRW-ZRO | 107 | **0행** |
| KRW-LSK | 104 | **0행** |
| KRW-MLK | 103 | **0행** |
| KRW-NCT | 88 | **0행** |
| KRW-GAS | 65 | **0행** |
| KRW-BEAM | 64 | **0행** |

> **즉 지금 DYNAMIC 은 백테스트 데이터조차 없는 코인들을 실시간으로 사고팔고 있다.**
> WF 게이트가 PAPER 에서 면제돼 있어 막히지 않을 뿐이다. LIVE 로 올리면 이 코인들은
> 게이트에 전부 걸린다 — 이것이 DYNAMIC 메인 전환의 실질적 장벽이다.

또 `candle_data` 최신이 **2026-08-24 에 멈춰 있다**(38코인). `DataCollectionService` 에
`@Scheduled` 가 없어 수동 호출뿐이라는 기존 문제가 다시 드러났다.

### 스크립트 3종 (전부 미실행 — 운영에서 순서대로 실행할 것)

1. [`restart_stopped_dynamic_sessions.sh`](../scripts/restart_stopped_dynamic_sessions.sh)
   — 세션 60·62 대체 생성. 즉시 실행 가능.
2. [`collect_watchlist_candles.sh`](../scripts/collect_watchlist_candles.sh)
   — 신규 11코인 전체 수집(2022-01-01~) + 기존 38코인 갭(최근 10일).
   **1~2시간 이상 소요**, 그동안 DYNAMIC 시세 조회가 지연될 수 있어 한산한 시간대 권장.
3. [`walk_forward_new_watchlist_coins.sh`](../scripts/walk_forward_new_watchlist_coins.sh)
   — 6전략 × 11코인 = 66조합. **PRECHECK 내장**: 2,000행 미만이면 중단하고 수집을 먼저
   시키다. 얇은 표본으로 돌리면 "통과"가 나와도 의미가 없기 때문이다.

### ⚠️ 이 작업은 일회성이 아니다

워치리스트는 계속 돈다. 08-25 배치의 대상 코인(FOLD·TRAC·WLFI·PUMP)은 지금 거의 안 잡히고,
지금 대상 11개도 다음 주엔 바뀔 수 있다. **주기적 재실행 체계가 필요**하다 —
`DataCollectionService` 에 스케줄을 붙일지, 워치리스트 편입 시 자동 수집할지는 별도 판단.

### ✅ 수집 완료 (2026-08-31) — 7개 확보, 4개는 구조적으로 검증 불가

`candle_data` **611,286 → 815,918행**, 41 → **48코인**. 08-24 에 멈춰 있던 갭도 08-30 까지 채웠다.

| 코인 | 행수 | 시작일 | 판정 |
|---|---|---|---|
| GAS | 40,786 | 2021-12-31 | ✅ WF 대상 |
| ONT | 40,747 | 2021-12-31 | ✅ |
| MLK | 40,735 | 2021-12-31 | ✅ |
| LSK | 40,703 | 2021-12-31 | ✅ |
| BEAM | 19,549 | 2024-05-31 | ✅ |
| ZRO | 19,176 | 2024-06-20 | ✅ |
| SLX | 2,166 | 2026-06-01 | ✅ |
| META2 | 770 | 2026-07-29 | ❌ 이력 부족 |
| PROM | 276 | 2026-08-12 | ❌ |
| LIT | 155 | 2026-08-24 | ❌ |
| NCT | 99 | 2026-08-26 | ❌ |

> ⚠️ **중간에 8개가 "실패"로 보였던 것은 오진이었다.** 스크립트가 코인마다 따로 요청해
> **48개 배치가 큐에 쌓였고**(전부 `STARTED` 응답), 순차 처리 중 **재빌드로 컨테이너가
> 재시작되면서 미처리 큐가 통째로 날아갔다.** 하나의 배치로 묶어 재요청하니 전부 정상
> 수집됐다. → **수집 중 재빌드 금지**, 그리고 스크립트를 단일 배치로 고칠 것.

WF 스크립트 대상을 확정된 **7개**로 갱신했다(6전략 × 7코인 = 42조합).

---

## 🟡 2026-08-31 워치리스트 최소 이력 조건 — 하드코딩 15를 전략 요구량으로 교체

`processScanningTick` 이 `candles.size() < 15` 로 하드코딩돼 있었다. **15는 어떤 전략의
요구량도 아니다** — `COMPOSITE_PULLBACK_MTF` 는 `getMinimumCandleCount()=200`(EMA200 산출에
H1 200개 필요)인데 15개만 있어도 평가에 들어갔다.

전략 내부 가드가 "데이터 부족" HOLD 를 돌려주므로 **잘못된 신호가 나오진 않는다.** 문제는
**장기 지표가 조용히 비활성된 채 도는 것을 아무도 모른다**는 것이다. 백테스트
(`BacktestEngine`)는 진작부터 같은 `getMinimumCandleCount()` 를 쓰고 있었으니, **실거래
경로만 기준이 달랐다**.

**두 사실이 같은 집합을 가리켰다:**

| 이력이 없어 지표를 못 채우는 코인 | 08-30 손익 분석 손실 상위 |
|---|---|
| META2 770 · PROM 276 · LIT 155 · NCT 99 | KRW-RE −5.85% · KRW-ONT −5.74% · KRW-BEAM −5.48% |

**수정**: `int minCandles = strategy.getMinimumCandleCount();` — 전략 해석을 캔들 검사보다
앞으로 옮겨 전략이 선언한 값을 쓴다. 요구량 미달 코인은 **평가 자체를 건너뛰어** 워치리스트에서
자연히 빠진다.

**검증**: 신규 [`DynamicMinimumCandleGuardTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/DynamicMinimumCandleGuardTest.java)
**5건**(통합 테스트) — `MarketDataSyncService.fetchWithCache` 를 목으로 캔들 수를 통제하고,
`strategy_log` 행 유무로 스킵 여부를 관측한다(평가되면 HOLD 도 기록되므로).
100개 스킵 / 250개 평가 / 경계(199 막힘·200 통과) / 운영 실측치(99·155) 스킵 / 전략별 요구량 문서화.

> 🔍 **첫 테스트는 배선을 못 잠갔다** — 전략 상수만 검증해서, `minCandles` 를 15로 되돌려도
> 통과했다. 실제 스캔 루프를 태우는 통합 테스트로 다시 썼고, **뮤테이션 검증에서 15로
> 되돌리면 3건이 실패**함을 확인했다.

`:web-api:test` **70개 클래스**. ⚠️ `RateLimiterEmergencyStopTest` 1건이 간헐 실패하는데
**선행 플래키**다 — `availablePermits() > 0 && acquire()` 의 check-then-act 경합으로
10스레드 중 가끔 8이 통과한다(`a3e82f4` 부터 존재). 이번 작업과 무관해 손대지 않았다.
재실행 시 통과. **미배포**.

---

## 🔴 2026-08-27 P0 — 세션 하나가 21시간 조용히 죽어 있었다 (중복 포지션 + 상태 불일치)

A/B 1단계 배선 검증차 점검하다 발견. **세션 60(DYN_PAPER, MOMENTUM_ICHIMOKU_V2 H1)이
08-26 03:00 이후 21시간 동안 틱을 한 번도 돌지 못했다.** 나머지 16세션은 전부 4분 이내 정상.
알림도 없었고 `status`는 계속 `RUNNING`이었다.

### 사고 연쇄 — 세 단계로 무너졌다

| # | 무슨 일 | 결과 |
|---|---|---|
| ① | 포지션 2852(KRW-STX, 08-23 13:46 진입)를 든 채 `scan_state`가 **SCANNING으로 어긋남** | 그 포지션이 **SL/TP/time stop 감시에서 통째로 빠짐** — `maxHoldHours=24`인데 **59시간+ 방치** |
| ② | 세션이 "나는 포지션 없다"고 알고 08-26 03:00 **같은 코인을 또 매수** | `(DYN_PAPER, 60, KRW-STX, OPEN)` 이 **2건** |
| ③ | `processMonitoringTick`의 `Optional` 조회가 2건에서 `IncorrectResultSizeDataAccessException` | **세션 영구 정지** — `tick()`이 세션별로 예외를 삼켜 앱은 멀쩡, 아무도 몰랐다 |

②가 가능했던 이유는 잔고 회계도 함께 어긋나 있었기 때문이다 — 08-26 00:51 시점
`available_krw == total_asset_krw == 8,875`로 **포지션이 없는 것처럼 계산**돼 있었다(2852의
7,108원이 묶여 있어야 하는데 가용으로 잡혀 있었다).

### 🔴 페이퍼에는 이 불일치를 잡는 안전망이 아예 없다

- `reconcileDynamicSessionBalance` — 첫 줄이 `if (session.isPaper()) continue;`
- `reconcileDynamicOrphanBuyPositions` — `findBySessionKindAndStatus(SESSION_KIND, ...)`로 **REAL만** 조회

둘 다 08-07에 "PAPER는 REQUIRES_NEW·비동기 갭이 없어 이런 사후 안전망 자체가 필요 없다"는
판단으로 제외한 것인데, **이번 사고가 그 전제가 틀렸음을 보여준다.**

### 수정 — 두 겹

**① 자가복구 가드** (`processTick`) — SCANNING인데 OPEN 포지션이 있으면 스캔하지 않고
POSITION_MONITORING으로 되돌린다. 불일치가 **어느 경로에서 생기든**(여러 reconcile이
`transitionToScanning`을 부른다) 다음 틱에 스스로 복구된다. 중복 진입도 원천 차단된다.
PAPER·REAL 공통으로 건다.

**② 죽지 않는 조회** (`pickMonitoredPosition`) — `Optional` 대신 List로 받아, 중복이어도
예외 없이 하나를 고른다. `currentPositionId`를 최우선(세션 회계가 그 기준), 없으면
**가장 오래된 것**(time stop·손절이 더 급한 쪽). 자동 병합·청산은 **하지 않는다** — 어느 쪽이
진짜인지는 주문 이력을 봐야 하고 잘못 청산하면 되돌릴 수 없다. 중복 자체는 ERROR로 남긴다.
신규 리포지터리 메서드 `findBySessionKindAndSessionIdAndCoinPairAndStatusList`.

**검증**: 신규 [`DynamicDuplicatePositionTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/DynamicDuplicatePositionTest.java)
**7건** — 빈 목록/정상 1건/중복에서 예외 없음/currentPositionId 우선/목록에 없으면 최고령/
null이어도 동작/3건 이상. **뮤테이션 검증**: 중복 분기를 제거하면 **2건이 실패함을 확인** 후 복원.
`:web-api:test` **67개 클래스 전부 그린**. **미배포**.

### ⚠️ 배포해도 세션 60은 자동 복구되지 않는다

포지션 2852·2997 둘 다 실제 체결된 BUY 주문(9569·9866)이 있는 정당한 포지션이다. 코드는
"죽지 않고 하나를 감시"할 뿐 중복을 정리하지 않는다. **운영 판단 필요** — 어느 쪽을 청산할지,
아니면 세션을 정지하고 재생성할지.

### ✅ 배포 검증 + 근본 원인 실시간 포착 (2026-08-27 00:58~01:00)

배포 직후 **세션 60이 22시간 만에 부활했고, 그 과정에서 ①의 발생 경로를 실시간으로 잡았다.**

```
00:58:42  포지션 2997 STOP_LOSS 청산 (−438원)   ← pickMonitoredPosition 이 골라 감시 재개
          ↳ executeSell → transitionToScanning → SCANNING
          ↳ 그런데 포지션 2852 는 OPEN 그대로 = 불일치가 생기는 바로 그 순간
00:59:47  ⚡ 서킷 브레이커 발동 → emergencyStop → 2852 강제 청산(FORCED_STOP, 보유 83.2시간, −11원)
          "MDD 20.64% 초과 (한도 20.00%, 피크 10,639 → 현재 8,443.54)"
```

22시간 만에 감시가 재개되자마자 손절이 나간 것이 **그동안 SL 이 무효였다는 직접 증거**다.

> ⚠️ **08-28 정정**: 위 00:59:47 을 처음엔 "자가복구 가드가 잡았다"고 적었으나 **오독이었다**.
> `exit_reason=FORCED_STOP` 과 `circuit_breaker_trip_count=1` 이 말해주듯 실제로는 **서킷
> 브레이커**가 발동해 `emergencyStop → closeOpenPositions` 가 정리한 것이다. 자가복구 가드는
> 아직 실전에서 발동한 적이 없다(현재 불일치 0이라 발동 기회가 없다).

### 🎉 08-25 P0 데드락 수정 — 실전 검증 완료

그 서킷 브레이커 발동이 곧 **3일간 기다리던 실전 검증**이었다. 08-25 에는 이 경로가 자기
자신과 DB 데드락을 일으켜 **전체 동적 세션을 29시간 멈춰 세웠다**. 이번에는:

| | 08-25 (수정 전) | 08-27 (수정 후) |
|---|---|---|
| 세션 상태 전환 | 실패 — 멈춤 | **EMERGENCY_STOPPED 정상 기록** |
| `circuit_breaker_trip_count` | 갱신 안 됨 | **1** |
| 좀비 트랜잭션 | 발생, 수동 `pg_terminate_backend` 필요 | **0** |
| 다른 세션 | 연쇄 정지(29시간) | **16세션 전부 정상**(7~8분 전 틱) |

`emergencyStop` 의 단일 `REQUIRES_NEW` 갱신으로 통합한 수정이 의도대로 동작했다.

### 🔴 근본 수정 — `transitionToScanning` 이 포지션을 두고 떠났다

호출부가 **9곳**(청산 완료·고아 정리·reconcile 등)인데 전부 "이 세션은 이제 빈손"이라고
가정하고 **무조건** SCANNING 으로 보냈다. 세션에 포지션이 하나뿐이라는 전제가 깨지는 순간
남은 포지션이 **모든 청산 장치에서 빠진다** — SL·TP·time stop 이 전부 monitoring tick 에서만
돌기 때문이다.

- 남은 OPEN 포지션이 있으면 SCANNING 대신 그 포지션으로 **POSITION_MONITORING 유지**.
  세션의 실제 `session_kind`(REAL/PAPER)로 조회해 D-2 sessionId 충돌을 피한다.
- `CLOSING` 은 대상이 아니다 — `executeSell` 이 CLOSING 전환 후 이 메서드를 부르므로,
  포함시키면 매도 진행 중 세션이 고착된다.

**검증**: 신규 [`DynamicScanTransitionGuardTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/DynamicScanTransitionGuardTest.java)
**7건**(통합 테스트, 실 DB) — 포지션 없으면 SCANNING / OPEN 잔존 시 감시 유지(PAPER·REAL) /
세션 60 시나리오 재현 / CLOSING 만 남으면 SCANNING / 타 세션 격리 / session_kind 격리.
**뮤테이션 검증**: 가드를 끄면 **3건이 실패하고 격리 4건은 통과함을 확인** 후 복원.
`:web-api:test` **68개 클래스 전부 그린**. **미배포**.

### 🔴 잔고 안전망 — 불변식이 반쪽이었다

세션 60은 포지션 2852(7,108원)를 든 채 `available_krw == total_asset_krw` 로 **포지션이 없는
것처럼** 계산돼 있었고, 그래서 그 돈으로 같은 코인을 재매수할 수 있었다.

**왜 기존 안전망이 못 잡았나** — `reconcileDynamicSessionBalance` 가 이렇게 시작한다:

```java
int cmp = available.compareTo(total);
if (cmp == 0) continue;          // ← available == total 을 항상 정상으로 취급
```

세션 60의 사고 상태가 **정확히 `available == total`** 이었다. 즉 PAPER 제외가 없었더라도
이 안전망은 세션 60을 못 잡았다. 불변식이 한쪽만 구현돼 있었기 때문이다:

| | 불변식 | 상태 |
|---|---|---|
| ① | 포지션이 **없으면** `available == total` | 기존에 있었음 |
| ② | 포지션이 **있으면** `available < total` | **없었음** ← 세션 60이 여기로 빠져나갔다 |

**수정**:

- **불변식 ② 신설** — 포지션 보유 중 `available >= total` 이면 감지. **자동 교정은 하지 않는다**:
  이 방향의 교정은 available 을 *깎는* 것이라 판단이 틀리면 멀쩡한 세션의 매수 여력을 없앤다
  (기존 `available>total` 분기가 감액을 피한 것과 같은 이유). 지금 없던 것은 교정이 아니라
  **감지**다 — 세션 60은 나흘간 아무 경고 없이 지나갔고 A/B 점검 중 우연히 발견됐다.
  ERROR 로그 + **텔레그램 알림**, 세션당 1회(정상 복귀 시 리셋)로 스케줄러 스팸 방지.
- **PAPER 제외 해제** — 08-07 의 `if (session.isPaper()) continue;` 를 제거했다. 당시 근거는
  "PAPER 는 REQUIRES_NEW·비동기 갭이 없어 이런 안전망이 필요 없다"였는데 세션 60이 그 전제를
  깼다. 08-07 이 실제로 두려워한 것은 **오탐**(포지션을 `SESSION_KIND`(REAL)로만 조회해 PAPER
  세션이 "포지션 없음"으로 오판)이었고, 그 원인은 **세션의 실제 `session_kind` 로 조회**하도록
  고쳐 해소했다. 주문 조회도 같이 고쳤다.

**검증**: 신규 [`DynamicUnlockedCapitalTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/DynamicUnlockedCapitalTest.java)
**7건** — 세션 60 재현(PAPER)/`available>total`/REAL 동일 적용/정상 보유는 침묵/포지션 없으면
대상 아님/반복 실행 시 1회만 알림/PAPER 가 REAL 포지션을 자기 것으로 세지 않음.
**뮤테이션 검증**: 불변식 ② 를 끄면 **감지 4건이 실패하고 오탐방지 3건은 통과함을 확인** 후 복원.

⚠️ **기존 테스트 1건을 의도적으로 뒤집었다** — `DynamicPaperTradingTest.balanceReconcile_ignoresPaperSessions`
가 08-07 의 PAPER 제외를 잠그고 있었다. 판정이 반전됐으므로
`balanceReconcile_restoresPaperOrphanCapital` 로 갱신하고, 08-07 이 진짜 막으려던 오탐
(보유 중인 PAPER 세션의 잔고를 건드리지 않는다)을 **별도 테스트로 신설**해 남겼다.

`:web-api:test` **69개 클래스 전부 그린**. **미배포**.

### 남은 조사

회계가 **처음에 어긋난 경로**는 여전히 못 찾았다. 이제 불변식 ② 가 감지·알림하므로 다음에
발생하면 즉시 알 수 있다 — 그때 로그로 경로를 특정한다.

---

## 🔴 2026-08-26 운영 전면 점검 — 진입 게이트가 역선택을 하고 있었다

> DB 직접 조회로 세션·신호·포지션·주문·캐시·락을 전수 점검. 상세 보고서는
> [Artifact](https://claude.ai/code/artifact/47cc5c95-e8a4-4407-95ff-dcb7bd340d15).

### 1. 🔴 실행된 신호가 차단된 신호보다 나빴다

| | n | 24h 사후수익률 |
|---|---|---|
| **실행된** BUY | 95 | **−0.96%** |
| **차단된** BUY | 474 | **+2.77%** |

7개 전략 중 **5개**에서 실행분이 차단분보다 나빴다. 게이트를 통과한 쪽이 더 나은 전략은
`MTF_BTC` 하나뿐(+2.16%p).

**범인은 세션 간 동일코인 노출 상한**이다. 차단 사유별로 가르면 압도적이다:

| 차단 게이트 | n | 24h | 승률 |
|---|---|---|---|
| **동일코인 노출 상한** | **303** | **+4.51%** | **69.3%** |
| POSITION_MONITORING(이미 보유) | 124 | +1.25% | 50.0% |
| BTC_MARKET_GUARD | 15 | −1.6% | 혼재 |

기전: 여러 세션이 같은 코인에 동시에 BUY를 내면 먼저 스캔한 하나만 들어가고 나머지가 전부
막힌다. 막힌 303건이 +4.51%·승률 69.3%라는 건 **여러 전략이 동시에 동의한 신호일수록 잘
맞는데 그걸 한 세션만 먹고 있다**는 뜻이다.

### 2. 조치 — 노출 상한을 PAPER에서 면제

`MAX_SESSIONS_PER_COIN`의 원 근거(08-06 세션 39·45 DOGE 4초 간격 동시 진입, 단일 코인에
동적 자본 23% 집중)는 전부 **실자본** 집중 리스크다. 지금 이 상한이 걸리는 곳은 각자 독립된
가상자본을 쓰는 **페이퍼 15세션 사이**라 방어할 공유 리스크가 없고, 대신 페이퍼 함대가
존재하는 목적인 **전략 비교를 오염**시킨다 — 어떤 전략이 기회를 얻는지가 실력이 아니라
스캔 순서로 결정되기 때문이다.

`PaperLiveAlignmentTest`의 "페이퍼는 실전과 동일 로직" 원칙과 충돌하지만, **상한이 걸리는
빈도는 세션 수에 비례**하므로 페이퍼(15세션)는 실전(1~3세션 예정)을 애초에 충실히 모사하지
못한다 — 왜곡 강도가 실전보다 5배 크다. 실전 경로는 상한을 그대로 유지한다.

- `crossSessionExposureBlockReason(boolean isPaper, long heldElsewhere)` — 규칙을 순수 함수
  안으로 옮겼다(호출부에만 두면 테스트가 못 잠근다). 근거·실측을 `MAX_SESSIONS_PER_COIN`
  javadoc에 기록.
- [`CrossSessionExposureTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/CrossSessionExposureTest.java)
  **4건 → 7건** — 실전 불변식 4건 유지 + PAPER 면제 3건 신설.
  **뮤테이션 검증**: 면제를 되돌리면 **PAPER 3건만 실패하고 실전 4건은 통과함을 확인** 후 복원.
- `:web-api:test` **66개 클래스 전부 그린**. ✅ **2026-08-26 01:20경 배포 완료**.

**배포 후 실측 — 면제가 정확히 작동한다:**

| | 배포 전 (~01:16) | 배포 후 (01:20~) |
|---|---|---|
| 동일코인 노출 상한 차단 | 23건 (마지막 01:16:06) | **0건** |
| BUY 실행률 | 3/27 = 11% | 3/7 = **43%** |

결정적 증거 — 세션 68이 **01:00:53 에 KRW-BTC 로 노출 상한 차단**을 당했고, 배포 후
**01:24:07 에 같은 KRW-BTC 에 진입**했다(세션 70이 08-25 18:01 부터 계속 보유 중인데도).
69도 01:25:28 에 들어와 **BTC 를 3세션이 동시 보유**했다.

**08-27 재확인 (배포 20시간 경과)**: 차단 **0건 유지**, BUY 실행률 15.7% → **43.6%**,
08-26 진입 **37건**(기준선 15~21건의 약 2배). 효과가 확실하다.

> ⚠️ **부작용 관찰 필요**: 현재 KRW-ETH 를 **9세션**이 동시 보유 중이다. 페이퍼라 자본
> 리스크는 없지만 함대 전체가 사실상 한 종목에 몰린 상태다. A/B 비교 관점에서는 종목 효과가
> 제거돼 오히려 유리하지만(같은 코인에서 전략만 다르게 비교), **함대 총수익률을 읽을 때는
> 분산이 없다는 점을 반드시 감안**해야 한다.

### 3. 전략별 성적 (14일, DYN_PAPER)

| 전략 | 거래 | 승률 | 실현% | 손익 | 누적표본 |
|---|---|---|---|---|---|
| **MTF_BTC** | 9 | 77.8% | **+4.85** | +3,939 | 15 |
| MTF_BTC_STRICT | 6 | 66.7% | +2.21 | +1,015 | 10 |
| MTF_CONFIRMED | 6 | 50.0% | +0.37 | +538 | 7 |
| PULLBACK_MTF | 58 | 37.9% | +0.24 | +1,349 | **64** |
| MOMENTUM_ICHIMOKU_V2 | 12 | 41.7% | −0.94 | −1,034 | 16 |
| **MEANREV_BB** | 11 | **9.1%** | **−3.52** | −2,912 | 14 |
| **MOMENTUM_ICHIMOKU** | 7 | 28.6% | **−3.99** | −2,242 | 12 |

`MEANREV_BB`·`MOMENTUM_ICHIMOKU`는 **신호 기대값과 실현손익이 양쪽 다 음수**로 방향이
일치한다. 아직 KILL_CRITERIA 엣지 기준(n≥20) 미달이라 자동 판정은 안 걸린다.

### 4. 🟡 PULLBACK_MTF은 진입이 아니라 청산에서 샌다 (미조치)

표본이 충분한 **유일한** 전략(n=58). 순손익 +1,349원인데 뜯어보면:

| 청산 사유 | 건수 | 승률 | 평균 | 합계 |
|---|---|---|---|---|
| TAKE_PROFIT | 10 | 100% | +683 | **+6,830** |
| TIME_STOP | 3 | — | +187 | +561 |
| STOP_LOSS | 2 | 0% | −441 | −882 |
| **STRATEGY_SIGNAL** | **39** | **23.1%** | −118 | **−4,594** |

전략 SELL로 나간 39건 중 **21건이 −1.0% 아래**에서 청산. 손절선에 닿기 전에 전략이 먼저
항복하는 패턴이고 평균 보유 6.7시간으로 짧다. 같은 전략의 BUY 신호가 24h +4.13%(차단분)
라는 것과 나란히 놓으면 **청산이 예측이 아니라 후행**한다는 쪽에 무게가 실린다.

08-18 본전 게이트 완화(−1.00 → −0.30)의 기여는 제한적 — −1.0~−0.3% 구간 청산은 9건,
평균 −59원에 그친다. 손실의 본체는 더 깊은 구간이다.

**→ 조치: 전략 SELL 청산 A/B 착수.** `ExitRuleOverrides` 에 청산 게이트 키 2종
(`minPnlPctForSignalExit` / `lossEscapeThresholdPct`)을 추가하고 DYNAMIC 게이트에 배선했다.

- 실험군 파라미터 `{"lossEscapeThresholdPct": -100}` — "손실 구간에서는 전략 SELL 금지"
  (원화 포지션이 −100% 아래로 못 가므로 손실 탈출 조건이 절대 참이 되지 않는다). 수익 구간
  청산은 그대로 살아 있다. 실험군의 손실 포지션은 SL / TP / time stop(24h) 으로만 청산된다.
- 묻는 것: **작은 손실에서 전략 말을 듣고 나가는 게 나은가, 손절선까지 버티는 게 나은가.**
- `isPresent()` 는 **일부러 안 켠다** — 이 플래그는 `suggestedStopLoss` 와 `min()` 을 타는
  SL 계산 경로 판정용이라, 청산 게이트 키가 섞이면 대조군 대비 실험군의 **SL 이 이유 없이
  달라진다**. 대신 `hasSignalExitOverride()` 를 분리했다.
- 지문: `RulesetRegistry` 가 `strategy.params` 를 통째로 싣고 있어 arm 이 자동으로 갈린다.
- 스크립트: [`scripts/create_ab_signal_exit_sessions.sh`](../scripts/create_ab_signal_exit_sessions.sh)
  — 대조군은 기존 세션 56(H1)/63(M15), 실험군 신규 2세션. 1단계(2~3일) 배선 검증 쿼리와
  2단계(3~4주) 판정 쿼리를 스크립트에 포함.
- ⚠️ **노출 상한 PAPER 면제가 이 실험의 전제다** — 면제 전에는 두 arm 이 같은 코인을 동시에
  못 들어서 서로 다른 종목을 보게 되어 비교가 성립하지 않았다.
- 검증: [`ExitRuleOverridesTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/ExitRuleOverridesTest.java)
  **8건 → 14건**. **뮤테이션 검증**: `lossEscapeThresholdPctOr` 가 항상 fallback 을 반환하게
  되돌리면 **2건이 실패함을 확인** 후 복원. `:web-api:test` 66개 클래스 그린.
  ✅ **2026-08-26 01:20경 배포 완료**, 세션 72(H1)/73(M15) 08-26 03:42 생성.

#### ✅ 1단계 배선 검증 통과 (2026-08-27 00:03, 20시간 경과)

| 세션 | arm | TF | SELL 신호 | SELL 실행 | 본전차단 |
|---|---|---|---|---|---|
| 63 | 대조군 | M15 | 174 | **2** | 4 |
| 73 | **실험군** | M15 | 100 | **1** | **46** |
| 56 | 대조군 | H1 | 35 | 2 | 0 |
| 72 | **실험군** | H1 | 6 | **0** | 0 |

차단된 SELL 을 pnl 부호로 가르면 결정적이다:

| arm | 손실구간 차단 | 수익구간 차단 |
|---|---|---|
| 대조군 | **4** | 0 |
| **실험군** | **43** | 3 |

실험군에서 손실 구간 SELL 이 실제로 막히고 있다(대조군의 4건은 −0.30~0% 데드밴드 안쪽이라
원래도 막히는 구간). 지문도 arm·타임프레임별로 4개가 정상 분리됐다
(56=`5aff1083`, 63=`95263420`, 72=`c4af756e`, 73=`432aab5f`).

**2단계 판정은 3~4주 뒤** — arm 당 청산 20건 이상 필요. 현재 실험 시작 후 청산은
대조군 4건(STRATEGY_SIGNAL, 평균 −170) / 실험군 1건(평균 +17)로 아직 판정 불가.

### 5. 이상 없음을 확인한 항목

- 세션 15/16 RUNNING, 마지막 틱 1~9분 전
- **사후수익률 6,062건 중 결손 0** — 분석 데이터 신뢰 가능
- 주문 실패·취소·거절 최근 3일 **0건**
- `idle in transaction` 0, Lock 대기 0, 커넥션 14/20
- `ruleset_hash` 08-19 이후 6개 유지 — 표본 안 쪼개짐
- 스로틀 수정 효과 확인 — 전 코인 갭 6건만 수신

### 6. 남은 것

- **세션 62**: 08-25 06:49 비상정지 후 방치(서킷 사유 없음 = 수동 정지). MOMENTUM_ICHIMOKU
  M15 축이 비어 있다 — 페이퍼라 재기동 비용 0.
- **서킷 브레이커 발동 이력 0** — 08-25 데드락 수정 실전검증 여전히 대기.
- **손절폭 A/B**(`paper_trading.virtual_balance` 330~369): 청산 26건 — 판정에 한참 못 미침.

---

## 🟢 2026-08-26 DYNAMIC 스캔 루프 캔들 조회를 캐시 경유로 전환 — 진짜 스로틀 병목 발견

08-25 `MarketDataSyncService.syncPair` 갭 최적화를 배포하고 운영 로그로 효과를 확인하던 중,
같은 시각 `scheduler-4` 스레드가 BTC·ETH·XRP·SOL·TRUMP·STX·ONDO·XLM·ZRO·SLX 등 다수 코인에
M15·H1 각 **500건씩 연속으로** REST 재요청하고 있는 것을 발견했다. 원인은
[`DynamicTradingService.fetchCandles`](../web-api/src/main/java/com/cryptoautotrader/api/service/DynamicTradingService.java#L1661)
— `MarketDataSyncService`와 무관하게 **DYNAMIC 스캔/모니터링 틱마다 워치리스트 코인 수만큼**
`UpbitCandleCollector.fetchCandles`를 직접 호출해 매번 500개 전량을 재수집하고 있었다.
08-25 수정은 PAPER/LIVE 전용 `syncPair` 경로만 손봤을 뿐, 실제 스로틀 경합의 대부분을 차지하는
DYNAMIC 경로는 건드리지 못한 것이었다.

**수정**: [`MarketDataSyncService.fetchWithCache`](../web-api/src/main/java/com/cryptoautotrader/api/service/MarketDataSyncService.java)
신설 — `syncPair`와 같은 "마지막 저장 시각 이후 갭만 REST, 나머지는 캐시에서" 로직을 재사용하되,
persist만 하고 끝나는 `syncPair`와 달리 lookback 전체 구간을 캐시와 병합해 `List<Candle>`로 반환한다.
`DynamicTradingService.fetchCandles`가 `UpbitCandleCollector`를 직접 쓰던 것을 이 메서드 호출로 교체
(생성자에 `MarketDataSyncService` 주입 추가). 이제 DYNAMIC도 PAPER/LIVE와 같은 `market_data_cache`를
공유해서 읽고, 코인당 REST 호출은 갭(보통 1~수 건)만 남는다.

**⚠️ 트랜잭션 범위 참고**: `fetchWithCache`는 `@Transactional`(기본 REQUIRED)이라 `processScanningTick`/
`processMonitoringTick`(둘 다 `@Transactional`)의 외부 트랜잭션에 합류한다. REST 호출(최대 15초 타임아웃)이
DB 커넥션을 물고 있는 시간 자체는 수정 전에도 이미 있었던 패턴(기존에도 같은 트랜잭션 안에서 REST를
불렀음)이라 새로운 위험 범주는 아니지만, 여기에 DB read/write가 추가돼 트랜잭션 점유 시간이 늘었다.
운영 관찰 필요 — HikariCP 풀 고갈이나 사이클 지연이 오히려 늘면 `fetchWithCache`를 트랜잭션 밖으로
빼내는 리팩터링을 검토할 것.

**검증**: `:web-api:compileJava`/`compileTestJava` ✅, `:web-api:test` 65개 테스트 클래스 전부 실패 0.

**배포 확인 (2026-08-26 00:41 KST)**: 배포 후 로그에서 BTC·DOGE·SOL·LINK·ADA·AVAX·PROM·EUL 등
전 코인이 H1·M15 모두 **6건**(갭)만 REST로 수신 — 500건 재수집 패턴 완전히 사라짐. 에러/예외 0건.

### 🟡 후속 — 캐시 커버리지 방어 추가 (⚠️ "회귀"라는 최초 진단은 오진이었다)

배포 직후 점검에서 캐시가 lookback 창을 못 덮는 코인을 발견했다 — KRW-LIT M15 180개,
KRW-PROM H1 334개. 캐시 전환이 만든 회귀로 판단하고 방어를 넣었는데, **배포 후 실측에서
오진으로 확인됐다**:

- KRW-LIT은 **08-25 06:50에 워치리스트에 처음 등장**한 신규 코인이고, Upbit에 M15 이력이
  46시간치(184개)밖에 없다. KRW-PROM도 `candle_data`에 08-12 이후만 존재한다.
- 즉 **전환 전에도 REST로 500개를 요청해 184개만 받았을 것**이다. 회귀는 없었다.
- 수정 배포 후 30분 쿨다운으로 전량 재수집을 두 번 이상 시도했는데도 행 수가 그대로인 것
  (184/336)이 거래소에 더 이상 데이터가 없다는 직접 증거다.

**수정 자체는 유효하다** — 캐시가 부분적인데 거래소에는 더 있는 경우를 방어하고, 신규 상장
코인의 무한 재수집도 막는다. 다만 **고친 회귀는 없었다**는 것을 기록해 둔다.

> 🟡 **여기서 진짜로 드러난 것**: 워치리스트에 EMA200(닫힌 캔들 201개)을 계산할 수 없는
> **신규 상장 코인이 들어온다**. 각 전략이 자체 최소 캔들 가드를 갖고 있어 잘못된 신호가
> 나오진 않지만(해당 지표만 조용히 비활성), 전략이 **축소된 지표 집합으로 도는 것을 아무도
> 모른다**. 세션 61이 실제로 LIT 포지션을 들고 있다. 워치리스트 필터에 최소 이력 조건을
> 넣을지는 별도 판단 — 아래 "보류·결정 대기 항목" 참조.

**수정**: [`shouldFetchFullRange`](../web-api/src/main/java/com/cryptoautotrader/api/service/MarketDataSyncService.java)
순수 함수 신설 — 캐시의 `MIN(time)`이 창 시작점을 (캔들 2개 여유 안에서) 덮는지 확인하고, 못 덮으면
전량 재수집한다. 상장 직후 코인처럼 거래소에 애초에 이력이 없으면 조건이 영원히 거짓이 되어 최적화가
무효화되므로, 전량 수집을 **시도**한 pair는 `FULL_BACKFILL_RETRY_MINUTES`(30분) 동안 재시도하지 않는다
— 최악의 경우도 시간당 2회로, 전환 전(코인당 매 틱)보다 훨씬 적다. `findMinTime` 리포지터리 메서드 추가.

**검증**: 신규 [`MarketDataCacheCoverageTest`](../web-api/src/test/java/com/cryptoautotrader/api/service/MarketDataCacheCoverageTest.java)
**7건** — 캐시 없음/창 덮음/여유 경계/실측 부족 재현/쿨다운 중/쿨다운 경과/덮으면 쿨다운 무관.
**뮤테이션 검증**: `shouldFetchFullRange`를 항상 `false`(수정 전 동작)로 되돌리면 **3건이 실패함을 확인** 후 복원.
`:web-api:test` 전체 그린. ✅ **2026-08-26 01:20경 배포 완료**.

---

## 🟢 2026-08-25 DYNAMIC 운영 가능성 점검 — P0 데드락 발견·수정 + WF 게이트 코인 단위 재설계

> "DYNAMIC을 메인으로 쓸 수 있는 상태인가"를 점검하다가 발견·수정한 것들. 상세 근거는 이 세션 대화 로그 참조.

- **✅ SL 손절폭 A/B 오버라이드가 조용히 무효화되던 버그 수정** — `PaperTradingService`/`LiveTradingService`/`DynamicTradingService`에서 "전략 제안 SL이 더 넓으면 채택"하는 로직이 세션별 `slAtrMultiplier`/`tpRrMultiplier` 오버라이드를 가려, 실험군·대조군 SL이 완전히 동일하게 나왔다. `exitOverrides.isPresent()`일 때 이 로직을 건너뛰도록 수정, §23 A/B 재기동 완료.
- **✅ SL 워치독이 PAPER 세션을 오탐하던 버그 수정** — `warnStaleSlCheck()`가 PAPER 세션엔 애초에 안 걸리는 WS 실시간 체크(`recordSlCheck`)를 기다리며 매분 "SL 미점검" 오탐 알림을 반복 발송. `!s.isPaper()` 필터 추가.
- **✅ candle_data 5개월 공백 백필** — 전 코인·전 타임프레임이 2026-03-30에 멈춰 있었다(수동 트리거 방식이라 아무도 재실행 안 함). 22개 기존 코인 갭 + LINK·AVAX·PROM·EUL 등 신규 코인 백필 완료.
- **✅ WalkForwardValidationGate를 (전략,코인) 단위로 재설계** — 기존엔 "가장 최근 실행된 WF 결과 하나"로 전략 전체를 판정해, 우연히 마지막 실행된 코인(대개 나쁜 코인) 성적으로 좋은 조합까지 가려졌다. `evaluate(strategy, coin)`(LIVE용, 코인 특정) / `evaluate(strategy)`(DYNAMIC용, "검증된 코인 하나라도 있으면 PASS") 로 분리. `docker-compose.prod.yml`에 `REQUIRE_WALK_FORWARD_GATE` 환경변수 전달 누락도 같이 발견·수정(게이트 코드는 있었는데 컨테이너에 안 넘어가고 있었음). 게이트 활성화 완료.
- **✅ WF 재검증 범위를 실제 DYNAMIC 감시목록 기준으로 확장** — 처음엔 페이퍼 함대 8코인만 돌렸다가, 실제 RUNNING 중인 16개 동적 세션의 watchlist_json을 합쳐보니 18개 고유 코인(BTC·ETH·SOL·XRP·XLM·ONDO·SUI·ADA·WLD·GRVT·ENA·RE·TRUMP·FOLD·TRAC·STX·WLFI·PUMP)이 나와 이 기준으로 6전략×18코인=108조합 재검증. 결과: 18코인 중 7~8개에서만 검증된 조합 발견(SUI·BTC·ONDO·WLD·ADA·ENA·PUMP). `MOMENTUM_ICHIMOKU_V2`는 딱 1개 코인(SUI)만 통과 — 유지 여부 판단 필요.
- **✅ WF 게이트를 DYNAMIC 스캔 루프(`processScanningTick`)에 실제 연결** — 이전엔 세션 생성 시점에만 걸려 있어서, 검증 통과한 전략이라도 실제 매수 코인은 전혀 다른(미검증) 코인일 수 있었다. `walkForwardValidationGate.evaluate(전략, 코인)`을 워치리스트 순회 루프에 연결, 미검증 조합은 BUY 후보에서 제외(blocked_reason 기록). **PAPER 세션은 제외**(탐색 도구라 막으면 안 됨) — 처음 배포 때 이 예외를 빠뜨려서 되돌린 적 있음, 최종본엔 반영됨.
- **🔴 P0 — DYNAMIC 전체가 29시간 무응답이던 사고 발견·수정.** 원인: `processTick()`가 서킷 브레이커 발동 시 바깥 트랜잭션에서 `dynamicSessionRepo.save(fresh)`로 세션 행을 먼저 쓰고(커밋 전) 곧바로 `emergencyStop()`을 불렀는데, 그 안의 `balanceUpdater`(REQUIRES_NEW)가 **같은 행**을 새 커넥션으로 다시 쓰려다 바깥 트랜잭션의 잠금을 기다리며 자기 자신과 순환 대기 — Postgres에 타임아웃이 없어 무한정 멈췄다(세션 60의 서킷 브레이커가 두 번 발동, 두 번 다 이 경로로 죽음). 처음엔 임시로 `pg_terminate_backend()`로 좀비 커넥션을 끊어 응급 조치했고, 이후 근본 수정: 서킷 브레이커 필드를 `emergencyStop()`의 단일 REQUIRES_NEW 갱신에 합쳐 바깥 트랜잭션이 그 행을 아예 안 건드리게 함. 배포 완료, 재발 여부는 다음 서킷 브레이커 발동 때 실제 확인 필요(아직 라이브 파이어 검증 안 됨).
- **✅ Upbit REST 클라이언트 타임아웃 부재 수정** — `UpbitRestClient`/`UpbitOrderClient`의 모든 HTTP 요청(9곳)에 connect 타임아웃만 있고 응답 대기 타임아웃이 없었다. `.timeout(Duration.ofSeconds(15))` 추가 — 위 데드락의 원인은 아니었지만(그건 DB 락) 별개의 잠재적 무한 대기 위험이라 같이 고침.
- **🟡 부분 완화 — Upbit REST 공유 스로틀 경합.** 앱 전체가 하나의 정적 스로틀(초당 ~9회)로 모든 Upbit 호출을 직렬화하는데, `MarketDataSyncService`가 60초마다 코인당 캔들 520개를 매번 전량 재수집하고 있어 불필요한 API 호출이 압도적으로 많았다(H1은 1시간에 1개, M15는 15분에 1개만 늘어나는데도). "마지막 저장 시각 이후 갭만" 받도록 수정(`MarketDataCacheRepository.findMaxTime` 추가) — 데이터 없으면(첫 동기화) 기존처럼 전체 수집. 효과는 아직 실측 안 됨.
- **텔레그램 봇 토큰 노출 의심 → 재발급·교체 완료.**

---

## 🟠 2026-08-24 운영DB 로그 분석 — DYN_PAPER(동적 페이퍼) / PAPER(고정코인 페이퍼)

> 대상: 운영 `yhpapa.iptime.org:8432`, 읽기전용. 기간 2026-08-19 재기동 ~ 08-23 24:00 UTC (5일).
> **주의**: 페이퍼 데이터는 두 곳에 나뉘어 있다 — 동적 세션은 `public`(`dynamic_session`/`position.session_kind='DYN_PAPER'`),
> 고정코인 세션은 **`paper_trading` 스키마**(`virtual_balance`/`position`/`order`). `public` 만 보면 PAPER는 로그만 있고
> 세션·포지션이 없는 것처럼 보인다(초판 분석의 오독 원인).
>
> 가동 현황: 동적 18세션(id 54~71, 전부 PAPER, 각 10,000원) + 고정코인 133세션(`virtual_balance` RUNNING, 각 1,000만원).
> REAL 동적/LIVE 세션은 08-18 05:13 전량 중지.

### 1. 핵심 결론 — `COMPOSITE_PULLBACK_MTF` 하나가 전체 손실을 만든다

PAPER 청산 354건(08-19~23) 기준:

| 그룹 | 청산 | 승률 | 평균이익 | 평균손실 | 누적손익 | 손절/익절 |
|---|---|---|---|---|---|---|
| **COMPOSITE_PULLBACK_MTF** | 163 | **11.7%** | +214,155 | -61,828 | **-4,834,211** | 135 / 18 |
| 나머지 6전략 | 191 | 25.7% | +229,163 | -73,307 | **+819,443** | 135 / 49 |

R:R 3.46 이면 손익분기 승률이 22.4%인데 PULLBACK은 11.7%다. **이 전략만 빼면 나머지 시스템은 흑자**이며,
전체 -401만원은 사실상 PULLBACK 단독 -483만원이다. n=163으로 표본도 충분하고 M15(-349만, 12.8%)·H1(-135만, 6.7%)
양쪽 타임프레임에서 동일하게 무너진다.

### 2. 타임프레임 — M15 > H1 (백테스트 결론과 반대)

| 소스 | M15 | H1 |
|---|---|---|
| DYN_PAPER (n=87) | +7,611 / 승률 58.5% | **-4,600** / 승률 17.6% |
| PAPER (n=354) | -1,375,998 / 승률 21.9% | **-1,859,122** / 승률 8.6% |

PAPER는 양쪽 다 적자지만(PULLBACK 포함) 전략별로 보면 7개 중 5개가 M15에서 더 낫고, H1에서 승률 0%인 전략이
3개(MOMENTUM_ICHIMOKU, ICHIMOKU_V2, MTF_CONFIRMED)다. **방향성은 두 소스에서 일치한다.**
→ 「🚨 배포 금지 — 전 코인 × M15(오버트레이딩 -99%)」 항목과 실측이 정면 충돌. 재검토 필요.

### 3. 청산 사유

| exit_reason | PAPER 건수 | 승률 | 누적 | DYN_PAPER 건수 | 누적 |
|---|---|---|---|---|---|
| STOP_LOSS | 262 | 0% | -17,544,562 | 19 | -9,595 |
| TAKE_PROFIT | 67 | 100% | +15,269,620 | 23 | +15,379 |
| STRATEGY_SIGNAL | 1 | 100% | +28,330 | 28 | **-2,661** |
| TIME_STOP | 0 | — | — | 10 | +717 |

손절이 익절의 3.9배(262:67). DYN_PAPER의 STRATEGY_SIGNAL 청산 28건은 승률 32%로 순손실 — 지표 기반 조기 청산이
익절 도달 전에 이익을 깎는 정황. 다만 n=28이라 단독 근거로는 약하다(PAPER에는 이 경로가 1건뿐).

### 4. 필터 효과 검증 (DYN_PAPER BUY 신호의 24시간 후 수익률)

| 분류 | 건수 | 평균 24h 수익률 | 상승비율 |
|---|---|---|---|
| 실행됨 | 73 | **-0.23%** | 49.3% |
| 동일코인 노출상한 차단 | 233 | **+5.75%** | 70.8% |
| 기타 차단 | 94 | +2.31% | 60.6% |
| BTC_MARKET_GUARD 차단 | 18 | -1.73% | 27.8% |
| BLACK_SWAN 쿨다운 차단 | 15 | **-9.57%** | 6.7% |

- **BTC_MARKET_GUARD / BLACK_SWAN 은 확실히 작동한다** — 차단 신호의 사후 수익률이 크게 음수.
- 동일코인 노출상한(`MAX_SESSIONS_PER_COIN=1`) 차단분의 사후 수익률이 +5.75%로 높지만, **이미 상승 중인 코인이
  반복 BUY를 내는 생존편향**이 섞여 있다. 게다가 이 상한은 수익 최적화가 아니라 2026-08-06 단일코인 23% 집중
  사고를 막으려 넣은 리스크 장치다(`DynamicTradingService` javadoc). 성급히 완화하면 안 된다 — A/B 전용 항목.
- 실행된 신호가 차단된 신호보다 나쁘다는 점 자체는 **진입 선별 로직의 역선택** 신호로 남는다.

### 5. 🐛 `order.quantity` 이중 단위 — 조치 완료

`public."order"` 의 **시장가 매수** 행은 `quantity` 에 코인 수량이 아니라 **투입 KRW 금액**이 들어간다.
Upbit `price` 타입 주문 파라미터가 KRW 총액이라서 생긴 <b>의도된</b> 오버로드지만
(`OrderExecutionEngine#submitToExchange`), 읽는 쪽이 이를 모르면 `price × quantity` 가 최대 10^8 배로 튄다.

실측: BUY 행의 `quantity = position.invested_krw` 일치율 DYN_PAPER 98/98, DYNAMIC 24/24, LIVE 195/271.
`quantity = position.size` 는 0건. 예) order 9609 KRW-TRAC — `quantity=10334.152`(투입금), 실제 `size=20.114`,
`price × quantity = 5,306,721`(실제의 513배).

**과거 데이터 보정은 불필요하다** — `filled_quantity` 가 전 구간에서 실제 코인 수량을 정확히 갖고 있다
(`filled_quantity = position.size` 일치: DYN_PAPER 98/98, DYNAMIC 24/24, LIVE 264/271). 읽는 쪽만 고치면 된다.
참고로 `paper_trading."order"` 는 이 문제가 없다(BUY/SELL 모두 코인 수량, 404/404).

조치:
- `web-api/util/OrderAmounts` 신설 — `coinQuantity()` / `krwAmount()` 로 단위 판정을 한 곳에 모음 (+단위 테스트 6건)
- `OrderEntity.quantity` 에 이중 단위 경고 javadoc
- `TradingController.toOrderMap` — 수수료를 `price × quantity` → `krwAmount × feeRate` 로 교정, `quantity` 는
  코인 수량으로 내보내고 `krwAmount` 필드 추가 (차트 툴팁이 시장가 매수에서 KRW를 "수량"으로 표시하던 버그)
- 프런트 `lib/utils.fmtOrderQuantity()` 신설 후 대시보드·세션 상세·업비트 로그 3곳 통일
  (업비트 로그 페이지에만 있던 임시 분기를 공용 헬퍼로 흡수)
- CSV 내보내기 헤더 `주문수량` → `주문수량(시장가매수=KRW)`

### 6. 세션 49 잔고 오염 — 이미 조치된 건 (재발 없음 확인)

세션 49는 청산 2건의 실현손익 합계가 -168원인데 `available_krw` 가 221,876원(+2,118%)이다.
원인은 **2026-08-19 P0로 이미 규명·수정된 매도대금 중복 지급**이다 — 매도대금 반영이 `REQUIRES_NEW` 라
바깥 트랜잭션이 롤백돼도 살아남는데 포지션 CLOSED 저장은 함께 롤백돼, "대금은 남고 포지션은 OPEN 복귀"가
반복되며 21회 중복 지급됐다(`DynamicTradingService#finalizeDynamicSell` 주석).
수정: `dynamic_sell_settlement` 정산 표식을 대금과 **같은 트랜잭션**에 기록해 포지션당 1회만 반영
(Flyway V72, 운영 적용 2026-08-19 06:02 — 신규 세션 54~71 기동 5분 전).

재발 없음 확인 — RUNNING 18세션 전부 `available_krw + 미청산 invested_krw = 10,000 + 누적 실현손익` 이
**정확히** 일치한다(오차 0원). §5 의 `quantity` 단위 문제와는 무관한 건이었다(초판 분석의 추정은 틀렸다).

### 7. 오독 정정 (초판 분석에서 잘못 보고한 것)

- ❌ ~~「PAPER 로그 고아 — 세션·포지션 0건」~~ → **사실 아님**. `paper_trading` 스키마를 조회하지 않아서 생긴 오독.
  `virtual_balance` 에 세션 133건, `position`/`order` 정상 기록 중.
- ❌ ~~「exit_reason NULL 7건 — 08-20 점검의 100%와 불일치」~~ → **이미 해결된 건**. NULL 행의 `closed_at` 최댓값이
  DYN_PAPER 2026-08-19 05:51, paper_trading 2026-08-19 05:57로 **전부 08-19 06:00 재기동 이전**이다.
  재기동 이후 NULL은 0건 — 08-20 점검 결과가 맞았다.
- ❌ ~~「세션 49 오염은 quantity KRW 단위를 코인 수량으로 오인한 강제청산 탓」~~ → **원인 오추정**.
  실제 원인은 매도대금 중복 지급이며 V72로 이미 수정됨(§6). 강제청산 경로는 `pos.getSize()` 를 쓰고 있어 정상이다.
- ❌ ~~「MEANREV_BB 중단 1순위」~~ → **표본 부족으로 철회**. DYN_PAPER n=9(-1,858)였으나 PAPER n=18에서는 +136,077.
  두 소스가 반대다. 진짜 중단 후보는 §1 의 PULLBACK_MTF(n=163).

### 8. ✅ PULLBACK_MTF 고정코인 세션 중지 완료 (2026-08-24)

`scripts/stop_pullback_paper_fleet.sh` 로 16세션 중지, 실패 0건. DB 확인:
`paper_trading.virtual_balance` 의 PULLBACK_MTF **RUNNING 0건 / STOPPED 19건**, 미청산 포지션 0건.

강제청산 3건은 모두 이익으로 닫혔다(207 KRW-BTC +4,738 / 143 KRW-DOGE +38,063 / 122 KRW-BTC +4,840,
합 +47,641) — 08-23 시점 평가손(-17,695)보다 개선된 값으로 청산됐다.
다만 분석 시점과 중지 사이(08-24 00:04~00:17)에 STOP_LOSS 4건이 추가로 터져 **-567,770** 을 더 잃었다.

**동적 PULLBACK 6세션(56·63·68·69·70·71)은 유지했다** — 근거 표본 163건이 전부 고정코인 쪽이고,
동적에서는 같은 전략이 n=42에 +1,865(7전략 중 2위)로 정반대다. 게다가 이 6세션은 08-19부터 돌고 있는
감쇠 A/B 그 자체다(56·63 대조군 / 68·69 EMA군 / 70·71 TRANS군).

> 스크립트 주의: 백엔드 API 는 **외부 미개방**이라 운영 서버에서 `localhost:8080` 으로만 접근되고
> `Authorization: Bearer $API_AUTH_TOKEN`(`.env`) 이 필요하다. 대상 ID는 하드코딩하지 않고 API 응답에서 뽑는다.

### 9. H1 축소 판단 근거 — PULLBACK 제거 후에도 H1은 적자

PULLBACK_MTF 를 뺀 잔여 6전략의 08-19~23 청산 191건:

| timeframe | 청산 | 승률 | 누적손익 | 평균이익 | 평균손실 | 손익분기 승률 |
|---|---|---|---|---|---|---|
| **M15** | 160 | 28.8% | **+1,766,303** | +218,806 | -72,796 | 25.0% |
| **H1** | 31 | 9.7% | **-946,860** | +387,979 | -75,386 | 16.3% |

→ **H1 부진은 PULLBACK 탓이 아니다.** 순수 타임프레임 효과로 남는다. H1은 이기면 크게(+387,979) 이기지만
빈도가 손익분기(16.3%)에 못 미친다(9.7%).

단 **H1 표본은 31건으로 얇다**(6전략 5일). 전략별로 보면 승률 0%가 3개로 갈리고, 나머지 3개는 n=4에
승률 25%(각 1승)라 사실상 운의 영역이다:

| H1 전략 | n | 승률 | 누적 |
|---|---|---|---|
| MOMENTUM_ICHIMOKU_V2 | 7 | **0.0%** | -579,744 |
| MTF_CONFIRMED | 6 | **0.0%** | -495,660 |
| MOMENTUM_ICHIMOKU | 6 | **0.0%** | -495,660 |
| MEANREV_BB | 4 | 25.0% | +361,495 |
| MTF_BTC / MTF_BTC_STRICT | 4 / 4 | 25.0% | +131,355 / +131,355 |

### 10. 🔴 벤치마크 대비 — 시스템은 **어느 장에서도 수익을 못 낸다**

지금까지 성적을 **같은 기간 BTC 단순보유**와 나란히 놓으면 성격이 드러난다.

| 구간 | BTC 단순보유 | 시스템 | 격차 |
|---|---|---|---|
| LIVE 실전 2026-03-17~08-17 | **-17.9%** (109.6M→90.0M) | -2.98% (44세션 110만원) | **+14.9%p 우위** |
| 동적 REAL 07~08월 | 하락장 | -0.54% (49세션 105만원) | 우위 |
| PAPER 함대 08-18~08-24 | **+17.3%** (90.6M→106.2M) | **-0.40%** (91세션 9.1억) | **-17.7%p 열위** |

→ **하락장에서는 잘 지키고, 상승장에서는 못 먹는다.** 방향과 무관하게 대략 0% 근처에 붙어 있는
"자본 보존 기계"다. 지금 상태로 실전에 넣으면 하락장 방어 가치는 있지만 상승장 기회비용이 그보다 크다.

누적 표본: 실전 307건(LIVE 277 + 동적 30) 전부 적자, 페이퍼 404건 적자. **700건 넘게 흑자 구간이 없다.**

### 11. 🔧 근본 원인 — 손절폭이 노이즈보다 좁다

PAPER 청산 360건(08-19~)을 청산 사유별로 보면 진단이 명확하다.

| exit_reason | n | 평균 보유 | 평균 수익률 |
|---|---|---|---|
| **STOP_LOSS** | 273 | **36분** | **-0.88%** |
| TAKE_PROFIT | 67 | 153분 | +2.87% |

손절이 **평균 36분 만에 -0.88%** 에서 걸린다. 암호화폐 15분봉의 일상적 노이즈 폭이 그 정도다 —
논지가 전개될 시간을 갖기 전에 잡음에 털린다. 이긴 거래는 4배 오래(153분) 들고 +2.87%를 낸다.

`ExitRuleCalculator`: SL폭 = `clamp(ATR(14) × 1.5, floorPct, 8%)`, TP = SL × 2.
설계 의도(변동성 적응)는 맞지만 **배수 1.5가 캔들 주기 기준이라 보유 horizon에 비해 너무 좁다.**

산술적으로는 아깝다 — 실측 R:R = 2.87/0.88 = **3.26**, 손익분기 승률 **23.5%**, 실제 승률 **19.7%**.
**승률 4%p 차이로 적자**다. 손절폭을 넓히면(SL_ATR_MULTIPLIER 1.5 → 2.5~3.0) 노이즈 손절이 줄어
승률이 올라가는 방향이라, 이게 단일 최대 레버다. 단 TP도 함께 멀어지므로 A/B 없이는 확언 불가.

### 12. 🐛 MTF_BTC 와 MTF_BTC_STRICT 는 **같은 전략이다**

두 전략의 청산 43건이 **코인·진입시각·손익까지 전부 동일**하다(진입 시각 차이 30ms — 같은 tick).
`STRICT` 변형이 신호를 전혀 좁히지 못하고 있다. 7전략을 돌린다고 믿고 있지만 실제로는 6개 이하다.
H1 구간에서는 MOMENTUM_ICHIMOKU 와 MTF_CONFIRMED 도 성적이 완전히 겹친다(n=6, -495,660 동일).

### 13. 전략별 활용가치 (paper_trading 전체 이력, 거래당 기대수익률 기준)

| 전략 | n | 승률 | 거래당 기대 | 누적 | 판정 |
|---|---|---|---|---|---|
| MTF_CONFIRMED | 23 | 21.7% | **+0.287%** | +346,326 | 후보 (n 부족) |
| MTF_BTC | 43 | 30.2% | **+0.160%** | +222,972 | 후보 |
| MTF_BTC_STRICT | 43 | 30.2% | +0.160% | +222,972 | **MTF_BTC 중복 — 제거 대상** |
| MOMENTUM_ICHIMOKU | 27 | 18.5% | +0.102% | +80,866 | 판단 보류 |
| MEANREV_BB | 20 | 20.0% | +0.098% | +104,594 | 판단 보류 |
| MOMENTUM_ICHIMOKU_V2 | 48 | 20.8% | -0.119% | -598,045 | 열위 |
| PULLBACK_MTF | 200 | 12.0% | **-0.427%** | -6,488,440 | ✅ 중지 완료 |

> 주의: 흑자 5개는 전부 **n<50** 이고 측정 구간이 BTC +17.3% 상승장이다. 통계적 유의성 없음 —
> "이 순서로 유망하다"까지는 말할 수 있어도 "수익성이 검증됐다"고는 못 한다.

### 14. 전략 구현 감사 — 실제로는 3개 신호원뿐 (2026-08-24)

프리셋 정의(`CompositePresetRegistrar`)를 코드로 훑은 결과, 활성 6전략이 **독립적이지 않다**.

| 신호 코어 | 이 코어를 쓰는 전략 |
|---|---|
| **MACD + VWAP + GRID** (CMI_V1) | MOMENTUM_ICHIMOKU · **MTF_CONFIRMED**(레짐 4개 중 3개를 CMI_V1로 위임) |
| **ATR + VD + MACD** (CB) | MTF_BTC · ~~MTF_BTC_STRICT~~ · **MTF_CONFIRMED**(VOLATILITY 레짐) |
| **MACD + SUPERTREND + GRID** | MOMENTUM_ICHIMOKU_V2 |
| **BB + RSI + VWAP** | MEANREV_BB ← **유일하게 직교** |

- `CompositeRegimeRouter` 는 TREND·TRANSITIONAL·RANGE(4중 3)를 전부 CMI_V1 에 위임한다.
  즉 **MTF_CONFIRMED ≈ MOMENTUM_ICHIMOKU + H4 Supertrend 필터** 다 — 별개 전략이 아니라 파생형.
- **MACD 가 6개 중 5개에 들어간다.** 6전략 분산 운용이라 믿고 있지만 실제 분산 효과는 훨씬 작고,
  §10의 "어느 장에서도 0% 근처" 성질도 이걸로 설명된다 — 다 같이 켜지고 다 같이 꺼진다.
- 이건 버그가 아니라 설계상 계층 구조다. 다만 **성과 비교 시 독립 표본으로 취급하면 안 된다.**

### 15. 🐛 `strictHtf` 구조적 무효 — MTF_BTC_STRICT 는 MTF_BTC 의 완전 중복

운영 실측에서 두 전략 청산 43건이 **코인·진입시각·실현손익까지 전부 동일**했다(진입 차 30ms = 같은 tick).
두 프리셋의 유일한 차이가 `strictHtf` 인데 결과가 같다 = 이 플래그가 아무것도 막지 못했다.

**원인**: `MtfConfirmedStrategy` 에서 `strictHtf` 가 동작을 바꾸는 분기는 둘뿐이다 —
① HTF 캔들 부족 ② HTF 신호가 HOLD. 그런데 HTF 확인자인 `SupertrendStrategy` 는
**데이터만 있으면 절대 HOLD 를 반환하지 않는다**(추세선 위=BUY / 아래=SELL 이분법).
게다가 `getMinimumCandleCount() = max(ltf, htfFactor×12)` 이라 호출 시점에 HTF 캔들 12개가
이미 보장된다. **두 분기 모두 도달 불가** → strict 는 no-op.

이 문제는 MTF_BTC_STRICT 만이 아니라 **HTF 확인자로 Supertrend 를 쓰는 모든 MTF 프리셋에 해당**한다.

- 회귀 고정: `core-engine/.../SupertrendStrictHtfNoOpTest` (300 시나리오 × BUY/SELL 동일성 + 도달불가 계약)
- 코드 표시: `CompositePresetRegistrar` 에 DEPRECATED 주석. **등록은 남긴다** — 과거 세션 조회와
  재활성화 경로를 깨지 않기 위해서다.
- 비활성화: `scripts/disable_duplicate_strategies.sh` (세션 정지 + `strategy_type_enabled` is_active=false)
- 되살리려면 HTF 확인자를 **HOLD 를 낼 수 있는 전략**으로 교체해야 한다(예: ADX 임계 미달 시 HOLD).

> `StrategyEnablementGate` 는 **신규 세션 생성만** 막는다(LIVE·DYNAMIC·PAPER 공통). 이미 RUNNING 인
> 세션은 계속 돌기 때문에 스크립트가 세션 정지까지 함께 처리한다.

### 16. 이미 비활성인 전략 (참고)

`strategy_type_enabled` 는 **차단 목록**으로 동작한다(행이 없으면 활성). 현재 21개가 false:

- 단일 지표 전부 — RSI · MACD · BOLLINGER · EMA_CROSS · VWAP · SUPERTREND · ATR_BREAKOUT ·
  STOCHASTIC_RSI · GRID · VOLUME_DELTA · ORDERBOOK_IMBALANCE · FAIR_VALUE_GAP · MACD_STOCH_BB
- 복합 — COMPOSITE · COMPOSITE_ETH · COMPOSITE_MOMENTUM · COMPOSITE_BREAKOUT ·
  **COMPOSITE_BREAKOUT_ICHIMOKU**(2026-04-12, "ADX 필터가 이미 다 막아 Ichimoku 가 무의미") ·
  COMPOSITE_REGIME_ROUTER · COMPOSITE_MTF_MOMENTUM · HEIKIN_ASHI_STOCH

→ COMPOSITE_BREAKOUT_ICHIMOKU 는 이번 건과 **같은 유형의 중복**(필터가 이미 걸러 추가 필터가 무효)이며
이미 4월에 처리돼 있었다. 같은 패턴이 반복되고 있으니, **새 필터 레이어를 추가할 때는
"이 필터가 실제로 막은 신호 수"를 먼저 세는 검증을 붙일 것.**

### 17. ⚠️ 손절폭 확대는 이미 한 번 실패했다 — A/B 설계 시 필수 고려

`ExitRuleCalculator.TP_PCT_MAX` javadoc에 남아 있는 2026-08-05 실측 기록:

> TP를 SL 폭의 2배로 따라 키우다 보니 KRW-META2는 TP가 **+14.10%** 로 잡혔다.
> 넓은 SL은 반드시 맞고 넓은 TP는 사실상 안 맞는다 — 07-31 개편 이후 5일간 **익절 0건 / 손절 3건**.

즉 §11의 "손절폭을 넓히자"는 처방은 **그대로 실행하면 07-31을 반복한다.** 원인은
`TP = SL × TP_RR_MULTIPLIER(2.0)` 연동이라 SL을 넓히면 TP가 같이 멀어져 도달 불가가 되는 것.

**따라서 A/B 실험군은 두 상수를 함께 움직여야 한다:**
- `SL_ATR_MULTIPLIER` 1.5 → 2.5 (노이즈 손절 감소)
- `TP_RR_MULTIPLIER` 2.0 → 1.2~1.5 (TP 절대거리를 현재 수준으로 유지)

목표는 R:R을 키우는 게 아니라 **승률을 손익분기 위로 올리는 것**이다. 현재 실측 R:R 3.26은
이미 충분하고 부족한 건 승률(19.7% vs 손익분기 23.5%)이다.

**구현 제약**: 이 값들은 `private static final` 상수이지 세션 파라미터가 아니다. A/B 하려면
세션별 오버라이드 경로를 만들고 **`behaviorParams()` 지문에도 반영해야 한다** —
안 하면 서로 다른 규칙의 거래가 한 표본에 섞인다(`RulesetFingerprintTest` 가 강제).
감쇠 A/B가 `strategy_params` jsonb를 쓴 것과 같은 방식이 선례다.

### 18. 감쇠 A/B 진행 상황 — 판정까지 1~2개월

08-19 시작, 5일 경과 시점의 arm별 청산 건수:

| arm | H1 | M15 |
|---|---|---|
| 대조군 (56·63) | 3건 (-717) | 9건 (+1,308) |
| EMA off (68·69) | 5건 (-489) | 9건 (+263) |
| TRANS off (70·71) | 3건 (+285) | 11건 (+1,510) |

**arm당 3~11건** — 통계적 판정에 필요한 50건 수준까지 현재 속도로 1~2개월 걸린다.
지금 어느 arm이 낫다고 말할 수 없다. 그대로 두고 기다리는 것 외에 할 일이 없다.

### 19. ✅ 손절폭 A/B 구현 완료 (2026-08-24) — 배포 후 실행 대기

**가설**: 손절이 평균 36분 만에 -0.88% 에서 걸린다(§11). 15분봉 노이즈 폭이 그 정도라
논지가 전개되기 전에 잡음에 털린다. 실측 R:R 3.26 은 이미 충분하고 부족한 건 승률뿐이다
(19.7% vs 손익분기 23.5% — **3.8%p 차이**).

**실험군 파라미터** — ⚠️ 두 값을 반드시 **함께** 움직인다(§17 의 07-31 실패 전례):
```json
{"slAtrMultiplier": 2.5, "tpRrMultiplier": 1.2}
```
SL 1.5→2.5 ATR (손절폭 1.67배), TP_RR 2.0→1.2 로 **TP 절대거리 유지**(2.5×1.2 = 3.0 ≒ 1.5×2.0).
목표는 R:R 확대가 아니라 승률을 손익분기 위로 올리는 것.

**구현**
- `ExitRuleOverrides` 신설 — `strategy_params` 에서 두 배수를 읽고 범위 검증([0.5,6.0] / [1.0,5.0]).
  잘못된 값은 **그 항목만** 무시하고 기본값 사용 + 경고 로그(조용히 기본값으로 도는 A/B 는 표본을 오염시킨다).
- `ExitRuleCalculator` 에 오버라이드 오버로드 추가. 기존 3인자 시그니처는 `NONE` 위임으로 보존 — **회귀 0**.
- 세 엔진(DYNAMIC·LIVE·PAPER) 호출부에서 `session.getStrategyParams()` 전달.
- `PaperTradingController.toSessionSummaryMap` 에 `strategyParams` 노출 — 없으면 어느 세션이
  실험군인지 API 로 구분할 수 없어 대조군만 골라 복제하는 게 불가능했다.
- 테스트 `ExitRuleOverridesTest` 8건 — 무오버라이드 동등성 / 배수 반영 / SL 상한 유지 /
  **07-31 실패 패턴 재현** / TP 거리 유지 설계 / 잘못된 값 방어 / JSONB 타입 혼재.
- 전체 `:web-api:test` `:core-engine:test` 통과.

**지문**: 별도 등록 불필요 — `RulesetRegistry` 가 세 엔진 모두 `strategy.params` 를 이미 담고 있어
arm 이 자동으로 갈린다. (`exitcalc.*` 키는 계속 코드 기본값을 가리킨다.)

**실행**: `scripts/create_ab_stoploss_sessions.sh` — 대조군 M15 40세션(5전략 × 8코인)을
같은 조건으로 복제한다. M15 만 쓰는 이유는 청산 속도다(5일간 M15 122건 vs H1 27건).
arm 당 주 ~170건 → **1주면 판정 가능**(감쇠 A/B 는 arm 당 3~11건으로 1~2개월, §18).

> ⚠️ **반드시 배포 후에 실행**할 것. `ExitRuleOverrides` 가 없는 백엔드에 실험군을 만들면
> 파라미터가 무시된 채 지문만 갈려 A/B 가 아니라 오염된 표본이 된다. 스크립트가 확인을 받는다.

**판정 기준** (스크립트가 쿼리까지 출력한다)
1. 실험군 승률 > 대조군 승률 — 가설의 직접 검증
2. 실험군 평균 보유시간 > 36분 — 실제로 덜 털렸는지
3. 실험군 거래당 기대수익률 > 0 — 최종 목표

1·2만 개선되고 3이 음수면 배수가 부족한 것(→ 3.0~3.5 재실험). **1부터 개선이 없으면 가설이 틀렸고,
원인은 손절폭이 아니라 진입 선별이다**(§4 "실행된 신호가 차단된 신호보다 나쁘다" 와 연결).

### 20. ✅ H1 승률 0% 3전략 중지 스크립트 (2026-08-24) — 실행 대기

`scripts/stop_h1_zero_winrate.sh` — 고정코인 PAPER 의 **H1 세션만** 정지한다.

| H1 전략 | n | 승률 | 누적 | 조치 |
|---|---|---|---|---|
| MOMENTUM_ICHIMOKU_V2 | 7 | **0.0%** | -579,744 | 중지 |
| MTF_CONFIRMED | 6 | **0.0%** | -495,660 | 중지 |
| MOMENTUM_ICHIMOKU | 6 | **0.0%** | -495,660 | 중지 |
| MEANREV_BB / MTF_BTC | 4 / 4 | 25.0% | +361,495 / +131,355 | 유지 (n=4, 각 1승) |

**13건 연속 전패**라 우연으로 보기 어렵다(승률 25% 가정 시 13연패 확률 ≒ 2.4%).
H1 전면 중지는 n=31로 근거가 얇아 하지 않는다.

**전략 자체는 비활성화하지 않는다** — 같은 전략의 M15 세션은 흑자다.
`strategy_type_enabled` 를 끄면 M15 신규 세션까지 막힌다.

### 21. ✅ H1 승률 0% 3전략 중지 완료 (2026-08-24)

DB 확인 — 대상 3전략의 H1 RUNNING 세션이 전부 0건이 됐다.

| H1 전략 | RUNNING |
|---|---|
| MOMENTUM_ICHIMOKU / ICHIMOKU_V2 / MTF_CONFIRMED | **0** (중지 완료) |
| MEANREV_BB / MTF_BTC | 8 / 8 (유지 — 의도대로) |

### 22. 🔴 손절폭 A/B 1차 시도 실패 — `strategyParams` 조용한 유실

**증상**: 실험군 40세션(id 250~289)이 생성됐는데 `strategy_params` 가 전부 NULL.
API 는 200 을 돌려줬고 세션도 정상 생성됐다.

**원인**: `PaperTradingService.createSession` 의 빌더에서 `strategyParams` 가 **빠져 있었다.**
`LiveTradingService:370` 과 `DynamicTradingService:415` 는 처음부터 넘기고 있었는데 **PAPER 만 누락**이었다.
감쇠 A/B 가 동적 세션이라 정상 동작했고, 이 경로는 아무도 쓴 적이 없어 드러나지 않았다.

**왜 위험한가**: 실패가 조용하다. 파라미터만 사라지고 세션은 멀쩡히 돈다 —
실험군 40개가 대조군과 **완전히 같은 규칙**으로 돌았고, 지문(`strategy.params`)까지 같아져
사후에 "이건 오염된 표본"이라고 구분할 방법조차 없었다. A/B 가 실패하는 게 아니라
**틀린 결론을 준다.**

**조치**
- `PaperTradingService.createSession` 에 `.strategyParams(req.getStrategyParams())` 추가
- 회귀 가드 `PaperSessionStrategyParamsTest` — 빌더 호출 존재 확인 + 세 엔진 엔티티 필드 계약
- `create_ab_stoploss_sessions.sh` 에 **생성 직후 사후 검증** 추가.
  저장된 개수가 생성 개수와 다르면 즉시 실패하고 원인을 안내한다. 이제 조용히 지나갈 수 없다.
- `cleanup_failed_ab_sessions.sh` — 오염된 40세션 정지 (ID 하드코딩 없이
  "01:00 이후 생성 + params 없음 + M15 + RUNNING" 조건으로 선별, 원래 대조군은 제외)

**교훈**: A/B 스크립트는 "만들었다"가 아니라 **"의도한 파라미터로 돌고 있다"까지 확인**해야 한다.
§12(strictHtf 무효)·§16(BREAKOUT_ICHIMOKU) 과 같은 계열의 문제다 —
설정한 것이 실제로 거동을 바꿨는지 세지 않으면 없는 실험을 했다고 믿게 된다.

### 23. ✅ 손절폭 A/B 재구성 완료 (2026-08-24) — 관측 시작

| 구분 | 세션 ID | 건수 | strategy_params |
|---|---|---|---|
| 1차 오염분 | 250~289 | 40 | — (전부 **STOPPED**, 정리 완료) |
| **대조군** | 08-18 생성분 | **40** | NULL |
| **실험군** | **290~329** | **40** | `{"slAtrMultiplier":2.5,"tpRrMultiplier":1.2}` |

5전략 × 8코인이 대조군·실험군 **8:8 로 정확히 짝지어져** 있다(MEANREV_BB · MOMENTUM_ICHIMOKU ·
ICHIMOKU_V2 · MTF_BTC · MTF_CONFIRMED). 같은 코인·같은 기간을 보므로 시장 국면 차이가 상쇄된다.

**아직 검증되지 않은 것 — 오버라이드가 실제로 SL/TP 를 바꾸는가**

`strategy_params` 가 DB 에 저장된 것까지는 확인했지만(§22 의 유실 재발 없음), 그 값이 런타임에서
실제로 손절폭을 바꾸는지는 **실험군이 첫 포지션을 열어야** 알 수 있다. 생성 직후라 아직 0건이다.

첫 진입이 생기면 아래로 확인할 것 — 기대값은 **실험군 SL 거리가 대조군의 약 1.67배**(1.5→2.5 ATR),
**TP 거리는 거의 동일**(2.5×1.2 = 3.0 ≒ 1.5×2.0). 대조군 실측 SL 거리가 0.426% 였으니 실험군은 ~0.71% 여야 한다.

```sql
SELECT CASE WHEN v.strategy_params IS NULL THEN '대조군' ELSE '실험군' END AS arm,
       count(*) AS n,
       round(avg(100.0*(1 - p.stop_loss_price/nullif(p.entry_price,0))),3) AS sl_dist_pct,
       round(avg(100.0*(p.take_profit_price/nullif(p.entry_price,0) - 1)),3) AS tp_dist_pct
FROM paper_trading.position p
JOIN paper_trading.virtual_balance v ON v.id = p.session_id
WHERE v.timeframe='M15' AND p.opened_at >= '2026-08-24 02:00'
GROUP BY 1;
```

**SL 거리가 두 arm 에서 같게 나오면 오버라이드가 안 먹은 것이다** — 그 경우 즉시 실험을 멈춘다.
파라미터 저장(§22)과 파라미터 적용은 별개 문제이고, 지금 확인된 건 저장까지다.

### 다음 액션

1. ✅ ~~PULLBACK_MTF 고정코인 세션 중지~~ — 완료 (§8)
2. 🔜 **손절폭 A/B 관측 중** — §23. 대조군 40 / 실험군 40(id 290~329) 가동.
   ① **첫 진입 시 SL 거리 확인**(오버라이드가 실제로 먹는지 — 아직 미검증)
   ② 1주 후 승률·보유시간·기대수익률 판정.
3. ✅ ~~MTF_BTC_STRICT 비활성화~~ — 완료 (2026-08-24 00:58, `is_active=false` · RUNNING 세션 0건 확인).
4. ✅ ~~H1 승률 0% 3전략 중지~~ — 완료 (§21, RUNNING 0건 확인).
5. 동일코인 노출상한 1 → 2 A/B — 리스크 장치라 보류(§4).
6. 감쇠 A/B(동적 56·63·68·69·70·71) — 성적과 무관하게 실험 종료까지 유지.

### 🚦 실전 투입 게이트 (시간이 아니라 지표로 판단할 것)

관찰 기간을 늘리는 것으로는 결론이 바뀌지 않는다 — 이미 700건 넘게 봤고 전부 적자다.
아래 3개를 **동시에** 만족하는 전략이 나오면 그때 소액 실전을 붙인다.

1. **승률 > 27%** (손익분기 23.5% + 마진 3.5%p) — n ≥ 100 청산 기준
2. **같은 기간 BTC 단순보유 대비 초과수익 > 0** — 상승·하락 구간을 **모두** 포함해서
3. **최대 낙폭(MDD) < 10%** — 실전 심리·서킷브레이커 여유

현재 이 기준을 통과하는 전략은 **없다**(최다 표본 흑자 전략이 MTF_BTC n=43).

---

## 🔴 2026-08-31 23:15 WF 게이트 OFF — 켜니 함대가 멈췄다

재기동 17시간 뒤 점검에서 **진입이 1건**뿐이었다. BUY 신호 18건 중 **17건이 WF 게이트 차단**.

원인은 두 결정이 곱해진 것이다: **게이트를 켰고**(08-31) + **PAPER 면제를 철회**(08-31)해서
전 함대에 걸렸다. 각각은 옳았지만 조합의 효과를 미리 계산하지 않았다.

| 세션 | 전략 | 감시 10종 중 WF 통과 |
|---|---|---|
| 77 · 80 | ICHIMOKU / MTF_CONFIRMED (H1) | 2 (BTC·ONDO) |
| 81 | ICHIMOKU (M15) | 1 (ONDO) |
| **76 · 78 · 79 · 82 · 83 · 84** | 나머지 6세션 | **0 — 아무것도 못 산다** |

전체 **90조합 중 5개(5.6%)** 만 통과. 진입 속도 **하루 1.4건** → 5전략 n≥20 도달에 **71일**.
2주 동결로는 아무것도 판정할 수 없다.

### 판단 — 게이트를 끈다 (A안)

- **이미 배운 것이 있다**: "WF 커버리지 5.6%로는 게이트를 켤 수 없다"가 17시간으로 증명됐다.
  더 기다려도 같은 답이다.
- 동결의 목적은 **전략 존폐 판정**인데, 게이트를 켜두면 그 목적을 달성할 수 없다.
- 끄는 것도 규칙 변경이지만 **동결 첫날**이라 잃는 표본이 1건뿐이다.

**페이퍼↔실전 일치는 유지된다** — 게이트가 양쪽 다 꺼지므로. 그리고 지금 함대가 사는
미검증 코인은 "게이트를 끈 실전"의 진짜 모습이다.

### 재기동 불필요 · 지문 영향 없음

`REQUIRE_WALK_FORWARD_GATE` 는 **지문에 실려 있지 않다**. 끈 뒤로 2주간 계속 꺼져 있으면
그 기간 동안 상수이므로 표본이 섞이지 않는다. 오염은 게이트 하의 거래 1건(세션 81 ONDO)뿐 —
분석에서 그 1건만 빼면 된다. **기준선은 "게이트 끈 시각".**

> 🔴 **09-14 에 게이트를 다시 켤 때는 반드시 지문에 먼저 등재할 것**
> (`gate.requireWalkForward` 같은 키). 안 그러면 08-25 노출 상한 때와 똑같이 —
> 규칙을 바꿨는데 사후에 못 가르는 상황이 된다. 그때 이미 한 번 당했다.

---

## 📅 점검 일정 — 규칙 동결 기간 (2026-08-31 ~ 09-14)

> 함대 재기동 완료 (08-31 06:25, 세션 **76~84**). MDD 고점 전부 리셋(NULL), 옛 세션 포지션
> 전부 청산, 정지된 세션은 보존(KILL_CRITERIA 표본 유지).

| 날짜 | 무엇을 볼 것인가 |
|---|---|
| ~~**09-01 (월) 오전**~~ ✅ 완료 | ① **게이트 OFF 후 거래가 돌아왔는지** — 하루 26~37건 수준이면 정상, 여전히 한 자리면 다른 원인이 있다. ② **WS 실시간 감시가 페이퍼에 걸리는지** — 08-31 배포의 핵심인데 아직 미검증(SL/TP 청산이 0건이라 증거가 없었다). 확인법: `[Dynamic] 실시간 손절(WS)` / `실시간 익절(WS)` 로그가 **DYN_PAPER 세션 id 로 찍히는지**(이전엔 REAL 만), 또는 페이퍼 SL/TP 청산의 `closed_at` 초 단위가 60초 격자에 안 걸리는지. ③ 불변식 3종·②, 최소 이력 가드, 노출 상한 2. |
| **09-07 (일) 09:00** | 동결 1주차. 표본 축적이 예상 속도대로인지(아래 표), 지문이 안 갈렸는지(=동결이 지켜졌는지), 서킷 발동 여부. **판정은 하지 말고 추이만.** |
| **09-14 (일)** | 동결 해제 · 판정일. 5전략 전부 n≥20 도달 예상 → KILL_CRITERIA 존폐 판정 + 같은 규칙 위 전략 비교 + WF 게이트 on/off 판단 → **DYNAMIC 메인 전환 결정**. |

**표본 축적 예상** (재기동 시점 거래 속도 기준):

| 전략 | 속도 | n≥20 도달 |
|---|---|---|
| MTF_BTC | 3.4건/일 | ~09-06 |
| MEANREV_BB | 2.4건/일 | ~09-09 |
| MTF_CONFIRMED | 2.0건/일 | ~09-10 |
| MOMENTUM_ICHIMOKU | 1.8건/일 | ~09-12 |
| MOMENTUM_ICHIMOKU_V2 | 1.8건/일 | ~09-12 |

> 🔴 **09-14 까지 매매 규칙 변경 금지.**
> 금지 — 게이트 임계값 · 노출 상한 · 청산 규칙 · 워치리스트 필터 · 전략 파라미터 · 신규 A/B
> 허용 — P0 버그 수정(버그는 규칙이 아니다) · 관측/로깅 추가 · WF 배치 · 캔들 수집
>
> 바꾸고 싶어지면 **그 변경이 2주를 다시 기다릴 만큼 급한지** 먼저 물을 것.
> 지난 12일간 지문이 18개로 갈린 이유가 정확히 이것이다.

---

## 🔴 보류·결정 대기 항목

> **최우선 (2026-08-25 — DYNAMIC을 메인으로 쓰기로 방향 잡음, 아래 섹션 참조)**
>
> 1. **DYNAMIC을 언제 "메인"으로 실제 전환할지 결정 필요.** 08-25 세션에서 DYNAMIC 운영 가능성을 점검하다가 스캔 루프가 코인별 WF 검증을 전혀 안 보고 있던 것(전략은 검증됐어도 실제로 사는 코인은 검증 안 됐을 수 있음)과, 29시간 동안 전체 동적 세션을 멈춰 세운 자기 자신과의 DB 데드락을 발견·수정했다(아래 08-25 섹션). 지금은:
>    - WF 게이트를 스캔 루프에 연결 완료(PAPER는 탐색용으로 제외, LIVE만 강제)
>    - 실제 감시목록 18코인 중 **7~8개에서만** 검증된 (전략,코인) 조합이 나옴 — 나머지는 아직 미검증
>    - Upbit REST 공유 스로틀 경합으로 사이클이 가끔 수 분씩 밀림(부분 완화함, 08-25 섹션 참조)
>    전환 전에 최소한 검증 커버리지를 더 넓히고(WF를 더 많은 후보 코인에 정기적으로 돌리는 체계 필요), 스로틀 경합이 실제로 사이클 지연을 얼마나 줄였는지 하루 정도 관찰한 뒤 판단할 것.

> **차순위 (2026-08-06 벤치마크 측정 결과 — 아래 별도 섹션 참조)**
>
> 1. **실자본 매매 중단 → 페이퍼 전환 검토** — 시스템이 시장 대비 알파 음수(동적 −3.30% vs 알트 −1.63%), 누적 실현손실 −21,854원, 승률 8.9~11.1%. 지금 속도(6일 8거래)로는 통계적 유의성에 도달하기 전에 계속 잃는다. 페이퍼로 전환하면 **잃지 않으면서 같은 데이터를 얻는다**. ✅ **2026-08-06 페이퍼를 LIVE와 동일 로직으로 정렬 완료**(위 별도 섹션) — 이제 페이퍼 결과가 실전 예측에 유효하다. `PaperTradingService`는 07-01 이후 미가동 상태이므로 재가동만 하면 된다.
> 2. **자본 증액 금지** — 기대값이 음수인 상태에서 금액을 늘리면 손실 속도만 빨라진다. 증액 조건은 "벤치마크 대비 알파가 양수임이 통계적으로 증명될 때" 하나뿐.
> 3. ~~**벤치마크 비교를 시스템에 내장**~~ ✅ **2026-08-07 완료** — `BenchmarkAlphaService` + 대시보드 상단 고정 노출(위 별도 섹션).
> 4. ~~**신호 반전 가설 검증**~~ ✅ **2026-08-07 기각** — 반전이 오히려 더 나쁘다(평균 −4.85% → −6.01%). 신호는 거꾸로인 게 아니라 **방향성이 없다**. 손실의 정체는 무작위 진입 + 마찰비용(위 별도 섹션).
> 5. ~~**중단 기준(kill criteria)을 미리 문서화**~~ ✅ **2026-08-18 완료** — [`docs/KILL_CRITERIA.md`](KILL_CRITERIA.md) 제정 + `StrategyKillCriteriaService`로 집행(매일 09:00 KST). 자본 보호 3종은 표본 무관 즉시 발동, 엣지 2종은 n≥20. 아래 08-18 섹션 참조. ⚠️ **자동 정지는 아직 꺼져 있다**(`kill-criteria.auto-stop=false`) — 판정이 며칠간 옳게 나오는지 관찰 후 켤 것.
> 6. **LIVE 세션을 H1로 재생성 필요** — 실전(M5)과 페이퍼(H1)의 타임프레임이 달라 A/B 비교가 성립하지 않는다. 세션 생성 후 타임프레임 변경 API가 없으므로 **정지 후 H1로 재생성**해야 한다(현재 포지션 0건이라 안전). 동적 멀티코인은 REAL/PAPER 쌍이 이미 H1로 맞춰져 있어 문제없다.

> 아래는 코드가 준비돼 있고 실행 여부·시점만 운영 판단이 필요한 것들.

- **워치리스트 최소 이력 조건** (2026-08-26 발견) — 상장 직후라 캔들이 201개 미만인 코인이
  워치리스트에 들어온다(KRW-LIT M15 184개, 08-25 06:50 최초 등장). 각 전략이 자체 최소 캔들
  가드를 갖고 있어 **잘못된 신호가 나오진 않지만**, EMA200 등 장기 지표가 조용히 비활성화된
  채 **축소된 지표 집합으로 도는 것을 아무도 모른다**. 세션 61이 실제로 LIT 포지션을 보유했다.
  `WatchlistFilterService` 에 최소 캔들 수 조건을 넣을지, 아니면 신호 로그에 "축소 지표" 플래그를
  남겨 사후 분리 가능하게 할지 판단 필요. 후자가 표본을 버리지 않아 더 낫다는 의견.
- **Walk Forward 게이트 활성화** (`REQUIRE_WALK_FORWARD_GATE`) — `WalkForwardValidationGate` 구현 완료, 기본값 비활성. 대부분 전략이 Walk Forward 이력이 없어 켜는 즉시 신규 세션 생성이 전면 중단된다. 켜기 전에 `GET /api/v1/strategies/walk-forward-gate-status`로 어떤 전략이 막히는지 먼저 확인하고, 운영 중인 전략들(COMPOSITE_MOMENTUM_ICHIMOKU_V2 등)에 Walk Forward부터 돌려 통과시킬지 판단할 것.
- ~~**LIVE time stop 활성화** (`maxHoldHours`)~~ ✅ **2026-08-18 완료** — 기본값 0 → 24 (V68, LIVE·DYNAMIC·PAPER 3종 동시). 운영 RUNNING 10세션에도 소급 적용. 아래 08-18 섹션 참조. ⚠️ 켜자마자 LIVE 경로에서 중복 SELL 1건이 재현됐다(같은 섹션 "새로 드러난 것" 참조) — 미해결.
- **신호 기대값 자체가 음수인 문제** — 최근 7일 동적 세션 BUY 신호 사후수익률 4h −2.17%/24h −4.47%(n=50). Walk Forward 게이트는 "검증 안 된 전략을 막는" 것이지 "전략 자체를 고치는" 게 아니라서, 게이트를 켜도 이 문제는 해결되지 않는다. 전략/신호 모델 자체를 봐야 하는 별도 과제.
- ~~**시간 초과 청산(time stop) 텔레그램 알림 부재**~~ ✅ **2026-08-18 완료** — `notifyTimeStop` 신설, LIVE·DYNAMIC 배선. 08-18 오전 XRP 4건 청산에 알림이 안 간 것이 실측 사례였다.
- **LIVE 유령 포지션 자동 정산** — 헬스체크(`OperationalHealthCheckService`)는 감지·알림까지만 하고 자동 정산은 안 한다. DYNAMIC의 `reconcileDynamicGhostPositions`에 대응하는 LIVE용 자동 정산을 추가할지는 실거래 자금에 직접 손대는 범위라 별도 검토 필요.
- ~~**e2e 스위트(`@playwright/test`) 미설치**~~ ✅ **2026-08-20 완료** — 설치·수정 후 **42통과/0실패/1스킵**. 아래 08-20 섹션 참조. `Header.tsx` 등 죽은 코드는 08-20 정비에서 삭제 — 현재 **44통과/0실패/0스킵**.
- **StrategyDegradationWatchdog을 DYNAMIC까지 확장할지** — 현재 LIVE(`sessionType=REAL`)만 감시. 저하 발견 시 Discord 알림에 그치지 않고 Walk Forward 게이트와 연동해 자동 재차단할지도 별건.
- **Walk Forward 미리보기 API를 `/strategies` 페이지에 노출** — 현재 API만 있고 프런트 표시 없음.

---

---

## 🟢 2026-08-19 지문 구멍 마감 (2) — `SignalQualityDampenGate` (`dampen.*`)

`CompositeStrategy` 와 같은 처리를 감쇠 게이트에도 적용했다. 상수 3개
(`nightDampenStartHourKst` 20, `defaultNightDampenFactor` 0.6,
`defaultTransitionalDampenFactor` 0.5)가 `dampen.*` 로 지문에 실린다.

- `SignalQualityDampenGate.behaviorParams()` + `RulesetRegistry.base()` 배선
- 리플렉션 가드 — **뮤테이션 검증**: 더미 상수 `MUTATION_PROBE` 추가 시 그 이름으로 실패
- 노출값이 실제 상수와 일치하는지도 검사 (지문이 거짓말하지 않도록)
- `RulesetRegistryCompositionTest` 필수 키에 `dampen.*` 3종 추가
- `:web-api:test` **356건**, `:core-engine:test` **211건** 전부 통과

> 같은 가드가 이제 세 곳이다 — `ExitRuleCalculator`(`exitcalc.*`),
> `CompositeStrategy`(`composite.*`), `SignalQualityDampenGate`(`dampen.*`).
> **"동작을 바꾸는 코드 상수는 지문에 싣는다"** 가 이 코드베이스의 규칙이다.

### ⚠️ 배포 순서 — A/B 세션보다 **먼저** 배포할 것

이 배포는 지문에 `dampen.*` 3키를 더해 **모든 해시를 다시 가른다**(현재 DYN_PAPER 56키 → 59키).
A/B 세션을 먼저 만들면 실험 도중에 표본이 쪼개진다:

```
[실험 시작] ─── 2주 ─── [배포] ─── 계속
          해시 X            해시 Y     ← 합쳐지지 않음
```

동적 세션 거래 빈도가 12일 7건이다. 이미 부족한 표본을 실험 중에 반으로 쪼개면 결론이 안 난다.

**올바른 순서**: ① 이 수정 배포 → ② A/B 세션 4개 생성 → ③ 수 주간 배포 없이 관찰.

---

## 🟢 2026-08-20 프런트엔드 정비 — 타입 가드 복구 · 죽은 코드 제거 · 테마 페인트

> 배포 무관(프런트 전용). 백엔드를 안 건드리므로 감쇠 A/B 표본에 영향 없음.

### 1. 🔴 `ignoreBuildErrors: true` 를 껐다 — 타입 오류 8건이 그대로 배포되고 있었다

`next.config.ts` 가 타입 검사를 통째로 무시하고 있었다. 그 그늘에서 자란 것들:

| 위치 | 내용 | 처리 |
|---|---|---|
| `components/ui/Badge.tsx` | `BadgeVariant` **26종 중 13종만** `variantClasses` 에 존재. `PENDING`·`FILLED`·`UP`·`LONG`·`MARKET` 등은 `undefined` → 색 없는 배지 | 파일 자체가 미사용이라 **삭제** |
| `hooks/useBacktest.ts` | `useMutation` 에 **존재하지 않는 `select` 옵션** 을 넘김 → 조용히 무시됨 | 훅 미사용이라 **삭제** |
| `admin/news-sources` | `ApiResponse` 에 없는 `.message` 접근 → 항상 `undefined` | `res.data?.['message']` 로 수정 |
| `account`·`backtest/walk-forward` | Recharts `formatter` 파라미터가 `number | undefined` | 시그니처 수정 |
| `admin/llm-config` | `unknown && JSX` → ReactNode 아님 | `Boolean(...)` 로 감쌈 |
| `settings/upbit-status` | `UpbitHolding[]` 을 `Record<string, unknown>[]` 로 캐스팅 | 캐스팅 제거, 타입 필드 직접 접근 |

**결과: 타입 오류 8 → 0.** `npm run build` 가 타입 검사를 켠 채로 통과한다(35 페이지).

### 2. 죽은 코드 제거

```
src/components/ui/{Badge,Button,Card,Spinner,index}   ← 디렉터리 통째로 참조 0
src/components/layout/Header.tsx                       ← 참조 0
hooks: useWalkForward · useToggleStrategy · useStrategyDetail
       · useCreateStrategy · useUpdateStrategy         ← 전부 참조 0
```

`ui/` 는 공용 디자인 시스템으로 만들어졌으나 **34개 페이지가 전부 각자 스타일을 손으로 짜고 있었다.**
`upbit-status` 는 같은 이름의 로컬 `Badge` 를 따로 정의해 쓴다. `useWalkForward` 도
해당 페이지가 `backtestApi.walkForward` 를 직접 호출하고 있어 잉여였다.

> 되살릴 일이 생기면 git 이력에서 꺼내면 된다. 지금 남겨두면 "있는 줄 알고 쓰다가
> 스타일 없는 배지를 만나는" 함정으로 남는다.

### 3. 테마 첫 페인트 — 다크 사용자가 보던 흰 깜빡임

SSR HTML 의 `<html>` 에는 `dark` 클래스가 없다. `body` 는 `bg-slate-50 dark:bg-slate-950` 이라
**React 가 마운트되기 전까지 흰 배경이 칠해진다.** 기본 테마가 dark 이므로 대부분의 사용자가 겪는다.

- `app/layout.tsx` 에 **블로킹 인라인 스크립트** 추가 — 파싱 중에 `localStorage` 를 읽어
  `<html>` 클래스를 확정한다.
- `ThemeProvider` 는 `useState` 초기화 함수에서 값을 읽도록 변경(렌더 1회 절약 +
  `react-hooks/set-state-in-effect` 해소).

**e2e 2건 신규 + 뮤테이션 검증:**

| 테스트 | 뮤테이션 | 결과 |
|---|---|---|
| 다크는 하이드레이션 **전에** 적용돼 있다 | 블로킹 스크립트 제거 | ✅ **실패함**(회귀를 잡는다) |
| 라이트 재방문 시 dark 가 한 순간도 안 보인다 | ThemeProvider 를 옛 effect 방식으로 되돌림 | ⚠️ **통과함**(아래) |

> ⚠️ **정정**: 처음에는 "라이트 사용자가 다크 플래시를 본다" 고 진단했으나 **React 19 에서는
> 재현되지 않는다.** effect 안 `setTheme` 이 뒤 effect 의 클래스 적용보다 먼저 반영된다.
> `ThemeProvider` 변경의 근거는 깜빡임이 아니라 **불필요한 렌더 1회 + lint 규칙** 이다.
> 실재하는 깜빡임은 **다크 사용자의 흰 화면** 쪽이고, 그건 블로킹 스크립트가 고쳤다.

### 검증

- e2e **44 통과 / 0 실패 / 0 스킵** (Header 삭제로 스킵 1건도 소멸)
- `tsc --noEmit` **0 오류**, `npm run build` 타입 검사 켠 채 통과
- lint 109 → **108**(선행 문제, `no-explicit-any` 86건이 대부분 — 별건)

### 검토에서 나온 미처리 항목

- 🟡 **MSW GET 목 14/61** — 누락 46개(`admin` 11 · `settings` 9 · `backtest` 7 ·
  `paper-trading` 6 · `logs` 4 …). mock 모드에서 해당 화면이 빈 상태라 **e2e 확장의 천장**이다.
  `/strategies` 와 같은 상태의 화면이 아직 많다.
- 🟡 **e2e 라우트 6/34**, 프런트 단위 테스트 **0건**.
- 🟢 lint `no-explicit-any` 86건 — 범위가 넓고 회귀 위험이 있어 별도 작업.

---

## 🟢 2026-08-20 e2e 스위트 가동 (`@playwright/test`) — 보류 항목 해소

> **배포 무관**: devDependency + 로컬 실행. 백엔드를 안 건드리므로 지문이 안 갈리고
> A/B 표본에도 영향이 없다. 감쇠 A/B 관찰 기간 중에 안전하게 할 수 있는 작업이라 먼저 처리.

`@playwright/test ^1.62.1` 설치 + chromium 다운로드. `playwright.config.ts`·`e2e/*` 는
이미 작성돼 있었고 **실행기만 없어 한 번도 안 돌아본 상태**였다.

```
npm run test:e2e      # 42 tests
```

### 결과

| | 통과 | 실패 | 스킵 | 소요 |
|---|---|---|---|---|
| 최초 실행 | 32 | **7** | 3 | 1.1분 |
| 수정 후 | **38** | **0** | 4 | 16.5초 |

### 실패 7건의 정체 — **제품 버그는 0건**

한 번도 실행된 적 없는 테스트라 애초에 통과 불가능한 셀렉터가 섞여 있었다.

| # | 테스트 | 원인 | 조치 |
|---|---|---|---|
| 1 | `/backtest` 타이틀 | `getByText('백테스트 이력')` 이 **4개** 매칭(사이드바 링크·모바일 앱바·라우트 어나운서·h1) → strict mode 위반 | `getByRole('heading')` 로 축소 |
| 2 | 새 백테스트 버튼 | 같은 이름 링크가 사이드바·본문 **2개** | `getByRole('main')` 으로 스코프 |
| 3 | 타임프레임 select | `locator('select').nth(2)` — `BacktestForm` 의 `<select>` 는 **1개뿐**(코인·전략은 커스텀 컴포넌트) | `option[value="H1"]` 보유 select 로 특정 |
| 4 | `/backtest/compare` 타이틀 | Next 의 `#__next-route-announcer__` 에도 같은 문구가 들어가 **2개** 매칭 | `getByRole('heading')` |
| 5 | `/strategies` 타이틀 | 1번과 동일(**4개** 매칭) | `getByRole('heading')` |
| 6 | Sidebar 펼치기 | **`<nextjs-portal>`(Next dev 오버레이)가 좌하단 고정으로 떠서 접힌 사이드바(w-16) 하단 버튼의 클릭을 가로챔.** 운영 빌드엔 없는 개발 전용 요소 | `addInitScript` 로 e2e 에서만 `display:none` |
| 7 | Header 테마 토글 | **`src/components/layout/Header.tsx` 가 어디에서도 import 되지 않는 미사용 컴포넌트** — 존재하지 않는 UI를 검증하던 테스트 | `test.skip()` + 사유 주석 |

### 새로 드러난 것

- 🟡 **`Header.tsx` 는 죽은 코드다.** `grep -rn "layout/Header\|<Header" src` 결과 0건.
  붙일지 지울지 결정 필요 — 지우면 `theme.spec.ts` 의 skip 도 함께 삭제.
- ✅ **`GET /api/v1/strategies` 목 부재 → 같은 날 해소**(아래 후속 섹션).
- 🟡 ~~**`GET /api/v1/strategies` 목이 없다.**~~ `src/mocks/handlers.ts` 에는 POST/PUT/PATCH 가
  "실서버 직접 연결" 이라는 주석만 있고 목록 조회 핸들러가 없다. 그래서 mock 모드에서
  전략 카드가 0개 → `strategies.spec.ts` 의 **3건이 조용히 self-skip**, 1건은
  "빈 상태 메시지" 로 **공허하게 통과**한다.
  > **08-06 MSW 사고와 같은 실패 양식이다** — 화면이 비어도 검증이 통과한다.
  > 목 핸들러를 추가해야 이 4건이 실제로 뭔가를 검증하게 된다.
- `.gitignore` 에 `test-results/`·`playwright-report/` 추가(`e2e/.auth/` 는 이미 있었음).

### 후속 — MSW 전략 목록 목 추가 + self-skip 제거

| | 통과 | 실패 | 스킵 |
|---|---|---|---|
| 앞 단계 | 38 | 0 | 4 |
| **후속 후** | **42** | **0** | **1** |

- **원인**: `strategyInfosMock` 은 `src/mocks/data.ts` 에 **있었는데** ① 핸들러가 없어 아무도
  안 쓰고 ② `isActive`·`isComposite` 가 타입에 추가된 뒤로 목만 낡아 있었다.
- `handlers.ts` 에 `GET /strategies`, `GET /strategies/:name`,
  `PATCH /strategies/:name/active` 3종 추가. 목록을 뺄 때 GET 까지 같이 빠졌던 경위를
  주석으로 박아 재발을 막았다.
- `strategyInfosMock` 에 `: StrategyInfo[]` 타입 명시 — **백엔드 스키마가 바뀌면 목이
  조용히 낡는 대신 컴파일에서 걸린다.** 복합 전략 3종도 추가해 "복합 전략" 탭을 채웠다.
- `strategies.spec.ts` 의 `if (cardCount > 0) ... else test.skip()` **3곳 제거** →
  `expect(...).not.toHaveCount(0)` 로 전환. "빈 상태 메시지도 통과" 였던 1건도 하드 단언으로
  바꿨다. 탭 전환 테스트 1건 신규 추가.
  > **목이 죽으면 초록불이 아니라 빨간불이 떠야 한다.** 08-06 사고의 교훈을 테스트에 박은 것.
- `npx tsc --noEmit`: `src/mocks` 오류 **0건**(저장소 전체 선행 오류 8건은 무관).

> 남은 스킵 1건은 `Header.tsx` 죽은 코드 건 — 붙일지 지울지 **결정 대기**.

### 참고 — 도커 빌드 영향 없음

`crypto-trader-frontend/Dockerfile` 이 `rm -f playwright.config.ts && npm run build` 로
이미 대비돼 있고, 런타임 스테이지는 `.next/standalone` 만 복사하므로 **운영 이미지에
playwright 가 안 들어간다.** chromium 바이너리도 `npx playwright install` 로 따로 받는 것이라
도커 빌드에는 포함되지 않는다.

> `npm run lint` 는 기존 문제 109건(89 에러)이 있으나 **e2e 디렉터리 기여분은 0건**이며
> 이번 작업과 무관한 선행 상태다.

---

## ✅ 2026-08-20 오전 점검 결과 (09:05 KST 실측)

> 운영 DB(`yhpapa.iptime.org:8432`) 직접 조회. 어제 계획한 4개 항목 전부 확인.

### 1. 09:00 kill criteria 첫 판정 — 🟢 예상대로 전부 KEEP

`kill_criteria_judgment` **0행**(09:02 조회). `persist()` 는 KEEP 이 아닌 것만 저장하므로
**빈 테이블 = 전 세션 KEEP** 이다. 지문 도입으로 `tradeCount` 가 전 세션 0 으로 리셋됐는데도
`NO_SIGNAL` WARN 이 하나도 안 떴다 → **`tradeCountAllRulesets` 수정이 의도대로 먹었다.**

- 스케줄러 생존 근거: `daily_health_snapshot` id **15** 가 오늘 08:30 에 정상 기록됨.
- ⚠️ **한계**: 0행만으로는 "전부 KEEP" 과 "09:00 잡이 아예 안 돎" 을 구별할 수 없다.
  확정하려면 백엔드 로그의 `[KillCriteria] 폐기 기준 판정 시작` 한 줄을 봐야 하는데,
  운영은 DB 포트만 외부 개방돼 있어 **이 작업 환경에서는 확인 불가**. 08:30 잡이 돈 것이
  간접 증거일 뿐이다. 서버 접속 기회가 있으면 로그로 한 번 확정할 것.

### 2. 지문 갱신 — 🟢 정상

DYN_PAPER **59키** / PAPER **56키**, `dampen.*`·`composite.*` 전부 포함. 6개 팔 해시가
08-19 16:04~16:12 에 서로 다르게 등록된 상태 그대로 유지.

### 3. `exit_reason` — 🟢 100% 기록, 그리고 **익절이 돌기 시작했다**

08-19 15:02 이후 청산 **13건 중 NULL 0 · UNKNOWN 0**. 사유를 안 넘기는 경로는 없다.

| session_kind | exit_reason | 건수 |
|---|---|---|
| DYN_PAPER | STOP_LOSS | 5 |
| DYN_PAPER | STRATEGY_SIGNAL | 5 |
| DYN_PAPER | **TAKE_PROFIT** | **3** |

> 🔄 **P1-b 재검토 필요**: 전체 이력 SELL 6,944건 중 `TAKE_PROFIT` 이 **1건**이었는데,
> 어제 오후 이후로는 13건 중 3건(23%)이 익절이다. "익절이 사실상 작동하지 않는다" 는
> 진단이 흔들린다 — 옛 수치가 **자유 텍스트 파싱 기반이라 익절을 놓치고 있었을** 가능성.
> 단 n=13 이라 결론은 이르다. **`TP_RR_MULTIPLIER` 는 건드리지 말고 며칠 더 볼 것.**

### 4. 감쇠 A/B — 1단계 진행 중 (16시간 경과, 판정 불가)

6개 팔 전부 RUNNING, `strategy_params` 의도대로 적재. 동적 세션 18 RUNNING / 53 DELETED.

| 세션 | TF | 팔 | 진입 | 청산 | 실현손익 |
|---|---|---|---|---|---|
| 56 | H1 | 대조군 | 0 | 0 | 0 |
| 68 | H1 | EMA off | 1 | 1 | −252 |
| 70 | H1 | TRANS off | 0 | 0 | 0 |
| 63 | M15 | 대조군 | 2 | 2 | +371 |
| 69 | M15 | EMA off | **3** | 2 | −466 |
| 71 | M15 | TRANS off | 2 | 2 | +695 |

- **H1 은 표본이 사실상 없다**(3팔 합쳐 1건). H1 결론은 훨씬 오래 걸린다.
- M15 에서 EMA off 가 진입을 늘리는 방향(3 vs 2)이 보이나 **n=3, 노이즈와 구분 불가**.
- ⛔ **손익 열은 아직 보지 말 것.** 2단계는 수 주짜리다. 지금 숫자로 판단하면 오독한다.
- 세션 69 `available_krw` 1,907원 — 포지션 보유 중이라 자본이 코인에 묶인 것(정상).

### 부수 관찰

- `daily_health_snapshot`: 잔고 불일치 0 · 유령 포지션 0 · 고착 포지션 0.
  `order_sequence_gap` 은 **16(08-17) → 7(08-18) → 1(08-19)** 로 감소 중.

### 다음 확인 시점

- [ ] **08-21~22**: A/B 1단계 판정 (진입 수 차이). M15 우선, H1 은 표본 부족으로 보류.
- [ ] **매일 09:00**: `kill_criteria_judgment` 신규 행 — 지금부터는 행이 생기면 그게 신호다.
- [ ] **수 주 뒤**: A/B 2단계(실현손익·`exit_reason` 분포). 그 전에는 감쇠 기본값 변경 금지.
- [ ] 백엔드 로그에 rate limit 이 뜨는지 (동적 18세션 ≈ 594/분 추정, 한도 420).

---

## 🗓️ 2026-08-18 ~ 08-19 작업 요약

> 상세 근거·재현 과정은 [`docs/old_progress.md`](old_progress.md) 하단 아카이브를 날짜로 검색.

이 구간의 주제는 **"데이터를 믿을 수 있게 만들기"** 다. 매매 성과를 개선한 게 아니라,
성과를 **측정할 수 있는 상태**를 만들었다.

### 마이그레이션

| 버전 | 내용 | 배포(KST) |
|---|---|---|
| V71 | 규칙 지문 (`ruleset_snapshot` + 3개 테이블에 `ruleset_hash`) | 08-19 12:37 |
| V72 | 매도 정산 멱등 표식 (`dynamic_sell_settlement`) | 08-19 15:02 |
| V73 | 청산 사유(`exit_reason`) + 페이퍼 포지션 컬럼 정렬 + 판정 지문 | 08-19 15:02 |
| V74 | 세션별 전략 파라미터 (`strategy_params`) — A/B 기반 | 08-19 15:33 |

### 핵심 성과

- **규칙 지문** — 규칙이 바뀌면 표본을 **무효화하는 대신 분할한다.** 현재 53키
  (engine 2 / exitcalc 5 / composite 12 / exit 16 / gate 11 / scan 4 / session 4 + strategy.params).
- **P0: 매도대금 21회 중복 지급** — 동적 세션 49 가 정지 실패 루프로 `available_krw`
  10,000 → 174,752. 원인은 `REQUIRES_NEW` 선커밋과 낡은 엔티티 `save()` 의 낙관적 락 충돌.
  멱등 표식을 **대금 반영과 같은 트랜잭션**에 두어 구조적으로 해결.
- **청산 사유 구조화** — 이전에는 자유 텍스트에 손익률이 박혀 있어 `GROUP BY` 불가.
  `ExitRuleChecker.ExitType` 이 이미 SL/TP 를 구분했는데 호출부가 버리고 있었다.
- **EMA 데드밴드** — 격차 0에 가까워도 "하락추세" 로 판정해 매수 점수를 0으로 만들던 문제.

### 데이터가 말하는 것 (판단 대기)

- **익절이 사실상 작동하지 않는다** — 전체 SELL 6,944건 중 `TAKE_PROFIT` **1건**, `STOP_LOSS` 6,702건.
  `take_profit_price` 는 종료 313건 중 260건에 설정돼 있는데 발동은 1건.
  → `TP_RR_MULTIPLIER = 2.0` 이 이 변동성에서 너무 먼가? **`exit_reason` 데이터로 판단.**
- **왕복 비용 0.3%** (슬리피지 0.1%×2 + 수수료 0.05%×2) 대비 **BUY 신호 기대값은 4h +0.076%**.
  기대값이 비용의 1/4 이다. 24h 에는 −0.499% 로 뒤집힌다.
  → `max_hold_hours = 24` 는 **감쇠 구간을 통과해 보유**하도록 설정돼 있다.
- **페이퍼 함대 112세션 중 93개가 12일간 거래 0건** — 게이트가 아니라 신호 생성이 병목.
  2일치 HOLD 7,033건 중 **88%가 `buy=0.00`**(지표가 하나도 안 켜짐). 임계값을 낮춰도 안 바뀐다.
  다만 감쇠 2종(TRANSITIONAL 45 + EMA 21 = 66건)이 같은 기간 통과분(51건)보다 많이 죽이고 있다.
  → **A/B 로만 판단 가능.** 죽인 신호가 손실이었을 수도 있다(함대 승률 22%).
- **60일 실적**: 24승 85패(22%), 평균 −153.91원. 단 지문 이전이라 규칙 세대가 섞였다 — 방향성 근거일 뿐.

### 미해결 / 의도적 보류

- **익절 파라미터(P1-b)** — 데이터 확보 후 판단. 지금 바꾸면 근거 없는 변경이 된다.
- **감쇠 기본값** — 전역으로 바꾸면 모든 세션이 한쪽 팔이 되어 A/B 가 불가능해진다.
- **`kill-criteria.auto-stop = false` 유지** — 지문별 표본이 재축적될 때까지.
- **페이퍼 함대 재생성 불필요** — 자본 오염 평균 −0.20%(최악 −6.50%, −10% 미만 0개),
  거래 귀속은 지문이 이미 해결. 재생성해도 유휴 93세션 문제는 안 고쳐진다.
- **LIVE 사실상 정지** — 14일간 BUY 신호 1건.
- **`public.position.exit_reason` 실측 미완** — LIVE/DYNAMIC 이 포지션을 안 열어 표본이 없다.

---

## ✅ 최근 작업 이력 (요약)

### 2026-08-06 [P2 후속] DYNAMIC SL 워치독 + 헬스체크 이력 화면 + MSW 미들웨어 버그

- **DYNAMIC SL 워치독 신설**(`DynamicTradingService.warnStaleSlCheck`) — LIVE에만 있던 §9 워치독(3분 미점검 시 경보 + 그 코인만 REST 강제 갱신)을 DYNAMIC에도 동일 패턴으로 추가. 이제껏 DYNAMIC엔 이런 안전망 자체가 없었다.
- **헬스체크 이력 화면**(`/admin/health-check`) — `GET /api/v1/admin/health-check/history` + Next.js 페이지 신설, 브라우저 실검증(Playwright, mock 모드) 완료.
- **부수 발견·수정**: `proxy.ts` 인증 미들웨어가 `/mockServiceWorker.js`를 제외 목록에서 빠뜨려 로그인 전 MSW 등록 자체가 리다이렉트로 막히던 버그 1줄 수정.
- 신규 테스트 6건 + 전체 스위트 **233건(스킵 1) 전부 통과**. 미배포·미커밋.

### 2026-08-06 [P2] 운영 헬스체크 자동화 + SL 워치독 대응 조치

- **`OperationalHealthCheckService`** 신설 — 세션 잔고 정합성·주문 시퀀스 갭·유령 포지션·무출구 고착 포지션(24h+) 4대 점검을 매일 08:30 KST 자동 실행, `daily_health_snapshot`(V65)에 기록, 이상 시 Discord 알림. **감지·알림만, 자동 정산은 안 함**(LIVE 유령 포지션 자동 정산은 실거래 자금에 직접 손대는 범위 확장이라 스코프 밖).
- **LIVE SL 워치독에 대응 조치 추가** — 그동안 `warnStaleSlCheck`가 경보만 보내던 것을, 그 코인 하나만 REST로 즉시 강제 갱신해 SL 감시를 스스로 복구 시도하도록 확장. 전역 WS 폴백(`isWsUnhealthy`)이 놓치는 "다른 코인은 정상인데 이 코인만 조용히 끊긴" 사각지대를 메운다.
- 신규 테스트 18건 + 전체 스위트 227건(스킵 1) 통과.

### 2026-08-06 [P1] LIVE/DYNAMIC 청산 엔진 통합 (2/2) — SL/TP 공식 + time stop 공유

- **`ExitRuleCalculator`** 신설 — DYNAMIC의 ATR 기반 SL/TP 공식(`resolveStopLossPct`/`resolveTakeProfitPrice`)과 time stop 판정(`shouldTimeStop`)을 공유 클래스로 추출. **LIVE는 이제껏 고정 `stopLossPct`만 썼는데**(세션 194 BTC 136시간 고착의 원인), 신규 진입부터 DYNAMIC과 동일한 ATR 기반 SL/TP를 쓴다.
- V64 마이그레이션 — `live_trading_session.max_hold_hours` 컬럼 신설, 기본값 0(비활성) — time stop은 기본값이 꺼져 있어 배포해도 트리거되지 않지만, **SL/TP 공식 자체는 배포 즉시 바뀐다**(플래그 없음 — 07-31 개편을 뒤늦게 이식하는 것뿐이라 DYNAMIC에서 이미 검증된 변경으로 간주).
- 기존 회귀 테스트 10건 이름만 바꿔 통과(계산 결과 불변 확인) + 신규 9건 + 전체 스위트 무회귀.

### 2026-08-06 [P1] 신호 기대값 검증 게이트 신설

- **`WalkForwardValidationGate`** 신설 — 전략별 최근 Walk Forward 결과(verdict·OOS 기대값)를 판정해 세션 생성에 배선. 기본값 비활성(`REQUIRE_WALK_FORWARD_GATE=false`) — 위 보류 항목 참조.
- 미리보기 API `GET /api/v1/strategies/walk-forward-gate-status` 신설.
- 신규 테스트 12건 + 전체 스위트 200건 통과.

### 2026-08-06 세션 카드 모바일 레이아웃 깨짐 수정 + MSW 목이 통째로 죽어 있던 문제

- **근본 원인**: MSW 핸들러 22개가 `/api/v1/...`에 등록됐는데 클라이언트는 `axios baseURL='/api/proxy'`를 거쳐 `/api/proxy/api/v1/...`로 호출 → 프록시 도입 이후 목이 전부 매칭 실패, 앞선 "28라우트 이상 없음" 검증이 사실 빈 화면만 훑은 것이었음.
- 경로 접두사 수정 + 동적 세션 픽스처 추가 후 재검증(28라우트 × 6폭 전부 통과). 세션 카드 `flex-wrap`/`shrink-0` 누락으로 좁은 화면에서 글자 단위 세로 배열되던 버그도 함께 수정.

### 2026-08-06 [P0 보안] DB 초기화 비밀번호 소스 하드코딩 제거

- `DbResetService`의 하드코딩된 비밀번호를 `DB_RESET_PASSWORD` 환경변수로 이관(fail-closed + 상수시간 비교 + 503 분리 응답). **사용자가 운영 `.env`에 새 값 적용 확인 완료.**

### 2026-08-06 FE 네비게이션 5개 대분류 재편 + 모바일 대응

- 사이드바 브레이크포인트 부재로 모바일에서 화면 65%를 먹던 구조를 `navConfig.ts` 단일 소스 + 데스크톱 사이드바/모바일 상단앱바·하단탭바·바텀시트·드로어로 교체. 그룹: 백테스트·모의투자 / 실전매매 / 전략관리 / 분석 / 설정.

### 2026-08-06 전 세션(동적 7 + 실전 2) 운영 DB 점검

- 기반 건전성 전부 정상(9/9 RUNNING·36h 무결손 틱·잔고 정합성 0원·주문 갭 0). 이 점검에서 나온 문제들(무출구 고착, 동적 세션 시가평가, LIVE time stop 부재, SL 워치독 알림 유실 등)은 이후 세션들(위 P1/P2)에서 대부분 해소 — 남은 것은 위 "보류·결정 대기 항목" 참조.

### 2026-08-05 이전 (요약만 — 상세는 `old_progress.md`)

- 08-05: "손절 5%인데 7~8%" 원인 규명(설계대로의 동작, 하한이었음) — SL 배수·상한 축소, TP 상한 신설, REST 폴백 DYNAMIC 코인 누락 수정, BLACK_SWAN 진입가 가드 추가. 배포 완료.
- 08-04: 멀티코인 24h 운영 분석, BLACK_SWAN 쿨다운 재기동 소실 버그 발견·수정.
- 08-03: 매도 롤백 P0(유령 포지션) 해소, 블랙스완 쿨다운, 미실현손익 복구.
- 07-31 이전: 세션 격리 버그, 청산 규칙 전면 개편(ATR 기반 SL 최초 도입), FK 위반 수정, 워치리스트 큐레이션 등 — DYNAMIC 시스템의 근간을 잡은 다수의 P0/P1 수정. 상세는 `old_progress.md` 07-02~07-31 구간 참조.

---

## 프로젝트 개요

- **서비스**: 업비트 기반 가상화폐 자동매매 시스템
- **운영 환경**: Ubuntu 서버, Docker Compose (`docker-compose.prod.yml`)
- **기술 스택**: Spring Boot 3.2 (Java 17) + Next.js 16.1.6 / React 19.2.3 (TypeScript) + TimescaleDB + Redis

### 모듈 구조

```
crypto-auto-trader/
├── web-api/          # Spring Boot 백엔드 (Gradle 멀티모듈)
│   ├── core-engine/      # 백테스팅 엔진, 리스크, 포트폴리오
│   ├── strategy-lib/     # 전략 22종 (단일 11 + 복합 11)
│   ├── exchange-adapter/ # Upbit REST/WebSocket
│   └── web-api/          # REST API, 스케줄러, 서비스
├── crypto-trader-frontend/  # Next.js 16.1.6 / React 19.2.3 프론트엔드
├── docs/                    # 설계 문서 및 진행 기록
└── docker-compose.prod.yml  # 운영용 (backend + frontend + db + redis + db-backup)
```

### 구현된 전략 22종

**단일 전략 (11종)**: VWAP / EMA Cross / Bollinger Band / Grid / RSI / MACD / Supertrend / ATR Breakout / Orderbook Imbalance / Stochastic RSI / Volume Delta

**복합 전략 (11종)**:

| 전략 | 구성 | 실적합 코인 | 요약 |
|------|------|------------|------|
| COMPOSITE | Regime 자동 선택 | 범용 | — |
| COMPOSITE_MOMENTUM | MACD×0.5 + VWAP×0.3 + Grid×0.2, EMA 필터 | ETH·SOL | ETH +53.6%, SOL +59.8% |
| COMPOSITE_ETH | ATR×0.5 + OB×0.3 + EMA×0.2 | ETH | 구버전 평균 +48.7% (재검증 필요) |
| COMPOSITE_BREAKOUT (CB) | ATR×0.5 + VD×0.3 + MACD×0.2, EMA+ADX+RSI Veto 필터 | **BTC·ADA** | BTC **+106.71%**, ADA **+86.98%** |
| COMPOSITE_MOMENTUM_ICHIMOKU (CMI_V1) | CB_MOMENTUM + Ichimoku 필터 | XRP | XRP +1.04% (유일 양수) |
| COMPOSITE_MOMENTUM_ICHIMOKU_V2 (CMI_V2) | MACD×0.5 + SUPERTREND×0.3 + Grid×0.2 + Ichimoku 필터 | **DOGE** | DOGE **+124.77%** |
| COMPOSITE_BREAKOUT_ICHIMOKU | CB + Ichimoku 필터 | — | ⚠ CB와 동일 (ADX 중복) |
| COMPOSITE_REGIME_ROUTER (CRR) | ADX/ATR 레짐 → CB/V1/V2 자동 위임 | **SOL·ETH** | SOL **+65.38%**, ETH +65.09% |
| COMPOSITE_MTF_BTC | CB(H1) + Supertrend(H4) | **ETH·AAVE·CHZ** | ETH **+127.70%**, AAVE +28.15% |
| COMPOSITE_MTF_MOMENTUM | CMI_V2(H1) + Supertrend(H4) | **BLUR·DOGE** | BLUR **+48.06%**, DOGE +83.40% |
| COMPOSITE_MTF_CONFIRMED | CRR(H1) + Supertrend(H4) | **XRP** 범용 | XRP **+3.37%** (유일 흑자) |
| MACD_STOCH_BB | MACD + StochRSI + 볼린저 6조건 AND | ❌ 비활성화 | BTC -2.32%, 거래 극희소 |

전체 코인별 백테스트 수치(2026-04-30 H1 FULL, 17코인 × 7전략)와 Walk-Forward 결과는 `old_progress.md` 참조.

---

## 🟢 배포 권고 / 🚨 배포 금지 (2026-04-30 MTF 백테스트 기준, 마지막 갱신)

> H1 FULL 2022~2026-04-30 백테스트 결과 기반. 이후 재검증 없음 — 오래된 기준이니 신규 배포 판단 시 최신 Walk Forward로 재확인 권장.

### Tier 1 — 백테스트 검증 통과

| 코인 | 권장 전략 | FULL 수익률 | MDD | Sharpe |
|------|-----------|------------|-----|--------|
| **BTC** | COMPOSITE_BREAKOUT | +106.71% | -8.88% | 1.24 |
| **ETH** | COMPOSITE_MTF_BTC | +127.70% | -7.24% | 1.35 |
| **SOL** | COMPOSITE_REGIME_ROUTER | +65.38% | -14.93% | 0.76 |
| **DOGE** | CMI_V2 | +124.77% | -30.75% | 0.87 |
| **ADA** | COMPOSITE_BREAKOUT | +86.98% | -14.14% | 0.96 |

### Tier 2 — 흑자 전환·신규 발굴, 관찰 후 투입

| 코인 | 권장 전략 | FULL 수익률 | MDD |
|------|-----------|------------|-----|
| **XRP** | COMPOSITE_MTF_CONFIRMED | +3.37% | -15.67% |
| **AAVE** | COMPOSITE_MTF_BTC | +28.15% | -31.01% |
| **BLUR** | COMPOSITE_MTF_MOMENTUM | +48.06% | -11.91% |
| **CHZ** | COMPOSITE_MTF_BTC | +14.09% | -14.88% |

### 🚨 배포 금지

- 전 코인 × M15 타임프레임(오버트레이딩 -99%), 전 코인 × FAIR_VALUE_GAP(구조 결함)
- ETH/XRP/ADA/CHZ/AAVE × 특정 전략 조합 다수 — 상세는 `old_progress.md` 2026-04-30 섹션 참조
- MOVE/SUPER/FLOCK/AXL/BIO/KERNEL — 거래수 부족으로 통계 신뢰성 없음

---

## Dev Workflow Orchestrator

`.claude/` 6개 서브에이전트(SparkAI → PLAN → Design → Do → Check → Report) 파이프라인 — 상세는 `.claude/ORCHESTRATOR.md` 및 프로젝트 루트 `CLAUDE.md` 참조.



