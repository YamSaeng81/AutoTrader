# MTF_MOMENTUM 확인 필터 — 백테스트 미사용 22코인 일반화 검사 사전 등록 v3

> **이 검사가 무엇인가**: [`SUPERTREND_FILTER_RESULTS.md`](SUPERTREND_FILTER_RESULTS.md) 에서 나온 후보가
> **백테스트에 쓰지 않은 다른 코인에서도 유지되는지** 확인한다.
>
> 🔴 **수익성을 확정하는 마지막 시험이 아니다.** 일반화 검사 한 단계다.

## 0. v1 에서 고친 것

| v1 | 문제 | v2 |
|---|---|---|
| "미열람 홀드아웃 22코인" | 🔴 **`backtest_run` 에 없다는 것만으로 미열람이 보장되지 않는다.** 실제로 17종은 페이퍼/동적 세션에서 거래됐다 | **"백테스트 미사용 22코인 일반화 검사"** (§1) |
| 코인 부트스트랩을 주 신뢰구간으로 사용 | 🔴 **22코인은 같은 시장 움직임을 공유한다.** 코인을 독립 재표집하면 불확실성을 과소평가한다 | **패널 시간블록 부트스트랩** — 전 코인·전 팔을 **같은 시간 블록**으로 함께 재표집해 **짝지은 차이** (§3) |
| 판정이 `A−B` 개선뿐 | 🔴 **A·B 가 모두 손실이어도 A−B 는 양수일 수 있다** | **변경안의 가치**와 **수익 후보 자격**을 분리해 각각 고정 (§4) |
| "팔당 200거래 또는 6개월" | 🔴 팔마다 따로 끝내면 **비교 기간이 달라진다** | **모든 팔이 200거래를 채운 공통 시점** 또는 6개월 (§5) |

---

## 1. 🔴 이 자료는 "미열람"이 아니다

`backtest_run` 에 한 번도 등장하지 않은 코인 중 H1 15,000봉 이상 23종에서
`KRW-USDT`(원화 스테이블, 변동 없음)를 제외한 **22종**:

```
IOTA WAVES CRO ONG SC POLYX NEAR WAXP BCH CVC POWR T
ANKR DKA GLM HIVE PUNDIX ELF BLAST JUP G INJ
```

### 실제 노출 이력 (착수 전 확인)

| 노출 | 코인 수 | 내용 |
|---|---|---|
| **포지션 있음** | **17 / 22** | 53건, 전부 **2026-07-30 ~ 09-22** 페이퍼·동적 세션 |
| 포지션 0건 | 5 | CVC · G · IOTA · POWR · WAVES |
| `strategy_log` 신호 기록 | **22 / 22** | 21,064행 (NEAR 11,324 최다) |
| `order` | 17 | 106건 |

🔴 **따라서 이 검사는 "미열람 자료 검증"이 아니라 "백테스트 미사용 코인으로의 일반화 검사"다.**
운영 결과를 집계 수준에서 본 적이 있으므로 완전한 독립도 아니다.

⚠️ 노출은 **2026-07~09 에 한정**되고 검사 기간(대부분 2023-12-31 ~ 2026-09-22)의 대부분은
거래 노출이 없다. 그러나 이를 근거로 "사실상 독립"이라고 주장하지 않는다.

⚠️ **시간 국면도 겹친다.** 같은 거시 환경을 공유하므로 **횡단면 검사이지 시계열 검사가 아니다.**
시간 방향으로 독립인 것은 §5 전향 검증뿐이다.

---

## 2. 대상과 팔

**대상**: `MTF_MOMENTUM` 하나 (Q2 에서 평균·중앙값·개선 코인 수가 같은 방향이었던 유일한 전략)

세 팔을 **같은 실행에서 동시에 기록**한다.

| 팔 | 내용 |
|---|---|
| `OFF` | 기본 전략 `COMPOSITE_MOMENTUM_ICHIMOKU_V2`, 확인 필터 없음 |
| `B` | **운영 배포본** — `CandleDownsampler` 그대로(형성 중 H4 포함) |
| `A` | **변경안** — 말미의 미완결 H4 봉을 버린다 |

| 비교 | 질문 | 위치 |
|---|---|---|
| **A − B** | 완결 H4 로 바꾸면 나아지는가 | 🔴 **주 비교** |
| `B − OFF` | 필터 유지 가치가 계속 관측되는가 | 보조 비교 (보고만) |

### 고정 사항

| | |
|---|---|
| 타임프레임 · 엔진 | H1 · `BacktestEngine` (Q2 와 동일) |
| 자본·청산·포지션 제한 | Q2 와 **완전히 동일**. 필터 외에 아무것도 바꾸지 않는다 |
| 주 비용 | 수수료 0.05%×2 + 슬리피지 0.1%×2 (Q2 와 동일) |
| **비용 스트레스** | 슬리피지 **0.2%×2** (왕복 0.5%) — 🔴 비용이 체결가·청산 판단·포지션 크기에 영향을 주므로 **엔진을 다시 실행**한다. 사후 차감이 아니다 |
| 전략 인스턴스 | (코인 × 팔) 마다 **새로 생성** |
| 평가 호출 수 | 팔별 기록 (진단용. **크기로 판단하지 않는다**) |

---

## 3. 🔴 불확실성 — 패널 시간블록 부트스트랩

**코인을 독립적으로 재표집하지 않는다.** 22코인은 같은 시장 움직임을 공유하므로
코인 단위 재표집은 불확실성을 **과소평가**한다.

### 🔴 거래 손익 합이 아니라 **자산가치 변화**로 집계한다

청산일에 거래 손익 전부를 넣으면 **여러 블록에 걸친 보유 손익이 마지막 블록으로 몰리고,
미청산 손익은 아예 빠진다.** 그래서 평가(mark-to-market) 자산곡선을 쓴다.

```
1) 코인·팔별로 **동일 시점의 일별 평가 자산곡선** E[coin, arm, d] 를 저장한다
       E = 현금 + 보유수량 × 그날 종가        (미청산 포지션 포함)
2) 일별 손익 변화율                r[coin, arm, d] = (E[d] − E[d−1]) / 초기자본
3) 검사 기간을 길이 L 의 연속·비중첩 **공통** 시간 블록으로 나눈다
       주 L = 30일,  민감도 14일 · 60일
4) 복제마다 블록을 복원추출 — **전 코인·전 팔에 같은 블록 집합을 적용**한다
5) δ* = 평균_코인( Σ_블록 r[coin,A,d] − Σ_블록 r[coin,B,d] )        ← **짝지은 차이**
6) B = 2000, 시드 20260924.  95% 구간과 중심화 단측 p값
```

4 가 **코인 간 동시 상관**을, 3 이 **시간 의존성**을, 5 가 **A·B 의 짝**을 보존한다.

🔴 **이 재표집은 "관측된 운용 성과의 불확실성" 분석이다.**
재배열된 시장에서 전략을 다시 운용한 시뮬레이션이 **아니다** — 블록을 섞어도 전략은
원래 시간축에서 내린 결정 그대로이며, 우리는 그 결과의 표집 변동만 본다.

(앞선 신호 실험 `SIGNAL_STUDY_PREREG.md` v5 §6 과 같은 구조다.)

⚠️ **평균·중앙값·15/22 개선 수는 서로 독립적인 증거 세 개가 아니다.**
같은 22개 값에서 나온 세 요약이며, **함께 봐야 하나의 판단**이 된다.

---

## 4. 🔴 판정 — 두 가지를 분리해 착수 전 고정

### (가) 변경안의 가치 — A 가 B 보다 개선됐는가

코인별 `순수익(A) − 순수익(B)` 에 대해:

| 조건 | 판단 |
|---|---|
| 평균 > 0 **AND** 중앙값 > 0 **AND** 개선 코인 ≥ **15/22** **AND** §3 구간 하한 > 0 | **A 로 변경할 근거 있음** |
| 평균 ≤ 0 **또는** 중앙값 ≤ 0 | **근거 없음.** A 변경안을 접는다 |
| 그 사이 | **판단 보류** |

### (나) 수익 후보 자격 — A **자체**가 비용을 넘는가

🔴 **(가)를 통과해도 A 와 B 가 모두 손실일 수 있다.** 그래서 따로 건다.

| 지표 | 주 비용 | 비용 스트레스(왕복 0.5%) |
|---|---|---|
| 코인 동일가중 평균 순수익 | **> 0** | **> 0** |
| 중앙값 순수익 | **> 0** | 보고만 |
| 양수 코인 수 | **≥ 12 / 22** | 보고만 |
| **거래당 순수익률**(아래 정의) | **≥ 0.10%** | **> 0** |

#### 🔴 거래당 순수익률의 분모 — 착수 전 고정

왕복 비용 0.30% 는 **가격 기준**(= 진입 포지션 금액 대비)이다. 비교하려면 분모를 맞춰야 한다.

```
왕복 거래 하나의 순수익률 =  (청산금액 − 진입금액 − 수수료 − 슬리피지) / 진입 포지션 금액 × 100
집계:  ① 코인별로 그 코인의 모든 왕복 거래에 대해 **단순평균**
       ② 그 코인별 평균들을 **동일 가중 평균**
```

🔴 `총수익률 / 거래 수`(= `PerformanceReport.expectancyPct`) 는 **초기자본 기준**이므로
이 기준과 **섞어 쓰지 않는다.** 두 값을 나란히 보고하되 판정은 위 정의로만 한다.

거래당 0.10% 기준의 근거: 주 비용 왕복 0.30% 의 1/3 — 비용을 넘고도 남는 최소 마진.
**자의적인 선택이며 착수 전에 고정했다는 데 의미가 있다.**

### 네 가지 결과

| (가) | (나) | 판단 |
|---|---|---|
| 근거 있음 | 충족 | **A 를 전향 검증(§5) 후보로** — 배포는 §5 이후 판단 |
| 근거 있음 | 미충족 | **A 의 상대적 개선은 관측됐으나 수익 후보 기준은 충족하지 못했다. 배포하지 않는다** |
| 근거 없음 | — | **A 변경안을 접는다.** B 유지 |
| 보류 | — | 판단 보류. 전향 검증에서 다시 본다 |

`B − OFF` 는 **판정에 쓰지 않고 보고만** 한다.

---

## 5. 전향 검증 — 종료 조건

### 🔴 T0 정의

> **T0 이전 캔들은 지표·전략 상태 워밍업에만 사용한다.
> 세 팔은 동일한 공통 T0 에 현금 100만원 · 보유 포지션 0 으로 시작하며,
> T0 이후 발생한 주문과 손익만 성과에 포함한다.**

사전 등록일(2026-09-24)을 **소급 적용하지 않는다.** T0 는 세 팔이 모두 가동된 시점이며
시작 후 이 문서에 기록한다.

**시간 방향으로 독립인 것은 이것뿐이다.**

### 시작 전 확인 — 셋

1. **T0 부터 새 자료로 계산한다** (위)
2. **코드·설정·코인·자본 배분을 고정**하고, 세 팔이 **자본도 전략 상태도 공유하지 않게** 한다
3. **공통 종료 조건과 수익 판정 기준을 유지**한다. 오류·위험 감시는 계속하되
   **중간 성과로 규칙을 바꾸지 않는다**

### 🔴 공통 T0 을 **어떻게 보장하는가** — 생성 절차 (2026-09-25 추가)

앞선 기술에는 빠진 것이 있었다. **현재 페이퍼 엔진에는 "준비됐지만 아직 거래하지 않는" 상태가 없다.**

| 근거 | 내용 |
|---|---|
| `PaperTradingService:189` | `createSession` 이 `status("RUNNING")` 을 **즉시** 넣는다 |
| `PaperTradingService:570` | `runStrategy()` 는 `fixedDelay = 60_000` 으로 돌며 그 시점의 **RUNNING 전량**을 평가한다 |
| 일괄 생성 경로 | `POST /paper/sessions/multi` 는 **코인 1종 × 전략 N종** 이다 — 22코인이면 **22번** 호출해야 한다 |

따라서 66개를 순차 생성하면, 먼저 만든 세션이 **나중 세션이 생기기 전에 평가 틱을 먼저 받아**
포지션을 열 수 있다. 그렇게 되면 세 팔의 시작 조건이 달라지고 위 T0 정의가 깨진다.

🔴 **엔진을 고쳐서 해결하지 않는다.** 예약 시작 필드를 추가하는 것이 정확하지만,
그것은 **배포 범위(66세션 생성·시작) 밖**이고 **검증 대상 엔진을 다시 바꾸는 일**이다.
대신 **생성 전체를 한 틱 간격 안에 넣고, 사후에 검증한다.**

```
① 배포 검증      GET /api/v1/strategies/types 에 COMPOSITE_MTF_MOMENTUM_CLOSED 가 있는가
                 (없으면 구 이미지가 돌고 있다 — 팔 A 는 평가 시점에 예외로 죽는다)
② 위상 측정      22코인 밖 코인(KRW-BTC)으로 프로브 세션 1개를 만들고
                 strategy_log 첫 행 시각 t_p 를 읽는다 → 틱 위상 = t_p (mod 60초)
                 프로브는 즉시 정지·삭제한다 (22코인 기록에 섞이지 않는다)
③ 일괄 생성      틱이 지난 직후 단건 66회를 **쉬지 않고** 실행한다 (여유 ≈ 55초)
                 코인마다 팔 순서를 한 칸씩 돌린다 — 아래 2026-09-26 개정 참조
                 createSession 은 DB insert 뿐이므로 시세 조회 대기가 없다
④ 검증           66개 전부에 대해 strategy_log 최초 행 시각이 **같은 틱**인가
                 T0 이전 paper_order 가 **0건**인가
⑤ 실패 시        전량 stop-all → 66세션 삭제 → ②부터 다시 한다
                 🔴 부분 보정하지 않는다. 시작 조건이 갈린 표본은 버린다
```

**T0 = 66개가 모두 RUNNING 인 상태에서 받은 첫 공통 평가 틱.** ④ 로 확인된 값을 기록한다.

#### 시작 전 확인 — 남은 셋

| 확인 | 왜 | 필요한 것 |
|---|---|---|
| 세 전략명이 H1 에서 차단돼 있지 않은가 | `StrategyEnablementGate` 는 **차단 목록**이다(부재 = 허용). 차단돼 있으면 생성이 예외로 실패한다 | 운영 DB 조회 |
| 22코인 H1 캔들이 워밍업 최소봉을 넘는가 | 부족하면 그 팔은 **조용히 아무것도 안 한다** — 0거래를 성과로 오해하게 된다 | 운영 DB 조회 |
| 현재 RUNNING 세션 수 + 66 ≤ 120 | `MAX_CONCURRENT_SESSIONS = 120` | API 조회 |

#### 📝 2026-09-26 개정 — 실제 평가 주기를 확인한 뒤

위 표에 빠진 것이 있었다. **전략 평가는 60초마다가 아니라 닫힌 캔들이 갱신될 때만 일어난다**
(`PaperTradingService:670`, `lastEvaluatedClosedCandle`). H1 이면 **시간당 1회**다.
다만 세션이 새로 생기면 `prevEvaluated == null` 이라 **생성 후 첫 틱은 반드시 평가된다.**

🔴 결론은 바뀌지 않는다 — 오히려 절차가 정확히 그 지점을 겨눈다. 갈릴 수 있는 것은
**생성 후 첫 평가 한 번**이고, 그 뒤로는 세 팔이 H1 경계에 자동으로 정렬된다.
따라서 "66개를 한 틱 간격 안에 만든다"가 필요충분하다.

**생성 경로를 `multi` 22회에서 단건 66회로 바꿨다.** 이유는 평가 순서다 —
엔진은 `findByStatusOrderByStartedAtAsc` 로 돌므로 **생성 순서가 평가 순서로 고정된다.**
코인마다 OFF→B→A 로 만들면 **A 가 언제나 마지막**이다. 한 틱의 캔들은 `TickCandleCache` 로
공유되므로 세 팔이 같은 자료를 보지만, 닫힌 캔들 판정은 `Instant.now()` 를 쓴다
(`:667`) — 루프가 캔들 경계를 가로지르면 먼저·나중 평가된 세션의 `lastCandleClosed` 가
갈릴 수 있다. 코인마다 팔 순서를 **한 칸씩 돌려**(OFF·B·A → B·A·OFF → A·OFF·B) 그 영향이
특정 팔에 쏠리지 않게 한다. 22 는 3 으로 나뉘지 않아 첫 팔 분포는 8·7·7 이다.
단건 66회도 DB insert 뿐이라 한 틱 안에 들어간다.

⚠️ **106세션이 한 루프를 돈다** — 기존 40 + 신규 66. 이미 평가 지연 p95 가 57~71초로 측정된
적이 있다(2026-09-15). 루프가 길어지는 것 자체는 세 팔에 공통이지만, 위의 경계 가로지르기
가능성을 키운다. 시작 후 첫 며칠은 평가 지연을 함께 본다.

#### 🔴 기존 세션 8개는 정지하지 않는다 (2026-09-26)

`check` 결과 세 팔과 **같은 전략명으로 이미 도는 페이퍼 세션이 8개** 있었다.
처음 스크립트는 "먼저 정리한다"고 적었는데 **그건 과했다 — 정정한다.**

페이퍼는 **세션별로 자본과 전략 인스턴스가 독립**이고(`sessionStatefulStrategies` 는
`sessionId` 키), LIVE 의 **cross-session 잔고 가드를 의도적으로 적용하지 않는다**
(`PaperTradingService:611-613`). 따라서 기존 세션이 신규 66세션의 거동을 바꾸지 않는다.
섞이는 것은 **집계**뿐이므로, ID 를 `preexisting_excluded.json` 에 기록해 제외한다.
🔴 남의 표본을 지우지 않고, 내 집계에서 빼는 쪽이 맞다.

#### 🔴 첫 create 실패 — 팔 B 가 운영 DB 에서 비활성이었다 (2026-09-26)

첫 `create` 는 **44개만 생성되고 22개가 400 으로 거부**됐다. 거부된 22개는 전부 팔 B 다.

```
전략 'COMPOSITE_MTF_MOMENTUM'은(는) 비활성화되어 세션을 생성할 수 없습니다.
폐기 기준으로 중단된 전략이라면 /backtest/walk-forward 재검증이 필요합니다.
```

`strategy_type_enabled` 은 **차단 목록**이고(부재 = 활성, `StrategyEnablementGate:35-40`),
`COMPOSITE_MTF_MOMENTUM` 은 그 표에서 `is_active=false` 인 21행 중 하나다 —
`docs/old_progress.md:4613-4623` 에 2026-08-24 시점 이미 비활성으로 기록돼 있다.
세 생성 경로(LIVE·DYNAMIC·PAPER)가 전부 이 게이트를 지나므로 페이퍼도 예외가 아니다.

**내 `check` 가 이것을 잡지 못한 것이 결함이다.** `GET /api/v1/strategies/types` 는
*레지스트리에 등재됐는가*만 답한다 — *세션을 만들 수 있는가*는 다른 질문이다.
두 검사를 추가했다:

| 검사 | 무엇을 보는가 | 왜 둘이 필요한가 |
|---|---|---|
| `check` ②' | `GET /api/v1/strategies/{name}` 의 `isActive` | 전략 레벨 차단을 **create 전에** 잡는다 |
| `probe` | 세 팔을 22코인 밖(`KRW-BTC`)에 **실제로 한 번 만들어 본다** | (전략 × 타임프레임) 표 `strategy_timeframe_enabled` 에는 **조회 엔드포인트가 없다** — 실제 생성 시도가 유일한 사전 검사다 |

#### 📌 이것은 운영 설정 문제만이 아니다 — 팔 B 의 위상을 정정한다

`start_66.py` 는 팔 B 를 "운영 기본값"이라고 적었다. **그 표현은 부정확하다 — 정정한다.**

- **코드 수준에서는 맞다**: 형성 중 H4 봉을 포함하는 `CandleDownsampler` 경로가 배포된 것이고,
  `COMPOSITE_MTF_CONFIRMED`·`COMPOSITE_MTF_BTC` 를 통해 실제로 운영에서 돈다.
- **프리셋 수준에서는 틀렸다**: `COMPOSITE_MTF_MOMENTUM` **그 자체로는 신규 세션을 만들 수 없는
  상태**였다. 비활성 사유는 이 계열에서 반복된 판정이다 — `StrategyBehaviourFingerprintTest` 의
  실효 중복 그룹에서 `COMPOSITE_MOMENTUM_ICHIMOKU_V2 == COMPOSITE_MTF_MOMENTUM` 이었다
  (`docs/SUPERTREND_FILTER_PREREG.md:20-26`). 즉 **B 는 OFF 와 구별되지 않아 꺼졌다.**

🔴 그래서 팔 B 를 되살리는 것은 단순한 설정 토글이 아니라 **"합성 자료에서 중복이라 껐던 것을
실제 자료로 다시 재본다"는 이 검증의 취지 그 자체**다. 그러나 그 재활성화는 운영 상태 변경이라
**내가 임의로 하지 않는다** — 사용자 확인을 받고, 아래에 시각과 사유를 기록한 뒤 진행한다.

⚠️ 함께 기록할 것: B 를 재활성화하면 `COMPOSITE_MTF_MOMENTUM` 으로 **누구나 세션을 만들 수 있게
된다.** 검증 종료 후 원래 상태(비활성)로 되돌릴지는 검증 결과로 판단한다 — 되돌리는 것이
기본값이고, 결과가 B 의 독립적 가치를 보이면 그때 유지를 논한다.

| 항목 | 값 |
|---|---|
| 재활성화 시각 (UTC) | *(기록 대기)* |
| 수단 | `python start_66.py arm-gate enable` — `PATCH /api/v1/strategies/{name}/active` 는 **set 이 아니라 toggle 이다**(`StrategyController:73-89`), 그래서 먼저 읽고 이미 원하는 상태면 건드리지 않는다 |
| 변경 기록 | `scripts/prospective/arm_gate_changes.json` (원래 상태·시각·사유) |
| 검증 종료 후 처리 | `python start_66.py arm-gate restore` — 기본값 = 비활성으로 복귀 |

🔴 **사용자 확인 받음 (2026-09-26): 검증 기간 한정 재활성화로 진행한다.**

#### 🔴 첫 시도의 표본은 전량 폐기했다

`verify` 는 생성된 44개에 대해 ② 첫 평가 시각 폭 0.4초 ✔ · ③ T0 이전 주문 0건 ✔ 을 냈지만
① 첫 평가 로그가 **30/44** 였고, 무엇보다 **66개가 아니다.** §5 의 "부분 보정하지 않는다"에
따라 44개를 전량 정지·삭제했다(`start_66.py abort` — 이번에 추가했다).

📌 ① 의 로그 없는 14개는 따로 볼 일이다 — 캔들이 부족한 코인에서 그 팔은 **조용히 아무것도
하지 않는다.** 그 0거래를 "성과"로 읽으면 안 된다. 재시도 후 다시 확인한다.

#### 🔴 `verify` 의 ① 은 "평가됐는가"를 재는 지표가 아니었다 (2026-09-26)

2차 `create` 는 **66/66 성공**했다(팔 B 재활성화 후). 그런데 바로 돌린 `verify` 가 **0/66** 을 냈다.

원인은 세션이 아니라 **내 검사 방식**이다. `strategy_log` 저장은 **새 닫힌 캔들을 평가하는
`else` 분기 안에만** 있다(`PaperTradingService:734-757`). 따라서 로그가 없는 이유가 셋이고,
셋은 서로 다른 처분을 요구한다.

| 원인 | 코드 | 처분 |
|---|---|---|
| ① 아직 첫 틱이 안 왔다 | `runStrategy` initialDelay 35초 · fixedDelay 60초, 106세션 루프 자체가 1분 가까이 걸린 적 있다(2026-09-15 p95 57~71초) | **기다린다** — `verify` 가 기본 300초까지 폴링하게 했다 |
| ② 전략 요구 캔들 미달 | `:679-682` — 평가와 로그를 **함께** 건너뛴다 | 그 코인은 표본에서 빠진다. 🔴 0거래를 성과로 읽지 않는다 |
| ③ 로그 저장 실패 | `:757` catch — `log.warn` 만 남는다 | 서버 로그를 본다 |

🔴 **첫 `create` 의 "30/44" 도 이 기준으로 다시 읽어야 한다.** 그때는 ①과 ②를 구별하지
않았으므로 "14개가 캔들 부족"이라고 단정할 수 없다 — 단순히 덜 기다린 것일 수도 있다.

📌 **세 팔의 최소 캔들 요구는 같다 — 100봉이다.** 팔 사이 비대칭은 없다.

🔴 **내가 처음 적은 78 은 틀렸다 — 정정한다.** `IchimokuFiltered` 의 `52 + 26 = 78` 까지만 보고
유도했는데, `momentumV2Core` 안의 **GRID 가 100 을 요구해**(`GridStrategy:154-156`) 그 값이
78 을 덮는다. 운영 로그가 세 팔 전부 `< 100건 필요` 로 찍어 드러났다.

| 팔 | 구성 | 요구 (엔진 보고) |
|---|---|---|
| OFF | `IchimokuFilteredStrategy(momentumV2Core)` | **100** |
| B·A | `MtfConfirmedStrategy(위 래퍼, Supertrend ×4)` | **100** |

HTF 쪽 요구는 `4 × 12 = 48`(`SupertrendStrategy:97-100`)이라 여기에도 묻힌다.

🔴 **교훈: 이 값을 코드에서 유도하지 않는다.** `GET /strategies/{name}` 의
`minimumCandleCount` 를 읽고, 세 팔이 서로 다르면 그 자체를 착수 불가 사유로 본다
(요구량이 다르면 캔들 부족이 **팔 비대칭**을 만든다). `start_66.py` 를 그렇게 고쳤다.

##### 🔴 78 과 100 의 관계를 분명히 한다 — 여유 기준이 아니다

혼동을 남기지 않도록 못 박아 둔다.

| | |
|---|---|
| **78** | 🔴 **내 계산 착오다. 기준이었던 적이 없고, 철회한다.** 잠시 이 문서에 적혀 있었을 뿐 어떤 판정에도 쓰이지 않았다 |
| **100** | **엔진이 실제로 요구하는 값** — `minimumCandleCount` 이자 운영 로그의 `< 100건 필요`. **유일한 준비 기준이다** |

즉 78 → 100 은 "여유를 더한 것"이 아니라 **틀린 값을 옳은 값으로 바꾼 것**이다.
**감시용 여유 기준은 현재 없다.** 두려면 별도로 명시해 고정한다 — 조용히 끼워 넣지 않는다.
(창이 흐르며 봉 수가 줄어드는 문제는 여유 기준이 아니라 **적재 지속성**으로 다룬다.)

`verify` 는 로그 없는 세션을 **코인 단위로 묶어** 보여준다 —
세 팔 전부 빠지면 코인 탈락, **일부 팔만 빠지면 원인을 밝히기 전에는 집계하지 않는다.**

#### 🔴 2차 착수도 성립하지 않았다 — 7코인이 평가되지 않았다 (2026-09-26)

절차 자체는 작동했다. 실패한 것은 **표본**이다.

| 검사 | 결과 |
|---|---|
| 66세션 생성 | **66/66** ✔ (팔 B 재활성화 후) |
| ② 첫 평가 시각 폭 | **0.5초** ✔ 같은 틱 |
| ③ T0 이전 주문 | **0건** ✔ |
| ① 첫 평가 로그 | **45/66** 🔴 — 7코인(ANKR·CRO·DKA·ELF·INJ·JUP·ONG)이 **세 팔 모두** 빠졌다 |

관측된 T0 은 `2026-09-26T00:55:33Z` 였다. **이 T0 을 채택하지 않는다.**

##### 🔴 정정 — "세 팔 모두 빠졌으니 편향이 아니다"는 단정할 수 없다

나는 요구 캔들 수가 세 팔 모두 같다는 점(실제로는 100봉, 아래 정정 참조)을 확인하고
"팔 편향이 아니다"라고 적었다.
**그 확인은 맞지만 결론이 과했다 — 정정한다.**

팔 사이 비대칭이 없다는 것과 편향이 없다는 것은 다르다. 준비된 15코인만 남기면
**코인 구성에 따른 선택 편향**이 생긴다 — 자료가 준비되는 조건(상장 시기·유동성·수집 대상
포함 여부)이 성과와 무관하다고 볼 근거가 없다. 게다가 §4 (가) 의 "개선 코인 ≥ 15/22" 는
**22 를 분모로 착수 전 고정한 값**이므로, 15코인으로 다시 계산하는 것은 사전 등록을 사후에
바꾸는 일이다. 🔴 **표본을 줄여 진행하지 않는다.**

##### 🔴 정정 — 원인을 "적재 지연 아니면 동기화 실패" 둘로 좁힌 것도 이르다

캐시에 봉이 있어도 **조회 키·시간 범위·봉 유효성·워밍업 조건** 때문에 평가에서 빠질 수 있다.
특히 `fetchRecentCandles` 는 `[now − 500 × 봉주기, now]` **창**만 조회하므로
(`PaperTradingService:1072-1083`, `CANDLE_LOOKBACK = 500`), **전체 건수가 많아도 창 안이 비어
있으면 0봉**이다. 즉 "총 건수 ≥ 78" 검사는 원인을 가리지 못한다 —
`GET /settings/upbit/status` 의 건수는 창 안이 아니라 **전체**다.

그래서 세 층을 이어서 본다 (`start_66.py diagnose`).

| 층 | 자료 | 무엇을 가리는가 |
|---|---|---|
| ① 캐시 | `GET /settings/upbit/status` — 코인별 건수·최초·최종 | 적재 자체가 있는가 |
| ② 동기화 | `GET /settings/server-logs` 의 "시장 데이터 동기화 실패" | **포착한 실패**가 무엇이었는가 |
| ③ 평가 | 같은 로그의 "모의투자 캔들 부족 N건" · "요구 캔들 미달 N건 < M건 필요" | 🔴 **엔진이 평가 시점에 실제로 조회한 봉 수** |

엔진이 본 봉 수는 `PaperTradingService:625`(<10) 과 `:642-645`(요구 미달)이 **건수까지
찍는다** — 추정하지 않고 읽는다.

🔴 **두 층의 해석 범위를 좁혀 둔다.**

- **②**: 경고가 있으면 실패한 시도를 확인할 수 있다. 🔴 **경고가 없다고 동기화 시도나 성공이
  확인되는 것은 아니다** — 이 채널은 실패만 보여준다.
- **①과 ③의 어긋남**: 전체 건수는 충분한데 평가 조회 건수가 적다면, 그것은 **전체 건수만으로
  준비 여부를 판단할 수 없었다는 증거**다. 🔴 **최근 구간의 적재 부족도 여전히 원인일 수
  있으므로 적재 문제를 제외하지 않는다.** 원인을 창·키·유효성으로 좁히지 않는다.

##### 🔴 ③층 실측 — 실패가 두 종류로 갈린다 (2026-09-26 01:18~01:19 UTC)

컨테이너 로그에서 엔진이 **평가 시점에 실제로 조회한 봉 수**를 읽었다. 추정이 아니다.

| 코인 | 엔진 관측 | 요구 | 분류 |
|---|---|---|---|
| ONG · ANKR · DKA · ELF · INJ | **0봉** | 100 | 🔴 **창 안에 아무것도 없다** |
| JUP | 72봉 | 100 | 🟡 부족 |
| CRO | 98봉 | 100 | 🟡 부족 |
| G | 99봉 | 100 | 🟡 부족 |

⚠️ **G 는 앞선 `verify` 의 7코인 목록에 없었다.** 즉 미달 코인 집합이 시점에 따라 달라진다 —
경계에 걸친 코인(98·99봉)이 있어 **한 번의 통과를 준비 완료로 읽을 수 없다.**

**두 종류를 같은 원인으로 묶지 않는다.**

- **0봉**: 창 안이 완전히 빈 것이라 "적다"와 질이 다르다. 적재가 전혀 안 되고 있는지,
  종목 자체가 거래되지 않는지를 먼저 갈라야 한다 — `diagnose` 가 이 코인들만 골라
  `GET /settings/upbit/ticker` 로 시세를 확인한다. 시세가 오면 종목은 살아 있고 적재가 안 된
  것이고, 시세도 안 오면 상장·거래 여부부터 확인할 일이다(동기화 문제가 아니다).
- **72~99봉**: 최근 500시간 창에 H1 봉이 100개도 차지 않는다는 뜻이다.
  🔴 **이것은 적재 실패일 수도 있고, 그 코인의 거래가 드물어 봉 자체가 없는 것일 수도 있다.**
  후자라면 backfill 로도 해결되지 않는다 — 500시간 동안 체결된 시간대가 100개 미만인 코인이다.

🔴 **후자라면 사전 등록한 22코인 구성 자체를 다시 판단해야 한다.** §1 의 22코인은
"H1 15,000봉 이상"이라는 **과거 이력** 기준으로 뽑았고, 그것은 **현재 유동성을 보장하지
않는다.** 전향 검증은 현재 시장에서 돌아가므로 이 둘이 어긋날 수 있다.
그 판단은 원인을 가른 뒤에 한다 — **지금 코인을 빼거나 기준을 바꾸지 않는다.**


##### 🔴 원인 확정 — 캐시 적재가 코인별로 멈췄다. 그리고 8코인 문제가 아니다

`diagnose` 의 캐시 최종 시각(`to`)과 엔진 실측 봉 수가 **정확히 맞아떨어진다.**
창 시작은 `now − 500시간` ≈ 2026-09-05T05 였다.

| 코인 | 캐시 `to` | 창 시작~`to` | 엔진 실측 |
|---|---|---|---|
| JUP | 09-08T05 | ≈71시간 | **72봉** |
| CRO | 09-09T07 | ≈97시간 | **98봉** |
| G | 09-09T11 | ≈101시간 | **99봉** ⚠️ 2봉 어긋남 |
| ONG·ANKR·DKA·INJ | 08-31 ~ 09-04 | 창 시작 **이전** | **0봉** |
| ELF | 행 자체가 0건 | — | **0봉** |

🔴 **유동성 문제가 아니었다.** 봉이 드물어서가 아니라 **적재가 특정 날짜에 멈춰서** 창 안이
비거나 모자란 것이다.
(앞서 내가 "거래가 드물어 봉 자체가 없을 수 있다"고 적은 가설은 **이 자료로 지지되지 않는다.**)

⚠️ **"전부 1봉 이내 일치"는 과장이었다 — 정정한다.** JUP(71↔72)·CRO(97↔98)는 1봉 차이지만
**G 는 101 ↔ 99 로 2봉 어긋나고, 하필 요구선(100봉)을 가로지른다** — 추정은 통과, 실측은
미달이다. 내 추정은 `now` 를 분 단위로 가정하고 창 시작~`to` 사이가 시간당 연속이라고
보는 것이므로 경계에서 어긋난다. 🔴 **그래서 이 추정은 준비 판정에 쓸 수 없고, 감시용
눈금일 뿐이다.** 판정은 엔진 실측으로만 한다.

또한 5코인 전부 **시세 조회는 정상**이었다(ONG 131.0 · ANKR 6.87 · DKA 5.88 · ELF 103.0 ·
INJ 10760.0). 종목은 살아 있다 — 적재 경로의 문제다.

##### 🔴 진단이 놓친 채널 — "실패 기록이 없다"는 오독이었다

`MarketDataSyncService.syncPair` 는 수집 결과가 비면 **예외 없이 `캔들 수신 없음` 만 남기고
돌아간다.** 나는 `시장 데이터 동기화 실패` 하나만 보고 있었으므로, 그 경고가 없는 것을
"실패 기록이 없다"로 읽을 수 있었다 — 바로 "경고 부재는 증거가 아니다"에 해당하는 사례다.
두 채널을 모두 세도록 고쳤고, `UpbitRestClient Bean 미등록` 도 함께 본다.

##### 🔴 이것이 진짜 문제다 — 22코인이 차례로 탈락한다

`to` 가 오래된 것은 8코인이 아니다. **NEAR 를 뺀 21코인 전부가 오래됐다.**
창은 시간당 1칸씩 흐르므로, 지금 통과하는 코인도 창 안 봉 수가 **시간당 1개씩 줄어든다.**

| 코인 | 창 안 추정 | 100봉 미달까지 |
|---|---|---|
| ONG·ANKR·DKA·ELF·INJ·JUP·CRO | 0 ~ 98 | **이미 미달** |
| G | ≈101 | **0일** |
| SC · PUNDIX · CVC · WAVES | 187 ~ 200 | 3.6 ~ 4.2일 |
| GLM · POLYX · WAXP · IOTA · T · HIVE · POWR | 220 ~ 268 | 5.0 ~ 7.0일 |
| BLAST · BCH | 354 · 428 | 10.6 · 13.7일 |
| NEAR | ≈498 | 16.6일 |

🔴 **§5 의 종료 조건은 최대 6개월이다. 적재가 계속되지 않으면 검증 자체가 성립하지 않는다.**
지금 8코인을 backfill 해도 같은 일이 며칠 뒤에 다시 일어난다.

따라서 재시작의 선행 조건이 바뀐다.

| 선행 조건 | 내용 |
|---|---|
| ① 적재 경로 수정 | 22코인 H1 이 **지속적으로** 갱신되는지. 일회성 backfill 로는 부족하다 |
| ② 준비 확인 | 엔진과 같은 조회 조건(창)·워밍업 조건(엔진 보고 요구량)으로 22코인 전부 통과 |
| ③ 지속 감시 | 검증 기간 중 캐시 정지를 조기에 잡는 확인. 멈추면 그 사실을 기록하되 **판정 기준은 바꾸지 않는다** |

⚠️ ① 은 **앞서 고정한 배포 범위("66세션 생성·시작만") 밖**이다. 적재 경로를 손대는 것은
검증 대상 엔진이 아니라 자료 공급 경로지만, 범위를 넓히는 결정이므로 따로 확인받는다.


##### 🔴 범위 확대 — 자료 공급 경로의 진단·수정 (2026-09-26, 사용자 확인 받음)

앞서 고정한 배포 범위는 "66세션 생성·시작만"이었다. **여기에 동기화 복구를 더한다.**

| 포함 | 제외 (그대로) |
|---|---|
| 캔들 적재 경로 진단·수정 — 22코인 H1 이 **지속적으로** 갱신되게 | 전략 구성·가중치·필터 |
| | 비용 가정 (수수료·슬리피지) |
| | 판정 기준 (§4) · 종료 조건 (§5) |
| | 기존 실거래 세션 재시작 · 자본 변경 |

수정 내용과 배포 버전은 **새 T0 의 기준점에 함께 기록한다**(§ 보존할 기준점).

##### 🔴 원인은 추정하지 않는다 — 호출 경로 3단계로 가른다

**"빈 응답"은 아직 가설이다.** 세 단계를 순서대로 확인하면 호출 누락·수집 문제·저장 문제가
갈린다.

| 단계 | 무엇을 보는가 | 관측 수단 |
|---|---|---|
| ① 호출 | 스케줄러가 실제로 돌았고 **대상 목록에 22코인이 들어갔는가** | `MarketDataSyncService` 의 `시장 데이터 동기화 시작: N 종목` — **DEBUG 라 현재 안 보인다.** ②로 간접 확인하고, 안 갈리면 그 클래스만 DEBUG 로 올린다 |
| ② 수집 | 코인별 **요청 범위와 응답 건수** | `UpbitCandleCollector` 의 `캔들 수집 완료: {코인} {봉} {N} 건` — 🔴 **INFO 다. 지금 바로 읽을 수 있는 유일한 직접 증거** |
| ③ 저장 | 응답이 있었다면 변환·저장 후 **캐시 `to` 가 전진했는가** | `시장 데이터 동기화 완료` 는 DEBUG 지만, 전진 여부는 `upbit/status` 의 `to` 로 직접 본다 |

```
②의 흔적 없음           → 호출 누락 (스케줄러 미실행 또는 대상 목록 누락)
② N = 0                 → 수집 문제 (요청 범위·API 응답)
② N > 0 인데 ③ to 정체  → 저장 문제 (변환·upsert)
```

`start_66.py diagnose` 가 이 표를 함께 출력한다. 🔴 **가르기만 한다 —
어느 칸이든 다음 확인 대상이 정해질 뿐이고, 무엇이라고 지금 단정하지 않는다.**

⚠️ 인메모리 로그 버퍼는 밀려나므로 ②의 흔적이 없을 때 "호출이 없었다"로 읽지 않는다.
컨테이너 로그를 먼저 본다 — 버퍼보다 길게 남는다.


##### 🔴 ①층 — 스케줄러는 정상이다. 남은 것은 "출력이 0인 경로"뿐이다 (2026-09-26)

| 관측 | 값 | 읽기 |
|---|---|---|
| `executor_active_threads` / `pool_size_threads` | **2 / 8** | 포화 아님 |
| 60초 완료 수 | **63회** | 정상 속도 (5초 주기 4개만으로도 기대 48회) |
| `스케줄러 작업 오류` (2시간) | 없음 | 예외로 죽지 않았다 |
| `캔들 수집 완료` (INFO, 2시간) | **없음** | 수집 호출이 없었다 |
| `시장 데이터 동기화 실패`·`건너뜀` | 없음 | 그 경로도 아니다 |

📌 **INFO 가 docker 로그에 보이는지는 설정으로 확인했다** — `logback-spring.xml` 에서
`com.cryptoautotrader.exchange` 가 INFO 이고 CONSOLE 에 붙어 있고, root 도 INFO 다.
즉 `캔들 수집 완료`(INFO)는 보여야 하므로 **부재가 의미를 가진다.** DEBUG 만 안 보인다.
⚠️ 그래도 설정을 근거로 한 추론이므로, 빈도 높은 INFO 한 줄로 **양성 대조**를 함께 본다.

**출력이 정확히 0이 되는 경로는 둘뿐이다.**

| 후보 | 근거 | 현재 판단 |
|---|---|---|
| ⓐ `pairs` 가 비어 조용히 return | `if (pairs.isEmpty()) return;` 이 DEBUG "시작" 로그보다 **앞**에 있다 | 🔴 **저장소 코드로는 성립하지 않는다** — `PaperTradingService:572` 가 **같은 `balanceRepo.findByStatusOrderByStartedAtAsc("RUNNING")`** 으로 66세션을 평가하고 있다 |
| ⓑ 호출이 끝나지 않아 다음 스케줄이 안 잡힘 | `fixedDelay` 는 현재 실행이 끝나야 다음이 잡힌다 | 저장소 코드에는 `buildGetRequest` 에 **15초 요청 타임아웃**이 있어(2026-08-25) 영구 블로킹이 어렵다 |

🔴 **그래서 남는 가능성을 분명히 적는다: 돌고 있는 이미지가 저장소 코드와 다를 수 있다.**
나는 저장소 코드를 읽고 컨테이너의 거동을 추론해 왔는데, **이 컨테이너의 빌드는 한 번도
검증되지 않았다**(`verify_deploy.sh` 출력을 아직 받지 못했다). 하필 ⓑ 를 막는 그 타임아웃의
javadoc 에 **같은 실패 양상의 사고 기록**이 있다 — "연결된 뒤 응답이 안 오면 영원히 블로킹되고,
`fixedDelay` 스케줄 자체가 죽는다. 29시간 동안 전체 동적 세션이 무응답이었다."

##### 확인 순서 — 사라지는 증거를 먼저 잡는다

1. **스레드 덤프** — `scheduler-N` 하나가 소켓 읽기에 멈춰 있는가. 🔴 **재시작하면 사라지므로
   가장 먼저 잡는다.** JRE 이미지라 `jcmd` 가 없을 수 있어 `kill -3` 으로 stdout 에 받는다.
   ⚠️ ENTRYPOINT 가 `sh -c "java …"` 이므로 **PID 1 이 java 인지 먼저 확인한다** —
   sh 라면 SIGQUIT 로 컨테이너가 죽는다.
2. **배포 검증** — `verify_deploy.sh`. 이제 두 가지 이유로 필요하다(RANGE 게이트 + 위 가능성).
3. 그래도 안 갈리면 **`MarketDataSyncService` 만 DEBUG** 로 올려 `시장 데이터 동기화 시작: N 종목`
   을 본다. 재시작이 필요하므로 1·2 뒤에 한다.


##### 🟢 배포 검증 통과 — 기준점 (2026-09-26)

| 항목 | 값 |
|---|---|
| 저장소 HEAD (검증 시점) | `32239b4` |
| 컨테이너 | `c48c0e4d6e13…` |
| 이미지 | `sha256:e5566c7adf43…` |
| 이미지 생성 (UTC) | **2026-09-26T00:28:09Z** |
| 컨테이너 생성 (UTC) | **2026-09-26T00:28:19Z** |
| `range_gate_has_arm_a` | **1** ✔ (RANGE 게이트 수정 포함) |
| `downsample_closed_only` | **1** ✔ (팔 A 프리셋 존재) |

🔴 **따라서 앞의 두 착수 시도(00:35 · 00:55)는 모두 이 검증된 이미지에서 돌았다.**
팔 A 의 RANGE 처우가 OFF·B 와 같은 조건이었다 — A−B 가 두 변경을 섞는 문제는 없다.
(⚠️ 이 검증은 **두 문자열의 존재**를 확인한 것이다. 이미지 전체가 저장소와 같다는 증명은 아니다.)

##### ⚠️ 내 관측 두 건을 정정한다

**(1) "누적 완료 5562 가 낮아 보인다"는 틀렸다.** 나는 가동 시간을 **이전 컨테이너**
(09-25T22:17 생성)로 잡았는데, 실제로 돌고 있는 컨테이너는 **09-26T00:28:19** 생성이다.
스레드 덤프의 `elapsed=6190.75s`(≈103분)가 이를 확증한다. 표본 시점 가동 97분으로 다시 계산하면
**분당 57.5회** — 기대 범위다. 🔴 **스케줄러에는 이상 징후가 없다.**

**(2) "돌고 있는 이미지가 저장소 코드와 다를 수 있다"는 가능성은 약해졌다.** 이미지가
오늘 00:28 에 빌드됐고 두 표지자가 모두 있다. 15초 요청 타임아웃(2026-08-25)도 그보다
오래된 변경이므로 포함돼 있을 것으로 본다 — 즉 **ⓑ(영구 블로킹) 도 약하다.**

##### 🔴 남은 확인 — 덤프 안에서 이름으로 찾는다

스레드 덤프는 이미 로그에 있다. 발췌만 봤고(scheduler-1·2 는 `DelayedWorkQueue.take` 대기 =
유휴), **전체에서 이름으로 찾는 것이 남았다.**

| 찾을 것 | 있으면 | 없으면 |
|---|---|---|
| `MarketDataSync` | 그 시점 실행 중이었다 | 그 순간 실행 중이 아니었다(주기상 정상일 수 있다) |
| `SocketRead`·`Upbit` 를 가진 `scheduler-` 스레드 | ⓑ 확인 | ⓑ 기각 쪽 |
| `캔들 수집 완료` (재시작 이후 3시간) | 동기화가 한 번은 돌았다 | 재시작 후 한 번도 안 돌았다 |

📌 세 번째가 특히 중요하다 — NEAR 는 캐시 `to` 가 09-26T00:00 이고 재시작 직후 동기화됐을
가능성이 높다. 그 시각은 앞서 본 `--since 60m` 창 **밖**이었다. 3시간으로 넓혀 확인한다.

⚠️ `모의투자 신호`(INFO) 를 양성 대조로 쓴 것은 **적절하지 않았다.** 그 로그는 닫힌 캔들이
갱신될 때만 나오므로 H1 에서는 세션당 시간당 1회다 — 10분 창에서 0건은 정상이다.
양성 대조는 **빈도 높은 INFO** 로 골라야 한다.


##### 🔴 확인 — `syncMarketData` 가 끝나지 않고 있다 (ⓑ 확인, 2026-09-26)

세 관측이 한 방향을 가리킨다.

| 관측 | 값 |
|---|---|
| 재시작 후 수집 로그 | **00:28:46 ~ 00:29:43 에만 있다.** 그 뒤 2시간 동안 한 줄도 없다 |
| 덤프(02:11:56) 안의 프레임 | `MarketDataSyncService$$SpringCGLIB$$0.syncMarketData(<generated>)` — 🔴 **그 시점에 실행 중이었다** |
| `scheduler-` 8개 상태 | 6개 `waiting on condition`(유휴) · **`scheduler-4`·`scheduler-7` 만 `runnable`** |

`scheduler-7` 은 00:29 의 H1 수집을 수행한 스레드다(cpu 1,263ms / 가동 6,190s).

🔴 **결론: 동기화 작업은 재시작 후 딱 한 번 시작해서 아직 끝나지 않았다.**
`fixedDelay` 는 현재 실행이 끝나야 다음을 잡으므로 **그 한 번이 끝나지 않으면 영구히
다음이 없다** — `UpbitRestClient` javadoc 이 기록한 2026-08-25 사고와 같은 양상이다
("이 호출 하나가 안 끝나면 스케줄 자체가 죽는다. 29시간 무응답").

이로써 앞서 설명되지 않던 것이 전부 맞물린다.

- 00:29 수집 대상은 **기존 40세션의 코인**(BTC·XRP·ETH·NEAR·ENA·XLM·WLD·SOL…)이었다.
  22코인은 그때 세션이 아직 없었다(생성 00:35 · 00:55).
- 그 다음 실행에 22코인이 들어가야 했는데 **다음 실행이 없었다.**
- NEAR 만 `to` 가 09-26T00:00 인 이유도 이것이다 — 00:29 의 그 한 번에 포함됐다.

⚠️ **아직 모르는 것: 어디서 멈췄는가.** 15초 요청 타임아웃(2026-08-25)이 있으므로
소켓 읽기 무한 대기는 설명이 되지 않는다. 남은 후보는 서로 다르다.

| 후보 | 확인 방법 |
|---|---|
| DB 저장 — `saveAll` 대량 upsert 또는 커넥션 대기 (Hikari `maximum-pool-size: 20`, 106세션이 DB 를 쓴다) | 멈춘 스레드의 스택 프레임 |
| 수집 루프 — `while (cursor.isAfter(from))` 가 끝나지 않는 조건 | 같은 스택 + 마지막 성공 코인의 다음 코인 |
| 다른 무언가 | 같음 |

🔴 **스택을 보기 전에는 단정하지 않는다.** 그리고 재시작하면 이 증거가 사라진다.

📌 참고로 같은 창에 `215건` 수집이 따로 있는데 이것은 `CandleDataFreshnessScheduler` 쪽이다
(고정 대상 목록). `MarketDataSyncService` 의 갭 동기화는 `6건` 쪽이다. **둘은 다른 경로이며,
22코인을 채우는 것은 세션 기반인 후자다.**


##### 🔴 멈춘 곳은 Upbit 호출이 아니라 **Postgres 쓰기**다 (2026-09-26)

스택을 읽었다. 두 스레드가 **모두 PG 소켓 읽기에 파킹**돼 있다.

| 스레드 | 어디 | 최상단 |
|---|---|---|
| `syncMarketData` 를 실행 중인 스레드 | `JpaTransactionManager.doCommit` → flush → `EntityUpdateAction` → `executeUpdate` | `sun.nio.ch.Net.poll` (postgresql `PGStream.receiveChar`) |
| `scheduler-7` | `SimpleJpaRepository.saveAndFlush` → flush → `EntityUpdateAction` → `executeUpdate` | 같음 |

🔴 **내가 멈춘 위치를 틀리게 짚었다 — 정정한다.** 나는 "15초 요청 타임아웃이 있으니 ⓑ(영구
블로킹)는 약하다"고 적었는데, 그 타임아웃은 **HTTP** 에만 걸린다. 실제 블로킹은 **JDBC** 이고
그쪽에는 `socketTimeout`·`statement_timeout`·`lock_timeout` 이 **하나도 설정돼 있지 않다**
(`application.yml` 에는 Hikari `connection-timeout: 30000` 뿐 — 이것은 커넥션 **획득** 대기이지
쿼리 대기가 아니다). 그래서 **무한히 기다린다.**

메커니즘에 대한 판단은 유지된다 — `fixedDelay` 는 현재 실행이 끝나야 다음을 잡으므로,
이 UPDATE 가 응답을 못 받는 동안 **동기화는 영구히 다음 회차가 없다.**

##### 시간선 정정 — "딱 한 번"이 아니었다

앞서 "재시작 후 딱 한 번 시작했다"고 적었는데 **정확하지 않다.** 수집 로그는 00:28:46 ~
**00:31:50** 에 걸쳐 있고, 최소 00:29 · 00:30 · 00:31 세 회차가 돌았다.
마지막 성공은 `00:31:50 KRW-BTC H1 6건` 이고, **정지는 그 직후 회차에서 시작됐다.**

📌 그래서 22코인이 빠진 이유가 더 분명해진다 — 66세션 생성은 00:35 · 00:55 였고,
동기화는 **그 전에 이미 멈춰** 대상 목록을 다시 읽지 못했다.

📌 그리고 이것은 DB 전면 장애가 아니다 — 00:55 에 45세션이 `strategy_log` 를 정상 기록했다.
**특정 대상에 대한 쓰기만** 응답을 못 받는다.

##### ⚠️ 관찰해 둘 설계 문제 (원인으로 단정하지 않는다)

`syncMarketData` 는 **루프 전체가 하나의 `@Transactional`** 이다. 그 안에서 코인마다
REST 호출과 110ms 스로틀 `sleep` 이 일어나므로, **`market_data_cache` 의 행 잠금을
네트워크 I/O 를 포함한 전 구간 동안 붙잡는다.** 잠금 경쟁을 만들기 쉬운 구조다.
또 이 스택에 `db` 서비스는 `timescale/timescaledb:latest-pg15` 이고, TimescaleDB 의
백그라운드 작업(압축·보존)도 같은 대상에 잠금을 잡는다.

🔴 **어느 쪽이 실제 blocker 인지는 DB 에서 확인한다.** `pg_stat_activity` 와 `pg_locks` 가
답을 준다 — 앱 쪽에서는 더 볼 것이 없다. DB 는 같은 compose 스택의 `db` 서비스이므로
컨테이너 안에서 조회하면 **비밀번호를 다루지 않고** 확인할 수 있다.


##### 🔴 blocker 확정 — `idle in transaction` 이 2시간 동안 잠금을 붙잡고 있다 (2026-09-26)

```
pid 323739  idle in transaction  ClientRead  xact_age 1:56:27
            마지막 문장: insert into position (...)
            blocked_by: {}          ← DB 를 기다리는 게 아니다. **앱이 다음 문장을 안 보낸다**

pid 323733  active  Lock/transactionid  blocked_by {323739}
            update market_data_cache set close=$1,…        ← 🔴 우리 동기화
pid 323730  active  Lock/transactionid  blocked_by {323739}
            update dynamic_session set available_krw=$1,…  ← 🔴 동적 매매 엔진
```

`pg_locks` 에도 두 건이 `transactionid ShareLock · granted=false` 로 남아 있다.

**읽는 법**: `update market_data_cache` 가 `transactionid` 를 기다린다는 것은, 323739 의
트랜잭션이 **그 행을 이미 수정했다**는 뜻이다. 즉 323739 는 한 트랜잭션 안에서
`market_data_cache` → `dynamic_session` → `position` 을 차례로 쓴 뒤 **커밋하지 않고 멈췄다.**
그 조합은 `DynamicTradingService.processTick` 의 범위와 일치한다 — `@Transactional` 이고
(`:680`), 캔들을 `marketDataSyncService.fetchWithCache` 로 가져오며(`:1950`) 주문까지 같은
트랜잭션 안에서 처리한다. ⚠️ **일치는 정황이다. 소유 스레드를 덤프에서 확인해야 확정된다.**

🔴 **이것은 전향 검증만의 문제가 아니다.** `dynamic_session` 갱신도 같은 잠금에 막혀 있으므로
**동적 매매 엔진이 2시간째 멈춰 있다.** 검증 표본 문제보다 우선순위가 높다.

📌 곁가지로 드러난 것: `strategy_log` 에 대한
`SELECT DISTINCT ON (coin_pair, date_trunc('hour', created_at)) …` 가 병렬 워커 4개를 물고
**2분 40초** 동안 돌고 있다(`IPC/MessageQueueSend`). blocker 는 아니지만 무거운 질의다.

##### ⚠️ 개입 전에 짚어둘 위험 — 되돌리면 기록이 사라진다

`pg_terminate_backend(323739)` 로 풀 수 있고 그러면 막힌 둘이 즉시 진행된다. 그러나
**그 트랜잭션이 롤백되므로 `position` insert 가 사라진다.**

🔴 **거래소에 실제 주문이 나갔는데 DB 기록만 롤백되면 장부와 실제가 어긋난다.**
그 세션이 실자본인지, 그 insert 에 대응하는 체결이 거래소에 있는지 확인하기 전에는
종료시키지 않는다. 백엔드 컨테이너 재시작도 같은 롤백을 일으키고 증거까지 없앤다.

##### 🔴 소유 스레드가 없을 가능성 — 커넥션에 트랜잭션이 남은 것 (2026-09-26)

덤프를 다시 떴다. 앱 프레임을 가진 스레드 중 **`scheduler-4`·`-6`·`-7` 세 개가 모두 PG 소켓
읽기에 파킹**돼 있다(RUNNABLE, `Net.poll`). 나머지는 `upbit-rate-refill`(정상 수면)이다.

🔴 **그런데 이 셋은 blocker 가 아니다.** 323739 는 `idle in transaction / ClientRead` —
**DB 가 앱의 다음 문장을 기다리는** 상태다. DB 호출 중인 스레드라면 백엔드 상태가 `active` 여야
하므로, **이 세 스레드 중 어느 것도 323739 의 주인이 아니다.**

그러면 주인은 누구인가. 후보가 둘로 갈린다.

| 후보 | 예측 | 확인 |
|---|---|---|
| ⓐ 어떤 스레드가 트랜잭션을 열어둔 채 DB 밖에서 대기 중 | 덤프에 그 스레드가 앱 프레임과 함께 보인다 | 덤프 전체 목록 |
| ⓑ **커넥션이 풀로 반납됐는데 트랜잭션이 안 닫혔다** (소유 스레드 없음) | Hikari 는 그 커넥션을 **idle** 로 보고, DB 는 **idle in transaction** 으로 본다 | Hikari 지표 vs `pg_stat_activity` |

⚠️ ⓑ 를 잡아낼 장치가 없다 — `application.yml` 의 Hikari 설정은 `maximum-pool-size: 20`,
`minimum-idle: 5`, `connection-timeout: 30000` 뿐이고 **`leak-detection-threshold` 가 없다.**
그래서 반납된 커넥션이 트랜잭션을 쥔 채 영구히 남아도 아무 경고가 없다.

##### 🟢 개입 위험이 낮아졌다 — OPEN 포지션이 없다

`position` 을 집계했다.

| `session_kind` | status | 건수 | 마지막 진입 |
|---|---|---|---|
| `DYNAMIC` (REAL 동적) | CLOSED | 30 | 2026-08-16 |
| `DYN_PAPER` (PAPER 동적) | CLOSED | 446 | 2026-09-25 |
| `LIVE` | CLOSED | 277 | 2026-08-07 |

🔴 **OPEN 행이 하나도 없다.** 막힌 insert 는 커밋되지 않아 보이지 않지만, 되돌려도
**"열려 있다고 DB 가 믿는 포지션"이 사라지는 일은 없다** — 애초에 없다.
남은 위험은 하나뿐이다: 그 insert 가 **REAL**(`DYNAMIC`) 이고 거래소에 실제 체결이 나갔다면,
롤백 후 거래소에만 포지션이 남는다. REAL 동적 진입은 **2026-08-16 이 마지막**이고 최근 활동은
전부 `DYN_PAPER` 이지만, **그것만으로 단정하지 않는다** — RUNNING 세션의 `trading_mode` 를 본다.

📌 컬럼 이름을 정정한다: `dynamic_session` 에는 `session_kind` 가 없고 **`trading_mode`**
(REAL/PAPER) 다. `session_kind` 는 `position`·`order` 쪽 컬럼이며 REAL 동적은 `DYNAMIC`,
PAPER 동적은 `DYN_PAPER` 로 갈린다(컬럼 주석).


##### 수정 범위 — 세 갈래로 나눈다 (원인·구조·안전장치)

| 갈래 | 내용 | 성격 |
|---|---|---|
| ① 직접 원인 | 소유 스레드가 트랜잭션을 열어둔 채 무엇을 기다리는지 — 그 대기를 없앤다 | 이번 사고의 원인 |
| ② 트랜잭션 경계 | `processTick`·`syncMarketData` 가 **네트워크 I/O 를 포함한 구간 전체를 한 트랜잭션**으로 묶는다. 행 잠금을 그만큼 오래 붙잡는다 | 구조적 위험 |
| ③ 무한 대기 | JDBC 에 `socketTimeout`·`statement_timeout`·`lock_timeout` 이 없다. 그래서 조용히 영구 정지한다 | 안전장치 — 원인이 아니라 **재발 시 드러나게 만드는 장치** |

③ 이 없으면 같은 정지가 또 조용히 일어난다. 이번에 우리가 알아챈 경로는 "22코인 캔들이
멈춘 것"이었고, 그건 **우연히 검증 준비 중이어서** 보였다.


##### 🔴 이번 시작은 준비 실패로 기록한다 — 뒤늦은 합류로 복구할 수 없다

15코인이 이미 T0 을 지나 거래를 시작했으므로, 7코인을 나중에 붙여도 **공통 T0 조건은
복구되지 않는다.** §5 의 T0 정의는 "세 팔이 같은 시점에 현금 100만원·포지션 0 으로 시작"인데
뒤늦게 합류한 코인은 그 시점을 공유하지 않는다.

따라서:

1. 이번 실행(T0 `2026-09-26T00:55:33Z`, 66세션)은 **준비 실패**로 기록한다
2. `diagnose` 로 7코인의 원인을 특정하고 고친다
3. **22코인 전부가 준비됐음을 확인한다.** 🔴 **준비 확인은 전체 캐시 건수로 하지 않는다 —
   엔진과 같은 조회 조건(`[now − 500×봉주기, now]` 창)과 같은 워밍업 조건(엔진이 보고하는
   요구 봉 수 — 현재 100봉)으로
   통과시켜야 한다.** `diagnose` 가 문제 0종을 내는 것은 그 확인이 아니다(①은 전체 건수이고
   ②·③은 경고가 남았을 때만 보인다). 재시작 전에 이 확인 절차를 먼저 정한다
4. 66세션을 **현금·포지션·전략 상태까지 동일한 초기화 절차로 새 T0 에서** 시작한다
   (세션을 새로 만들면 `sessionStatefulStrategies` 도 새로 생성된다 — `:690-694`)
5. 🔴 **기존 실행 기록은 보존하고 검증 성과에는 섞지 않는다.** 세션 ID 를 제외 목록에 남긴다

⚠️ 이 실패가 알려준 것 하나 — 검증 기간 중에도 **어느 코인의 캐시가 조용히 멈출 수 있다.**
평가 지연과 함께 `diagnose` 를 주기적으로 확인한다. 중간에 멈춘 코인이 생기면 그 사실을
기록하되, **성과 판정 기준을 사후에 바꾸지 않는다.**

### 🔴 착수 전 발견한 결함 — RANGE 게이트에서 팔 A 만 빠져 있었다 (2026-09-25)

`RangeRegimeGate.RANGE_BLOCKED` 은 **전략명 문자열 집합**이고 **부재 = 허용**이다.
팔 A 프리셋을 추가할 때(`3cb4d87`) 이 목록에 넣지 않았다.

| 팔 | 전략명 | 고치기 전 RANGE BUY |
|---|---|---|
| OFF | `COMPOSITE_MOMENTUM_ICHIMOKU_V2` | 차단 |
| B | `COMPOSITE_MTF_MOMENTUM` | 차단 |
| **A** | `COMPOSITE_MTF_MOMENTUM_CLOSED` | **허용** 🔴 |

그대로 시작했다면 `A−B` 는 **"완결 H4 봉만 쓰는 변경"이 아니라 그 변경 + RANGE 진입 허용**
두 가지를 섞어 잰 값이 된다. §2 의 "확인 필터만 변경한다"를 위반한다.
게이트는 LIVE(`:1017`) · DYNAMIC(`:939`) · PAPER(`:721`) · `BacktestEngine:321` 이
모두 전략명으로 호출한다 — 한 군데만 맞추면 되는 문제가 아니었다.

**조치**: A 를 목록에 추가하고, 세 팔의 처우가 같은지 단정하는 테스트를 붙였다
(`RangeRegimeGateArmParityTest`, 변이 검증 완료 — 항목을 지우면 실패한다).
🔴 **이 수정은 재빌드·재배포가 필요하다.** T0 는 수정된 이미지가 돌기 시작한 뒤에 잡는다.

`BACKTEST_RULESET_VERSION` 은 **올리지 않는다** — 거동이 달라지는 것은 같은 날 추가돼
기록된 백테스트 실행이 없는 프리셋 하나뿐이고, v4 로 기록된 결과 중 의미가 바뀌는 것은 없다
(팔 A 추가 `3cb4d87` 과 같은 판단이다). ⚠️ 단, 이 수정 전에 `COMPOSITE_MTF_MOMENTUM_CLOSED`
이름으로 실행한 백테스트가 `backtest_run` 에 있다면 그 행은 **낡은 것으로 표시해야 한다.**

#### 역사적 22코인 검사와의 차이 — 기록해 둔다

그 검사는 팔 이름을 `COMPOSITE_MOMENTUM_ICHIMOKU_V2_OFF/_B/_A` 로 붙여 돌렸다
(`SupertrendValidationRunner:205`). 셋 다 `RANGE_BLOCKED` 에 없으므로
**세 팔 모두 RANGE 에서 진입이 허용된 조건**이었다 — 팔 사이는 대칭이었으나
**운영 조건(OFF·B 차단)과는 다르다.**

🔴 따라서 전향 검증은 역사적 검사와 **같은 게이트 조건이 아니다.** 두 결과의 차이를
"새 자료에서 재현되지 않았다"로만 읽을 수 없다 — 게이트 조건도 함께 달라졌다.

### 전향 검증 설정 — 착수 전 확정 (2026-09-25)

| 항목 | 결정 |
|---|---|
| 코인 | **기존 22코인 전부** (§1) |
| 구성 | `OFF` · `B` · `A` × 22코인 = **66개 페이퍼 세션** |
| 시간봉 | **H1** |
| 초기자본 | **세션당 100만원**, 팔별 2,200만원 |
| 자본 관리 | **세션별 독립.** 세션 간 이동·추가 입금 없음 |
| 팔 A 프리셋 | `COMPOSITE_MTF_MOMENTUM_CLOSED` (2026-09-25 추가, 운영 기본값 아님) |

⚠️ **100만원은 비교를 위한 모의 운용 규모다.** 실제 체결 가능성을 검증한 금액이 아니다.

### 🔴 고정 슬리피지의 한계 — 상대 비교에도 남는다

페이퍼 엔진은 **고정 슬리피지 `PAPER_SLIPPAGE_PCT = 0.1%`/편도**를 쓴다
(`TradingConstants:94`). 주문 크기·호가 깊이·유동성을 반영하지 않는다.

🔴 **저유동성 코인을 세 팔에 똑같이 넣어도 체결 편향이 상쇄되지 않는다.**
팔마다 **거래 시점·횟수·주문 크기가 다르므로** 실제 슬리피지의 영향도 다를 수 있다.
(앞선 보고에서 "동일하게 적용되므로 비교를 왜곡하지 않는다"고 쓴 것은 **틀렸다. 철회한다.**)

따라서 이 한계는 **절대수익뿐 아니라 A−B·B−OFF 상대 비교에도 남는다.**

### 배포 범위 — 66개 세션 생성·시작만

| 포함 | 제외 |
|---|---|
| 66개 페이퍼 세션 생성·시작 | **기존 실거래 세션의 재시작** |
| | **자본 변경** |

세 팔은 **공통 시작 기준(T0)** 을 쓴다 — 정의는 위 §5 T0 항목과 같다.
배포 전 자료는 **지표·전략 상태 워밍업에만** 쓰고, **T0 이후 발생한 주문과 손익만** 성과에 넣는다.

### 보존할 기준점

시작 시 다음을 이 문서에 기록한다: **배포 커밋 SHA · 이미지 식별자 · 세션 설정 · 실제 시작 시각(T0)**.

### 🔴 전향 검증 중에는 같은 전략의 가중치·필터를 손대지 않는다

검증 기간에 `COMPOSITE_MOMENTUM_ICHIMOKU_V2` 계열의 가중치·필터·임계값을 조정하면
고정한 시스템을 검증하는 것이 아니게 된다. **설명을 더 정교하게 만드는 작업을 멈추고,
고정한 시스템이 새 시장에서 비용을 빼고 남기는지만 본다.**

### 🔴 종료 시점 — 팔마다 따로 끝내지 않는다

```
종료 = min( 모든 팔이 각각 200거래를 채운 **공통 시점**,  6개월 경과 시점 )
```

| 6개월 시점 상태 | 처리 |
|---|---|
| 모든 팔 ≥ 200거래 (이전에 도달) | 그 공통 시점에서 판정 |
| 모든 팔 ≥ 100거래 | 그 시점까지로 판정하되 **표본 부족을 명시** |
| 한 팔이라도 < 100거래 | **판정 불가** |

### 중간 관찰 — 두 가지를 구분한다

| 허용 | 금지 |
|---|---|
| 운영 오류 감시 (체결 실패·데이터 결측·세션 중단) | **중간 성과를 보고 규칙·기준·팔 구성을 바꾸는 것** |
| 위험 감시 (한도 초과·비정상 손실) | 중간 결과를 근거로 조기 종료·조기 배포 |

위험 사유로 중단하는 경우 **그 사실과 시점을 기록**하고, 남은 자료로 판정하지 않는다.

---

## 6. 🔴 결과를 읽을 때

- **절대 순수익이 양수여도 수익 전략 확보가 아니다.** 앞선 Walk-Forward 는 이 계열 전략들을
  과최적화/불합격으로 판정했다
- **A−B 가 양수여도** 그것은 **상위봉 구성 방식**의 차이이며, Supertrend 의 방향 판별 능력을
  입증하는 것이 아니다
- 래퍼는 **BUY 뿐 아니라 SELL 도** 막고, 그 결과 포지션·청산 시점 → 호출 생략 → 내부 상태 경로로
  이후 신호까지 바꾼다. 결과는 **"배포된 래퍼와 엔진의 결합"** 의 효과로만 귀속한다
- **비개선·비유의 결과가 효과의 부재를 입증하지 않는다**

## 6.5 🔴 착수 전 회계 검증 — 먼저 한 코인으로

전체 실행 전에 **한 코인**으로 다음을 확인하고, 통과해야 22코인으로 확대한다.

| 검증 | 기준 |
|---|---|
| 일별 손익 합 = 최종 자산 변화 | `Σ_d (E[d] − E[d−1])` 가 `finalEquity − 초기자본` 과 일치 (반올림 오차 내) |
| 확장 전 러너와 동일 | 주 비용에서 `총수익률`·`거래 수`·`MDD` 가 `SupertrendFilterQ2Runner` 와 **같은 값** |
| 자산곡선 정합 | 마지막 시점 포지션이 0(엔진의 강제청산 후)이고 `E[마지막] = finalEquity` |

자산곡선은 **엔진을 고치지 않고** 거래 기록(`TradeRecord`: side·price·quantity·fee·executedAt)과
캔들 종가로 재구성한다 — 운영 코드 변경 없이 검증 가능한 경로다.

## 7. 실행 규칙

1. §1~§4(22코인)는 **한 번** 실행한다
2. 결과를 보고 판정 기준·코인 집합·비용·블록 길이를 고치지 않는다
3. 고치고 싶어지면 새 사전 등록을 쓴다
4. **필터 외의 파라미터를 함께 조정하지 않는다**
5. 운영 DB 는 읽기 전용, 비밀번호는 `PGPASSWORD` 환경변수로만 받는다

---

# 결과 (2026-09-24) — 실행 1회

`SupertrendValidationRunner` · 22코인 H1 · 2023-12-31 ~ 2026-09-22 · 502,425행
`scripts/supertrend_filter/validation_analysis.py` · 블록 30일(민감도 14·60) · B=2000 · 시드 20260924

## 🔴 최종 기록

> **A 변경안의 우위는 확장 코인 집합에서 재현되지 않아 채택하지 않는다.
> 기존 B 설정은 유지하되, B 의 필터 기여도와 전략의 향후 수익성은 검증된 것으로 간주하지 않는다.**

**"B 유지"는 수익성이 검증됐다는 뜻이 아니다** — A 로 변경할 근거가 없어 기존 설정을 그대로 둔다는 뜻이다.
🔴 **이번 결과로 실전 운용이나 자본 확대를 정당화할 수 없다.**

## ① (가) 변경안의 가치 — A − B (주 비용)

| 지표 | 값 | 기준 | |
|---|---|---|---|
| 평균차 | **−1.693%p** | > 0 | ✗ |
| 중앙차 | +0.580%p | > 0 | ✔ |
| 개선 코인 | **11 / 22** | ≥ 15 | ✗ |
| 구간 하한 | **−7.239%p** | > 0 | ✗ |

θ = −1.693 · 95% [−7.239, +3.679] · p = 0.7366 · 블록 34개

**→ (가) 근거 없음.** 평균 ≤ 0 이므로 사전 기준상 "판단 보류"가 아니다.
중앙값만 양수인 것은 반증이 되지 않는다 — 세 요약은 **같은 22개 값**에서 나왔다.

**13코인에서 A−B 가 +9.89%p · 11/13 이던 것이 22코인에서 −1.69%p · 11/22 로 재현되지 않았다.**

## ② (나) 수익 후보 자격 — A 자체

| | 주 비용 | 스트레스(왕복 0.5%) |
|---|---|---|
| 평균 순수익 | +19.623% ✔ | +11.955% ✔ |
| 중앙값 | +5.205% ✔ | −0.875% (보고만) |
| 양수 코인 | 13/22 ✔ | 11/22 (보고만) |
| **거래당 순수익률**(진입금액 기준) | **+0.7121%** ✔ | **+0.4884%** ✔ |
| [참고] `expectancyPct`(초기자본 기준) | +0.5044% | +0.3203% |

**→ (나) 충족.** 🔴 **그러나 이것으로 ①의 탈락을 뒤집지 않는다.**
절대수익 기준 통과는 무시할 사실이 아니지만, **상대 비교의 결론과 별개**다.

## ③ 이번에 얻은 결론 — 세 가지를 분리한다

| | 결론 |
|---|---|
| **A 의 B 대비 우위** | 사전 기준 **미충족**. 변경안 채택 **중단** |
| **B 의 OFF 대비 우위** | 이번 22코인에서 **확인되지 않음**. 필터 유지의 수익상 이점은 **여전히 불확실** |
| **전략 자체의 수익 가능성** | A 는 **역사적 자료**에서 수익 후보 기준을 충족했으나 **독립적인 전향 검증이 남아 있다** |

🔴 **A 변경 연구를 마무리하는 것과 수익 전략 연구 전체를 끝내는 것은 다르다.**

## ④ 보조

### 블록 길이 민감도 — 세 길이 모두 같은 결론

| L | θ | 95% 구간 | p | 블록 |
|---|---|---|---|---|
| 30일(주) | −1.693 | [−7.239, +3.679] | 0.737 | 34 |
| 14일 | −1.693 | [−8.677, +4.456] | 0.690 | 72 |
| 60일 | −1.693 | [−7.166, +3.200] | 0.729 | 17 |

### 코인별 분포 (주 비용, %)

`A−B` 가 **−25.60(HIVE) ~ +29.32(CVC)** 로 넓게 흩어진다. 상세는 `d:/tmp/stval_out/per_coin.csv`.

보조 비교 **`B−OFF`: 평균 −0.327%p · 중앙 +2.235%p · 개선 12/22** —
13코인의 +5.85%p · 10/13 이 **재현되지 않았다.** (판정에는 쓰지 않는다.
**부정 증거로 확정할 수도 없다.**)

### 회계 검증 — 132개 조합 (22코인 × 3팔 × 2비용)

```
독립 재구성 자산 ↔ 엔진 finalEquity    최대 절대오차 0.00원   (100원 초과 0 / 132)
```

⚠️ 일별 차분 합 = 최종 변화 검사는 **망원합이라 집계 누락만** 잡는다.
거래 회계의 근거는 위 `finalEquity` 일치다.

### 🔴 발견한 결함 — 코인 이름 파싱

`"st_blast_h1.csv".replace("st_","")` 가 **`bla[st_]h1.csv` 안쪽의 `st_` 까지** 지워
`KRW-BLAST` 가 `KRW-BLAH1.CSV` 로 기록됐다. 22종 중 BLAST 하나만 해당하며
**13코인 실행은 영향이 없다**(확인).

이름을 고쳐 BLAST 만 재실행한 결과 **6개 조합 전부 수치가 완전히 동일**했다
(OFF +0.890% / B −15.560% / A −22.170%, 거래 수·최종자산까지 일치) —
`coinPair` 문자열이 이 전략의 계산에 쓰이지 않았다는 뜻이다.
러너 3종 모두 `substring(3)` 으로 고쳤다.

## ⑤ 다음 — 전향 검증을 할지 결정하는 단계

**더 유리한 코인이나 설정을 찾는 재탐색이 아니다.** §5 에 이미 고정한 전향 검증을
진행할지 결정한다. 기존 관측 계획을 유지하면 팔의 역할은 명확하다.

| 팔 | 역할 |
|---|---|
| `B` | **현행 기준** |
| `OFF` | **필터 필요성의 대조군** |
| `A` | **채택 보류된 변경안** |

🔴 **성과를 보고 중간에 팔이나 기준을 바꾸지 않는다.**
